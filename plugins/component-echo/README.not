= component-echo

这是 `plugin` world 的最小示例，演示两类元素贡献：

- `echo` 是 computed 元素：handler 把 trailing body 原样透传；body 为空时
  返回一个携带 `message` 参数的 `core::text` 节点（生产性契约——不返回指向
  自身的节点）；
- `note` 是 data-only 声明：不注册 handler，文档中的 call 保持未规约，由
  name + fields 直接参与渲染兜底。

宿主装载时调用一次 `init` 取得全部共享声明（schema 自描述），求值阶段调用
`evaluate`。WIT 只暴露 opaque bytes；两种 payload 都由 wire version byte 与
`notist-model` 共享类型的 postcard 编码组成。插件本体是用
`notist-plugin-sdk` 编写的 Rust crate（见 `src/lib.rs`），以 `export_plugin!` 导出。

重新构建组件：

```sh
nix develop ~/Files/notist -c cargo build --target wasm32-unknown-unknown --release
cp target/wasm32-unknown-unknown/release/*.wasm semantic.wasm
```
