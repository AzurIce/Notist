= shader plugin package

该目录是可分发的 Notist Wasm component 插件包：

- `plugin.json` 只声明 package envelope：identity、`wasm.module` 与顶层
  `render` contribution；
- `semantic.wasm` 是由 `plugins/shader-wasm` 构建的 wasip2 component；其
  `init` 通过 version byte + postcard 返回共享 `notist-model` declarations；
- computed `shader` handler 将调用规约为 data-only `shader::canvas` 节点，
  保留 `source`、`width`、`height`、trailing body 与根 source range；
- `assets/shader.js` 和 `assets/shader.css` 为 HTML target 提供 WebGPU/Web
  Component 投影。

从 `Notist.toml` 装载：

```toml
[plugins.shader]
path = "../plugins/shader"
```

重建 component fixture：

```sh
cd plugins/shader-wasm
cargo build --target wasm32-wasip2 --release -j4
cp target/wasm32-wasip2/release/notist_plugin_shader_wasm.wasm ../shader/semantic.wasm
```
