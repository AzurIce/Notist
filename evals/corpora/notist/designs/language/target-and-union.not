@![implementation = "aligned"]
= Target and Union

本文定义引用目标的一等值设计与 union 类型的最小切片，替代旧 `[[...]]` wiki 引用与 `ref(url: String)`。

== Target 字面量

```not
#<vault::guide/install>
#<vault::wiki link>
#<self::setup>
#<https://example.com>            # 非法：外部 url 是 String，写 link("https://...")
```

- `<` 与未转义 `>` 之间是字面量 body；`\<` `\>` `\\` 转义定界符与反斜杠，其它 `\x` 是语法错误；
- body 是 `ModulePath[/label]`：第一个 `/` 前是模块路径，之后是 label（label 可再含 `/` 与 `#`）；
- 段内允许空格、数字开头与除 control 外的 Unicode；`::` 是段分隔符；`vault`/`self`/`super` 只允许出现在路径开头；
- Target 是一等值：`Type::Target`、`Value::Target`、`NodeValue::Target`，可以 `let` 绑定与传递。

== link

```text
fn link(target: Target | String) -> Content
```

- `Target` 分支：模块内引用（module/scope/resource），check 解析 RefTarget、进引用图；
- `String` 分支只表示外部 url（必须带 scheme）；无 scheme 字符串报“内部目标请用 `<...>` 字面量”；
- 插入规则：Target 插入 Content 自动包成 `core::reference(target)` 节点，与 `String → core::text` 同级；
- `#<...>` 因此是 `link(<...>)` 的语法糖。

== Union

- `Type::Union(Vec<Type>)`：flatten、去重、稳定排序、单成员塌缩；`accepts` 期望侧任一成员接受、实际侧全体可接受；
- surface 语法 `T | U`；`T?` 现阶段仍是独立 `Optional` 构造器；
- 当前唯一公开用法是 `link` 的签名；`T?` 向 `T | None` 的归一化留待集合类型切片。

== import

```not
#import <vault::wiki link>::{format_title as ft}
```

- 路径必须是 `<...>` 字面量且不能带 label；旧裸路径 `import path::{...}` 已移除；
- 依赖边、环诊断与 root binding 语义不变。

== 静态引用图

- check 只索引两类静态证据：直接插入的 Target 字面量（含 Content block 内部）与 `link(<...>)` / `link("https://...")` 的字面量实参；
- `let` 绑定的 Target 传给 `link` 是受控动态洞：值层照常渲染，但不进静态引用图。

== 分层

Target 值是值层拼写（结构化，不是解析产物）；RefTarget（Module/Scope/Resource/External/Missing）仍是分析层解析结果。HTML 投影消费 `core::reference(target)` 结构值，不再 re-parse url 字符串。
