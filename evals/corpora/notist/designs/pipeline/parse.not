@![implementation = "aligned"]
= Parse Stage

本文定义 Notist 的第一阶段：把 source text 解析为 mode-aware 语法树。Markup 与 Code 的完整表面语法见 #<vault::designs::language::markup-surface> 与 #<vault::designs::language::code-grammar>；语法糖语义见 #<vault::designs::language::syntax-sugar>。

```text
source: String ── parse ──> Parse {
  root: Markup
  errors: Vec<SyntaxError>
}
```

== 输入与输出

- 输入：一个 UTF-8 source string，以及调用方提供的 absolute base offset。
- 输出：`Parse`。
  - `root: Markup`：文档根节点。Document root 与 Content literal body 共享 `Markup` 类型。
  - `errors: Vec<SyntaxError>`：可恢复语法错误，每个错误带 byte range。
- parse 不做名称解析、不查函数签名、不执行用户代码。

== Markup / Code 交替

parser 默认处于 Markup 模式。Markup 扫描在遇到下列结构时切换到 Code 或进入受保护的子扫描：

| 结构 | 行为 |
|---|---|
| `#expr` | 嵌入 Code 表达式，记录 scope range 与完整 range |
| `[markup]` | Content literal；方括号内回到 Markup 模式 |
| 反引号 / fenced raw | 保护扫描，不把内容当 Markup/Code |
| `#<target>` | Target 字面量扫描（`<` 到未转义 `>`） |
| `@[...]` / `@![...]` | 属性列表，不是 Markup 内容 |

Markup 与 Code 的边界规则由 #<vault::designs::language::markup-surface/嵌入的边界> 定义；parser 只消费静态边界，不依赖函数签名判断 `[` / `-` 含义。

== Markup 节点

```text
MarkupItem =
  | Text(SpannedText)
  | Raw(RawLiteral)
  | Embedded(EmbeddedExpression)
  | Heading(HeadingSugar)
  | Rule(TextRange)
  | List(ListSugar)
  | Table(TableSugar)
  | BlockAnnotation(BlockAnnotation)
  | ModuleAnnotation(BlockAnnotation)
```

- Text / Raw / Embedded 是通用内容节点；
- Heading / Rule / List / Table 是 syntax frontend 专用糖节点，lowering 时获得 `core::*` 构造器身份；
- BlockAnnotation 与 ModuleAnnotation 是属性挂载点，不直接成为内容节点。

== Code 节点

```text
ExpressionKind =
  | None | Bool | Int | Float | String
  | Content(ContentBlock) | Name | Call
  | Binary | Unary | Block | Let | If | Lambda
  | Import | LetFunction | Parenthesized
  | Error
```

- ContentBlock 的 body 是递归的 `Markup`；
- Call 记录 name、arguments 与 trailing Content blocks；
- Error 是保留源码范围的可恢复占位，不是合法值。

== 受保护扫描

- raw literal 的 delimiter、inline/fenced 形态与关闭规则见 #<vault::designs::language::syntax-sugar/raw>；
- Target 字面量的 `<...>` 内是路径拼写，不再扫描 Markup；
- Code 注释与 string literal 按 #<vault::designs::language::code-grammar> 扫描，不泄漏到外层结构。

== 错误恢复

parser 是总函数：错误记录到 `Parse.errors`，当前节点以 `Error` 或最佳局部结果继续。规则：

- 语法错误不阻止后续扫描；
- 诊断 range 指向造成错误的精确区间；
- 调用方应展示错误，同时仍可使用 `Parse.root` 执行后续阶段。

== 不变量

- **S1 顺序**：`Markup.items` 与表达式子节点保持 source order。
- **S2 Range**：每个节点携带半开 byte range；parser 不改变 source 文本。
- **S3 无语义猜测**：parse 不 resolve 名字、不查 registry、不猜 trailing Content 属于哪个形参。
- **S4 糖保留身份**：语法糖以专用节点保留，直到 lowering 才替换为 `core::*` intrinsic 调用。

== Conformance

| 输入 | 预期 | 测试 |
|---|---|---|
| Markup 与 Code 混合文档 | 一个 mode-aware 根 Markup | `parses_markup_and_code_modes_into_one_tree` |
| `= Title` / `---` / `- item` | Heading / Rule / List sugar 节点 | `parses_heading_rule_and_list_sugar_as_frontend_items` |
| pipe table | Table sugar，body cell 补到列数 | `parses_pipe_table_sugar_with_alignment_and_protected_pipes` |
| unclosed block comment | 记录恢复错误 | `reports_unclosed_block_comments_in_code_contexts` |
| raw literal 内 Markup 字符 | 保持原文，不嵌套扫描 | `parses_module_references_and_hides_markup_inside_raw` |

== 已裁定 / 待定

- 已裁定：parse 是纯函数；输出 `Parse { root, errors }`；不做名称解析。
- 已裁定：Markup 与 Code 共用一棵 mode-aware 树，ContentBlock body 递归为 Markup。
- 已裁定：语法糖由 parser 建立专用节点，lowering 获得 `core::*` 构造器身份。
