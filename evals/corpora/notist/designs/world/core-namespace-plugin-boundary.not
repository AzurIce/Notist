@![implementation = "partial"]
= Core Namespace and Plugin Boundary

本文是 core namespace 与插件边界的独立成篇，吸收第一代 core namespace 设计的裁决并更新到当前 surface。当前内置构造器见 #<vault::designs::plugin-system::core>；函数签名与调用模型见 #<vault::designs::language::call-model>；Vault 配置容器见 #<vault::designs::world::boundary-discovery>。

== Core Namespace

`core` 是语言标准 package 的保留 namespace，保存语言 surface 定义的标准构造器与基础操作。它由默认 App 预装，但不属于 eval engine 的内建 dispatch 表：

```text
core::ref
core::heading
core::raw
core::rule
core::callout
core::details
core::item
core::table-cell
core::table
core::figure
core::strong / emph / underline / strike
```

- `core` 的保留 namespace 与标准 surface 由 language policy 定义；具体 handler 仍来自 `core` package contribution；
- `core` 不能成为 `let` binding、import alias 或其他 plugin namespace；
- 短名 prelude 可以把已安装 core package 的常用构造器以裸名带入当前 Module scope；
- 短名遵守普通 lexical shadowing；qualified `core::...` 永远指向标准 package 名字，即使当前 App 尚未安装对应 handler。

当前实现中 `core::` qualified call 与 prelude 裸名均已启用；宿主语法糖（`= Title`、pipe table 等）始终取得对应 core intrinsic identity，不经过名字查找，因此 local binding 不影响糖。

== Prelude

Prelude 是每个 Module root 自动获得的普通 binding 层，不是第二套全局环境：

- prelude 名字可以被 author `let` 或 import 遮蔽；
- import 显式 selector 可以引入非 prelude 的 core 短名；
- prelude 内容随语言版本变化，并在 WorkspaceSnapshot 的 function environment identity 中记录（#<vault::designs::host::analyzer-snapshot>）。

== Call 契约

所有内容统一为一种节点：call。不再有内置 Element 变体与 `Custom` 的区分（#<vault::designs::pipeline::plugin-call-reduction>）：

```text
Node {
  name      // core::* 或 {package}::*
  args      // 名称 + 带类型的参数
  children  // 子 call 森林
  block     // 是否打断段落流
}
```

- 内置内容使用 `core::*` 名字；插件内容使用 `{package}::*` 名字；
- `name` 与参数 schema 由构造器或插件 `init` 注册声明，进入索引与渲染兜底；
- `children` 是 call 森林，不是 source string，也不重新 parse；
- `block` 是语义参数，不由 call-site delimiter 猜测；
- serializer 对未注册投影的名字有安全 fallback；富渲染的资产注入与 trusted 收口属于 target 侧（#<vault::designs::plugin-system::projection>）。

== 信任模型

完整插件 package 模型与宿主 ABI 见 #<vault::designs::plugin-system>；能力系统的删除裁决见 #<vault::designs::plugin-system::capability>。

- 插件 Function 是带 signature 与 implementation 的普通 Function Value；
- 内容组合不做 per-call 授权：wasm 沙箱（无 IO import + 资源预算）是唯一执行边界；
- 若未来出现文件/网络级需求，作为独立 capability 面另行设计。

== 加载、作用域与生命周期

- 插件通过显式 namespace 与 import/prelude 进入环境，不注册到 `core`；
- 插件 schema 由 Vault 配置声明（见下），Analyzer 在 snapshot 中捕获 schema identity；
- plugin runtime 默认不在普通编辑/诊断路径执行；
- 插件失败是 host boundary diagnostic，不伪装成 `None` 成功；
- 插件不能修改 parser、不能重新解释已有 token。

== Notist.toml 配置内容模型

Vault marker 从空文件扩展为受限配置容器：

```toml
language-revision = "2026-08-25"
[plugins]
mermaid = { package = "mermaid" }
[site]
styles = ["assets/user.css"]
```

- 未知字段预留，不静默忽略；分析层携带配置 identity 与字节载荷；
- language revision 决定语法、core/prelude 与诊断 surface；
- plugins 是显式加载表；
- `[site]` 是站点级呈现配置：`styles` 列出额外样式表，条目必须是 vault 根内的相对 `.css` 路径（拒绝绝对路径、`\` 分隔与 `..` 逃逸），解析在 notist-plugin-host 的配置读取处统一校验；
- 配置变更进入 source-set fingerprint（#<vault::designs::host::query-contract>）。

具体字段 schema、版本迁移与插件加载协议由实现切片补齐；在实现前，Notist.toml 保持空标记或未知键预留。

== 语法扩展边界

语义层的"宏"已被规约模型吸收：handler 把 call 重写为新 call 就是宏展开，不存在独立的 macro design（见 #<vault::designs::pipeline::plugin-call-reduction>）。仍然独立的是记号层扩展：parser 的新语法由独立的 grammar-extension design 定义；普通 Function/plugin registration 永远不授予修改 parser、重新解释已有 token 的能力。
