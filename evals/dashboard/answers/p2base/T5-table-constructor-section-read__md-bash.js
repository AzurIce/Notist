window.EVAL_ANSWERS["p2base||T5-table-constructor-section-read__md-bash"]="\n\n**结论：**\n\n- **列数**：由 `columns`（Int，必填）控制——每 `columns` 个 cell 组成一行。\n- **表头**：由 `header`（Bool，默认 `false`）控制——决定首行是否进入表头。\n\n完整签名：\n```text\ntable(columns: Int, header: Bool = false, align: String? = none, trailing body: Content) -> Content\n```\n\n**出处：** `functions.md`（Built-in Functions 函数参考），节标题 **`== table-cell / table`**（第 177–192 行）。该节还说明：`align` 是逗号分隔的列对齐串（数量必须等于 `columns`）；Table 本身无 caption，需要题注时用 `figure` 包住 table。\n";
