window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["p2base||T5-table-constructor-section-read__notist-bash"]="\n\n**结论：**\n\n- **列数**由参数 `columns`（Int，必填）控制——每 `columns` 个 cell 组成一行。\n- **表头**由参数 `header`（Bool，默认 `false`）控制——决定首行是否进入表头。\n- 完整签名：`table(columns: Int, header: Bool = false, align: String? = none, trailing body: Content) -> Content`（另有 `align` 控制列对齐）。\n\n**出处：** 文档文件 **functions.not**，节标题 `== table-cell / table`（约第 177–192 行，模块 `vault::functions`）。\n\n注：designs/plugin-system/core.not 中也有同名签名，但那是设计文档；函数参考的完整说明在 functions.not。\n";
