window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["p2base||T4-module-annotation-syntax__notist-bash"]="\n\n## 结论\n\n**确切语法**：在文件开头（第一个有意义的 token 之前）写模块属性标注，标签用 `#tag` 条目：\n\n```not\n@![#design, #wip]\n\n= 标题\n```\n\n要点：\n- `@![...]` 必须出现在文件开头，可被索引器不求值地快速扫描；\n- `!` 表示\"作用于所在模块\"（对应 Rust inner attribute 语义），绑定为模块属性（根 scope）；\n- 列表内可写任意多个 `#tag`、`.class`、`key = value`，但**不使用 id**（模块本身已是可寻址目标）；\n- 未闭合 `@![...]`、`@!` 后不是 `[` 都会报诊断。\n\n**权威定义出处**：`designs/language/annotation-syntax.not` 的「三种挂载位置」一节第 3 条\"模块：`@![...]`\"（该篇是 L8 标注语法的独立成篇）。\n\n旁证：`grammar.not`「标注与 scope 形态」节和 `intro.not`（第 153 行）都只给出摘要并指向该篇；`example/annotations.not` 首行 `@![#reference, kind = \"demo\"]` 是实际用例。\n";
