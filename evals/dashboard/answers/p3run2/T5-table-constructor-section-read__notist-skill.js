window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["p3run2||T5-table-constructor-section-read__notist-skill"]="\n\n**结论**（出处：`functions.not`，模块 `vault::functions`，节「table-cell / table」）：\n\n- **列数**：由 `columns`（Int，必填）控制——每 `columns` 个 cell 为一行。\n- **表头**：由 `header`（Bool，默认 `false`）控制——决定首行是否进入表头。\n\n签名：`table(columns: Int, header: Bool = false, align: String? = none, trailing body: Content) -> Content`\n";
