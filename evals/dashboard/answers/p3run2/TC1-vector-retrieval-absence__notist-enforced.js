window.EVAL_ANSWERS["p3run2||TC1-vector-retrieval-absence__notist-enforced"]="\n\n**结论：目前不支持。** 当前 `notist search` 只有四种显式模式（lexical/exact/fuzzy/regex，基于 BM25 词项检索），没有 embedding/向量模式；dense vector、hybrid、reranker 属于未来路线（阶段 3/4）。\n\n**官方规范规定：**\n- `designs/host/search-retrieval-index.not`（Search Modes and Retrieval Index，§检索单元与索引）：「向量、semantic 与 hybrid mode 不属于默认契约」——未来只有在版本化 benchmark 上相对 lexical 明确提升 Recall、nDCG 与 Agent task success，并保留 citation、ACL、snapshot 与 output budget 后，才能增加显式 mode。\n- `designs/host/cli-command-surface-skill-distribution.not`（§向量化属于 Vault 运行时状态）：向量模型与向量索引是 Vault 的可重建派生运行时状态（daemon 维护，记录 model identity、chunking schema、indexed revision），不嵌入 CLI 二进制。\n- 佐证：`ai/2026-07-21 notist agent mcp semantic retrieval and vector indexing.not`（§先给结论、阶段 3/4）：实现顺序为「稳定身份和 snapshot → 结构/全文索引 → 只读 MCP → dense hybrid（EmbeddingProvider + exact vector scan，RRF/MMR 融合）→ ANN/reranker」；若 dense 在 benchmark 上无提升则不引入向量数据库复杂度。\n";
