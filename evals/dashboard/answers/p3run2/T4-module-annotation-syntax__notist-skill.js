window.EVAL_ANSWERS["p3run2||T4-module-annotation-syntax__notist-skill"]="\n\n**语法**：在文件开头（第一个有意义 token 之前）写模块属性行，标签用 `#tag`：\n\n```not\n@![#design, #wip]\n```\n\n- `@!` 表示\"作用于所在模块\"（对应 Rust inner attribute），必须位于文件开头；\n- `@![...]` 内按属性列表解析：`#tag`、`.class`、`key = value` 可任意多个，但**不使用 id**（模块本身已是可寻址目标）。\n\n**权威定义出处**：`vault::designs::language::annotation-syntax`（`designs/language/annotation-syntax.not`）的「三种挂载位置」一节第 3 条\"模块：`@![...]`\"。`grammar.not`「声明」一节仅给出 `@![...]` 并指向该篇。\n";
