window.EVAL_ANSWERS["it1||T6-cursor-continuation-rule-doc__md-bash"]="\n\n**续读行为**：检索结果页由服务端 runtime policy 限定（search 默认 8 items / 16 KiB，硬上限 100 / 64 KiB），CLI 不暴露 page/budget 参数。一页看不完时，输出末尾给 `continue: notist next <TOKEN>`（读完则显示 `complete`）；续读只需执行 `notist next <TOKEN>`——token 是 opaque、base64url、带完整性校验的自包含句柄，恢复原 query/selector/scope/filter/grouping/ordering 与 offset，`next` 不携带原查询参数。source set 变化报 `cursor_stale`，token schema 过期报 `cursor_expired`，损坏或不属于该 Vault/View 报 `invalid_cursor`。停止规则：`coverage.complete=false` 只表示当前页未覆盖完整匹配集，不是\"未找到\"；read 已给出充分证据即可停止，只有当前页不足且任务要求穷举、或结论依赖证明\"不存在\"时才需沿 cursor 读到 complete。\n\n**权威设计文档**：`designs/host/query-contract.md`（标题 *Query Contract*）。检索语义篇 `search-retrieval-index.md` 开头明确把「分页预算、cursor 与 selector」委托给它；停止规则（coverage.complete / stop_reason 那段）就在其 **`== Core Query Contract`** 一节内。\n\n（CLI 侧的落地表述在 `cli.md`：「继续读取：next」节讲 next 用法，「Agent 工作流」节有字面的\"停止规则：\"清单。）\n";
