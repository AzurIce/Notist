window.EVAL_ANSWERS["it1||T2-callout-signature__notist-skill"]="\n\n**确切答案：** `kind` 的类型是 **`String`**，默认值是 **`\"note\"`**。\n\n出处：\n- `functions.not` 的 `== callout` 节（第 126 行）：\n  ```text\n  callout(kind: String = \"note\", title: Content? = none, trailing body: Content) -> Content\n  ```\n- 同签名亦见于 `cheatsheet.not`（第 32 行）与 `designs/plugin-system/core.not` 的 `=== callout` 节（第 53 行）。\n\n补充说明：文档指出 `kind` 是面向语义与导出的稳定分类，为空会产生诊断；未提供 `title` 时不生成标题。\n";
