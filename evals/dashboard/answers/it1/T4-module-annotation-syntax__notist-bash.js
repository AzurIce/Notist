window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["it1||T4-module-annotation-syntax__notist-bash"]="\n\n## 结论\n\n**语法**：在文件开头（第一个有意义的 token 之前）写模块属性 `@![...]`，标签用 `#tag` 条目：\n\n```not\n@![#design, #wip]\n```\n\n要点（来自该文档）：\n- `!` 表示\"作用于所在模块\"，等价于 Rust inner attribute；属性列表绑定为模块属性（根 scope）；\n- 必须出现在文件开头，索引器可不求值直接扫描；\n- 列表里**不用 id**（模块本身已是可寻址目标）；tag、class、`key = value` 可任意多个，如 `@![#design, #wip, status = \"draft\"]`。\n\n**权威定义出处**：`designs/language/annotation-syntax.not`（模块 `vault::designs::language::annotation-syntax`），节「三种挂载位置」第 3 条\"模块：`@![...]`\"（约 58–71 行），token 区分规则在「语法边界」节。\n\n旁证：`grammar.not:145` 明确写\"模块属性：`@![...]` 见 #<vault::designs::language::annotation-syntax>\"，`intro.not`「标注」节与 `cheatsheet.not:97` 也均指向该篇，故 annotation-syntax 是权威定义所在。\n";
