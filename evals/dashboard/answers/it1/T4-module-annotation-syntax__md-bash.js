window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["it1||T4-module-annotation-syntax__md-bash"]="\n\n## 结论\n\n**1. 确切语法：`@![...]`，必须写在文件开头（第一个有意义的 token 之前）**\n\n```not\n@![#design, #wip, status = \"draft\"]\n\n= 标题\n```\n\n- `!` 表示\"作用于所在模块\"（对应 Rust inner attribute 语义），把属性列表绑定为**模块属性（根 scope）**；\n- 标签条目写法是 `#tag`，可任意多个，与 `.class`、`key = value` 逗号分隔混用；\n- 属性列表中**不使用 id**——模块本身已是可寻址目标，id 无意义；\n- \"文件开头\"约束保证模块元数据可被索引器不求值地快速扫描，随 ModuleResult 发布。\n\n**2. 权威定义出处：`designs/language/annotation-syntax.md`（《Annotation Syntax》）**\n\n- 具体在「== 属性 → === 三种挂载位置」第 3 条\"模块：`@![...]`\"，以及「== 语法边界」「== 与查询的关系」两节；\n- `grammar.md`（「标注与 scope 形态」「引用索引」节）和 `cheatsheet.md`（「标注速查」节）只是摘要，均指向该篇为完整规则。\n";
