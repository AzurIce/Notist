window.EVAL_ANSWERS["it1||T5-table-constructor-section-read__notist-skill"]="\n\n**结论**（出处：`functions.not`，节标题 `== table-cell / table`）\n\n- **列数**：由参数 `columns: Int` 控制，必填；body 中每 `columns` 个 `table-cell` 组成一行。\n- **表头**：由参数 `header: Bool = false` 控制，决定首行是否进入表头。\n\n完整签名：\n\n```text\ntable(columns: Int, header: Bool = false, align: String? = none,\n      trailing body: Content) -> Content\n```\n\n补充：`align` 是逗号分隔的列对齐串（数量必须等于 `columns`）；文档还给出了 pipe table 语法糖说明。该说明位于函数参考文件 **functions.not**（第 177–205 行附近的 `table-cell / table` 一节，签名清单另见文件第 16 行）。\n";
