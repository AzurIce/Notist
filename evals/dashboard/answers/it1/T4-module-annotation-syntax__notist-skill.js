window.EVAL_ANSWERS["it1||T4-module-annotation-syntax__notist-skill"]="\n\n## 结论\n\n**确切语法：模块级标注 `@![...]`，必须写在文件开头（第一个有意义的 token 之前）**\n\n```not\n@![#design, #wip, status = \"draft\"]\n\n= 标题\n```\n\n要点（来自该文档\"三种挂载位置\"第 3 条）：\n- `!` 表示\"作用于所在模块\"（对应 Rust inner attribute 语义），属性列表绑定为**模块属性（根 scope）**；\n- 必须出现在文件开头——模块元数据可被索引器快速扫描，不依赖求值；\n- 属性列表条目为 `id | #tag | .class | key = value`，逗号分隔；**模块属性中不使用 id**（模块本身已是可寻址目标）；tag、class 与 key=value 可任意多个。\n\n**权威定义出处：`vault::designs::language::annotation-syntax`（文件 `designs/language/annotation-syntax.not`）**，\"属性 → 三种挂载位置\"一节。\n\n旁证：`grammar.not` 的\"标注与 scope 形态\"一节只给了摘要（`@![...]` 文件开头，绑定为模块属性），并明确\"完整规则见 #<vault::designs::language::annotation-syntax>\"。\n";
