window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["it1||T6-cursor-continuation-rule-doc__notist-skill"]="\n\n## 续读行为\n\nCollection 类命令（search/locate/modules/outline/read/refs）只返回一个有界页，输出结尾是 `continue: notist next <TOKEN>` 或 `complete`。续读就是执行 `notist next <TOKEN>`：\n\n- token 是 opaque（base64url + 完整性校验）且**自包含**，恢复原 query/selector/scope/filter/grouping/ordering 与 offset——不需要重复原命令参数，daemon 也不保存 page session。\n- token 失效按 typed error 恢复：source set 变化 → `cursor_stale`；schema 过期 → `cursor_expired`；损坏或跨 Vault/View → `invalid_cursor`（exit 3）。\n- **停止规则**：正向事实查找在 `read` 取得充分 authored evidence 后即可停止，不必为形式上的 complete 读完所有页；只有当前页不足、任务要求穷举（\"列出全部\"）、或结论依赖证明\"不存在\"时，才必须沿 cursor 读到 `complete`（或用 export）。\n\n## 权威设计文档\n\n- **文档**：`designs/host/query-contract.not`（Query Contract，governing architecture；全库唯一定义 `PageRequest`/`QueryPage` 与 `stop_reason` 的设计文档）。\n- **讲停止规则的节标题**：**Core Query Contract**（其中写明：`coverage.complete=false` 只表示当前页未覆盖完整匹配集，不表示\"未找到\"；read 证据充分即可停止，穷举/否定结论必须读到 complete）。\n\nCLI 命令面层面的对应描述在 `cli.not` 的「继续读取：next」与「Agent 工作流」（内含\"停止规则\"块）两节，但权威设计以 query-contract.not 为准。\n";
