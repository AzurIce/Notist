window.EVAL_ANSWERS["it1||T5-table-constructor-section-read__notist-bash"]="\n\n查到了。函数参考中 `table` 构造器的完整说明如下：\n\n**签名**（functions.not，第 179–183 行）：\n```text\ntable(columns: Int, header: Bool = false, align: String? = none,\n      trailing body: Content) -> Content\n```\n\n**参数控制关系**（第 185 行说明）：\n- **列数 → `columns`**（Int，必填）：table 消费按行平铺的 `table-cell`，每 `columns` 个 cell 组成一行。\n- **表头 → `header`**（Bool，默认 `false`）：决定首行是否进入表头。\n- 另有 `align`（逗号分隔的列对齐串，数量必须等于 `columns`）。\n\n**出处**：文档文件为 **functions.not**，节标题为「== table-cell / table」（第 177 行起）。\n\n补充：该节还提到 pipe table 语法糖（`| Name | Value |`），其中 header cell 数即 `columns`，分隔行必须与表头同列数。\n";
