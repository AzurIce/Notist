window.EVAL_ANSWERS["it1||T6-cursor-continuation-rule-doc__notist-bash"]="\n\n## 结论\n\n**续读行为**：有界 collection 查询（search/modules/outline/read…）一页看不完时，结果尾部给出 `more results available (原因)` 提示和一个稳定续读 cursor；CLI 上统一的续读动作是 `notist next <TOKEN>`（翻页的唯一入口）。token 自包含、opaque（base64url + 完整性校验），恢复原 query/selector/scope/filter/grouping/ordering 与 offset，无需重复原参数、daemon 也不保存 page session。source set 变化返回 `cursor_stale`，token schema 过期返回 `cursor_expired`，损坏或跨 Vault/View 返回 `invalid_cursor`。停止规则：`coverage.complete=false` 只表示当前页未覆盖完整匹配集，不等于\"没找到\"；正向事实 `read` 取得充分证据即可停止，只有当前页不足、需穷举或要证明\"不存在\"时才沿 cursor 读到 `complete`（否定/列全部结论必须在 complete 后下，或改用 export）。\n\n**权威设计文档**：`designs/host/query-contract.not`（模块 `vault::designs::host::query-contract`，标题 *Query Contract*）。\n- 依据：`cli.not:4`「查询契约见 #<vault::designs::host::query-contract>」；`search-retrieval-index.not:4`「分页预算、cursor 与 selector 见 #<vault::designs::host::query-contract>」；且按 `cli-command-surface-skill-distribution.not` 的权威性划分，`cli.not` 描述当前公开行为、活动 `designs/` 才是设计。\n\n**讲停止规则的那一节标题**：`Core Query Contract`（query-contract.not 第 34 行起；停止规则段落在该节内，定义 `coverage.complete`/`stop_reason` 与何时可停）。\n\n（补充：公开行为文档 `cli.not` 里对应的节是「继续读取：next」与「Agent 工作流」下的\"停止规则\"清单，但那是行为描述而非设计权威。）\n";
