window.EVAL_ANSWERS["baseline||T6-cursor-continuation-rule-doc__notist-bash"]="\n\n**续读行为**：有界结果页看不完时，输出末尾给出 `continue: notist next <TOKEN>`（看完则给 `complete`）。调用者执行 `notist next <TOKEN>` 即可续读——token 是自包含的 opaque cursor，绑定了 snapshot、query/selector/scope/filter/ordering fingerprint 与排序位置，`next` 本身不携带原查询参数；沿 cursor 逐页读到 `complete`。source set 变化返回 `cursor_stale`，fingerprint 不匹配返回 `invalid_cursor`。\n\n**权威设计文档**：`designs/host/query-contract.not`（Query Contract）。\n\n**讲停止规则的节标题**：`== Core Query Contract`。其中规定：`coverage.complete=false` 只表示当前页未覆盖完整匹配集合，不表示\"未找到\"；一旦 `read` 已给出足够 authored evidence 即可停止，只有当前页不足、任务要求穷举或结论依赖证明\"不存在\"时才需沿 cursor 读到 complete；`stop_reason`（item_limit / byte_budget / query-specific）是 core 内部观测字段，不进入普通 text 输出。\n\n（补充：`cli.not` 的「Agent 工作流」一节也列了面向 Agent 的停止规则清单，但分页与 cursor/stop_reason 的契约设计以 query-contract.not 为准。）\n";
