window.EVAL_ANSWERS["baseline||T5-table-constructor-section-read__notist-bash"]="\n\n**结论（出自 `functions.not`，节标题「== table-cell / table」）：**\n\n```text\ntable(columns: Int, header: Bool = false, align: String? = none,\n      trailing body: Content) -> Content\n```\n\n- **列数**：由 `columns`（Int，必填）控制——table 消费按行平铺的 table-cell，每 `columns` 个 cell 为一行。\n- **表头**：由 `header`（Bool，默认 `false`）控制——决定首行是否进入表头。\n\n出处：文档文件 **functions.not**（函数参考，第 177–206 行「table-cell / table」一节）。\n\n补充：`align`（逗号分隔的列对齐串，数量须等于 `columns`）控制列对齐；表格无 caption，需要题注时用 `figure` 包住 table。另见设计文档 designs/plugin-system/core.not「table」节有相同签名，但函数参考正文在 functions.not。\n";
