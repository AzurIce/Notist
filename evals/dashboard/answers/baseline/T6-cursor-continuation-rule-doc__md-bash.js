window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["baseline||T6-cursor-continuation-rule-doc__md-bash"]="\n\n## 结论\n\n**续读行为**：`notist search` 一页只返回有界结果（默认 8 items / 16 KiB，硬上限 100 items / 64 KiB）。页未读完时 stdout 给出 `continue: notist next <TOKEN>`（末页给 `complete`）；续读只需执行 `notist next <TOKEN>`——token 是自包含的 opaque cursor（base64url、带完整性校验），自动恢复原 query/scope/filter/排序与 offset，无需重复原命令参数。source set 变化报 `cursor_stale`、schema 过期报 `cursor_expired`、token 损坏/不属于该 Vault 报 `invalid_cursor`。停止规则：`coverage.complete=false` 只表示当前页未覆盖全部匹配，不等于\"没找到\"；`read` 已给出足够证据即可停止，只有当前页不足、任务要求穷举或结论依赖证明\"不存在\"时才需沿 cursor 读到 complete（`stop_reason` 区分 item_limit / byte_budget / query-specific 上限）。\n\n**权威设计文档**：`designs/host/query-contract.md`（标题 *Query Contract*）。\n- 依据：`designs/host/search-retrieval-index.md` 开篇即声明\"分页预算、cursor 与 selector 见 [query-contract]\"；`cli.md` 也注明\"查询契约见 query-contract\"。\n- **讲停止规则的那一节标题**：`== Core Query Contract`（其中 `coverage.complete=false` / `stop_reason` 段落与默认/硬上限预算表都在该节内）。\n\n（CLI 侧的调用细节在 `cli.md` 的「继续读取：next」一节，但分页与停止规则的权威设计以 query-contract.md 为准。）\n";
