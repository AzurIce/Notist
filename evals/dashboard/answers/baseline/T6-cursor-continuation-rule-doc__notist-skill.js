window.EVAL_ANSWERS["baseline||T6-cursor-continuation-rule-doc__notist-skill"]="\n\n**续读行为**：Collection 类命令（search/outline/read 等）每页输出末尾给出 `continue: notist next <TOKEN>`（还有下一页）或 `complete`（读完）。续读只需执行 `notist next <TOKEN>`——token 自包含，恢复原 query/selector/scope/filter/grouping/ordering 与 offset，不重复原命令参数；token 是 opaque、base64url、带完整性校验。source set 变化返回 `cursor_stale`，token schema 过期返回 `cursor_expired`，损坏或跨 Vault/View 返回 `invalid_cursor`。\n\n**停止规则**：正向事实查找不需要为了形式上的 complete 读完所有页，`read` 提供足够证据即可停；否定性结论或\"列出全部\"类任务必须在 `complete` 之后才能下结论。\n\n**权威文档与出处**：\n- 文档名：**`cli.not`**（官方 docs Vault 中规定目标命令面的文档，即 `vault::cli`）\n- 讲停止规则的节标题：**「继续读取：next」**（位于「设计总览」下，cli.not:115；停止规则在 cli.not:126）\n\n补充：`Explore > next` 一节（cli.not:366）是 `next` 命令的参考条目，但分页与停止规则的设计性描述在「继续读取：next」节。\n";
