window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["baseline||T5-table-constructor-section-read__md-bash"]="\n\n**结论**（出自 `functions.md`，标题 *Built-in Functions*，节 **== table-cell / table**）：\n\n- **列数**由 `columns`（Int，必填）控制——body 中每 `columns` 个 `table-cell` 组成一行。\n- **表头**由 `header`（Bool，默认 `false`）控制——决定首行是否进入表头。\n\n完整签名：\n```text\ntable(columns: Int, header: Bool = false, align: String? = none, trailing body: Content) -> Content\n```\n\n出处：`functions.md`（内置函数参考），\"table-cell / table\" 一节；签名总览中亦有同一行。\n";
