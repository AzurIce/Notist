window.EVAL_ANSWERS["baseline||T4-module-annotation-syntax__notist-bash"]="\n\n## 结论\n\n**确切语法：模块属性用 `@![...]`，必须写在文件开头（第一个有意义的 token 之前），标签在括号内用 `#tag` 书写，可多个：**\n\n```not\n@![#design, #wip, status = \"draft\"]\n\n= 标题\n```\n\n要点（来自该文档）：\n- `!` 表示\"作用于所在模块\"，对应 Rust inner attribute 语义；\n- 必须位于文件开头——模块元数据可被索引器快速扫描，不依赖求值；\n- 属性列表里**不使用 id**（模块本身已是可寻址目标），tag / class / key=value 任意多个。\n\n**权威定义出处：** `designs/language/annotation-syntax.not`（模块 `vault::designs::language::annotation-syntax`，篇名 \"Annotation Syntax\"），具体在「== 属性 → === 三种挂载位置」第 3 条\"模块：`@![...]`\"。\n\n旁证（非权威定义，仅引用）：\n- `intro.not:153` 概述三种挂载位置（行内 postfix / 块级 `@[...]` / 模块属性 `@![...]`），并指向 annotation-syntax；\n- `cheatsheet.not:86` 速查表列出 `#tag 标签（任意多个）`；\n- `grammar.not:207/241` 给出属性列表文法 `条目 = id | #tag | .class | key = value`。\n";
