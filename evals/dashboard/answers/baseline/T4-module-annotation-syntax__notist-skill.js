window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["baseline||T4-module-annotation-syntax__notist-skill"]="\n\n## 结论\n\n**1. 确切语法：文件开头写 `@![...]`（模块属性，根 scope）**\n\n```not\n@![#design, #wip, status = \"draft\"]\n\n= 标题\n```\n\n要点：\n- `@![...]` **必须出现在文件开头**（第一个有意义的 token 之前），这样模块元数据可被索引器不求值地快速扫描；\n- `!` 表示\"作用于所在模块\"（对应 Rust inner attribute 语义）；\n- 方括号内是统一的属性列表：`条目 = id | #tag | .class | key = value`，逗号分隔——**标签写成 `#tag`**，可任意多个；\n- 模块属性中不使用 id（模块本身已是可寻址的 Module target）。\n\n**2. 权威定义出处：`designs/language/annotation-syntax.not`（ModulePath `vault::designs::language::annotation-syntax`）**\n\n- 具体在 **\"三种挂载位置\"** 一节的第 3 条\"模块：`@![...]`\"，含位置约束、`!` 语义与 id 规则；\n- \"属性列表\"一节定义条目语法（id / #tag / .class / key=value）；\n- \"与查询的关系\"一节说明模块属性随 ModuleResult 发布、tag 支持按属性查询。\n\n旁证：`grammar.not` 的\"标注与 scope 形态\"一节有同一语法的摘要（`@![...]` 文件开头，绑定为模块属性），并明确指向 `vault::designs::language::annotation-syntax` 作为完整规则所在。\n";
