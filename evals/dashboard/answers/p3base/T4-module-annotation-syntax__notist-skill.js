window.EVAL_ANSWERS["p3base||T4-module-annotation-syntax__notist-skill"]="\n\n**语法**：文件开头（第一个有意义 token 之前）写 `@![#tag, ...]`，例如：\n\n```not\n@![#design, #wip, status = \"draft\"]\n\n= 标题\n```\n\n`!` 表示\"作用于所在模块\"（对应 Rust inner attribute）；属性列表内不用 id，tag/class/key=value 可任意多个。\n\n**权威定义出处**：`designs/language/annotation-syntax.not` 的「三种挂载位置」一节（第 3 条：模块 `@![...]`）。`grammar.not`「声明」一节仅以一行转引该处（\"模块属性：`@![...]` 见 #<vault::designs::language::annotation-syntax>\"）。\n";
