window.EVAL_ANSWERS["p3base||T4-module-annotation-syntax__notist-enforced"]="\n\n**语法**：在文件开头（第一个有意义的 token 之前）写 `@![...]`，内部是属性列表：\n\n```not\n@![#design, #wip]\n```\n\n`!` 表示\"作用于所在模块\"（inner attribute 语义），tag 可任意多个，也可混写 `key = value`；模块属性中不使用 id。\n\n**权威定义出处**：`designs/language/annotation-syntax.not`（模块 `vault::designs::language::annotation-syntax`）的「三种挂载位置」一节第 3 条\"模块：`@![...]`\"。`grammar.not`、`code-grammar.not` 等处对该语法的提及均回指此篇。\n";
