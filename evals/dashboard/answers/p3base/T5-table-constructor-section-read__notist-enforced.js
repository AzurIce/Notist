window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["p3base||T5-table-constructor-section-read__notist-enforced"]="\n\n**结论**（出自函数参考 `functions.not`，模块 `vault::functions` 的「table-cell / table」节）：\n\n- **列数**：由 `columns`（Int，必填）控制——每 `columns` 个 cell 组成一行。\n- **表头**：由 `header`（Bool，默认 `false`）控制——决定首行是否进入表头。\n\n完整签名：\n```text\ntable(columns: Int, header: Bool = false, align: String? = none,\n      trailing body: Content) -> Content\n```\n\n出处：`functions.not`（第 178–207 行，§table-cell / table）。\n";
