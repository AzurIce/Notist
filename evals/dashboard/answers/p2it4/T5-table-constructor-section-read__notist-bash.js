window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["p2it4||T5-table-constructor-section-read__notist-bash"]="\n\n**结论：**\n\n`table` 构造器签名为：\n\n```text\ntable(columns: Int, header: Bool = false, align: String? = none, trailing body: Content) -> Content\n```\n\n- **列数**：由 `columns`（Int，必填）控制——body 中每 `columns` 个 `table-cell` 组成一行。\n- **表头**：由 `header`（Bool，默认 `false`）控制——决定首行是否进入表头。\n\n**出处：** 文档文件 `functions.not`（模块 `vault::functions`），节标题「== table-cell / table」（约第 177–185 行）。\n\n补充：`align` 是逗号分隔的列对齐串（数量必须等于 `columns`）；表格无 caption，需要题注时用 `figure` 包裹。\n";
