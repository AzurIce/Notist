window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["p2it4||T4-module-annotation-syntax__md-bash"]="\n\n## 结论\n\n**1. 确切语法：模块属性 `@![...]`，必须写在文件开头**\n\n```not\n@![#design, #wip, status = \"draft\"]\n\n= 标题\n```\n\n规则要点：\n- `@![...]` 必须出现在**文件开头**（第一个有意义的 token 之前）——模块元数据可被索引器快速扫描，不依赖求值；\n- `!` 表示\"作用于所在模块\"（对应 Rust inner attribute 语义），绑定到根 scope；\n- 内部是统一的属性列表语法（`id | #tag | .class | key = value`，逗号分隔），其中 **不使用 id**——模块本身已是可寻址目标（Module target）；\n- 给整个文档挂标签就是往列表里放 `#tag`，可任意多个。\n\n**2. 权威定义出处：`designs/language/annotation-syntax.md`（L8 标注语法独立成篇）**\n\n- 节标题：`== 属性 → === 三种挂载位置` 第 3 条\"模块：`@![...]`\"（含示例与全部约束）；\n- 同文 `== 语法边界` 节定义了 lexer 区分（`@!` 后必须跟 `[`）与错误恢复；\n- `== 与查询的关系` 节说明模块属性随 ModuleResult 发布、可不求值读取。\n\n旁证（非权威定义，仅引用）：`grammar.md:212/221`、`cheatsheet.md:97`（速查示例 `@![#design, status = \"draft\"]`）、`intro.md:153`、`designs/language/property-table.md:25`（模块属性作为模块级条目进入属性表，随求值结果发布为模块元数据）。\n";
