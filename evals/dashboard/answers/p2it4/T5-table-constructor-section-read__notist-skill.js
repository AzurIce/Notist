window.EVAL_ANSWERS["p2it4||T5-table-constructor-section-read__notist-skill"]="\n\n**结论**（出自函数参考 `functions.not`，模块 `vault::functions` 的 “== table-cell / table” 一节，第 177–206 行）：\n\n`table` 构造器签名为：\n\n```text\ntable(columns: Int, header: Bool = false, align: String? = none,\n      trailing body: Content) -> Content\n```\n\n- **列数**：由 `columns`（Int，必填）控制——body 中每 `columns` 个 `table-cell` 组成一行。\n- **表头**：由 `header`（Bool，默认 `false`）控制——决定首行是否进入表头。\n\n出处：文档文件 **functions.not**（`vault::functions`，节标题 “table-cell / table”）。\n\n补充：同文件还说明 `align` 是逗号分隔的列对齐串（数量必须等于 `columns`）；表格题注需外层用 `figure` 包裹。注意设计文档 `designs/plugin-system/core.not` 中有一处旧签名（`header: Bool = false, align: String?`），但函数参考以 `functions.not` 为准。\n";
