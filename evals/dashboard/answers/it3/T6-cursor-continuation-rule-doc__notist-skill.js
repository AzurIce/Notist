window.EVAL_ANSWERS["it3||T6-cursor-continuation-rule-doc__notist-skill"]="\n\n## 续读行为\n\nCollection 类命令（search / modules / outline / refs / read）只返回有界的一页，输出结尾是 `continue: notist next <TOKEN>` 或 `complete`。续读时执行 `notist next <TOKEN>`：\n\n- token 是 opaque、base64url、带完整性校验的自包含句柄，恢复原 query/selector/scope/filter/grouping/order 与 offset——**不重复传原始参数**。\n- token 被拒时按错误码处理：source set 变化 → `cursor_stale`；schema 过期 → `cursor_expired`；损坏或不属于该 Vault/View → `invalid_cursor`，按错误提示走而不是重复同一调用。\n- 停止规则：正向事实查找在 `read` 取得充分 authored evidence 后即可停止，不必为形式上的 complete 读完所有页；只有当前页不足、任务要求穷举全部匹配、或结论依赖证明\"不存在\"时，才沿 cursor 读到 `complete`（或用 export）。\n\n## 权威设计文档\n\n- **文档**：`designs/host/query-contract.not`（Query Contract，A2 查询契约独立成篇；定义 `PageRequest`/`QueryPage`、默认与硬预算表、Snapshot Stamp 与可恢复 Cursor）。\n- **讲停止规则的节标题**：`Core Query Contract`（其中 `coverage.complete=false` 只表示当前页未覆盖完整匹配集合、\"一旦 read 已给出足够证据即可停止，否则沿 cursor 读到 complete\"的段落就在此节内）。\n\n旁证：CLI 命令面规范 `cli.not`（设计总览下的「继续读取：next」小节，以及「Agent 工作流」一节里的\"停止规则\"清单）是同一契约在命令层的投影，但权威设计以 query-contract.not 为准。\n";
