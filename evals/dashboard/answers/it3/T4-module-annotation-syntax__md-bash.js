window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["it3||T4-module-annotation-syntax__md-bash"]="\n\n## 结论\n\n**1. 确切语法：`@![...]`，写在文件开头（第一个有意义的 token 之前）**\n\n给整个文档模块挂标签：\n\n```not\n@![#mytag]\n```\n\n多个条目用逗号分隔，属性列表语法为：\n\n```text\n属性列表 = 条目（逗号分隔）\n条目     = id | #tag | .class | key = value\n```\n\n例如 `@![#design, #wip, status = \"draft\"]`。要点：\n- `!` 表示\"作用于所在模块\"（对应 Rust inner attribute 语义），绑定为模块属性（根 scope）；\n- **必须出现在文件开头**，这样索引器可不求值直接扫描模块元数据；\n- 属性列表中不使用 `id`——模块本身已是可寻址目标，id 无意义。\n\n**2. 权威定义出处：`designs/language/annotation-syntax.md`（《Annotation Syntax》独立成篇）**\n\n具体在「== 属性 → === 三种挂载位置」的第 3 条\"模块：`@![...]`\"，以及「== 语法边界」「== 与查询的关系」两节（token 区分、错误恢复、模块属性随 ModuleResult 发布）。\n\n其他文档只是摘要并回指该篇：`grammar.md`「标注与 scope 形态」节（\"完整规则见 annotation-syntax\"）、`intro.md`、`cheatsheet.md` 第 97 行速查条目。\n";
