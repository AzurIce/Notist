window.EVAL_ANSWERS["it3||T5-table-constructor-section-read__md-bash"]="\n\n**结论：**\n\n- **列数**：由 `columns` 参数控制（类型 `Int`，必填）。文档说明\"`table` 消费按行平铺的 `table-cell`（每 `columns` 个 cell 为一行）\"。\n- **表头**：由 `header` 参数控制（类型 `Bool = false`）。文档说明\"`header` 决定首行是否进入表头\"。\n\n完整签名：\n```text\ntable(columns: Int, header: Bool = false, align: String? = none, trailing body: Content) -> Content\n```\n\n**出处：** `functions.md`（内置函数参考），节标题 **`== table-cell / table`**。\n\n补充：该文档还说明 `align` 是逗号分隔的列对齐串（数量必须等于 `columns`），以及 pipe table 语法糖中\"header cell 数即 `columns`、分隔行必须与 header 同列数\"。\n";
