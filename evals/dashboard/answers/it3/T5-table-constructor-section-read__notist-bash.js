window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["it3||T5-table-constructor-section-read__notist-bash"]="\n\n**结论**（出自函数参考 `functions.not`，节标题 `== table-cell / table`，第 177–206 行）：\n\n- **列数**由参数 `columns`（Int，必填）控制——table 消费按行平铺的 table-cell，每 `columns` 个 cell 组成一行。\n- **表头**由参数 `header`（Bool，默认 `false`）控制——决定首行是否进入表头。\n\n完整签名：\n\n```text\ntable(columns: Int, header: Bool = false, align: String? = none,\n      trailing body: Content) -> Content\n```\n\n（另有可选参数 `align`：逗号分隔的列对齐串，数量必须等于 `columns`。）\n\n**出处**：文档文件为 **functions.not**（模块 `vault::functions`），节标题「table-cell / table」。\n\n补充：设计文档 `designs/plugin-system/core.not` 中也有同名签名，但函数参考正文在 functions.not。\n";
