window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["it3||T4-module-annotation-syntax__notist-skill"]="\n\n## 结论\n\n**确切语法**：在文件开头（第一个有意义的 token 之前）写模块级标注 `@![...]`，标签用 `#tag` 条目：\n\n```not\n@![#design, #wip]          ← 给整个模块挂标签（可多个，逗号分隔）\n\n= 标题\n```\n\n要点：\n- `@!` 表示\"作用于所在模块\"（对应 Rust inner attribute 语义），绑定为模块属性（根 scope）；\n- 必须出现在文件开头，这样索引器可快速扫描、不依赖求值；\n- 属性列表里**不使用 id**（模块本身已是可寻址目标），tag / class / key=value 均可任意多个，例如 `@![#design, status = \"draft\"]`。\n\n**权威定义出处**：\n- 主文档：`vault::designs::language::annotation-syntax`（文件 `designs/language/annotation-syntax.not`），节「三种挂载位置」第 3 条\"模块：`@![...]`\"（约 58–71 行），含位置约束、`!` 语义与 id 规则；「语法边界」节还定义了 `@![` 的 lexer token 区分与错误恢复。\n- 语法总览佐证：`vault::grammar`（`grammar.not`）「标注与 scope 形态」节——\"模块：`@![...]` 文件开头，绑定为模块属性（根 scope）\"。\n";
