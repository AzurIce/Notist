window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["it3||T6-cursor-continuation-rule-doc__notist-bash"]="\n\n## 续读行为是怎么回事\n\n- Collection 类命令（`search`、`modules`、`outline`、`references` 等）返回有界的一页，输出末尾给出 `continue: notist next <TOKEN>` 或 `complete`。\n- 续读统一动作是 `notist next <TOKEN>`：token 是 opaque、base64url、带完整性校验的自包含 cursor，恢复原 query/selector/scope/filter/grouping/ordering 与 offset，**不需要重传原查询参数**。\n- token 绑定 Vault/View identity、source-set fingerprint 与 schema/query 指纹：源集变化返回 `cursor_stale`，schema 过期/不匹配或 token 损坏返回 `invalid_cursor`（CLI 侧还有 `cursor_expired`）。\n- 页窗口由服务端版本化 runtime policy 决定（如 search 默认 8 items / 16 KiB，上限 100 / 64 KiB），CLI 不暴露 page/budget 参数。\n- **停止规则**：`coverage.complete=false` 只表示当前页没覆盖完整匹配集，不等于\"未找到\"。正向事实查找在 `read` 取得充分证据后即可停止；只有当前页不足、任务要求穷举、或结论依赖证明\"不存在\"时，才需沿 cursor 读到 `complete`。\n\n## 权威设计文档与节标题\n\n- **文档**：`designs/host/query-contract.not`（标题 *Query Contract*，A2 查询契约的独立成篇；分页预算、cursor 与停止规则均在此定义）。\n- **讲停止规则的节**：`== Core Query Contract`（其中规定 `coverage.complete` 语义、`stop_reason` 区分 item_limit / byte_budget / query-specific 上限，以及各 operation 的默认/硬上限预算表）。\n\n（CLI 侧的操作化描述在 `cli.not` 的「继续读取：next」与「Agent 工作流 → 停止规则」，但权威设计以 query-contract.not 为准。）\n";
