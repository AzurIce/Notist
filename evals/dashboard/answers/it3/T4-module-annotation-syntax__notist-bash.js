window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["it3||T4-module-annotation-syntax__notist-bash"]="\n\n## 结论\n\n**确切语法**：在文件开头（第一个有意义的 token 之前）写模块属性 `@![...]`，标签用 `#tag` 条目、逗号分隔：\n\n```not\n@![#design, #wip]          ← 给整个模块挂标签\n\n= 标题\n```\n\n要点：\n- `!` 表示\"作用于所在模块\"（对应 Rust inner attribute 语义），绑定为模块属性（根 scope）；\n- 必须出现在文件开头，这样索引器可快速扫描、不求值即可读取（随 ModuleResult 发布）；\n- `#tag` 可任意多个，也可混写 `.class`、`key = value`；属性列表中**不使用 id**（模块本身已是可寻址目标）；\n- 文档中的完整示例：`@![#design, #wip, status = \"draft\"]`。\n\n**权威定义出处**：`designs/language/annotation-syntax.not`（模块 `vault::designs::language::annotation-syntax`），节标题「**三种挂载位置**」第 3 条\"模块：`@![...]`\"，以及「语法边界」「与查询的关系」两节。\n\n其他文档只是引用它：`grammar.not` 和 `designs/language/code-grammar.not` 均写\"模块属性：`@![...]` 见 #<vault::designs::language::annotation-syntax>\"；`cheatsheet.not:97` 有一行速查 `@![#design, status = \"draft\"]    # 模块属性：文件开头`。\n";
