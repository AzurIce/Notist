window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["it2||T4-module-annotation-syntax__notist-skill"]="\n\n## 结论\n\n**1. 确切语法：模块级标注用 `@![...]`，必须写在文件开头**\n\n```not\n@![#design, #wip, status = \"draft\"]\n\n= 标题\n```\n\n要点：\n- `@![...]` 必须出现在文件开头（第一个有意义的 token 之前），绑定为模块属性（根 scope）；\n- `!` 表示\"作用于所在模块\"（对应 Rust inner attribute 语义）；\n- 方括号内是属性列表：`#tag`（标签，任意多个）、`.class`、`key = value` 逗号分隔；**不使用 id**——模块本身已是可寻址目标，id 无意义。\n\n**2. 权威定义出处：`vault::designs::language::annotation-syntax`（文件 `designs/language/annotation-syntax.not`，标题 \"Annotation Syntax\"）**\n\n- 具体在「== 属性 → === 三种挂载位置」一节的第 3 条\"模块：`@![...]`\"，含完整规则（位置要求、`!` 语义、id 禁用）；\n- 「== 语法边界」一节补充了 lexer 区分（`@![` vs `@[` vs `@id`）与错误恢复规则。\n\n旁证：`grammar.not` 的「== 标注与 scope 形态」一节有同一规则的摘要（`@![...]` 文件开头，绑定为模块属性），并明确指向 annotation-syntax 作为完整规则出处。\n";
