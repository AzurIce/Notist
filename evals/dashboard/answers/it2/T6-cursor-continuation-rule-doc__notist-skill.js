window.EVAL_ANSWERS["it2||T6-cursor-continuation-rule-doc__notist-skill"]="\n\n## 续读行为\n\n- Collection 类命令（search/modules/outline/refs…）只返回一个有界页：item 数或字节预算放不下时，stdout 以 `continue: notist next <TOKEN>` 结尾；已覆盖全部匹配则输出 `complete`。\n- 续读只执行 `notist next <TOKEN>`：token 是 opaque、base64url、带完整性校验的自包含句柄，恢复原 query/selector/scope/filter/grouping/ordering 与 offset，**不需要重复原命令参数**。\n- token 失效按 error code 恢复：source set 变化 → `cursor_stale`；schema 过期 → `cursor_expired`；损坏/跨 Vault-View → `invalid_cursor`（exit 3），按 hint 走，不重复同一调用。\n- **停止规则**：正向事实查找一旦 `read` 给出足够 authored evidence 即可停，不必为形式上的 complete 读完所有页；只有当前页不足、任务要求穷举、或结论依赖证明\"不存在\"时，才沿 cursor 读到 `complete`。\n\n## 权威设计文档与节标题\n\n- **文档**：`designs/host/query-contract.not`（Query Contract）。依据：`search-retrieval-index.not` 开头明确\"分页预算、cursor 与 selector 见 #<vault::designs::host::query-contract>\"；该篇集中定义了 PageRequest/QueryPage 模型、默认与硬预算表、可恢复 Cursor 及停止规则。（`cli.not` 的「继续读取：next」只是 CLI 命令面规格，非分页设计权威。）\n- **讲停止规则的节标题**：`Core Query Contract`（其中 `coverage.complete=false` 段落规定何时可停止、何时必须读到 complete；`stop_reason` 区分 item_limit / byte_budget）。\n\n出处：`designs/host/query-contract.not` §Core Query Contract、§Snapshot Stamp 与可恢复 Cursor；`cli.not` §继续读取：next、§Runtime policy：默认预算。\n";
