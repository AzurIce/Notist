window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["it2||T6-cursor-continuation-rule-doc__notist-bash"]="\n\n**续读行为**（`cli.not`「继续读取：next」节 + 查询契约）：\n- Collection 命令（search/modules/outline/references…）只返回一个有界页，页尾给出 `continue: notist next <TOKEN>` 或 `complete`。\n- 看不完就执行 `notist next <TOKEN>`：token 是 opaque、base64url、带完整性校验，自包含恢复原 query/selector/scope/filter/ordering 与 offset——不需要重复原命令参数，daemon 也不保存 page session。\n- 错误分类：source set 变化 → `cursor_stale`；token schema 过期 → `cursor_expired`；损坏或不属于该 Vault/View → `invalid_cursor`。\n- 停止：`coverage.complete=false` 只表示当前页没覆盖全部匹配，不等于\"未找到\"；read 已拿到充分证据即可停止，只有当前页不足、任务要求穷举或结论依赖证明\"不存在\"时才需沿 cursor 读到 complete。\n\n**权威设计文档**：`designs/host/query-contract.not`（标题 *Query Contract*，模块 `vault::designs::host::query-contract`）。依据：`search-retrieval-index.not` 开篇明确\"分页预算、cursor 与 selector 见 #<vault::designs::host::query-contract>\"。\n\n**讲停止规则的那一节标题**：`== Core Query Contract`（其中定义 `QueryPage.coverage { complete, stop_reason }`，并规定何时可停止、何时必须沿 cursor 读到 complete，续读动作统一为 `notist next <TOKEN>`）。\n";
