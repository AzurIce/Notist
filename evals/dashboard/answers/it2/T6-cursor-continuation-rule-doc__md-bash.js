window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["it2||T6-cursor-continuation-rule-doc__md-bash"]="\n\n**续读行为**：Collection 类命令（search/locate/modules 等）一页放不下时，结果带 `continue: notist next <TOKEN>`（或 `complete`）。续读统一用 `notist next <TOKEN>`：token 是 opaque、自包含的，恢复原 query/selector/scope/filter/ordering 与 offset；source set 变化报 `cursor_stale`、token schema 过期报 `cursor_expired`、损坏或不属于该 Vault/View 报 `invalid_cursor`。停止规则：`coverage.complete=false` 只表示当前页没覆盖完整匹配集，不等于\"未找到\"——一旦 `read` 已给出足够证据即可停止；只有当前页不足、任务要求穷举或结论依赖证明\"不存在\"时才需沿 cursor 读到 complete（`stop_reason` 区分 `item_limit`/`byte_budget`，属内部观测字段）。\n\n**权威文档与节标题**：\n- 文档：`designs/host/query-contract.md`（标题 *Query Contract*）——search-retrieval-index 明确\"分页预算、cursor 与 selector 见 query-contract\"，cli.md 也指查询契约以它为准。\n- 讲停止规则的那一节：**`== Core Query Contract`**（其中 `coverage.complete=false … 调用者可以停止…` 段落，含默认/硬上限预算表：search 8 items / 16 KiB，最大 100 / 64 KiB）。\n";
