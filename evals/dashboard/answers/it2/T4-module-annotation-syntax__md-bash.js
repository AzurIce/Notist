window.EVAL_ANSWERS=window.EVAL_ANSWERS||{};
window.EVAL_ANSWERS["it2||T4-module-annotation-syntax__md-bash"]="\n\n## 结论\n\n**1. 确切语法：模块属性 `@![...]`，写在文件开头**\n\n给整个文档模块挂标签时，在文件最开头（第一个有意义的 token 之前）写：\n\n```not\n@![#design, #wip]\n```\n\n要点（来自权威文档）：\n- `!` 表示\"作用于所在模块\"，对应 Rust inner attribute 语义；\n- 必须出现在文件开头——这样索引器可快速扫描、不求值即可读取，模块属性随 ModuleResult 发布为稳定元数据；\n- `#tag` 条目可任意多个，用于归类与查询（编辑器/Agent 按属性检索）；\n- 模块属性列表中**不使用 id**——模块本身已是可寻址目标（Module target），id 无意义；\n- lexer 层面 `@![`、`@[`（块级前缀）、`@id`（行内 postfix）是三种不同 token 序列，`@!` 后必须跟 `[`。\n\n**2. 权威定义出处：`designs/language/annotation-syntax.md`（\"Annotation Syntax\"）**\n\n具体在「== 属性 → === 三种挂载位置 → 3. 模块：`@![...]`」一节，含完整示例与约束。其他文档只是引用它：\n- `grammar.md`（L145、L212）：\"模块属性：`@![...]` 见 annotation-syntax\"；\n- `intro.md`（L153）：概述三种挂载位置后指向 annotation-syntax；\n- `cheatsheet.md`（L97）：速查行 `@![#design, status = \"draft\"]  # 模块属性：文件开头`。\n";
