window.EVAL_ANSWERS["p2it5||T4-module-annotation-syntax__md-bash"]="\n\n**语法**：在文件开头（第一个有意义的 token 之前）写 `@![...]`，标签用 `#tag` 条目。给整个模块挂一个标签就是：\n\n```not\n@![#design]\n```\n\n多个条目逗号分隔，如 `@![#design, #wip, status = \"draft\"]`。要点：\n- `!` 表示\"作用于所在模块\"（对应 Rust inner attribute 语义），绑定为模块属性（根 scope）；\n- 必须出现在文件开头，这样索引器可不求值直接扫描模块元数据（随 ModuleResult 发布）；\n- 属性列表里**不用写 id**——模块本身已是可寻址目标，id 无意义；tag、class、key=value 任意多个。\n\n**权威定义出处**：`designs/language/annotation-syntax.md`（《Annotation Syntax》）的「三种挂载位置」第 3 条\"模块：`@![...]`\"，含文件开头约束、`!` 语义与 id 规则。\n\n旁证：`grammar.md`「标注与 scope 形态」一节给出同一语法并明确\"完整规则见 annotation-syntax\"；`cheatsheet.md`「标注速查」也列了 `@![#design, status = \"draft\"]  # 模块属性：文件开头`。\n";
