window.EVAL_ANSWERS["p2it5||T4-module-annotation-syntax__notist-bash"]="\n\n## 结论\n\n**确切语法：`@![...]`，必须写在文件开头（第一个有意义的 token 之前）**\n\n```not\n@![#design, #wip, status = \"draft\"]\n\n= 标题\n```\n\n要点（来自权威文档）：\n- `!` 表示\"作用于所在模块\"，对应 Rust inner attribute 语义；整个属性列表绑定为**模块属性（根 scope）**\n- 必须出现在文件开头——模块元数据可被索引器快速扫描，不依赖求值\n- 属性列表内**不使用 id**（模块本身已是可寻址目标）；标签写作 `#tag`，可与 `.class`、`key = value` 混用\n- lexer 层面 `@![` 与 `@[`、`@id` 是三种不同 token；`@!` 后不是 `[` → 诊断\n\n**权威定义出处：`designs/language/annotation-syntax.not`（《Annotation Syntax》）**，具体在「== 属性 → === 三种挂载位置」第 3 条\"模块：`@![...]`\"一节。\n\n旁证（均指向该篇为完整规则）：\n- `intro.not` 第 153 行：\"模块属性 `@![...]`（文件开头）。完整语法见 #<vault::designs::language::annotation-syntax>\"\n- `grammar.not`「标注与 scope 形态」节：`@![...]` 文件开头，绑定为模块属性；引用索引中 `@[...] / @![...] → 标注（annotation-syntax）`\n- `cheatsheet.not`「标注速查」：`@![#design, status = \"draft\"]  # 模块属性：文件开头`\n";
