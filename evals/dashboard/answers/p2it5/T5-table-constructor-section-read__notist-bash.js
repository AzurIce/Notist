window.EVAL_ANSWERS["p2it5||T5-table-constructor-section-read__notist-bash"]="\n\n**结论**（出处：`functions.not`，标题 \"Built-in Functions\"，节 `== table-cell / table`）：\n\n- **列数**由参数 `columns: Int` 控制（必填；每 `columns` 个 cell 折成一行）。\n- **表头**由参数 `header: Bool = false` 控制（决定首行是否进入表头）。\n\n完整签名：\n```text\ntable(columns: Int, header: Bool = false, align: String? = none, trailing body: Content) -> Content\n```\n\n补充：`align` 是逗号分隔的列对齐串（数量必须等于 `columns`）。该说明出现在文档 **functions.not** 中；另在 `designs/plugin-system/core.not`（§table）有同族签名记录，但函数参考正文以 functions.not 为准。\n";
