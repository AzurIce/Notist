target/
# notist build 的默认输出目录；只锚定仓库根，vendored 插件资产里的
# dist/（如 plugins/math/assets/vendor/typst-ts/dist）不受影响。
/dist/
.direnv
result
plugins/*/lib/
.env

# ZCode session state
.zcode/

# fastembed 的默认模型缓存（已被用户缓存目录取代，防误提交）
.fastembed_cache/
