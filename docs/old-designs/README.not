= Old Design Records（第一代设计档案）

本目录保存 Notist 第一代设计记录（D0001–D0016）。它们是项目早期随实现逐步积累的设计，缺少全局一致的视野（例如后来的"文档即程序"求值模型在原设计中并不存在），因此在 2026-07 被整体归档，不再治理当前项目。新一代设计从 [[vault::designs]] 的 D0001 重新开始。

本目录仅作历史与演进证据保留。[[vault::grammar]]、[[vault::functions]] 与 [[vault::types]] 仍是当前工具 surface 的精确参考；设计层面的当前方向见 [[vault::designs]]。

其中 D0002（scope 与标注）、D0003（vault 与 module）、D0005（lowering 与 structuring）、D0006（type system）、D0007（Notist.toml）、D0008（language server）、D0009（preview 与 HTML）、D0010（String 字面量与 raw）、D0011（Analyzer 与 snapshot）、D0012（daemon 与 client interface）、D0013（Agent Skill）、D0014（agent-facing CLI/MCP/search）、D0015（可编程文档核心）、D0016（表面语言与核心语法）的内容已被新一代设计（[[vault::designs]] D0001–D0010）完整吸收或明确推翻，已移入 [[self::archive]]；其中 D0006 的 Array/Dict/Union 等推迟主题将在未来重新设计（不从此篇恢复），D0014 的 MCP 部分因产品决策（不做 MCP）而作废。

== 第一代历史设计

除 [[self::D0004-core-functions-and-syntax-sugar]]（core namespace 拍板与 plugin 边界的参考）外，全部设计已被新一代设计（[[vault::designs]] D0001–D0010）吸收或推迟重写，见 [[self::archive]]。

- [[self::D0004-core-functions-and-syntax-sugar]]：first-class core Function、稳定 core namespace、Element constructor 与宿主 syntax sugar 的边界。

== 更早的历史记录

第一代早期方案（Markdown/Rua）见 [[self::archive]]，其中保存了更早失效的基础假设。
