= Old Design Records（历史说明）

本目录不再保存第一代设计文档的全文。第一代设计已经由当前 [[vault::designs]] 完整吸收、推翻或重新设计；其历史文件只保留在 Git 历史中，不留在工作树里。

需要追溯时，在旧提交中查看：

```shell
git log --oneline -- docs/old-designs/archive
git show <commit>:docs/old-designs/archive/<file>.not
```

当前设计的入口是 [[vault::designs]]，语义目录为 `language/`、`world/` 与 `host/`。
