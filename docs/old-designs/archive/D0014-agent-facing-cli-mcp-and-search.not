= D0014: 面向人和 Agent 的 CLI、MCP 与检索接口

一个 Agent 为了回答“WorkspaceSnapshot 为什么不可变”而探索文档时，正常路径应当是：

```shell
notist search "workspace snapshot" docs
notist outline vault::old-designs::D0011 docs
notist read vault::old-designs::D0011 docs --from-line 1 --lines 80
```

第一条命令返回少量有排序的候选和短 excerpt，第二条只展开一个 Module，第三条才读取被选中的原文。人在终端中看到的是带颜色、相对路径和续读提示的紧凑页面；脚本看到的是稳定 JSON；MCP Host 看到的是有 output schema、大小上界和 citation 的 tool result。三种调用者不使用三套查询语义。

Notist 因此把查询产品分为三种形态：

```text
ordinary query
  bounded, composable, resumable

human rendering
  readable projection of the same result

full export
  explicit file or artifact, never an accidental tool result
```

普通查询始终有默认预算与服务端硬上界。调用者可以用参数在硬上界内收紧或扩大页面，但不能关闭上界；需要完整数据时必须显式进入 export。这个规则不是只为 Agent 优化 token，它同时避免终端被大结果淹没、脚本意外缓存整库正文，以及 MCP tool output 在后续对话中反复回放。

本文拥有 core query contract、普通 CLI 命令、MCP tool/resource 投影、全文检索和输出预算。[[vault::old-designs::D0011-analyzer-and-workspace-snapshot]] 继续定义 snapshot、identity 与 query consistency；[[vault::old-designs::D0012-daemon-and-client-interfaces]] 继续定义每 Vault daemon、Analyzer View 和协议适配层；[[vault::old-designs::D0013-self-contained-agent-skill]] 继续定义官方 Skill 与文档 Vault 的发布方式。把这些接口放在单独的设计中，是因为查询与输出会共同演进，而不应反过来改变 daemon 拓扑或 Skill 包装。

== 一个 Core Query Contract

CLI 与 MCP 都把输入投影到协议无关的 core operation。Core operation 接受一个已经捕获的 WorkspaceSnapshot 和结构化 request，返回 typed result；adapter 只负责参数拼写、协议 framing 和展示。

```text
CLI args ─┐
          ├─> CoreRequest -> captured WorkspaceSnapshot -> QueryResult
MCP JSON ─┘                                             ├─> CLI text/JSON
                                                       └─> MCP result
```

`--format`、颜色、pager 和 MCP text mirror 都不能改变匹配集合、排序、source range 或错误分类。CLI 与 MCP 可以有意提供不同数量的入口：CLI 包含 daemon、build、preview、debug 和 export 等面向运维或人的命令；MCP 只暴露适合 model-controlled 调用的窄 primitive。相同能力一旦出现在两个 surface 上，就必须共享 request type、selector、排序、cursor 和 result record。

Core collection query 使用以下概念模型：

```text
PageRequest {
  limit
  max_bytes
  cursor?
}

QueryPage<T> {
  snapshot
  items: [T]
  page { requested_limit, applied_limit, returned, has_more, next_cursor? }
  budget { requested_bytes, applied_bytes, logical_bytes, exhausted }
  coverage { complete, stop_reason }
  hints? [String]
}
```

`limit` 限制 item 数，`max_bytes` 限制规范化 logical result 的 UTF-8 JSON bytes。两者同时生效；二十个超长 excerpt 不能利用 item limit 绕过 byte limit。服务端在加入 item 前计算最终序列化大小，并为 page metadata 与 cursor 预留空间，绝不截断已经序列化的 JSON、UTF-8 scalar 或 source record。

`coverage.complete=false` 只表示当前页面没有覆盖完整匹配集合，不表示“未找到”。它也不要求正向事实查找机械地读取所有页面：一旦 `read` 已经给出足够 authored evidence，调用者可以停止。只有当前页不足、任务要求穷举，或者结论依赖证明“不存在”时，才需要沿 cursor 读到 complete。这个区别防止缩小默认页后反而产生更多无意义历史回放，同时保留否定与完备性声明的证据门槛。`stop_reason` 区分 `item_limit`、`byte_budget` 与 query-specific 上限。单个 excerpt 是否完整由 item 自己的 `excerpt_truncated` 表达，不能把所有情况压成一个含糊的 `truncated`。

普通 collection 的默认 `limit` 和硬上限如下：

| Operation | Default items | Maximum items | Default logical bytes | Maximum logical bytes |
| --- | ---: | ---: | ---: | ---: |
| modules | 20 | 100 | 16 KiB | 64 KiB |
| search | 8 | 100 | 16 KiB | 64 KiB |
| outline | 100 | 100 | 16 KiB | 64 KiB |
| references | 20 | 100 | 16 KiB | 64 KiB |
| diagnostics detail | 20 | 100 | 16 KiB | 64 KiB |
| read | 120 lines | 1,000 lines | 16 KiB | 64 KiB |

这些值是可版本化的产品 policy，不是语言常量。客户端可以请求更小或更大的值，response 总是返回实际应用值。`max_bytes` 优先于 item 或 line limit；一个 96 KiB source 必须通过 continuation 分块读取。

== Snapshot Stamp 与可恢复 Cursor

一次 query 在开始时捕获一个 snapshot，并且所有 item、excerpt、range、source fingerprint 与计数都来自它。公开 stamp 同时携带对用户有帮助的 revision 和可跨进程验证的内容 identity：

```text
SnapshotStamp {
  vault_id
  view_kind
  revision
  source_set_fingerprint
  daemon_instance?
}
```

Revision 只在所属 View 与 daemon instance 内有意义；cursor 因而不能只保存 revision。`source_set_fingerprint` 覆盖参与该 snapshot 的 canonical source identity、内容 fingerprint 与影响查询的配置 identity，使 `--no-daemon` 的下一次独立进程也能判断当前 source set 是否仍是同一个语义世界。

Cursor 是 opaque、self-contained、带 schema version 和完整性校验的 token。公开 token 使用紧凑 binary payload 再做 base64url；字段名不需要在每次翻页时重复进入模型上下文。Reader 仍可在迁移期接受旧的 base64url JSON cursor，但只签发最新版本。它绑定：

- Vault、View kind 与 source-set fingerprint。
- operation、selector、query、filter、field、mode 与排序 fingerprint。
- tokenizer、ranking 和 index schema version。
- 上一 item 的稳定排序 key。

`limit`、`max_bytes` 和 excerpt 大小只控制下一页窗口，可以在继续读取时改变；任何会改变匹配集合或排序的参数都必须与 cursor 一致。source set 已变化时返回 `cursor_stale`，cursor schema 或 query fingerprint 不匹配时返回 `invalid_cursor`。服务不能在新 snapshot 中猜测一个相近 byte offset 继续，也不能把 cursor 当成权限凭证。

Source-order query 的稳定 key 是 ModulePath、Vault-relative path、match range 和 record kind。Ranked query 的 key 是固定精度 score 的降序值、ModulePath、range 和 search-unit identity；浮点 backend score 必须先按 ranking version 规定的方式量化，再参与排序与 cursor。这样相同 index generation 中的 tie 不依赖 hash iteration、线程调度或 segment 枚举顺序。

== Selector 与 Citation

CLI positional selector 和 MCP selector string 使用同一种紧凑表示，只接受三类精确身份：完整 ModulePath、Vault-relative `.not` path，或 ModulePath 加 Annotation label。`search`、`list_modules` 和 location 输出的 ModulePath 因而可以直接作为 `read_source`、`get_outline` 与 `get_references` 的 selector，不需要调用者先猜出另一种 JSON shape。短 basename、模糊路径和“最接近的 Module”不能静默解析；零匹配返回 `not_found`，多匹配返回 `ambiguous_selector` 和有界候选。模糊发现属于 search，不属于 selector resolution。

```text
ModuleSelector  { module: "vault::old-designs::D0011" }
PathSelector    { path: "designs/D0011-analyzer-and-workspace-snapshot.not" }
SectionSelector { module: "vault::old-designs::D0011", label: "snapshot" }
```

上面的对象仍作为 MCP 的兼容输入保留，方便已经生成结构化 selector 的 Host；规范输入则是等价字符串 `vault::old-designs::D0011`、`designs/D0011-analyzer-and-workspace-snapshot.not` 或 `vault::old-designs::D0011#snapshot`。Tool schema 把字符串放在 `oneOf` 的首个分支，description 直接说明可复制上游 `module` 字段。类型错误必须指出参数名、允许的字符串/对象形状与实际 JSON 类型，不能只返回无上下文的 `expected object parameters`。

所有可作为证据或编辑位置的 record 使用统一 location：

```text
Location {
  module
  relative_path
  byte_range
  line_range?
  label?
  source_fingerprint
  snapshot
}
```

Machine result 默认不返回 canonical absolute path，也不为每个 location 重复整篇 source。excerpt 是候选选择信息，不是独立事实来源；search 与 references 的 tool description 必须引导调用者在形成事实性回答前使用 `read_source`。读取结果保留精确 byte range 和 fingerprint，使引用、后续 query 与 edit precondition 能指向同一 authored source。

== Search 不是一种模式

BM25、模糊匹配与精确子串解决不同问题。把它们混成一个隐式“智能搜索”会使结果无法预测，也无法解释为什么某个 identifier 被召回。公开 search 因而有四种显式模式：

| Mode | 语义 | 排序 | 典型用途 |
| --- | --- | --- | --- |
| `lexical` | fielded token match + BM25 | relevance | 默认自然语言与主题检索 |
| `exact` | literal UTF-8 substring | source order | Module 名、错误消息、源码片段 |
| `fuzzy` | lexical BM25 + bounded term expansion | relevance | 英文、拼音或 identifier 拼写错误 |
| `regex` | Rust-compatible regular expression | source order | CLI 专家级源码调查 |

CLI 的规范参数是 `--mode lexical|exact|fuzzy|regex`。`--exact`、`--fuzzy` 与 `--regex` 是互斥的可读 alias，不能与 `--mode` 同时使用。默认是 `lexical`，不是为了假装理解语义，而是因为普通检索通常需要相关性排序；用户需要旧式严格字符串匹配时明确使用 `--exact`。

```shell
notist search "workspace snapshot" docs
notist search --exact "WorkspaceSnapshot" docs
notist search --fuzzy "langauge sever" docs
notist search --regex "D00[0-9]+" docs
```

=== Lexical：可解释的默认相关性

Lexical mode 把 query 当普通文本，不提供隐藏的 query mini-language。引号、冒号、减号和括号默认也是待分词文本；phrase 或源码序列使用 `--exact`，regex 使用 `--regex`。这避免用户输入文档内容时意外触发 boolean operator。

默认 `--operator all` 要求每个原始 query term group 至少命中一个 token variant；`--operator any` 用于宽召回。BM25 只在满足 boolean 条件的候选之间排序。短 query 不做 stop-word 删除，技术标识符不做 stemming；否则 `not`、类型名、数字和缩写会在知识库中丢失真实含义。

可检索字段为：

| Field | 内容 | 默认权重 |
| --- | --- | ---: |
| `title` | Module 的语义标题 | 5.0 |
| `heading` | 当前 Heading 与祖先 Heading | 4.0 |
| `label` | Annotation ID | 6.0 |
| `module` | ModulePath segment 与完整路径 | 6.0 |
| `path` | Vault-relative path 与文件 stem | 3.0 |
| `tag` | typed tag/metadata | 4.0 |
| `body` | 普通 authored Content | 1.0 |
| `raw` | inline/fenced Raw | 0.7 |
| `comment` | source comment | 不默认检索 |

`--fields` 接受逗号分隔或重复值，控制参与匹配的字段，不控制 response projection。默认字段是除 `comment` 外的全部字段。字段权重属于 ranking version，不暴露任意用户 boost；任意 boost 会使 cursor、benchmark 和不同 client 的结果难以复现。未知字段立即报 usage error，不能静默忽略。

BM25 的参数、字段权重和长度归一化都记录在 `ranking_version` 中。每个 hit 返回固定精度 `score`；同页共有的 `ranking_version` 和 `index_stamp` 只在 search page metadata 返回一次。Score 只能在相同 query、ranking version 与 index stamp 中比较，不能被解释为概率。把 provenance 提升到页级不会丢失可解释性，却避免每个 hit 重复完全相同的 index identity。

=== Tokenizer 同时照顾中文与标识符

Tokenizer 对 index 与 query 使用同一套 versioned pipeline。Unicode normalization 只作用于检索 term，绝不改写 authored source 或 citation range：

- 使用 Unicode NFKC 与 case folding 生成匹配 term，同时保留原 source byte offsets。
- Latin、数字和其他 word sequence 按 punctuation、snake_case、kebab-case 与 camelCase 边界拆分，并同时保留完整 identifier 与组成部分作为同一 term group 的 variant。
- 连续 Han 字符使用 overlapping bigram；单个 Han 字符保留 unigram。混合中英文在 script boundary 分开。
- 路径和 ModulePath 额外保留完整 path、segment、stem 与 `::`/directory 分段。
- 不做语言猜测、远程分词或词干还原；以后替换 tokenizer 必须提升 version 并使旧 cursor 明确失效。

例如 `WorkspaceSnapshot` 同时产生完整 term 与 `workspace`、`snapshot`，`vault::研究生毕设` 同时保留 Module segment 和中文 bigram。Query 的 boolean 条件按原始 term group 计算，identifier 的完整 term 与拆分 term 是 variant，不会因为同时保留而让 `--operator all` 变成不可能满足。

=== Exact：真正的字面匹配

Exact mode 对选择字段的 authored text 执行非重叠 literal UTF-8 substring search，默认大小写敏感，与现有严格字符串查找一致。`--ignore-case` 显式启用 Unicode case-folded 比较；服务仍把 normalized match 映射回原 source 的精确 byte range。空 query 非法。

Exact 不返回 relevance score，按 source order 排序。它不依赖全文索引，因此在 index 正在重建时仍然可用，并适合作为诊断与 bootstrap 路径。它仍受 scope、field、item、excerpt 与 byte budget 约束。

=== Fuzzy：受控扩展，不是另一种评分器

Fuzzy mode 先执行与 lexical 相同的分词和 boolean 语义，再对适合纠错的 query term 查询 index term dictionary。精确 term 始终保留；扩展 term 按 edit distance、term document frequency 与词典顺序确定性选择，随后以距离 penalty 加入同一 BM25 计算。

默认最大 Damerau-Levenshtein distance 为 1；`--fuzzy-distance 2` 是公开最大值。长度小于四个 Unicode scalar 的 term、纯数字、单个 Han 字符和结构 delimiter 不做 fuzzy expansion。每个 term 最多扩展 32 个候选，整个 query 最多扩展 128 个候选；达到上限时 search page metadata 明确返回 `expansion_limited=true`。这些限制防止短词把词典几乎全部召回。

Fuzzy 主要修正 `langauge`、`WorkspaceSnaphot` 之类的 Latin/identifier typo。中文继续使用 lexical bigram，不把单个汉字的 edit distance 宣传为语义相似。Fuzzy 也不自动切换到向量、同义词或翻译。

=== Regex：有界的 CLI 专家能力

Regex mode 使用 Rust regex 语义，不支持 backreference 或 look-around，从算法层避免灾难性回溯。Pattern 长度、编译大小、扫描时间、item 数和 response bytes 都有上限；`--ignore-case` 通过 regex builder 实现。命中按 source order 返回精确 range。

Regex 是 CLI 的专家调查能力，不出现在 model-controlled MCP search schema 中。Agent 需要确定性 identifier 查找时使用 exact；需要自然语言或 typo 查找时使用 lexical/fuzzy。这个边界减少任意 pattern 的复杂度与提示注入面，同时不牺牲人类 shell 工作流。

== Scope、Excerpt 与 Search Result

`--scope` 是一个精确 ModulePath prefix，可以重复；多个 scope 取并集。`--fields` 在 candidate generation 前生效，不能先搜索整库再由 formatter 丢字段。访问控制同样必须在 candidate generation 前过滤；Vault 外路径永远不进入 index 或 result。

每个 search hit 表示一个可引用的 source region：

```text
SearchHit {
  location
  matched_field
  match_range?
  unit_range
  excerpt
  excerpt_range
  excerpt_truncated
  score?
}

SearchPageMetadata {
  group_by
  ordering
  ranking_version?
  index_stamp?
  expansion_limited
}
```

默认 excerpt 上限为 256 bytes，`--snippet-bytes` 可在 64 至 2048 bytes 内调整。截取优先以实际 query term 的 source range 为中心；lexical 能在 authored unit 中定位一个 query term 时返回 `match_range` 并围绕它截取，不能因为 unit 大于预算就固定展示文件尾部。无法直接映射的 fuzzy expansion 才退回 unit 中心。单行超过上限时在 UTF-8 scalar boundary 截断。Lexical/fuzzy 的 token 可能分散在一个 search unit 内，此时 `match_range` 可以为空，但 `unit_range`、excerpt range 与每个 highlighted token range 必须仍可追溯到 source。

`group_by` 控制首页的候选多样性，而不是改变底层匹配。`source` 每个 Module 只保留排序最高的 hit；`section` 以最近的 authored Heading 为边界保留最佳 hit；`match` 返回每个独立 source region。Lexical/fuzzy 默认按 source 分组，因为它们服务于“先发现文档、再 read 取证”；exact/regex 默认按 match 分组，因为用户通常在定位每次出现。分组属于 query identity，继续读取时不能改变；需要逐项调查的调用者可以显式选择 `match`。

Page 可以携带少量有界 `hints`。存在 continuation 时，hint 简短要求对相同 query 使用 `next_cursor`；完整的 cursor 绑定规则由 schema、Skill 与 invalid-cursor error 共同说明，不在每页重复长段自然语言。Human renderer 显示“more results available”与可复制的 `--cursor` 参数。完整零结果仍是成功：lexical/fuzzy 的 `operator=all` 零结果建议减少或简化关键词，或者显式改用 `operator=any` 做宽召回；exact 零结果建议缩短字面串或改用 lexical。Hint 不能偷偷改写 query 或自动重试，也不把零结果改成 error。

相同 location 或重叠 search unit 产生的重复候选在排序前合并；合并不能把两个不同 Heading 下的内容伪装成一个 citation。`total` 只在 index 已经廉价计算时返回；exact/regex 不为了显示总数继续扫描已经达到预算的 corpus。

== 检索单元与索引实现

Notist 使用独立的 `SearchIndex` 边界，而不是把 Tantivy type 暴露进 CoreRequest。默认实现选择 Tantivy：它是 Rust-native、支持 fielded BM25、term dictionary、fuzzy term query、增量 segment 与 immutable reader，和 VaultEngine 的 publication 模型契合。SQLite FTS5 适合单文件部署，但其 tokenizer、field scoring、fuzzy expansion 与 snapshot reader 需要更多自定义旁路；它不是首选 backend。

```text
WorkspaceSnapshot
  -> SearchUnitBuilder
  -> SearchIndexWriter (Tantivy)
  -> immutable SearchIndexReader + IndexStamp
  -> SearchService
```

`SearchUnitBuilder` 从 snapshot 中已经捕获的 source、parse、Module/Heading/Annotation facts 构造检索单元，不执行有外部副作用的 plugin。普通 paragraph、list/table item、Heading section 和 Raw block 优先成为独立 unit；超过 4 KiB 的单元按 source-safe boundary 切分，并使用至多 256 bytes 的 overlap。Chunk policy 属于 index schema version。每个 unit 存储 FileId、ModuleId、ModulePath、relative path、source fingerprint、source range、Heading ancestry 和 typed field text；完整 source 不复制进 index，excerpt 始终从捕获的 WorkspaceSnapshot 读取。

VaultEngine 持有 `SearchIndexManager`。Disk View 的 index 使用 WorkspaceDelta 增量更新，写入新的 generation 后原子发布 reader；删除 source 产生 tombstone，segment merge 只改变物理布局，不改变逻辑 index stamp。持久化 index 位于 Notist user cache，以 Vault identity 分区，而不是写入 authored Vault；manifest 至少包含 index schema、tokenizer、ranking version、Vault identity、source-set fingerprint 和每个 source fingerprint。任何不匹配都触发丢弃或重建，不能静默读取 stale index。

Session View 不能直接把 disk reader 当成包含 overlay 的结果。它在匹配的 disk generation 上叠加小型 in-memory overlay segment，并用 FileId tombstone 屏蔽被 overlay 替换或删除的 disk unit；overlay 释放后整个派生 segment 可丢弃。跨 Vault 查询由上层分别捕获每个 Vault 的 snapshot/index pair，再合并仍带各自 identity 的结果，不制造伪全局 revision。

Lexical/fuzzy query 只在 `IndexStamp.source_set_fingerprint` 与捕获 snapshot 相同时执行。Index lag 时请求按 `--wait-index` 在公开的短 deadline 内等待，默认 2 秒、最大 10 秒；deadline 后返回 typed `index_not_ready`，附当前 indexed stamp 与 `--exact` fallback hint，绝不把 stale result 当成当前 snapshot。Exact/regex 直接扫描 snapshot，不受 index health 影响。`status` 与 `index status` 公开 ready/building/stale/error 状态，使等待和重建可观察。

向量、semantic 与 hybrid mode 不属于这套默认 contract。未来只有在版本化 Notist benchmark 上相对 lexical 明确提升 Recall、nDCG 与 Agent task success，并保留 citation、ACL、snapshot 和 output budget 后，才能增加新的显式 mode。向量 index 仍是可重建派生状态，不能覆盖 BM25 score 或把模型版本藏在同一个 ranking version 中。

== CLI 的共同参数

有限普通命令共享以下 global options：

| Parameter | 含义 |
| --- | --- |
| `--format text|json` | 默认 text；JSON 使用 versioned envelope |
| `--color auto|always|never` | 只影响 text；非 TTY 的 auto 不输出 ANSI |
| `--pager auto|always|never` | 只分页当前 bounded page，不自动读取 continuation |
| `--no-daemon` | 在当前进程运行同一 service/core operation |

每个需要 Vault 的命令都把 `[ROOT]` 放在最后一个 positional，默认 `.`，并按 [[vault::old-designs::D0007-notist-toml]] 解析最近的 Vault root。`lsp` 按 editor workspace 动态发现多个 root；`daemon` 与 `mcp` 固定一个 root。Protocol stdio 命令拒绝 text/json/color/pager 组合，而不是把日志混入协议 stdout。

Collection command 共享 `-n, --limit`、`--max-bytes` 与 `--cursor`，但 help 必须展示该 operation 的实际 default 与 maximum。`limit` 是统一的页大小概念，不另设含义含混的 `top`。Cursor 不存在时 query/filter 参数定义第一页面；cursor 存在时相同参数仍然显式提供并由服务验证。普通命令不提供 `--all`，完整输出只能使用 export。

JSON response 使用 schema version 2：

```json
{"schemaVersion":2,"ok":true,"command":"search","result":{"items":[],"page":{},"budget":{},"coverage":{},"snapshot":{}}}
```

`max_bytes` 约束 `result` 的 compact JSON。CLI outer envelope 的字段均有长度上限，完整 JSON stdout 的最坏上界是 `applied max_bytes + 4 KiB`；human renderer 的最坏上界是 `2 × applied max_bytes + 8 KiB`，用于 source-frame gutter、颜色和 continuation command。Pager 不改变这些上界。Help、protocol stream、preview event 和 explicit export 不属于 ordinary query response，但各自仍使用专门的合理限制。

== Vault 探索与读取命令

=== status

```shell
notist status [ROOT]
```

`status` 返回 Vault identity、disk snapshot stamp、source/module/diagnostic counts、daemon mode 和 lexical index health。它没有 cursor，不返回 Heading、source 或 semantic items，并有固定 8 KiB logical maximum。

=== modules

```shell
notist modules [ROOT] [--prefix MODULE] [--kind any|source|virtual]
               [--limit N] [--max-bytes N] [--cursor TOKEN]
```

Module record 只含 ModulePath、Vault-relative source path、source/virtual kind、可选 semantic title 和 source fingerprint。默认按 ModulePath 排序。`--prefix` 是精确 ModulePath prefix；`--kind` 在 core 过滤。

=== search

```shell
notist search <QUERY> [ROOT]
  [--mode lexical|exact|fuzzy|regex | --exact | --fuzzy | --regex]
  [--scope MODULE]... [--fields FIELD,...]
  [--operator all|any] [--ignore-case] [--fuzzy-distance 1|2]
  [--group-by source|section|match]
  [--wait-index DURATION]
  [--limit N] [--snippet-bytes N] [--max-bytes N] [--cursor TOKEN]
```

`--operator` 与 `--wait-index` 只适用于 lexical/fuzzy；`--fuzzy-distance` 只适用于 fuzzy；`--ignore-case` 只适用于 exact/regex。`--group-by` 对所有 mode 可用，lexical/fuzzy 默认 source，exact/regex 默认 match。无关参数是 usage error，不能被忽略。Result 的 search metadata 明确返回应用后的 grouping 与 ordering；source order 没有伪 score。

=== outline

```shell
notist outline <MODULE|PATH> [ROOT]
  [--depth 1..6] [--limit N] [--max-bytes N] [--cursor TOKEN]
```

Outline 只查询一个 source-backed Module。Heading record 返回 name、level、label、location、parent identity 和 subtree range；没有标题的根 README 仍能通过 modules/read 发现。`--depth` 控制返回到哪一级，不改变 Heading name extraction。全 Vault outline 不是普通 query；需要全量结构时使用 export。

=== read

```shell
notist read <MODULE|PATH|MODULE#LABEL> [ROOT]
  [--from-line N --lines N | --byte-range START..END]
  [--max-bytes N] [--cursor TOKEN]
```

三种 window 互斥：默认从 selector 起点读取；line window 面向人；byte range 面向 citation/edit；带 label 的 selector 读取对应 section。默认最多 120 行并受 16 KiB 限制。Result 返回原始 authored source、实际 byte/line range、source fingerprint、是否到达 selector 末尾和 next cursor。Line number 只是 text renderer gutter，不进入 authored source string。

=== references

```shell
notist references <MODULE|MODULE#LABEL> [ROOT]
  [--direction incoming|outgoing|both]
  [--include-definition]
  [--limit N] [--snippet-bytes N] [--max-bytes N] [--cursor TOKEN]
```

默认 direction 是 incoming，以保持 backlinks 的直觉。Outgoing 使用 captured reference graph，不重新扫描文本。每条 edge 返回 source/target identity、location 和有界 excerpt，不重复 full source；排序是 ModulePath、range、edge kind。

=== query definition

```shell
notist query definition <PATH> <UTF8_BYTE_OFFSET> [ROOT]
  [--expected-fingerprint HASH]
```

Definition 返回零或一个 location，不分页。Offset 明确是 UTF-8 byte offset；需要 line/column 的协议由 adapter 使用 snapshot LineIndex 转换。Expected fingerprint 不匹配时返回 `snapshot_changed`，避免把旧 offset 用到新 source。

== 检查、编辑与维护命令

=== check

```shell
notist check [ROOT] [--scope MODULE] [--summary]
  [--severity error|warning|info]
  [--limit N] [--max-bytes N] [--cursor TOKEN]
```

Check 始终完整分析选择的 scope；预算只限制 diagnostic detail，不限制分析或 summary。Summary 返回 checked sources、total diagnostics 和按 severity/code 的计数。即使 detail 只显示第一页，只要完整检查存在 error，`ok=false` 与 exit status 都必须反映它。

Human text 使用 rustc/Typst 风格的 bounded source frame：相对路径、行列、message、主 range、少量相邻行和可行动 hint。JSON diagnostic 返回 code、severity、ModulePath、relative path、byte/line range、message、hint 与 bounded excerpt，不携带 full source。`--summary` 只省略 detail；完整 machine artifact 使用 `export diagnostics`。

=== edit

```shell
notist edit replace <PATH> <START> <END> <REPLACEMENT> [ROOT]
  --expected-fingerprint HASH --idempotency-key KEY [--yes]

notist edit rename <FROM> <TO> [ROOT]
  --expected-fingerprint HASH --idempotency-key KEY [--yes]
```

CLI convenience edit 仍经过 D0012 的 propose/validate/apply pipeline，不直接写文件。Byte range、target path、fingerprint 和 idempotency key 都是显式参数；冲突不做 fuzzy patch。Text mode 在 TTY 展示 bounded diff 并要求确认，script/JSON 必须显式传 `--yes` 才应用；没有 `--yes` 时只返回 proposal summary。这一行为不能通过检测“调用者像 Agent”来改变。

MCP 不提供 convenience auto-apply，而是保持 `propose_edit` 与 `apply_edit` 两个 tool。这样 Host 能在 destructive boundary 处显示 consent，并在多文件、大 diff 或策略检查时保留 plan identity。

=== index

```shell
notist index status [ROOT]
notist index rebuild [ROOT] [--wait]
```

Index status 返回 schema/tokenizer/ranking version、indexed snapshot、health、unit count 与最近错误。Rebuild 删除的只是可恢复 cache generation，不修改 authored Vault；默认提交后台任务并返回 operation handle，`--wait` 以 bounded progress event 等待完成。MCP 不暴露 reindex tool，普通 search 在 `index_not_ready` 时只提供状态与 exact fallback。

== Runtime、构建与显式全量命令

| Command | Parameters | 边界 |
| --- | --- | --- |
| `daemon [ROOT]` | foreground root-bound service；隐藏的 child 参数不属于 public API | stdout 不混入 query JSON |
| `lsp` | stdio；root 由 workspace source 发现 | 只输出 LSP framing |
| `mcp [ROOT] [--text-mirror full|brief]` | root-bound stdio MCP adapter；默认 full compatibility mirror | 只输出 JSON-RPC framing |
| `skill init <OUTPUT> [--force]` | 默认要求新目录；`--force` 只原子替换已有目录中的 `SKILL.md` | 遵循 D0013，不猜 host 安装位置，也不删除无关文件 |
| `build [ROOT] -o DIR [--clean]` | 生成完整静态站点 | output directory 是显式 artifact |
| `preview [ROOT] [--host IP] [--port N] [--no-open]` | bounded event stream + browser preview | 非 loopback host 需要显式选择 |

`--clean` 只允许清理已经解析、验证位于目标 output root 内的 build artifact，不能扩大到 Vault root。Preview 的持续事件不是 ordinary collection；每个 event 自身有上限，旧 generation 被新 generation 取代，不把完整 build payload写入 stdout。

Debug 与 export 不进入 Agent 的默认 discovery：

```shell
notist debug inspect [ROOT]
  --section modules|references|semantic [--module MODULE]
  [--limit N] [--max-bytes N] [--cursor TOKEN]

notist export snapshot|diagnostics|outline [ROOT]
  --output FILE [--format json|jsonl]
```

Debug inspect 仍然分页，并要求显式 section；它不再把 modules、references、calls、annotations 和 raw literals 混成默认 megabyte dump。Export 捕获一个 snapshot 后流式写临时文件，完成后原子替换目标；stdout 只返回 record count、bytes、snapshot、checksum 与 artifact path。`--output -` 是专家级无界 opt-in，只能在 CLI 明确请求，MCP 与官方 Skill 不使用它。

== Human Text、JSON、错误与 Exit Status

Human renderer 的第一页必须同时回答“发现了什么”和“怎样继续”。Search 在完整页显示实际结果数，在未完成页明确写 `showing N ...; more available`，不能把 returned 伪装成 total；位置优先显示 Vault-relative `path:line`，精确 bytes 留给 JSON。下一页提示只重复必要的停止原因和可复制 `--cursor` 参数；TTY pager 只浏览已经返回的这一页，不能自动翻页。Search 使用短 excerpt，check 使用 source frame。

JSON 使用固定 field name、compact UTF-8 与 versioned envelope；普通 page 不改成 JSON Lines。JSON Lines 只用于 export 或 preview/index 等事件流，其中事件至少区分 meta、item/progress 与 summary。零搜索结果是成功，`returned=0` 且 exit 0；“没有结果”不是 protocol error，但可返回有界 `hints` 指出如何显式放宽 query。

Typed error 至少包括：

| Code | 含义 |
| --- | --- |
| `not_found` | 精确 selector 不存在 |
| `ambiguous_selector` | selector 有多个候选 |
| `invalid_cursor` | schema/query/filter 不匹配或 token 损坏 |
| `cursor_stale` | source set 已变化 |
| `snapshot_changed` | fingerprint/revision precondition 失败 |
| `index_not_ready` | lexical index 尚未对应捕获 snapshot |
| `budget_too_large` | 请求超过 operation hard maximum |
| `edit_conflict` | authored source 已变化 |
| `plan_expired` | edit plan 不再属于活动 service generation |
| `service_unavailable` | daemon/protocol 暂不可用 |

CLI exit status 保持小而稳定：0 表示命令成功，包括零搜索结果；1 表示有效完成但业务结论为失败，例如 check 发现 error 或 proposal 未应用；2 表示 usage/schema error；3 表示 selector、cursor 或 index 等可纠正 query error；4 表示 service/protocol unavailable；5 表示 edit conflict/precondition failure；70 表示未分类 internal failure。脚本按 exit status 做粗分，再按 JSON error code 做精确恢复，不能解析自然语言 message。

== MCP Tool 与 Resource Surface

Model-controlled tool 只保留组合知识工作所需的 primitive：

| Tool | Core operation | 分页 |
| --- | --- | --- |
| `vault_status` | status | 否 |
| `list_modules` | modules | 是 |
| `search` | lexical/exact/fuzzy search | 是 |
| `get_outline` | one-Module outline | 是 |
| `read_source` | bounded authored source read | chunked |
| `get_references` | incoming/outgoing graph edges | 是 |
| `get_definition` | exact definition location | 否 |
| `check` | complete summary + diagnostic page | 是 |
| `propose_edit` | checked edit plan + bounded preview | bounded |
| `apply_edit` | apply existing plan | 否 |

MCP 不暴露 debug inspect、Vault-wide outline、global diagnostics singleton、regex、reindex、build、preview 或任意 execute command。Tool description 说明成本、默认 grouping 和下一步：search 要求在形成事实前调用 `read_source`，并说明只有当前页不足、任务要求穷举，或需要证明否定结论时才继续 cursor。

每个 input schema 使用 `additionalProperties=false`；selector 的 `oneOf` 首先接受与 CLI 相同的字符串，并兼容 module/path object；query、cursor、replacement 和 idempotency key 都有长度上限；limit/maxBytes/snippetBytes 的 default 与 maximum 写进 schema。MCP search 使用 camelCase 等价参数：`query`、`mode=lexical|exact|fuzzy`、`scope[]`、`fields[]`、`operator`、`groupBy`、`fuzzyDistance`、`ignoreCase`、`waitIndexMs`、`limit`、`snippetBytes`、`maxBytes` 和 `cursor`。`waitIndexMs` 默认 2000、最大 10000；模式无关参数由 schema 条件或服务验证拒绝。Search description 明确要求多词事实查找保留默认 `operator=all`，`operator=any` 只用于宽召回。

每个 tool 声明 output schema。Success result 与 typed tool error 使用 `oneOf`；pagination、budget、coverage、snapshot 与 index stamp 位于 `structuredContent`，不能只放进 Host 可能隐藏的 `_meta`。默认 `--text-mirror full` 为旧 client 在 TextContent 中复制同一 compact JSON；确认 Host 消费 `structuredContent` 后，部署者可以选择 `brief`，让 TextContent 只报告 returned 与 coverage。这个开关属于 MCP server compatibility 配置，不进入每个 tool schema，也不改变 logical result。

MCP 的 `maxBytes` 约束单份 logical structured result。考虑 structuredContent、text mirror、JSON escaping 与 JSON-RPC metadata，完整 transport response 的可证明上界是：

```text
3 × applied maxBytes + 16 KiB fixed overhead
```

默认 16 KiB logical result 在 full mirror 下对应最多 64 KiB transport；普通 hard maximum 64 KiB 对应最多约 208 KiB transport。Brief mirror 的实际 response 更小，但仍按 full 公式声明保守上界。Tool schema 和 capabilities resource 必须公开这些数，Host 可以请求更小的 `maxBytes`，但不能请求关闭上界。

Resources 只提供小而稳定的 application-controlled 对象：

```text
notist://<vault>/summary
notist://<vault>/capabilities
```

Resource Template 可以表达带 fingerprint 的精确 bounded object：

```text
notist://<vault>/source/<module>?start=<byte>&end=<byte>&fp=<fingerprint>
notist://<vault>/module/<module>/metadata
```

省略 range 不能返回整篇 96 KiB source。Search/read result 可以携带 exact resource link、size 和 audience annotation。MCP 标准 cursor 用于 resources/list 等标准 list operation；custom search cursor 位于 Notist tool output，二者只共享 opaque continuation 原则。

JSON-RPC error 只处理 parse、invalid request、unknown method、malformed params 和 protocol internal failure。Malformed params 的 message 指出具体参数与修复形状，error data 标记 `retryable=false`，避免调用者以完全相同的参数重试。Tool 已经开始执行后出现的 not found、ambiguous selector、stale cursor、index not ready 或 edit conflict 返回 `isError=true` 的 typed tool result，包含 code、message、retryable、hint 和有界 candidates。`invalid_cursor` 的 hint 说明 cursor 绑定原 selector/query/filter/mode/order，调用者必须原样重发这些参数，或省略 cursor 重新开始。Unknown tool 是 `-32601`，不能把所有 dispatch failure 都映射成 `-32602`。

`propose_edit` 返回 plan hash、expected fingerprints、affected locations、diagnostics 和 bounded diff preview；大 diff 只返回 artifact/resource handle。`apply_edit` 接受 plan hash、expected fingerprints 与 idempotency key，返回 applied/no-op/conflict 和新 snapshot。Tool annotation 可以提示 read-only、destructive 与 idempotent，但不替代 Host consent、Vault scope 或服务端 precondition。

== 可观察的边界

Notist 可以记录 operation、mode、applied budgets、matched/returned count、stop reason、index/ranking version、latency 与序列化 bytes，用于调整默认 policy。Telemetry 默认不记录 query 正文、excerpt、source 或 replacement；token 数只能是模型相关的估算，硬 invariant 始终使用 UTF-8 bytes。

公开 contract 必须让以下情况不靠猜测即可区分：完整零结果、只读了第一页、因 byte budget 提前停止、index 尚未追上 snapshot、cursor 因内容变化失效、selector 本身有歧义，以及 edit 被 precondition 拒绝。人类文本把恢复动作写出来，JSON/MCP 用稳定字段表达同一事实。

这个模型让搜索算法、输出成本与引用正确性处在同一条边界上：BM25 可以改善默认发现，fuzzy 可以修正有限 typo，exact/regex 可以保留确定性调查；无论选哪一种，结果都来自同一 snapshot、带可验证 citation、有明确最坏输出上界，并能用稳定 cursor 继续读取。
