@!(implementation: "aligned")
= LSP 请求支持对照

对 `notist lsp`（`crates/notist-cli/src/lsp.rs`，设计见 #<vault::lsp>）与 LSP 客户端可达方法面的逐项对照，方法全集以仓库依赖 `lsp-types 0.97.0`（对应 LSP 3.17 规范面）的 request/notification 模块为准。所有未支持的方法一律回 `MethodNotFound (-32601)`，客户端可安全探测。目的：给后续补充能力一份清单——哪些是缺口、哪些是有意不做。协议契约的权威设计见 #<vault::designs::host::client-interface-protocol>（core request 与 CLI 的映射在其「Client Interface」一节）；客户端依赖侧记录在 obsidian-notist `src/lsp/session.ts` 头注释。

== 能力声明（initialize 结果）

```text
positionEncoding        utf-8 only——不提供 utf-8 的客户端在 initialize 被拒连（RequestFailed）；下行走行表而非全文
textDocumentSync        Options { openClose: true, change: INCREMENTAL = 2, save: supported }
completionProvider      trigger ["[", ":", "#", "(", ",", "<", "/"]，resolveProvider = false
hoverProvider           true
definitionProvider      true
referencesProvider      true
documentSymbolProvider  true
workspaceSymbolProvider true
experimental.notist     documentReferences { directions } + renderDocument {}
```

capabilities 全静态声明，无 `client/registerCapability` 动态注册；未列出的 provider 既未声明也未实现。

== 请求（client → server）

| 方法 | 状态 | 备注（core request） |
| :--- | :--- | :--- |
| `initialize` | 支持 | 两段式握手；编码协商；workspaceFolders 首项/rootUri 定根；serverInfo |
| `shutdown` | 支持 | 主循环特判直答，随后等 exit |
| `textDocument/completion` | 支持 | `Completion`；数组返回（非 CompletionList，无 isIncomplete） |
| `completionItem/resolve` | 未支持 | `resolveProvider=false`，已声明不做 |
| `textDocument/hover` | 支持 | `Hover`；markdown contents + range |
| `textDocument/signatureHelp` | 未支持 | SignatureSet 已在 model，补全内部已有签名上下文；低成本候选 |
| `textDocument/declaration` | 未支持 | .not 无独立声明概念，倾向与 definition 合流 |
| `textDocument/definition` | 支持 | `Definition`；单 Location（Scalar） |
| `textDocument/typeDefinition` | — | 无独立类型定义位置 |
| `textDocument/implementation` | — | 无接口/实现二分 |
| `textDocument/references` | 支持 | `References`；includeDeclaration 直通 |
| `textDocument/documentHighlight` | 未支持 | 同文件 references 过滤即可派生；最低成本候选 |
| `textDocument/documentSymbol` | 支持 | `DocumentSymbols`；标题层级嵌套（SymbolKind::NAMESPACE） |
| `textDocument/codeAction` / `codeAction/resolve` | 未支持 | 诊断 code 已结构化，quick fix 候选 |
| `textDocument/codeLens` / `codeLens/resolve` / `workspace/codeLens/refresh` | 未支持 |  |
| `textDocument/documentLink` / `documentLink/resolve` | 未支持 | resolved `#<...>` 引用与属性 url 可自然映射；候选 |
| `textDocument/documentColor` / `colorPresentation` | — | 无颜色字面量语法 |
| `textDocument/formatting` / `rangeFormatting` / `onTypeFormatting` | 未支持 | 无格式化器，独立工程 |
| `textDocument/rename` / `prepareRename` | 未支持 | 返回 WorkspaceEdit 由客户端应用，不违反 daemon 无写路径（2026-08-28 裁决）；语义重命名是独立工程 |
| `textDocument/foldingRange` | 未支持 | 标题层级 + 代码块天然可折叠；低成本候选 |
| `textDocument/selectionRange` | 未支持 | AST 可派生，低优先 |
| `textDocument/linkedEditingRange` | 未支持 | target literal 配对；`<>` 自动配对已裁决定为编辑器侧（2026-08-28），LSP 通道是另一形态 |
| `textDocument/moniker` | 未支持 | `vault::module::item` 天然是 moniker，暂无消费方 |
| `textDocument/prepareCallHierarchy` / `callHierarchy/incomingCalls` / `callHierarchy/outgoingCalls` | — | 引用图已由 references / documentReferences 覆盖，语义不符 |
| `textDocument/prepareTypeHierarchy` / `typeHierarchy/supertypes` / `typeHierarchy/subtypes` | — | 无子类型层级 |
| `textDocument/inlineValue` | — | 无调试器 |
| `textDocument/inlineCompletion` | — | AI 补全 |
| `textDocument/inlayHint` / `inlayHint/resolve` | 未支持 | 解析值/类型提示；中成本候选 |
| `textDocument/diagnostic` | 未支持 | pull 诊断；push 侧（publishDiagnostics）已完备，pull 低优先 |
| `workspace/diagnostic` | 未支持 | 工作区级 pull，同上 |
| `workspace/symbol` | 支持 | `WorkspaceSymbols`；多 vault 合并排序；Flat SymbolInformation（KEY/FILE） |
| `workspaceSymbol/resolve` | — | 返回自包含 SymbolInformation，无 stale-resolve 语义 |
| `workspace/executeCommand` | — | 操作面归 CLI，无写路径 |
| `workspace/willCreateFiles` / `willRenameFiles` / `willDeleteFiles` | 未支持 | 只读校验（改名是否破坏引用）与无写路径裁决兼容；候选 |

== 通知（client → server）

| 方法 | 状态 | 备注 |
| :--- | :--- | :--- |
| `initialized` | 忽略 | 无动态注册需求；协议允许 |
| `exit` | 支持 | 退出主循环，io_threads join 后以 0 退出 |
| `textDocument/didOpen` | 支持 | 进 overlay，标记 dirty 触发 latest-wins 重建 |
| `textDocument/didChange` | 支持 | INCREMENTAL：ranged 与整文档混批依序应用（整文档取最后一个为基）；越界钳位、start>end 跳过；版本仅记录不做门控；未打开文档丢弃（2026-08-30 起，见 `#<vault::ai::2026-08-30 LSP incremental sync 设计>`） |
| `textDocument/willSave` | 忽略 | 可选通知，无消费场景（`willSaveWaitUntil` 同） |
| `textDocument/didSave` | 部分 | 仅当携带 text 时应用；不带则不动作，磁盘变化交给 watcher |
| `textDocument/didClose` | 支持 | 丢 overlay，诊断回落磁盘态并推空集清除 |
| `workspace/didChangeConfiguration` | 忽略 | `UpdateView.configuration` 恒 Unit；配置流入是候选 |
| `workspace/didChangeWatchedFiles` | 有意忽略 | 自带 PassiveDebouncedWatcher（250ms）驱动 WorkspaceChanged，不依赖客户端转发 |
| `workspace/didChangeWorkspaceFolders` | 忽略 | 根集 initialize 时固定；root 下按最近 Notist.toml 路由多 vault |
| `workspace/didCreateFiles` / `didRenameFiles` / `didDeleteFiles` | 忽略 | watcher 已覆盖磁盘感知；改名引用修复属 `will*` 域 |
| `notebookDocument/*` | 忽略 | 无 notebook 概念 |
| `$/cancelRequest` | 支持 | 两条路径都是真取消（2026-08-31 起客户端多路复用接线，daemon 侧 `Cancel` 帧直达 `execute_cancellable`）；命中回 `RequestCanceled (-32800)` |
| `$/setTrace` | 忽略 | 从不发送 `$/logTrace` |
| `window/workDoneProgress/cancel` | — | 从不创建 progress token |

== 服务端下行（server → client）

| 方法 | 状态 | 备注 |
| :--- | :--- | :--- |
| `textDocument/publishDiagnostics` | 支持 | 初始 baseline 先于任何客户端活动；签名 diff 增量；消失/关闭推空集；打开的文档带 version |
| `window/logMessage` | 支持 | didChange/didOpen 不可路由 URI WARNING、rebuild 失败与诊断发布失败 ERROR；畸形通知仅记 stderr 不打扰客户端 |
| `window/showMessage` | 未用 | 以 logMessage 代替 |
| `telemetry/event` | 未用 |  |
| `$/progress` | 未用 | rebuild 期无 busy 指示；`window/workDoneProgress/create` + progress 是候选 |
| `window/showMessageRequest` | 未用 |  |
| `window/workDoneProgress/create` | 未用 |  |
| `workspace/applyEdit` | 未用 | 无写路径纪律 |
| `workspace/configuration` | 未用 |  |
| `workspace/workspaceFolders` | 未用 | 根集启动时一次性读取 |
| `client/registerCapability` / `client/unregisterCapability` | 未用 | 全静态声明 |
| `*/refresh`（semanticTokens / inlayHint / inlineValue / codeLens / diagnostic） | 未用 | 对应 pull 能力均未启用 |

== 实验扩展（capabilities.experimental.notist）

- *`notist/documentReferences`*：模块级引用，无位置歧义（文档以标题开头时 `textDocument/references` 会解析到标题符号而非模块）；`direction = incoming|outgoing|both`（默认 incoming）+ `includeDefinition`；返回 `{ revision, items[] }`（uri/range/direction/sourceModule/targetModule/targetName/targetKind/url/isDefinition）。消费方是 obsidian-notist 入链/出链面板（见 #<vault::ai::2026-08-27 obsidian-notist ux polish and document references>）。
- *`notist/renderDocument`*：单文档渲染，与 preview site 同一求值管线；返回 `{ revision, page, resources }`；非 `.not` 或未归属 vault 的路径返回 null（见 #<vault::ai::2026-08-30 obsidian-notist preview mode design>）。

== 错误码分类

| 码 | 场景 |
| :--- | :--- |
| `InvalidParams (-32602)` | 参数解码失败（客户端错误，修好后可重试） |
| `InternalError (-32603)` | 解码成功后的服务端条件 |
| `MethodNotFound (-32601)` | 未实现方法（全部未支持项的统一出口） |
| `RequestCanceled (-32800)` | `$/cancelRequest` 命中在途请求 |

== 补能力候选（按性价比）

低成本（现成数据，纯投影）：

- `textDocument/documentHighlight`：references 按同文件过滤。
- `textDocument/foldingRange`：标题层级 + 代码块。
- `textDocument/documentLink`：resolved 引用 + 属性 url 外链。

中成本：

- `textDocument/signatureHelp`：SignatureSet 已在 model，补全内部已做签名上下文。
- `workspace/didChangeConfiguration` → `UpdateView.configuration`：管道已存在，缺客户端拉取与字段映射。
- `$/progress` + `window/workDoneProgress/create`：latest-wins 重建期间的 busy 指示（现在客户端只能从诊断延迟间接感知）。
- `textDocument/codeAction`：从诊断 code 的 quick fix 起步。

待裁决/独立工程：

- `rename` / `prepareRename`、`will*Files` 只读校验：与「daemon 无写路径」裁决兼容（编辑由客户端应用），但语义重命名与引用修复是独立工程。
- pull 诊断（`textDocument/diagnostic`、`workspace/diagnostic`）、`inlayHint`、`selectionRange`、`moniker`：按客户端需求再启。
- `semanticTokens`：与姊妹项目高亮栈（zed queries / obsidian tree-sitter）冗余，暂无 tree-sitter 之外的高亮消费方，不主动做。
