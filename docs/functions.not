= Built-in Functions

本文记录当前工具 surface 实际提供的 14 个内置构造器。未列出的名称不是现行 built-in：未注册名称由 check 层报 unknown function 诊断，调用本身保留为内容节点（渲染有 fallback 标签兜底）；加载插件后 `{package}::{element}` 是注册名，不报 unknown。调用模型与类型规则见 #<vault::types>，语法细节见 #<vault::grammar>，速查见 #<vault::cheatsheet>。

== 签名总览

```text
link(target: Target | String) -> Content
heading(level: Int = 1, trailing body: Content) -> Content
raw(source: String, lang: String? = none, block: Bool = false) -> Content
rule() -> Content
callout(kind: String = "note", title: Content? = none, trailing body: Content) -> Content
details(summary: Content? = none, open: Bool = false, trailing body: Content) -> Content
item(ordered: Bool = false, trailing body: Content) -> Content
table-cell(colspan: Int = 1, rowspan: Int = 1, trailing body: Content) -> Content
table(columns: Int, header: Bool = false, align: String? = none, trailing body: Content) -> Content
figure(kind: String? = none, supplement: Content? = none, caption: Content? = none, trailing body: Content) -> Content
strong(trailing body: Content) -> Content
emph(trailing body: Content) -> Content
underline(trailing body: Content) -> Content
strike(trailing body: Content) -> Content
```

签名记号：`T?` 是可空类型（可传 `none`）；`= 默认值` 表示形参可省略且只能具名绑定；`trailing` 表示末尾 Content 形参，调用时写作尾随 `[内容]`。

== 调用规则

```not
#heading(level: 2)[标题]
#raw(r#"fn main() {}"#, lang: "rust", block: true)
#callout(kind: "warning", title: [风险])[正文]
```

- 命名实参写作 `name: value`；
- 位置实参必须全部出现在具名实参之前，trailing Content block 是唯一例外——`#f(kind: "warning")[内容]` 与 `#f(kind: "warning", [内容])` 等价；
- 带默认值的形参只能具名绑定（可选即 named），不带默认值的形参按声明顺序位置绑定；
- 重复绑定、未知实参、缺失必填实参与类型不匹配都产生指向调用位置的诊断；
- 内置构造器是 first-class 函数：可以绑定、传递与组合——`#let make_title = heading` 合法。

== link

```text
link(target: Target | String) -> Content
```

产生 Reference 元素。`Target` 是 ModulePath 加可选 label 的结构化拼写，`String` 只表示外部 url。`#<...>` 是 Target 字面量糖，与 `link(<...>)` 共享同一解析入口：

```not
#<vault::grammar>
#link(<vault::grammar>)
#<vault::guide/install>
```

- 目标空间：文档模块（`vault::path::to::file`）、模块内目标（`... #id`，标题默认 id 亦匹配）、资源文件（`vault::path::to::assets/logo.png`，完整文件名精确匹配）；
- 带 scheme（`https://` 等）的外部 URL 语法合法：解析为 External 目标，当前渲染为未解析的可见文本并产生 info 级 `external-reference-unsupported` 诊断；
- 目标解析产物与引用图见 #<vault::designs::world::reference-ref-target>。

== heading

```text
heading(level: Int = 1, trailing body: Content) -> Content
```

产生 Heading 元素。`level` 必须至少为 1，无上限（渲染时折叠到 h6 以内）。糖：行首的 `=` 串（无空白）后接内容，`=` 数量即 level：

```not
= Title     ~= heading(level: 1, [Title])
== Title    ~= heading(level: 2, [Title])
```

- 语法边界：行首 `=` 连续出现一个或多个，其后必须跟空白（或行尾），再后是标题 body；`=foo` 是普通文本；
- 错误恢复：行首 `=` 后无内容 → 空 body 的 Heading；
- 标题拥有默认名：body 提取的纯文本（Text 元素按顺序拼接，含嵌套）——`= 安装指南` 默认即可被 `#<.../安装指南>` 引用；显式 `@id` 覆盖默认 id；无效文本回退为 `loc-<起始字节偏移>` 锚点；
- 同级标题与其后内容归组为嵌套的 Section 节点（见 #<vault::designs::pipeline::structure> 与 #<vault::designs::host::html-renderer>）。

== raw

```text
raw(source: String, lang: String? = none, block: Bool = false) -> Content
```

产生 Raw 元素。构造器校验：`block` 为 false 时 `source` 含换行产生诊断——多行源码必须写 `block: true`。

```not
#raw("cargo test", lang: "text")

#raw(
  r#"""
fn main() {
    println!("hello");
}
"""#,
  lang: "rust",
  block: true,
)
```

糖：行首围栏代码块（三个以上反引号）。围栏内容为 source（原样，不解析 markup、不做转义），opening line 上的标注为 lang，block 恒为 true：

````not
```rust
println!("Hello");
```
````

等价于 `raw("println!(\"Hello\");", lang: "rust", block: true)`。行内反引号（一对相同长度的 backtick run）同样产生 Raw 元素，block 为 false；未闭合围栏 → 内容到文件尾并产生诊断。

== rule

```text
rule() -> Content
```

产生 Rule 分割线。糖：行首三个以上连续 `-` 且其后只有空白：

```not
---
```

- `---` 是 rule；`-` 后跟空白与内容 → 列表条目糖（见 item）；
- 不足三个的连字符（如 `--`）按普通文本处理，不产生诊断。

== callout

```text
callout(kind: String = "note", title: Content? = none, trailing body: Content) -> Content
```

产生 Callout 提示框。`kind` 是面向语义与导出的稳定分类，为空产生诊断；`title` 未提供时不生成标题。无语法糖——通过显式调用使用：

```not
#callout(kind: "note")[普通提示。]

#callout(kind: "warning", title: [风险])[保存前请检查配置。]
```

== details

```text
details(summary: Content? = none, open: Bool = false, trailing body: Content) -> Content
```

产生 Details 可折叠内容。`summary` 为 `none` 时由渲染器提供通用折叠标题；`open` 决定初始展开状态。无语法糖：

```not
#details(summary: [查看详情], open: false)[
这里是折叠内容。
]
```

== item

```text
item(ordered: Bool = false, trailing body: Content) -> Content
```

产生列表条目：`ordered` 为 false 时是 ListItem，为 true 时是 EnumItem。糖：行首 `- ` 或 `+ `（符号后必须有空白）：

```not
- A    ~= item([A])
+ A    ~= item(ordered: true, [A])
```

- 语法边界：行首空白 + `-`/`+` + 空白 + 内容，内容到行尾；
- 错误恢复：符号后无内容 → 空条目（合法）；
- 缩进嵌套：缩进增加的条目嵌套到此前最近的较浅条目之下；
- item 是 structural sugar：相邻的同类条目（ordered 相同、之间无块级间隔）由成型组合为 List（无序）或 Enum（有序）——显式调用动态产生的 item 进入同一组合算法。

```not
- parent
  - child
- sibling

#item[显式条目]#item[另一个条目]
```

== table-cell / table

```text
table-cell(colspan: Int = 1, rowspan: Int = 1, trailing body: Content) -> Content
table(columns: Int, header: Bool = false, align: String? = none,
      trailing body: Content) -> Content
```

`table-cell` 产生表格单元；`table` 消费按行平铺的 `table-cell`（每 `columns` 个 cell 为一行）并产生 Table 元素。`columns` 必填；`header` 决定首行是否进入表头；`align` 是逗号分隔的列对齐串（`left` / `center` / `right` / `default`，数量必须等于 `columns`，`none` 表示全部默认）。Table 本身没有 caption——需要题注时用 `figure` 包住 table。body 中允许纯空白与段间分隔作为源码排版，其余可见元素必须是 `table-cell`：

```not
#table(columns: 2, header: true, align: "left, right")[
  #table-cell[Name] #table-cell[Value]
  #table-cell[one]  #table-cell[1]
]
```

糖：pipe table。连续的行首 `|` 行，第一行 header，第二行分隔行，其后 body：

```not
| Name | Value |
| :--- | ----: |
| one  | 1     |
```

- 分隔行 cell 形如 `:?-+:?`；`:` 在左、右或两侧分别表示 left / right / center，无 `:` 表示默认；
- header cell 数即 `columns`；分隔行必须与 header 同列数；
- cell 内是行内 Markup：`*强调*`、`#表达式`、`` `raw` `` 等正常解析，`\|` 是字面 `|`；Raw、Code 表达式内部的 `|` 不分割 cell；
- body 行缺 cell 补空 cell；多 cell 产生诊断并截断；
- 没有合法分隔行时，整段按普通文本处理。

== figure

```text
figure(kind: String? = none, supplement: Content? = none,
       caption: Content? = none, trailing body: Content) -> Content
```

产生 Figure 块级 wrapper，语义对齐 Typst 0.14 的 `figure`：body 是任意块内容，caption 是题注，kind 是稳定分类，supplement 是题注前缀。`kind` 省略时从 body 自动推断：Table → `"table"`，Raw → `"raw"`，块级插件节点 → 其 name，其他 → `"figure"`。渲染时 supplement 输出为 `supplement + ": " + caption`：

```not
#figure(caption: [接口延迟], supplement: [表], kind: "table")[
  #table(columns: 2)[
    #table-cell[接口] #table-cell[延迟]
    #table-cell[query] #table-cell[12 ms]
  ]
]
```

Typst figure 的 `placement` / `scope` / `gap` / `outlined` / `numbering` 依赖尚未进入 Notist 的 float、counter 与 outline；当前只提供 caption/kind/supplement 子集。无语法糖。

== strong / emph / underline / strike

```text
strong(trailing body: Content)    -> Content
emph(trailing body: Content)      -> Content
underline(trailing body: Content) -> Content
strike(trailing body: Content)    -> Content
```

行内强调构造器。糖：

```text
*text*    ~= strong([text])
_text_    ~= emph([text])
__text__  ~= underline([text])
~~text~~  ~= strike([text])
```

- 语法边界：开符后必须跟非空白、非同符字符，闭符前必须是非空白——`/* 不是强调 */` 保持文本；
- `__` 优先匹配 underline（最长匹配优先于单个 `_`）；
- 可嵌套其他强调（如 `*_text_*`：strong 内嵌 emph）；
- 错误恢复：行尾未闭合 → 恢复为普通文本并产生诊断；未配对的符号保持文本。

```not
*加粗*、_斜体_、__下划线__、~~删除线~~。
```

== 未知函数

不在上表的内置名称由 check 层报 unknown function 诊断；`::` 命名空间形式在对应插件经 `Notist.toml` 加载后是合法注册名（见 #<vault::plugins>），不报 unknown。

调用不会被静默丢弃——调用本身原样保留为内容节点，预览与工具恢复可以继续处理其余源码；渲染器把未注册投影的名字投影为 `<{package}-{element}>` 形式的 fallback 标签。插件元素是普通内容节点，不改变内置构造器集合（节点模型见 #<vault::designs::pipeline::evaluate/Call 与 Node>）。
