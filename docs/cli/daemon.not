@!(implementation: "aligned")
= Daemon and Analyzer Views

本文定义 daemon 的进程模型、每个 Vault 的 Analyzer View，以及 WorkspaceSnapshot 的身份、发布与查询语义。请求如何被 CLI 与 LSP 投影到 core request、本地协议与 daemon 生命周期见 #<vault::cli::protocol>；检索语义与派生索引见 #<vault::cli::search>；Vault 边界与发现规则见 #<vault::world::boundary>；LSP adapter 的对外行为见 #<vault::lsp>。

== 为什么需要 daemon

用户连续执行几条命令时，不应该为每条命令重新扫描、解析与索引同一个 Vault：

```shell
notist inspect search "workspace revision"
notist check --vault docs
notist inspect refs vault::world::model
```

编辑器与 Agent 同时工作时，也不应该为同一个 Vault 维护两份 watcher、Module 图与索引。Notist 为每个活动 Vault 使用一个本地 daemon 持有长期状态，让 CLI、LSP 与 Agent 共享同一份 Module 树、缓存与索引；CLI 与 LSP 是面向不同调用者的 client interface，Agent 通过 CLI 命令与官方 Skill 使用（#<vault::cli>）。短命令是 client，后台进程保存可复用状态——这个类比只到进程拓扑为止：Notist daemon 绑定一个 Vault，而不是当前用户访问过的所有知识库。

== 形态：每 Vault 一个 daemon

```text
Editor          shell / Agent
  |                  |
notist lsp        notist inspect ...
  |                  |
  +------------------+
                     |
               local Notist protocol
                     |
              notist daemon <vault>
                     |
                VaultEngine
                     |
       Analyzer Views + WorkspaceSnapshots
```

两个 Vault 的 Module 树、配置、watcher、全文与向量索引、embedding 模型与权限互不相关；放进同一长期进程只会耦合内存、崩溃与升级生命周期，没有可复用的语义状态。因此采用"每用户的私有 endpoint namespace、每 Vault 一个 daemon"，而不是"每用户一个 application daemon"。

同一 canonical root 只对应一个 daemon 实例。canonical path 必须在 endpoint 派生前完成解析，避免同一目录因相对路径、symlink 或大小写表示得到多个 daemon。由 Notist 发布和替换内容的 managed Vault 可以额外声明 generation；同一 root 与 generation 组合对应一个实例，升级前的 daemon 因 endpoint 不同而停止接收新 client，并在 idle 后退出。generation 不是普通用户 Vault 的版本号，也不能用于绕过每 Vault 隔离。

== Daemon 持有什么

Daemon 是一个 Vault application core 的长期进程宿主，不是新的语言语义层。启动时传入的 canonical root 固定它服务的 Vault（发现规则见 #<vault::world::boundary>）；进程内只有这个 root 对应的 VaultEngine，client 不能连接一个已存在的 daemon 后要求它再打开任意 root。

VaultEngine 持有适合在客户端之间复用的状态：

- 磁盘 source catalog 与文件 watcher；
- Vault 内稳定的 FileId、ModuleId 分配与 Module 树身份（#<vault::world::model>）；
- 以 source content 与语义环境为 key 的 parse、静态检查、求值与成型缓存；
- 全文、向量、reference graph 与摘要等可重建派生索引；
- 对磁盘写入、rename 与索引发布的串行化入口。

Daemon 不成为 authored source 的真相来源：`.not` source 与明确的 editor overlay 仍是输入；缓存、索引与 daemon 内存都可以在进程退出后重建。这是求值确定性的推论——求值（#<vault::language::evaluation>）与成型（#<vault::language::structure>）是纯函数，增量更新只属于分析层的优化，不进入求值语义。持久化索引必须记录 schema、模型、Vault identity 与 source fingerprint，任何一项在加载时校验不匹配都触发丢弃或重建，不能静默解释为新数据。

== 组合根：WorkspaceSnapshot 捕获 semantic environment

plugin 首先是语义 package，而不是一种编译产物；native 与 Wasm 只是可替换的 backend，二者的输出都先归一化为同一份 PluginContribution（package identity、functions、signatures、element schemas、target projections、backend 元数据），进入同一个 registry 与同一个 evaluator。contribution 契约见 #<vault::plugins::contributions>，package 形态见 #<vault::plugins::package>。

把 package set 组合成一次可运行 semantic environment 的位置，就是 WorkspaceSnapshot 的构建过程：它捕获函数 registry、shaping registry、签名集合、插件实例与环境 identity。求值引擎是可注入的组件，不知道有哪些 package，也不自行装载 `core`；plugin host 只负责 Wasm 实例化、ABI 与 fuel，不决定一个 Vault 安装哪些 package。

组合根执行注册时必须保持原子性：

1. 校验 package id、namespace、签名、schema 与 target contribution；
2. 检查与既有 package 的名字冲突；
3. 校验全部通过后，一次性写入 functions、signatures、shaping 与 projection registry；
4. 任一失败都不得留下半个 package。

native 与 Wasm contribution 经过同一套校验与注册路径；`backend` 是描述性装载元数据，不参与 reduction 语义，函数 owner 表达 package identity（`core::heading` 的 owner 是 package `core`），而不是进程来源。

`core` 是默认预装的语言标准 package，不是求值引擎的特权模块：它的 handlers、signatures、element schema 与 alias 都由普通 contribution 提供；自定义环境可以不安装 core，此时 `core::*` 与未安装的任意 package 一样成为未注册 call，reduction engine 不为它增加特殊分支。语法糖 lower 出的 `core::*` 名字表达语言 surface 依赖标准 package，不等于 lower 或 reducer 内嵌了 core handler。

package、manifest、Wasm module、native plugin surface 或 target contribution 的任何变化都必须改变 environment identity。一次文档求值只消费已捕获的 registry，不在中途重新加载 package，也不根据 backend 猜测语义。

== 共享 Engine，不共享 Overlay

多个客户端可能同时看到同一文件的不同内容：

```text
disk source                 A
editor-1 unsaved overlay    B
editor-2 unsaved overlay    C
Agent default view          A
```

因此一个 VaultEngine 可以有多个 Analyzer View：View 保存某个 client session 的 overlay set、配置投影与 snapshot revision。

```text
VaultEngine
  shared disk sources and caches

  View(editor-1)   overlays { file -> B }   revision 17
  View(editor-2)   overlays { file -> C }   revision 9
  View(disk)       overlays {}              revision 24
```

相同 source content 可以复用 parse 与其他纯派生缓存，但每个 View 独立发布 snapshot；一个 View 的 revision 不能与另一个 View 比较，daemon 也不制造跨 Vault 或跨 View 的伪全局 revision。

CLI 默认使用 disk View，只读取已保存的 Vault。Agent 只有在用户或宿主显式授予 View handle 后，才能查询某个编辑器的未保存状态——View handle 是受限会话能力，不能通过进程列表或"最近打开的编辑器"猜测。`notist lsp` 为每个编辑器连接维护自己的 View；adapter 断开后，它拥有的 View 与 overlay 在租约结束后释放，daemon 不能把断开的未保存文本合并进 disk View。

== Analyzer View 与 WorkspaceSnapshot

同一份正在编辑的文档，可能同时被 diagnostics、definition、preview、静态构建和 Agent 查询读取。它们不能分别在不同时间重新读盘，再把各自得到的 source、Parse 与 reference graph 拼在一起——否则一个结果中的 byte range 可能已经不再对应另一个结果中的文本。

分析分成三个角色：

```text
filesystem sources + configuration + function schemas
                              |
                              v
                         VaultEngine
                     shared identities/cache
                              |
                 client overlays and View config
                              |
                              v
                        Analyzer View
                    build candidate revision N
                              |
                              v
                    Arc<WorkspaceSnapshot N>
```

- *VaultEngine*：Vault 的共享长期状态——磁盘 source catalog、稳定 identity、可复用 cache、派生索引；不假设所有客户端看到相同的未保存文本；
- *Analyzer View*：一个客户端会话的长期分析状态——在磁盘状态上叠加 overlay、配置投影与 revision counter；多个 View 共享 Engine 中以 source content 为 key 的 Parse 与分析缓存，发布彼此独立的 snapshot；
- *WorkspaceSnapshot*：Analyzer 对一组确定输入完成分析后的不可变结果——Vault root 与配置、每个 source 的文本/Parse/LineIndex、Module tree（含 virtual module）、import/binding graph 与 lexical environment identity、属性与引用索引、全部 diagnostics、resolved/typed HIR（或足以确定性派生它的 immutable input）。

消费者捕获 snapshot 后只在其上完成一次查询或渲染；即使 Analyzer 发布了 revision N+1，旧查询的 source、range、Module 与 diagnostics 仍彼此对应。消费者可以选择丢弃旧结果，但不能混合两个 revision 的对象。

=== 输入首先成为 Source Set

Analyzer 不允许每个分析阶段自行读取文件。一次 candidate build 开始时先形成确定的 source set：

```text
SourceInput {
  file_id
  canonical_path
  text
  origin          // disk or overlay
  document_version?
}
```

Overlay 按 canonical path 覆盖磁盘文本；只存在于 overlay 中的新 `.not` 文件也可以进入 source set。Nested Vault 的 source 不进入外层 Analyzer——这一边界在收集输入时完成，而不是在引用解析时补救。

source set 一旦开始分析便保持不变。读取失败、路径逃逸或无法确定 Vault 边界属于 candidate build failure：Analyzer 保留上一个可用 snapshot，而不是发布混合新旧文件的结果。Syntax error、unresolved reference 与 type mismatch 不是 build failure——它们是该 revision 的合法分析结果，进入 snapshot diagnostics，其余内容仍可查询与渲染。

=== Revision 是 View 内的语义世界

Revision 是 View 内单调递增的不透明编号：表达"这些对象来自同一次已发布分析"，不是 wall-clock time、文件 hash，也不能跨 View、Vault 或 daemon instance 比较。只有完整 candidate 构造成功并成为当前结果时，revision 才对消费者可见；较旧任务即使更晚完成，也不能覆盖较新的已请求状态——publication check 是一致性边界，取消旧任务只是优化。

Preview generation、LSP document version 与 snapshot revision 是三个不同概念，可以互相记录关联，但不共用一个 counter。

=== 稳定身份与 snapshot-local 身份

路径、显示名称与 source range 都不足以独自充当语义对象的身份；另一方面，并非每个语法节点都能在任意编辑后保持永久身份。

- `FileId`：VaultEngine 为 canonical source identity 分配，Engine 生命周期内跨 View 与 revision 稳定；文件删除后不立即复用，普通路径重命名产生新 FileId（除非上层以明确 rename event 保留身份）；
- `ModuleId`：Vault 内的逻辑 Module identity，对应一个 ModulePath；virtual 与 source-backed Module 都有；给 virtual Module 增加 `README.not` 不改变 identity，source move 改变 ModulePath 就是语言层面的 Module rename（#<vault::world::model>）；
- 显式 ItemId：可跨 revision、可引用（`@id` 标注与标题默认名，见 #<vault::world::reference>）；没有显式 `@id` 标注的节点只获得 snapshot-local identity：

```text
SnapshotNodeId {
  revision
  file_id
  local_id
}
```

不能通过"相同 byte range"假装无 ID 元素在编辑前后是同一对象。若需要稳定的自动 ElementId，必须单独设计匹配与冲突语义——snapshot 不把启发式匹配伪装成语言保证。

=== 分析管线

Analyzer 从 source facts 逐步建立更高层语义，每个阶段只引用同一 candidate 中的对象：

```text
captured sources
  -> Parse + LineIndex per FileId
  -> Module tree and source-to-module mapping
  -> imports, module root bindings, lexical definitions and Function schema environment
  -> Reference graph and Code name resolution
  -> static type checking and resolved calls
  -> resolved/typed HIR
  -> immutable indexes and diagnostics
  -> publish WorkspaceSnapshot
```

Parse 保留源码事实与 recoverable syntax errors；静态分析验证调用与类型，但不为了回答编辑器查询而执行具有外部副作用的插件。求值（#<vault::language::evaluation>）与成型（#<vault::language::structure>）可以按 Module 惰性计算，但必须消费 snapshot 捕获或派生的 HIR，缓存 key 至少包含 snapshot revision、ModuleId、imported module-binding identities 与 Function environment identity。

Analyzer 负责 orchestration，不成为新的语言语义层：Parser、checker、evaluator 的规则由语言层各篇定义；Analyzer 只保证它们看到同一组输入，并把结果组织成可查询的世界。

=== 查询属于 Snapshot

definition、references、completion、hover、items、diagnostics、渲染与 search 都表现为 snapshot query：

```text
snapshot.module_at(file_id)
snapshot.source(file_id)
snapshot.definition_at(file_id, byte_offset)
snapshot.references_to(module_id)
snapshot.hover_at(file_id, byte_offset)
snapshot.document_symbols(file_id)
snapshot.structured_document(module_id)
snapshot.resolve_reference(url)      // RefTarget，见 #<vault::world::reference>
snapshot.references_in(module_id)    // 正向引用，见 #<vault::cli::protocol>
```

查询结果携带 snapshot revision，优先返回 FileId、ModuleId、显式 ItemId 与 TextRange；Path、ModulePath、标题与 snippet 是展示与序列化信息，不取代核心 identity。查询不修改 Analyzer 输入、不推进 revision；completion 或 search 的 memoization cache 可丢弃，只影响性能。

SnapshotStamp 的 `source_set_fingerprint` 由 source set 的 identity 与内容确定性派生；source-order 查询的稳定 key（ModulePath、relative path、range、record kind）与 ranked 查询的量化 score key 由 snapshot 内的 identity 保证——Analyzer 是这些稳定性的实现者，检索索引的就绪判定（`index_not_ready`）建立在它的 fingerprint 上（#<vault::cli::search>）。

== Overlay 与 watcher

Editor overlay 属于单个 Analyzer View：`didOpen` 建立、`didChange` 更新、`didClose` 移除；overlay 只进入 source 所属 Vault 的最近 View，disk View 永远不吸收未保存文本。磁盘写入一律来自宿主编辑工具或其他外部进程；Editor View 上的 overlay 不会因为 disk write 自动消失，editor buffer 与磁盘的偏离由编辑器的正常冲突流程决定。

Watcher event 只说明"某个路径可能变化"，不能直接充当语义增量。WorkspaceDelta 由两个已完成 snapshot 比较得出：

```text
WorkspaceDelta {
  from_revision
  to_revision
  added_files / changed_files / removed_files
  added_modules / changed_modules / removed_modules
  changed_references
  changed_diagnostics
}
```

LSP 据此只重新发布受影响文件的 diagnostics，preview 只重建受影响页面，索引只更新对应 Module。Delta 是优化与通知接口；任何消费者都必须能丢弃派生状态并从完整 snapshot 重建——增量日志不是真相来源。

== 并发与发布

发布后的 snapshot 由 `Arc` 共享，消费者不需要持有 Analyzer 的可变锁：

```text
input change
  -> update Analyzer inputs
  -> reserve revision N
  -> build candidate N outside publication lock
  -> if N is still current, atomically publish Arc<Snapshot N>
  -> notify consumers with Delta(N-1, N)
```

构建是 latest-wins single-flight：新输入使在途构建的结果作废，旧 revision 的慢任务不覆盖更新结果。请求开始时读取当前 `Arc<WorkspaceSnapshot>`，随后不追随 current pointer——长查询不阻塞编辑器产生新 snapshot，也不在执行中途改变语义世界。

每个 Vault 一个 VaultEngine，每个需要不同 overlay 或配置投影的客户端一个 Analyzer View；跨 Vault 查询位于更高层——分别捕获 snapshot 再合并，不制造伪全局 revision。Vault 隔离意味着引用解析默认不跨 Engine 边界。

== 请求路由

一条连接按其 client kind 与被授予的能力解析到一个 Analyzer View（CLI 与 Agent 默认 disk View，LSP adapter 用自己的 View），此后该连接上的请求都在这个 View 内执行。每个请求转换为协议无关的 core request（#<vault::cli::protocol>），开始时捕获当前 snapshot，在其上完成并返回携带 daemon instance、Vault identity、View identity 与 snapshot revision 的结果。

Core request 面是只读的：daemon 不提供写路径，磁盘写入来自宿主编辑工具等外部进程，经 watcher 发布为新 snapshot。读取结果提供可验证的 source fingerprint，供外部写入方在变更前后自行校验（如 definition 查询声明 `expected_fingerprint` 防止旧 offset 命中新文本），daemon 不做中介写入授权。

长查询支持 cancellation；notification 与 response 使用有界队列，一个缓慢 client 不能阻塞 watcher publication 或其他 View。
