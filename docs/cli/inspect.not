@![implementation = "aligned"]
= CLI Inspect: Agent Query and Navigation

#[
CLI 中有一系列用于检查库内容的命令，它们被统称为 Inspect 命令。

它们是 Agent 在 Notist 库中导航、探索、查找、阅读的重要工具：

@[todo="需要改进并根据实际情况修改，或许后续挪到 README 然后一个命令一个文档"]
```
status / modules     发现：这是哪个 Vault、有哪些逻辑地址
  → search           检索：找候选（候选不是证据）
  → outline          结构：一个 Module 的求值后 Heading 树
  → read             取证：唯一的事实入口，产出 citation
  → references / definition 导航：沿已解析的引用图移动
  → check            验证：完整 Vault 健康结论（不接受 scope）
```

一个核心思想是利用 Notist 的 ModulePath 以及 ScopeId

]@type=user,.user


本文是 `notist inspect` 调查组的逐命令规范：status、modules、search、outline、read、references、definition 与 Selector/Scope/Citation 寻址契约。`inspect --help` 是这些命令的唯一发现入口。命令面治理、全局参数、错误与退出状态见 #<vault::cli>；查询契约与检索语义见 #<vault::designs::host::query-contract> 与 #<vault::designs::host::search-retrieval-index>。

2026-08-29 裁决：collection 命令返回完整结果，没有分页与预算；`inspect --help` 之外不需要记忆其他入口。普通知识工作只使用本文命令与 `check`。


== Selector、Scope 与 Citation

Selector 是精确身份，Scope 是集合过滤：

```text
selector:
  vault::designs::host::query-contract          ModulePath
  designs/host/query-contract.not               Vault-relative path
  vault::designs::language::property-table/属性表  ItemName（ItemPath）

scope:
  --scope vault::designs                       include，段边界匹配，可重复
```

`vault::foo` 命中自身与 `vault::foo::*`，不命中 `vault::foobar`。Scope 属于查询语义本身：改变 scope 即是另一个查询。短 basename、模糊路径不能静默解析；`not_found` 与 `ambiguous_selector` 是 typed error。

所有可作为证据的位置统一为 `module + relative path + byte range + line range + id + source fingerprint`。普通 text 输出默认显示 `path:line`；excerpt 是候选选择信息，不是独立事实来源——search 与 references 的结果必须引导调用者在形成事实性回答前使用 `read`。

== status

用途：轻量入口，回答“这是哪个 Vault、快照是否可信、index 是否 ready”。

```shell
notist inspect status [--vault DIR]
```

输出：

```text
Vault        cfc3d18da3049056
Snapshot     disk revision 42
Runtime      daemon
Sources      31
Modules      31
Diagnostics  0
Index        ready (712 units)
Daemon       pid 12345
```

字段：

- Vault identity、disk snapshot stamp、runtime mode、source/module/diagnostic counts。
- Index health：`ready`、`building`、`stale`、`error`、`not_built`。
- 不返回正文、Heading 或 semantic item。
- exit 恒为 0，除非 Vault 无法打开或服务不可用。

== modules

用途：列出稳定逻辑地址，是 `ls` 的语义版。发现 ModulePath，而不是发现文件。

```shell
notist inspect modules [--vault DIR]
  [--prefix MODULE]
  [--kind any|source|virtual]
```

- `--kind`：`any` 默认，`source` 只列有 `.not` source 的 Module，`virtual` 只列目录/资源命名空间 Module。
- `--prefix MODULE`：按 ModulePath 前缀（段边界）过滤。
- 排序固定为 ModulePath 字典序。
- 每条逻辑 record 包含 ModulePath、relative path、kind、可选 semantic title、source fingerprint；text 投影显示 ModulePath、path 与 title。

```text
vault::cli  cli/README.not — Notist CLI: Command Design and Reference
vault::cli::inspect  cli/inspect.not — CLI Inspect: Agent Query and Navigation
```

== search

用途：按概念或字面量检索候选。候选不是证据。

```shell
notist inspect search <QUERY> [--vault DIR]
  [--mode lexical|exact|fuzzy|regex]
  [--exact | --fuzzy | --regex]
  [--scope MODULE]...
  [--fields title,heading,id,module,path,tag,body,raw,comment]
  [--operator all|any]
  [--group-by source|section|match]
  [--ignore-case]
  [--fuzzy-distance 1|2]
  [--wait-index <ms>]
  [--snippet-bytes <n>]
```

设计规则：

- 默认 lexical：fielded token match + BM25，`operator=all`；`--exact`（字面子串）、`--fuzzy`（有界 typo expansion）、`--regex`（Rust regex）是 `--mode` 的速记。
- `--operator any` 把默认 all 改为 any，用于宽召回；多词事实查找不要默认打开。
- `--fields`：可重复或逗号分隔；默认除 `comment` 外全部字段；未知字段 usage error；只影响匹配，不影响输出结构。
- `--group-by source` 默认（lexical/fuzzy）：每 Module 保留最佳 hit；`section` 每最近 Heading 保留最佳 hit；`match` 返回每个独立 region；exact/regex 默认 `match`。
- scope 在 candidate generation 前下推。
- index 未 ready 时内部等待 2s（`--wait-index` 可调，上限 10s）；超时返回 `index_not_ready`。
- excerpt 默认最多 256 bytes（`--snippet-bytes` 可调，上限 2048），优先围绕命中 term；截断会显式标出。
- 结果完整：零命中即证明所选 scope 内不存在匹配；空结果带 actionable hint。

```text
2 sources

1. vault::designs::host::query-contract  host/query-contract.not:42 field=body score=5.109
   查询契约与候选生成 ...
2. vault::designs::host::search-retrieval-index  host/search-retrieval-index.not:12 field=title score=7.115
   == Search 不是一种模式 ...
```

score 只能在同 query、index stamp、ranking version 内比较，不是概率。

== outline

用途：只看一个 Module 的 Heading 结构，不读正文。结构是求值后的 Heading，不是源码正则猜测。

```shell
notist inspect outline <MODULE|PATH> [--depth 1..6] [--vault DIR]
```

- `--depth` 默认 6，范围 1..=6。
- 每条 heading record 包含 name、level、id（显式 id 或标题默认 id）、location、parent range 与 subtree range；text 投影显示缩进、id 与 `path:line`。
- 全 Vault 结构发现用 modules/search，不用 outline 遍历全库。

```shell
notist inspect outline vault::designs::host::query-contract --vault docs --depth 2
```

== read

用途：按稳定地址读 authored source，是形成事实结论的唯一证据入口。

```shell
notist inspect read <MODULE|PATH|MODULE/NAME> [--vault DIR]
  [--from-line N [--lines N] | --byte-range START..END]
```

窗口规则（语义窗口，不是截断）：

- 默认：读取 selector 选中的完整内容——Module 风格 selector 读整篇，ItemPath 读整节 subtree。
- `--from-line N`：1-based 行号；单独使用读从该行到末尾。
- `--lines N`：必须与 `--from-line` 同用，限制读取行数。
- `--byte-range START..END`：半开 UTF-8 byte range；与 line 窗口互斥。
- `MODULE/NAME`（ItemPath）：NAME 先按显式 `@id`、后按标题默认名（标题文本）精确匹配；读取整个节 subtree（从节标题到下一个同级或更高级 heading 前），不是只读标题行。

text 输出 = 带行号 gutter 的 authored source；location（module、path、byte/line range、fingerprint）由服务端 record 携带，JSON 场景（LSP、未来 artifact 投影）可直接消费。

== references

用途：遍历已经解析的 Reference graph。默认 incoming，回答“谁引用这个目标”。

```shell
notist inspect references <MODULE|MODULE/NAME> [--vault DIR]
  [--direction incoming|outgoing|both]
  [--include-definition]
  [--snippet-bytes <n>]
```

- 默认 incoming（backlinks）。
- `--direction outgoing`：正向列出该 Module 或节引用了什么；使用 captured reference graph，不重新扫描文本。
- `--direction both`：同时返回两个方向；`--include-definition`：附上目标自身定义位置。
- 每条 item 包含 source/target Module、方向、location 与最多 256 bytes excerpt；不重复整篇 source。

```shell
notist inspect references vault::designs::host::query-contract --vault docs
```

== definition

用途：由精确 source offset 找定义，是 LSP definition 的 CLI 投影。

```shell
notist inspect definition <PATH> <UTF8_BYTE_OFFSET> [--vault DIR]
  [--expected-fingerprint HASH]
```

- offset 是 UTF-8 byte offset，不是字符下标；line/column 转换由调用方 LineIndex 完成。
- `--expected-fingerprint` 不匹配返回 `snapshot_changed`；防止旧 offset 查询新文本。
- 返回零或一个 location；text 打印 `path:start..end`。

== Agent 工作流

```text
inspect status / inspect modules
  -> inspect search 发现候选
  -> inspect outline 看结构
  -> inspect read 读取 authored evidence
  -> inspect references / definition 沿语义边导航
  -> check 验证状态
  -> 用宿主编辑工具直接修改 .not，修改后再次 check
```

停止规则：

- 正向事实：read 取得充分证据后停止。
- 否定或“列出全部”：search 结果本身完整，零命中即不存在；需要穷举的按 scope 分次查询。
- search excerpt 不是最终证据；references 不是正文替代品。
