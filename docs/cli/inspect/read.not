@!(implementation: "aligned")
= Inspect Read: 带属性注解的读取

Agent 的读、编辑范围（path + 行号/字节偏移）与 Item 树边界没有任何保证关系，而属性只在其管辖子树上有确定值。`inspect read` 对任意范围求*有效属性环境的一致分解*：把范围切成若干有效环境一致的极大段，每段各自有确定的属性 Dict，并默认内嵌该段覆盖的完整源文行——read 由此是带属性注解的读取，段与文本的对应由工具完成，调用者不再自行按行号拼接。完整算法与归属裁决见 #<vault::ai::2026-08-31 区域属性投影与 inspect info 命令>。

@(todo: "ItemPath selector 当前返回标题自身 range；「读取整个节 subtree」语义待裁决")

== 命令面

```shell
notist inspect read <SELECTOR> [--vault DIR]
  [--line START..END | --offset N | --byte-range START..END | --from-line N [--lines M]]
  [--origins] [--attrs-only]
```

- SELECTOR 两族形态：`ModulePath[/ItemName]`（ItemName 为标题链 ItemId 或 `@id` 标注名，先按显式 `id:` 标注、后按标题默认名精确匹配）或 `PATH[#ItemName]`（OS path 形态，以 `#` 分隔因 `/` 属于文件系统）。坐标全部缺省 = selector 选中的范围：Module selector 为整个模块，Item selector 为其 range。四种坐标 flag 互斥，给出时覆盖 selector 自身的 item 选择（与 ancestors 同一优先级）。
- `--line` 是面向 Agent 的一等坐标（行号是 grep/Read 的原生产出）：1-based 闭区间；`--byte-range` 为 UTF-8 半开区间（由 notist 的 citation 发放，Agent 不自造字节偏移）；`--offset` 为零宽点查询；`--from-line N [--lines M]` 为行窗口（与宿主 Read 的 offset+limit 习惯同构），窗口在源文末尾钳制而非报错。越界或非 UTF-8 边界报 `invalid_argument`，selector 不存在报 `not_found`。
- 每段默认内嵌其覆盖的完整源文行，行号 gutter 为 1-based 源文行号，与 grep/宿主 Read 的坐标一致；`--attrs-only` 只投影属性面。
- 属性默认显示为合并后的最终 Dict——直接回答"这些行上什么在生效"；`--origins` 切换到分层溯源视图（`common` 块 + 按声明分组的条目行，各带出处区间），`common` 块仅在 origins 模式出现。
- header 携带 module 身份、源文相对路径、行/字节区间与 fingerprint——路径向宿主编辑工具的交接在这里完成（身份→路径的唯一反向出口；`inspect locate` 是路径→身份的正向桥）。除 header 外的行内不出现文件路径（与 items/ancestors 同规）。

== 分解规则

1. 属性条目先展开为管辖 scope 的区间（标题绑定上卷到节；模块 `@!` 以全文件范围合成条目），scope 之间的全空白缝隙归并给上一段——每段从其首个有效行（注解/标题/内容）开始，选区尾部空白吸收进末段；空白行不属任何节点，段内空白与段落原样呈现。
2. 按管辖边界原子化后逐段求环境（区间 stabbing，逐 key 最内层值胜），相邻有效环境相等则合并为极大段——段边界只由属性差异产生。
3. 段边界完整覆盖所含块是常态，"局部视图"不设专门标记：`container` 行携带最深包含整段范围的 Item 的完整区间，选区边缘是否切进内容由调用者对照自身坐标判断。

== 输出逐行契约

header 一行（module、相对路径、行区间、字节区间、fingerprint）；`container` 一行（最深包含整段范围的 Item）；`segments N`；默认模式每段依次为 `[i]` header（`<ItemPath>` + 行区间 + 字节区间）、合并环境 Dict 行、该段的行号源文。Dict 为 notist 字面量 `(k1: "v1", k2: "v2")`，key 排序、字符串值带引号、空环境为 `(:)`。`--origins` 模式以 `common N` 块 + `@(k: v) <- <ItemPath> lines a..b bytes s..e` 形式的分层溯源行取代各段 Dict 行。身份一律为可复制进 selector 的 ItemPath；颜色由全局 `--color` 控制且只影响渲染（见 #<vault::cli>）。

== 实测示例

示例 Vault `docs/example/test.not` 是常驻测试案例（`|>`/`<|` 标出请求范围 lines 12..22）：

```shell
notist inspect read vault::example::test --line 12..22 --vault docs
```

```text
module <vault::example::test> example/test.not lines 12..22 bytes 654..1607 fingerprint af44739a652532a8
container <vault::example::test/Heading 1> lines 2..47 bytes 1..2252
segments 3
[1] <vault::example::test/Heading 1/Heading 2.1> lines 12..15 bytes 654..1260
    (a: "a", o: "o")
   12 | Lorem ipsum dolor sit amet, consectetur adipiscing elit. Donec efficitur ligula sed nunc sagittis, |>non volutpat mi luctus eu. …
   13 |
   14 | Lorem ipsum dolor sit amet, consectetur adipiscing elit. Donec efficitur ligula sed nunc sagittis, …
   15 |
[2] <vault::example::test/Heading 1/Heading 2.2> lines 16..20 bytes 1260..1588
    (b: "b", o: "o")
   16 | @(b: "b")
   17 | == Heading 2.2
   18 |
   19 | Lorem ipsum dolor sit amet, consectetur adipiscing elit. Donec efficitur ligula sed nunc sagittis, …
   20 |
[3] <vault::example::test/Heading 1/Heading 2.2> lines 21..22 bytes 1588..1607
    (b: "b", c: "c", o: "o")
   21 | @(c: "c")
   22 | - List<|
```

（实测输出中 Lorem 行为完整行，此处以 … 截断版面。）示例阅读：`@(b: "b")` 与 `@(c: "c")` 各自打开其声明的段，注解与它声明的属性同段呈现；段间空白（行 15、行 20）归并给上一段；段 [2] 与段 [3] 的区分完全来自匿名 list Item 上的 `c`；全段共享的 `o` 在每段 Dict 里重复——每段自足，单独读任何一段都完整。

模块 `@!` 继承的呈现（`vault::cli` lines 30..37，`--origins --attrs-only`；`agents` 条目值过长，此处截断）：

```shell
notist inspect read vault::cli --line 30..37 --origins --attrs-only --vault docs
```

```text
module <vault::cli> cli/README.not lines 30..37 bytes 2576..2699 fingerprint 016f0863a28898cd
container <vault::cli/Notist CLI: Command Design and Reference/设计总览/命令分组> lines 29..46 bytes 2559..3451
common 2
    @(agents: 所有 CLI 命令的输出示例必须来自实测，并完整体现字段说明声称的每一种形态——虚拟模块、@id 标记、ambiguous、definition 标记、省略等边界；…) <- module lines 1..249 bytes 0..13529
    @(implementation: partial) <- module lines 1..249 bytes 0..13529
segments 1
[1] <vault::cli/Notist CLI: Command Design and Reference/设计总览/命令分组> lines 30..37 bytes 2576..2699
```
