= 插件示例：Shader

本文演示 Notist 插件系统的一个 Wasm 插件包闭环：`shader` 插件是一个通过 `docs/Notist.toml` 声明加载的 WebAssembly 插件，LSP/analysis 会因此认识 `shader` 函数。 插件系统设计见 [[vault::designs::plugin-system]]。

== 插件声明

在 `Notist.toml` 中声明要加载的插件包：

```toml
[plugins.shader]
path = "../plugins/shader"
```

插件包目录包含：

```text
plugins/shader/
├── plugin.json
├── semantic.wasm
├── semantic.wat
└── assets/
    ├── shader.js
    └── shader.css
```

`plugin.json` 是信封：身份、Wasm 装载参数与 HTML contribution；manifest 不含权限请求。注意 `shader` 包是 2026-08-18 初版 raw ABI 的存量包：它的元素 schema 来自 manifest 的 `interfaces.semantic`，`semantic.wasm` 没有 `init` 导出。宿主在打开 vault 时读取 `Notist.toml`，实例化 `semantic.wasm` 完成注册，把 `shader` 函数放进当前分析的函数环境。新式组件的装载流程（`init` 自描述 schema、manifest 不携带 semantic 接口）以 `plugins/component-echo` 为准。

== 调用方式

`shader` 是一个插件函数，签名等价于：

```text
shader(source: String, width: Int = 800, height: Int = 600,
       trailing body: Content) -> Content
```

`source` 是 WGSL fragment 主函数：

```wgsl
fn mainImage(fragCoord: vec2<f32>) -> vec4<f32> {
    let uv = fragCoord / vec2<f32>(800.0, 600.0);
    return vec4<f32>(uv, 0.5, 1.0);
}
```

== 实际效果

下面这段文档会渲染为一个可交互的 shader canvas：

#shader(
  source: r#"""
fn mainImage(fragCoord: vec2<f32>) -> vec4<f32> {
    let uv = fragCoord / vec2<f32>(800.0, 600.0);
    return vec4<f32>(uv, 0.5, 1.0);
}
"""#,
  width: 800,
  height: 600,
)[
这是一个用 WGSL 写的渐变 shader。
]

如果看到普通 fallback，说明当前浏览器不支持 WebGPU，或 Wasm 插件没有被正确加载。
