= Notist CLI

`notist` 同时服务终端用户、shell script、编辑器和 Agent。有限查询默认输出适合人阅读的 text；`--format json` 输出 schema version 2 的稳定 envelope。LSP 自己使用 JSON-RPC，不能再叠加 `--format json`。

普通查询遵循同一条边界：默认有 item 和 UTF-8 byte 预算，调用者可以在硬上限内调整；没有读完时返回 opaque cursor。需要完整数据时使用显式 `export --output`，而不是让普通 search、outline 或 check 意外输出整个 Vault。

== 命令总览

```text
notist status [ROOT]
notist modules [ROOT]
notist search <QUERY> [ROOT]
notist outline <MODULE|PATH> [ROOT]
notist read <MODULE|PATH|MODULE#ID> [ROOT]
notist references <MODULE|MODULE#ID> [ROOT]
notist query definition <PATH> <UTF8_BYTE_OFFSET> [ROOT]
notist check [ROOT]

notist edit replace ...
notist edit rename ...
notist index status|rebuild [ROOT]
notist debug inspect [ROOT]
notist export diagnostics|snapshot|outline [ROOT] --output FILE

notist build [ROOT]
notist preview [ROOT]
notist skill init <OUTPUT> [--force]
notist daemon [ROOT]
notist daemon stop [ROOT]
notist lsp
```

运行 `notist --help` 查看顶层命令，运行 `notist <COMMAND> --help` 查看当前版本实际接受的参数。CLI help 是安装版本命令面的最终依据。

== ROOT 与 Vault 发现

需要 Vault 的命令把 `[ROOT]` 作为最后一个 positional，默认是当前目录。ROOT 可以是 Vault 内的文件或目录；Notist 按 [[vault::designs::world::boundary-discovery#发现规则]] 的发现规则向上寻找最近的 `Notist.toml`，canonicalize 后再连接该 root 对应的 daemon。

```shell
notist status .
notist search "type system" docs
notist check docs/designs/host/query-contract.not
```

Nested Vault 不进入外层扫描。Machine result 默认使用 Vault-relative path；canonical absolute path 只出现在明确需要文件系统身份的维护操作中。

== 全局选项

| Option | 含义 |
| --- | --- |
| `--format text|json` | 选择 human text 或 schema-version-2 JSON；默认 text |
| `--color auto|always|never` | 控制 human diagnostic 颜色；不改变 query result |
| `--pager auto|always|never` | 只分页当前 bounded text page；不会自动读取 continuation |
| `--no-daemon` | 在当前进程运行同一 service/core operation |
| `--help` | 显示命令或 subcommand help |
| `--version` | 显示版本 |

Global option 可以放在 subcommand 前后，但脚本建议统一放在命令前。Pager 的 auto 只在 human text TTY 中启用；JSON、daemon、LSP 和 preview 不经过 pager：

```shell
notist --no-daemon --format json search "WorkspaceSnapshot" docs
```

`--no-daemon` 只改变部署形态，不关闭 analysis、snapshot、预算或校验。唯一需要注意的是后台 index task 没有可继续持有它的 daemon，因此 `index rebuild --no-daemon` 会等待 task 完成。`lsp` 和 daemon protocol stdout 只允许协议 framing；对它们传 `--format json` 会直接报错。

== 分页与输出预算

Collection command 共享：

| Option | 含义 |
| --- | --- |
| `-n, --limit N` | 本页最多返回的 item；普通 hard maximum 是 100 |
| `--max-bytes N` | logical result 的 UTF-8 byte 预算；范围 4096..65536 |
| `--cursor TOKEN` | 继续同一个 snapshot/query/filter/sort 序列 |

默认 logical budget 是 16 KiB，普通 hard maximum 是 64 KiB。Search 默认 8 条；modules、references 和 diagnostics detail 默认 20 条；单 Module outline 默认最多 100 个 Heading。Byte budget 比 item limit 优先。

Collection result 形状：

```json
{
  "snapshot": {},
  "items": [],
  "page": {
    "requested_limit": 8,
    "applied_limit": 8,
    "returned": 8,
    "has_more": true,
    "next_cursor": "opaque"
  },
  "budget": {
    "requested_bytes": 16384,
    "applied_bytes": 16384,
    "logical_bytes": 7210,
    "exhausted": false
  },
  "coverage": {
    "complete": false,
    "stop_reason": "item_limit"
  }
}
```

`coverage.complete=false` 表示当前页没有覆盖完整集合，不等于“未找到”。`stop_reason` 区分 `item_limit`、`byte_budget`、`query_limit` 和完整结束。正向事实查找在 `read` 已取得充分 authored evidence 后可以停止，不需要为了形式上的 complete 读完所有 search page；只有首页不足、任务要求穷举，或者结论依赖证明“不存在”时才必须继续。下一次调用必须保留原 query、selector、scope、field、mode、grouping 和排序参数，只改变 cursor 以及可选的页面预算。未完成页面的 `hints` 和 text renderer 都会要求对相同查询追加 cursor。

Cursor 绑定 Vault source fingerprint。当前 cursor 使用带校验的紧凑 binary payload 和 base64url 表示，同时继续接受旧 JSON cursor；新响应只签发紧凑版本。Vault 已变化时返回 `cursor_stale`，query/filter/grouping 不匹配或 token 损坏时返回 `invalid_cursor`；hint 会要求原样保留 selector、query、filter、mode 与排序参数，或者省略 cursor 重新开始。服务不会把旧 byte offset 猜测到新 source 中。

== status

```shell
notist status [ROOT]
```

返回 Vault identity、disk snapshot、runtime mode、source/module/diagnostic counts 和 lexical index health。Index health 可以是 `ready`、`building`、`stale`、`error` 或 `not_built`。它不返回正文、Heading 或 semantic item，固定 logical 上限为 8 KiB，适合成为人和 Agent 的第一个轻量入口。

```text
Vault        cfc3d18da3049056
Snapshot     disk revision 42
Sources      31
Modules      31
Diagnostics  0
Index        ready (712 units)
```

== modules

```shell
notist modules [ROOT]
  [--prefix MODULE]
  [--kind any|source|virtual]
  [--limit N] [--max-bytes N] [--cursor TOKEN]
```

每条 record 只包含 ModulePath、Vault-relative source path、source/virtual kind、可选 semantic title 和 source fingerprint。默认按 ModulePath 排序。

```shell
notist --format json modules docs --prefix vault::old-designs --kind source --limit 20
```

== search

```shell
notist search <QUERY> [ROOT]
  [--mode lexical|exact|fuzzy|regex]
  [--exact | --fuzzy | --regex]
  [--scope MODULE]...
  [--fields title,heading,id,module,path,tag,body,raw,comment]
  [--operator all|any]
  [--group-by source|section|match]
  [--ignore-case]
  [--fuzzy-distance 1|2]
  [--wait-index DURATION]
  [--snippet-bytes 64..2048]
  [--limit N] [--max-bytes N] [--cursor TOKEN]
```

Search 有四种明确模式：

| Mode | 匹配 | 排序 | 用途 |
| --- | --- | --- | --- |
| `lexical` | fielded token match | BM25 relevance | 默认自然语言与主题检索 |
| `exact` | literal substring | source order | 类型名、错误消息、源码片段 |
| `fuzzy` | lexical + Damerau-Levenshtein expansion | relevance | 英文或 identifier typo |
| `regex` | Rust regex | source order | 人类 shell 调查 |

`--exact`、`--fuzzy`、`--regex` 是互斥 alias。Lexical/fuzzy 默认 `--group-by source`，让首页优先覆盖不同 Module；exact/regex 默认 `--group-by match`，保留逐处定位。`section` 返回每个最近 Heading 下的最佳 hit，显式 `match` 可以关闭折叠。`--operator all` 是 lexical/fuzzy 默认值，也是多词事实查找的首选；`--operator any` 只用于宽召回。`--ignore-case` 只适用于 exact/regex；`--fuzzy-distance` 只适用于 fuzzy，最大为 2。`--wait-index` 默认 `2s`、最大 `10s`，只适用于 lexical/fuzzy；当前 snapshot 的 index 已经 ready 时不会额外等待。显式传入与 mode 无关的参数属于 usage/schema error，不会被静默忽略。

```shell
notist search "workspace snapshot" docs
notist search --exact "WorkspaceSnapshot" docs --group-by match
notist search --fuzzy "langauge sever" docs
notist search --regex "D00[0-9]+" docs
```

Lexical tokenizer 使用 Unicode normalization 和 case folding；保留完整 identifier，并拆分 snake_case、kebab-case、camelCase、ModulePath 和 path segment；连续汉字产生 overlapping bigram。它不使用 stop-word removal 或 stemming，避免技术名称、短数字和 `not` 等词消失。

Default field 是除 comment 外的全部字段。Field weight 属于 ranking version：id/module 6、title 5、heading/tag 4、path 3、body 1、raw 0.7。`id` 字段同时索引显式 scope id 与标题默认 id。Result 中的 score 是固定精度 BM25 排序值，只能在同 query、index stamp 与 ranking version 内比较，不能解释为概率。

`--scope` 是精确的绝对 ModulePath prefix：`vault::foo` 匹配自身及 `vault::foo::*`，不会匹配 `vault::foobar`。Scope 与 field 在 candidate generation 前进入 query。Ranked search 每次最多考虑 10000 个候选；达到上限时 `coverage.stop_reason=query_limit`。Regex 使用线性时间的 Rust regex engine，另有 2 秒 scan deadline；超时返回 typed `query_timeout`，建议缩小 scope/field 或改用 exact/lexical。

Search hit 返回 ModulePath、relative path、source fingerprint、byte/line range、matched field、可选 match range、有界 excerpt 和可选 score。应用后的 `group_by`、ordering、ranking version、index stamp 和 fuzzy expansion 状态位于页级 `search` metadata，不在每个 hit 中重复。Lexical excerpt 默认最多 256 bytes，并优先围绕实际 query term 截取；无法映射的 fuzzy expansion 才退回 unit 中心。Excerpt 只用于挑选候选；回答事实前使用 `read` 读取 authored source。完整零结果仍然 exit 0，并带有界 `hints` 建议减少关键词、显式改用 `operator=any`，或切换 exact/lexical；CLI 不会替调用者自动放宽 query。

== outline

```shell
notist outline <MODULE|PATH> [ROOT]
  [--depth 1..6]
  [--limit N] [--max-bytes N] [--cursor TOKEN]
```

Outline 只读取一个 source-backed Module，不再把整个 Vault 的所有 Heading 一次输出。每条 item 包含 name、level、id（显式 id，否则标题默认 id）与精确 location。

```shell
notist outline vault::designs::host::query-contract docs --depth 3
```

全 Vault discovery 使用 `modules` 或 `search`；需要完整 outline artifact 时使用 `export outline`。

== read

```shell
notist read <MODULE|PATH|MODULE#ID> [ROOT]
  [--from-line N --lines N | --byte-range START..END]
  [--max-bytes N] [--cursor TOKEN]
```

Read 返回原始 authored source、实际 byte/line range、source fingerprint、是否到达 selector 末尾和 continuation。Line、byte 与 id section 是三种窗口：

```shell
notist read vault::designs::host::query-contract docs \
  --from-line 130 --lines 80

notist read vault::designs::host::query-contract docs \
  --byte-range 4000..8000
```

默认最多读取 120 行，并始终受 16 KiB logical budget 限制。长 source 自动返回 cursor，不需要 Agent 猜 shell byte offset。Text output 带行号 gutter；JSON 的 source string 不包含 gutter。

== references

```shell
notist references <MODULE|MODULE#ID> [ROOT]
  [--direction incoming|outgoing|both]
  [--include-definition]
  [--snippet-bytes N]
  [--limit N] [--max-bytes N] [--cursor TOKEN]
```

默认 direction 是 incoming。每条 item 返回 source/target Module、edge direction、location 和有界 excerpt，不为同一个 location 重复整篇 source。

== query definition

```shell
notist query definition <PATH> <UTF8_BYTE_OFFSET> [ROOT]
  [--expected-fingerprint HASH]
```

Offset 明确使用 UTF-8 bytes。Definition 返回零或一个 bounded location，不携带 full source。Expected fingerprint 不匹配时返回 `snapshot_changed`，用于防止旧 offset 查询新文本。

== check

```shell
notist check [ROOT]
  [--scope MODULE]
  [--summary]
  [--severity error|warning|info]
  [--limit N] [--max-bytes N] [--cursor TOKEN]
```

Check 始终完整分析选定 scope；预算只限制 diagnostic detail，不限制 summary 或 exit status。JSON 返回 checked source 数、完整 diagnostic count、按 code 计数和 diagnostics page。`--summary` 省略 detail；`--severity` 选择最低展示级别，当前 core diagnostics 均为 error。

Human text 使用有界的 rustc/Typst 风格 source frame：

```text
error[invalid-syntax]: unclosed Content block
  --> ai/example.not:13
   |
 13 | #callout[

3 diagnostics in 2 sources; showing 3
```

需要完整 machine artifact 时使用：

```shell
notist export diagnostics docs --output diagnostics.json
```

== Edit

```shell
notist edit replace <PATH> <START> <END> <REPLACEMENT>
  --idempotency-key KEY
  --expected-fingerprint HASH
  [--yes]

notist edit rename <FROM> <TO>
  --idempotency-key KEY
  --expected-fingerprint HASH
  [--yes]
```

Convenience edit 仍经过 propose、validate、fingerprint precondition 和 apply pipeline。没有 `--yes` 时只返回 proposal 并使用 exit 1；脚本不能因为 stdin 不是 TTY 就被隐式授权写入。Range 使用 UTF-8 bytes，冲突不会 fuzzy patch。

同一边界在 CLI 上由 propose（无 `--yes` 只返回 proposal，exit 1）与 apply（显式 `--yes`）两个步骤实现，Agent 与人类走同一条 guarded pipeline。

== Index

```shell
notist index status [ROOT]
notist index rebuild [ROOT] [--wait]
```

Index status 返回 `ready/building/stale/error/not_built`、schema/tokenizer/ranking version、indexed source fingerprint、unit count、最近错误和可选 operation handle。Lexical/fuzzy search 首次使用时为捕获的 snapshot 提交共享 build task，并按 `--wait-index` 等待；deadline 后 task 继续在 daemon 中运行，调用者得到 `index_not_ready` 和 exact fallback。Exact/regex 不依赖 index。

Disk View 的新 generation 从 manifest 的逐 source fingerprint 计算 delta：复用旧 Tantivy segment，对修改/删除 source 写 tombstone，只重建新增或变化的 source，然后最后原子发布 manifest 与 immutable reader。Session overlay 使用独立的完整 in-memory generation，不会把未保存文本写入持久 cache。`index rebuild` 只删除并替换当前 snapshot 的可重建 generation，不修改 authored Vault；默认返回后台 task handle，`--wait` 输出一个有界 waiting event 后等待最终状态。Embedded `--no-daemon` 会隐式等待，以免进程退出时丢失后台 task。

== Debug 与 Export

```shell
notist debug inspect [ROOT]
  --section modules|references|semantic
  [--module MODULE]
  [--limit N] [--max-bytes N] [--cursor TOKEN]
```

Debug inspect 必须选择有界 projection。旧的普通 `inspect` 命令已经退出公开 command surface；Agent 不应使用 debug 做正常 discovery。

```shell
notist export diagnostics [ROOT] --output FILE
notist export snapshot [ROOT] --output FILE
notist export outline [ROOT] --output FILE
```

Export 捕获一个 snapshot，将完整 artifact 原子写入显式文件；stdout 只返回 kind、path、record/byte count、snapshot 与 SHA-256 checksum。`--output -` 是专家级无界 opt-in，不供官方 Skill 使用。

== Build 与 Preview

```shell
notist build [ROOT] --output DIR [--clean]
notist preview [ROOT] [--host IP] [--port N] [--open]
```

Build 从一个 captured snapshot 生成多页静态 HTML。`--clean` 只清理已经验证的 output directory。Preview 默认监听 `127.0.0.1`，port 0 让系统选择空闲端口；默认不打开浏览器，加 `--open` 才在默认浏览器中打开 preview URL。Preview 的 JSON 格式是持续 JSON Lines event，不是有限 query envelope。

== Agent Skill 与官方文档

```shell
notist skill init <OUTPUT> [--force]
```

默认情况下 OUTPUT 必须是尚不存在的目录，命令只生成一个确定性的 `SKILL.md`，不猜 Codex、Claude 或其他 Host 的安装位置。传入 `--force` 时，OUTPUT 必须是已有目录；命令原子替换其中的 `SKILL.md`，并保留其他文件。已有的普通文件永远不是有效输出。当前 executable 同步自己的官方 docs Vault；详细发布边界见 [[vault::designs::host::cli-command-surface-skill-distribution]]。

官方 Agent 工作流是：

```text
status / modules
  -> search or one-Module outline
  -> read authored source
  -> references / definition / check
  -> propose edit / apply edit
```

Search 是候选发现而不是穷举仪式。正向事实查找在 `read` 已核实答案后停止；当前页没有合适候选时再继续 search。只有“列出全部”“确认没有”之类依赖完备性的任务必须沿 cursor 读到 `coverage.complete=true`。

== LSP 与 Daemon

```shell
notist daemon [ROOT]
notist lsp
```

Daemon 为一个 canonical Vault root 持有共享 Engine、watcher、cache 和 disk View。CLI 默认惰性连接或启动对应 daemon；`--no-daemon` 使用同一个 service object 的 embedded mode。Local endpoint 包含 protocol version，升级后不会误连到仍在 idle grace period 内的旧 daemon。

Daemon 在握手时回传自身二进制的 stamp；client 发现 daemon 运行的是不同二进制（本地 `cargo run` 重建后的典型情况）时，会请求其关闭（无其他活跃客户端时接受）并自动用当前二进制重启，因此不会默默查询上一次构建的旧代码。`notist daemon stop [ROOT]` 显式停止某个 Vault 的 daemon（没有 daemon 时成功退出，脚本可幂等调用）；daemon 的 pid 文件只用于诊断，`notist status` 的 text 输出会显示当前 daemon pid。LSP 管理 editor overlay View；CLI 默认查询已保存的 disk View。

== JSON Envelope、错误与退出状态

有限 CLI JSON 使用：

```json
{"schemaVersion":2,"command":"search","ok":true,"result":{}}
```

Typed error 使用：

```json
{
  "schemaVersion": 2,
  "command": "search",
  "ok": false,
  "error": {
    "code": "cursor_stale",
    "message": "Vault sources changed after the cursor was issued",
    "retryable": true,
    "hint": "restart the query without a cursor"
  }
}
```

稳定 error code 包括 `not_found`、`ambiguous_selector`、`invalid_cursor`、`cursor_stale`、`snapshot_changed`、`index_not_ready`、`query_timeout`、`query_limit`、`budget_too_large`、`edit_conflict` 与 `plan_expired`。包括 clap parse failure 在内的 JSON usage error 也使用同一 envelope。脚本按 exit status 粗分，再按 error code 恢复，不解析 message substring。

| Exit | 含义 |
| ---: | --- |
| 0 | 成功；零 search result 也属于成功 |
| 1 | 命令有效完成但业务结论失败，例如 check 有 diagnostic 或 proposal 未应用 |
| 2 | clap usage/schema error |
| 3 | selector、cursor、index 等可纠正 query error |
| 4 | service/protocol unavailable |
| 5 | edit conflict 或 precondition failure |
| 70 | 未分类 internal failure |

接口模型与选择理由见 [[vault::designs::host::cli-command-surface-skill-distribution]] 与 [[vault::designs::host::query-contract]]。
