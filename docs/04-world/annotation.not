@!(implementation: "aligned")
= Annotation Table

标注把属性绑定到求值结果上；求值收尾时，这些绑定转写为旁置的*属性表*（annotation table）：以节点序列区间为键、属性集为值的映射。树（scope 树、结构树）本身不携带属性——属性是位置注解，与结构解耦。标注的书写语法（`@`/`@!` 前缀、Dict 载荷、挂载位置的字面形式）见 #<vault::03-language::grammar::annotation>；本文定义数据模型与语义。

== 坐标：节点序列区间

属性表的坐标是节点序列区间 `[start, end)`。节点是语法构造的产物，粒度由语法决定：

- `core::text`：文本流中不被其他构造（表达式嵌入、标注、构造调用、段间分隔）打断的连续片段——`文字@note 继续` 在标注处切开，`@note` 挂在第一个 `core::text` 节点上；
- `core::parbreak`：段间分隔，也是节点；
- 其他节点：每个构造调用一个个体；容器节点的 body 是子序列。

序列是分层的（容器 body 是子序列），区间在同一层序列上，可以跨越段落边界。成型只重组节点，不合并、不删除个体（#<vault::03-language::structure>）——区间在求值、成型与投影之间稳定；文本拼接是渲染层优化，不是成型语义。

== 条目与挂载位置

属性表有三类条目，对应三种挂载位置：

- *Item 条目*：Item 上的属性 → 覆盖该 Item 区间的条目。ScopeItem（`#[...]`、Item literal）的区间覆盖其全部内容，可以跨越段落边界；
- *节条目*：修饰节头部 heading 的属性（`@(wip: true)`、`@(id: "安装")` 紧贴 heading）→ 该节覆盖的区间；节边界由 heading 归组规则确定（#<vault::03-language::structure>），节属性语义上作用于整个子树；
- *模块条目*：模块属性（`@!(class: ..)`）作为模块级条目进入属性表，随求值结果发布为模块元数据。

挂载位置在求值期的含义：`@expr` 绑定其后紧邻的 Item，`@!expr` 绑定根 scope（模块）。

*前导注解行属于所附块*：独立注解行（或堆叠的注解行块）绑定其后紧邻的 Item，所附块的 span 从注解行自身起算，条目区间相同——声明属性的字节受该属性管辖，块间不留注解隙地。后缀注解的字节本就在所附块行内，不在此列。

*属性是值的属性*：绑定在值上的属性随值复制——`#let alert = @(wip: true)[注意]` 之后 `#alert #alert` 产生两个节点个体，属性表里有两条 `wip` 条目。

== 继承与有效环境

条目的管辖范围是其附着节点的整个子树：Item 条目覆盖该 Item 的全部内容，节条目作用于整节子树，模块条目覆盖全文件。一个位置的有效环境由覆盖它的条目叠加而成：按管辖范围从大到小遍历、逐 key 首写者胜——最内层的值存活。继承由此自然产生：模块与外层节的属性对内部全部位置生效，内层同 key 条目就地覆盖；没有特设的继承规则，也没有合并停止条件——两侧共同的祖先对两侧贡献逐 key 相同，天然不产生环境边界。

有效环境的计算是纯区间 stabbing：属性表是唯一入口，不沿树走；对求值后才存在的属性（spread 展开的条目）天然正确，按源码 grep 原文在原理上做不到这一点。

== 查询面

查询一律以区间命中（单节点区间是特例），没有「节点命中」与「区间命中」两种形状——属性表是唯一入口。查询面覆盖全部 Item，包括匿名 Item；寻址面只覆盖有 ItemId 的 Item（#<vault::04-world::model/数据模型/Item 树>）。

属性表的消费者：region 属性投影与 annotated read（#<vault::05-cli::inspect>）、检索索引、LSP 的属性相关查询（#<vault::06-lsp>）。值层与属性表是唯一真相，成型后的结构树是它们的投影——可丢弃、可重建，任何消费者都不能把投影当作新的真相；消费端联合消费结构树与属性表。

== 与求值、成型的关系

求值期：标注表达式在文档环境中求值（含 import），结果必须是 Dict，绑定到挂载目标；求值规则见 #<vault::03-language::evaluation>。求值收尾：绑定转写为属性表条目，Item 条目、节条目与模块条目按各自管辖范围入表。成型只重组节点（#<vault::03-language::structure>），区间因此跨阶段稳定。
