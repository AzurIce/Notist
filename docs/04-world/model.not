@!(implementation: "aligned")
= 数据模型

Notist 的数据模型是两层树：*Module* 树直接对应 Vault 内的文件系统结构，*Item* 树是一篇文档的求值产物。Module 有同一身份的两种语义——作为知识库中的稳定逻辑节点被引用，作为可编程文档的求值单元被 import。Vault 边界见 #<vault::04-world::boundary>；跨模块的两条边分别见 #<vault::04-world::import> 与 #<vault::04-world::reference>；属性的一面见 #<vault::04-world::annotation>。

== Module 树

文件系统决定 Module 的逻辑层级，source 不需要再声明一遍。形如 `<vault_root>/path/to/m.not` 的路径映射为模块 `vault::path::to::m`：

```text
vault-root/
├─ Notist.toml
├─ README.not                 # vault
├─ intro.not                  # vault::01-intro
└─ guide/
   ├─ README.not              # vault::guide
   └─ setup.not               # vault::guide::setup
```

- 普通文件名（去扩展名）成为 ModulePath 的最后一段；`README.not` 表示它所在的目录；
- `guide.not` 与 `guide/README.not` 都映射到 `vault::guide`，二者互斥、不能同时存在——一个逻辑 Module 至多一个 source，使定义、import、引用与输出路径共享单一身份；
- 移动 source 若改变 ModulePath，就是逻辑 Module 重命名，而不仅是文件移动——Reference 与 import 需要同一个 rename plan。

== virtual module 与资源

目录只要有 `.not` 后代或资源文件后代就形成 *virtual module*（即使没有自己的 `README.not`）：只提供命名空间与 child identity，不凭空产生 Item 或 bindings。

资源文件（图片等）通过「目录模块 + 完整文件名」寻址：`vault::docs::images/logo.png` 指向 `docs/images/logo.png`。资源目录因此也进入 Module 树，作为 virtual module：文件名即模块内 Item 的身份，但资源目录没有 source、没有 Item、没有可 import 的 bindings——求值与导入的语义只属于 `.not` source。

== ModulePath

`vault::guide::setup` 这样的路径是 *ModulePath*。它与文件路径一一对应，由目录结构直接得到，不需要声明。

文法侧，ModulePath 出现在 Target 字面量中（完整文法见 #<vault::03-language::grammar::code/Code/Target 引用>）：

- `::` 是段分隔符；`vault`、`self`、`super` 是只允许出现在路径开头的保留段；
- 路径段不能包含 control character 或 `::`；除此之外语言不加风格约束——空格、数字开头、中文与 `@` 等知识库常用命名都合法（如 `2026-08-26 lsp test.not`、`obsidian plugin.not`）；
- Target 内 ModulePath 与 ItemId 的分隔符是 `/`：文件系统的名字段天然不含 `/`，`::` 又不占用它，于是第一个 `/` 之后皆为字面 ItemId。

段本身是文件系统名，其可用字符最终由所在文件系统与操作系统决定（`/`、NUL、OS 保留字符、长度上限），以实现为准；语言不在这之上额外收紧。

== Item 树

一篇文档（一个 `.not` source）的求值结果是一棵 *Item* 树。按照一切皆 call 的模型，Item 的实际表示就是一个个 call（名称 + args）；`#[...]` 的求值产物是名称为 `scope` 的 call，称为 *ScopeItem*。文档内容的一切因此统一到 Item 之下。

Item 树有层级，标识却是拍平的：*ItemId* 在模块内唯一，同一模块出现两个相同 ItemId 产生 `duplicate-item-id` 诊断。Item 分为具名与匿名：

- 被 `@(id: "xxx")` 标注的 Item 以 `xxx` 为 ItemId；
- heading 的默认 ItemId 是标题链——从顶层标题到自身的标题文字以 `/` 连接，不受链上任一标题自身 id 标注的影响；例如本文 `= 数据模型` 下的 `== Item 树` 的默认 ItemId 是 `数据模型/Item 树`；
- 其余 Item 是匿名 Item。

包含 ModulePath 与 ItemId 的完整地址叫 *ItemPath*：`vault::04-world::model/数据模型/Item 树`。

匿名 Item 不可寻址，要指向它必须先给它一个 id；不可寻址不等于不存在——匿名 Item 同样可以附加属性、被查询。查询面覆盖全部 Item，寻址面只覆盖有 ItemId 的 Item。

== Module 求值单元与 ModuleResult

每个有 source 的 Module 是独立求值单元：在自己的根节点中按源码顺序求值（求值与名称环境规则见 #<vault::03-language::evaluation>），得到 *ModuleResult*：

```text
ModuleResult {
  bindings:    Environment,      // Module root 自有的 binding，可被其他 Module import
  content:     Item,             // 本文档的求值结果，成型后供渲染、检索与查询
  annotations: AnnotationTable,  // 属性表，含模块级条目
}
```

bindings 是程序的一面——跨模块复用的入口；content 是文档的一面——渲染与查询消费的产物；annotations 是属性的一面——标注与模块元数据的唯一入口（#<vault::04-world::annotation>）。

只有 Module root 自己声明的 `let` binding 进入 bindings：imported binding、core/prelude binding、函数形参与嵌套 scope 的 binding 只存在于相应词法环境，不自动成为当前 Module 的 binding；需要转发 imported 值时，作者用新的 root `let` 显式拥有它。这条规则阻止一条 import 把整条传递依赖链带进当前命名空间。
