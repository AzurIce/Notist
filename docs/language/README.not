= Language

本分区是 Notist 语言本身的规范：书写面（语法）与语义面（类型、求值、结构）。概念的叙事介绍从 #<vault::intro> 开始，日常速查见 #<vault::cheatsheet>。

== 篇目

- *grammar*（#<vault::language::grammar>）：语法面总入口——三个模式、引用索引与错误恢复；精确文法在三个子篇：#<vault::language::grammar::markup>（最小骨架、嵌入边界、语法糖）、#<vault::language::grammar::code>（字面量、运算符、表达式、声明、Target 引用）、#<vault::language::grammar::annotation>（标注的求值与挂载位置）。
- *types*（#<vault::language::types>）：类型系统的唯一权威规范——类型与值、可空与 Union、集合、Target、函数类型与调用绑定、静态检查。
- *functions*（#<vault::language::functions>）：内置构造器的唯一权威——14 个构造器的签名、语义与校验。
- *evaluation*（#<vault::language::evaluation>）：求值——lowering、call 森林、规约不动点、名称环境、名字与插件边界。
- *structure*（#<vault::language::structure>）：成型——元素分类、段落与列表组合、Section 归组、区间稳定不变量。
- *parse*（#<vault::language::parse>）：解析——mode-aware 语法树、Markup/Code 交替、受保护扫描与错误恢复。

== 阅读顺序

- *书写面*：从 #<vault::language::grammar> 进入，先 #<vault::language::grammar::markup>，再 #<vault::language::grammar::code>，后 #<vault::language::grammar::annotation>——按写一篇文档的顺序把三个模式读完。
- *语义面*：#<vault::language::types> → #<vault::language::functions> → #<vault::language::evaluation> → #<vault::language::structure>——先掌握值与签名，再看求值如何产生内容，最后看内容如何成型为结构。
- *参考篇*：#<vault::language::parse> 面向语法实现与移植作者，说明 parser 的输入输出、扫描策略与恢复规则，放在最后阅读。
