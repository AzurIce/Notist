== 存在的问题

- @(parse: true)#[下面的内容删除线没有正确在 preview 中显示出来]
  ```
  - ~~`notist.wasm` 兼容性~~（已实锤并解决）
  ```

== 未决设计

- Notist.toml 未知键策略两处来源矛盾（不静默忽略 vs 按预留策略忽略）；language revision、plugins/site 等配置字段的语义模型需要归属（plugins 或 cli/build 侧认领）。
- whitespace-only `core::text` 段与成型不变量 I1 的张力。
- `let rec` 递归绑定语义。
- `#let x = #f(...)` 的绑定值规约状态。
- HTML serializer 词表收口。
- 类型系统是否引入 date 类型（校验属性值的日期格式）。
- 显式 module 定义与排序（控制 preview 章节顺序；隐式/显式并存策略）。
- Web UI 信息增强（符号表、出入链、scope 与属性显示）与 Web Editor。
- git import。

== 已解决

- @(parse: true, date: "2026-08-24")#[下面两段解析结果不一致，换行会导致 preview 里两段之间多一个空格]
  ```notist
  既然我们无法扩展 Obsidian 自己的双链、图谱等系统，那么不如干脆与 Obsidian 自己的系统完全隔离。目前的想法是，我们将一个 Obsidian Vault 分为两个 World，一个是它本身原生的那套 Markdown World，一个是我们的 Notist World。
  ```

  ```notist
  既然我们无法扩展 Obsidian 自己的双链、图谱等系统，那么不如干脆与 Obsidian 自己的系统完全隔离。
  目前的想法是，我们将一个 Obsidian Vault 分为两个 World，一个是它本身原生的那套 Markdown World，一个是我们的 Notist World。
  ```

- @(lsp: true, date: "2026-08-24")#[在 `[[]]` 里输入 `#` 也会触发函数符号的补全]
- @(parse: true, date: "2026-08-24")#[List 内无法使用换行的 scope]
  ```notist
  - #[
    ...
  ]
  ```
- @(parse: true, date: "2026-08-24")#[代码块没法在缩进到列表内]