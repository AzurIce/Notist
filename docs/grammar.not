#heading(level=1)[Notist Grammar]

本文严格定义当前实现的 Notist 源文件语法。概念、使用方式和执行模型见 [[vault::intro]]；设计动机见 `designs` 下的记录。

#heading(level=2)[记号]

- `"text"` 表示固定字符。
- `A B` 表示顺序连接，`A | B` 表示选择。
- `A?`、`A*`、`A+` 分别表示零或一次、零或多次、一次或多次。
- `SameBangs`、括号配对等上下文相关规则由正文定义。

#heading(level=2)[源文件]

源文件是 UTF-8 文本，换行符可以是 LF 或 CRLF。源码位置使用半开 byte range：start 包含，end 不包含。

#raw(lang="ebnf")![
Document = (Text | WikiLink | TransparentScope | ContentCall | RawCall)* ;
]!

未被其他结构占用的源码是 Text。Backtick shield、Raw Call body 内不扫描宿主语法；Transparent Scope 和 Content Call body 内递归扫描。

#heading(level=2)[标识符与名称]

#raw(lang="ebnf")![
Identifier = (UnicodeAlphanumeric | "_" | "-")+ ;
QualifiedName = Identifier ("::" Identifier)* ;
]!

`::` 两侧不允许空白。当前实现允许 Identifier 以数字开头。

#heading(level=2)[Wiki Reference]

#raw(lang="ebnf")![
WikiLink = "[[" WikiReferenceBody "]]" ;
WikiReferenceBody = ModulePart ("#" Label)? ;
ModulePart = ModuleReference | EmptyWhenLabelExists ;

ModuleReference = AbsoluteReference
                | ParentReference
                | SelfReference
                | RelativeReference ;
AbsoluteReference = "vault" ("::" ModuleSegment)* ;
ParentReference = "super" ("::" "super")* ("::" ModuleSegment)* ;
SelfReference = "self" ("::" ModuleSegment)* ;
RelativeReference = ModuleSegment ("::" ModuleSegment)* ;
]!

ModulePart 和 Label 两侧会 trim whitespace。引用最多包含一个 `#`；Label 不能为空，只有带 Label 时 ModulePart 才能为空。

ModuleSegment 不能包含 control character、`/`、`\`、`#`、`[` 或 `]`。按 `::` 分段后每段会 trim，空段非法。`vault`、`super`、`self` 是只允许出现在路径开头的保留段。

#heading(level=2)[Transparent Scope]

#raw(lang="ebnf")![
TransparentScope = "#[" ContentBody "]" Attributes? ;
]!

ContentBody 的 `[` 与 `]` 可以嵌套。双引号中的 bracket 不参与配对；`\` 跳过紧随其后的字符；Backtick shield 作为整体跳过。

#heading(level=2)[Content Call]

#raw(lang="ebnf")![
ContentCall = "#" QualifiedName Arguments? "[" ContentBody "]" Attributes? ;
]!

call head 内不允许额外空白：Function 名称、Arguments 和 `[` 必须相邻。ContentBody 使用 Transparent Scope 的配对规则，并递归解析其中的 Notist 结构。

#heading(level=2)[Raw Call]

#raw(lang="ebnf")![
RawCall = "#" QualifiedName Arguments? RawOpen RawBody RawClose Attributes? ;
RawOpen = Bangs "[" ;
RawClose = "]" SameBangs ;
Bangs = "!"+ ;
]!

SameBangs 的数量必须与 RawOpen 完全相同。Parser 从 body 起寻找第一个 `]` 加相同数量 `!` 的序列；RawBody 中的所有字符均没有宿主语法意义。

Inline Raw Call 的 body 紧贴 delimiter：

#raw(lang="not")!![
#raw![text]!
]!!

Block Raw Call 的 opening 和 closing delimiter 必须各自位于一行，delimiter 后和 closing delimiter 前的 framing newline 不属于 RawBody：

#raw(lang="not")!![
#raw(lang="text")![
line one
line two
]!
]!!

上例的 RawBody 严格等于 `line one\nline two`。LF 和 CRLF 都按同样规则处理。body 若包含 `]!`，必须提高 delimiter level，例如 `#raw!![ ... ]!!`。

#heading(level=2)[Body Form]

Content Call 或 Raw Call 的 `[` 后立即出现 LF 或 CRLF 时，BodyForm 为 Block，否则为 Inline。Transparent Scope 没有 BodyForm。

对于 Block Raw Call，parser 在记录 BodyForm 后从 body range 排除 opening 后的一个换行，以及 closing 前的一个换行。其他 block body 不执行该裁剪。

#heading(level=2)[Arguments]

#raw(lang="ebnf")![
Arguments = "(" Whitespace* ArgumentList? Whitespace* ")" ;
ArgumentList = Argument (Whitespace* "," Whitespace* Argument)* ;
Argument = NamedArgument | PositionalArgument ;
NamedArgument = Identifier Whitespace* "=" Whitespace* Expression ;
PositionalArgument = Expression ;

Expression = NoneLiteral | BoolLiteral | IntLiteral
           | FloatLiteral | StringLiteral ;
NoneLiteral = "none" ;
BoolLiteral = "true" | "false" ;
IntLiteral = "-"? ASCII_DIGIT+ ;
FloatLiteral = "-"? ASCII_DIGIT* "." ASCII_DIGIT* ;
StringLiteral = '"' StringCharacter* '"' ;
]!

Float 必须至少有一个 digit 且只能有一个 `.`。String 支持 `\"`、`\\`、`\n`、`\r`、`\t`。Arguments 允许空白和换行，不允许 trailing comma。Named argument 之后不允许 positional argument。

#heading(level=2)[Attributes]

#raw(lang="ebnf")![
Attributes = "@" FirstAttribute ("," AttributeItem)* ;
FirstAttribute = Id | AttributeItem ;
AttributeItem = Tag | Class | KeyValue ;
Id = Identifier ;
Tag = "#" Identifier ;
Class = "." Identifier ;
KeyValue = Identifier "=" AttributeValue ;
AttributeValue = Identifier | QuotedAttributeValue ;
QuotedAttributeValue = '"' AttributeCharacter* '"' ;
]!

Attributes 中不允许空白。可选 ID 只能是第一个 bare Identifier；Tag、Class、KeyValue 可以重复并保持源码顺序。QuotedAttributeValue 中的 `\` 会跳过下一个字符。

#heading(level=2)[Backtick Shield]

开头有 N 个反引号时，后续第一个长度至少为 N 的反引号 run 结束 shield。shield 内不扫描 Wiki Reference、Scope 或 Call。此结构仅用于词法屏蔽，不会 lowering 为 Raw Element。

#heading(level=2)[文本与段落]

普通单换行属于 Text。一个换行后经过零个或多个 space、tab 或 CR，再遇到第二个换行时产生 Parbreak。当前没有独立的 Markdown heading、list、strong、emphasis 或 quote 语法。

#heading(level=2)[错误与恢复]

未闭合或非法的 Wiki Reference、Scope、Call body、Arguments、Expression 和 Attributes 产生 SyntaxError。Raw closing delimiter level 不匹配时，错误必须报告期望的 level。

未知 Function 不是 syntax error。它在 evaluation 阶段产生 diagnostic，并保留为 UnresolvedCall。类型、参数绑定、模块解析和内置 Function 校验错误同样属于语法之后的 diagnostics。

#heading(level=2)[非当前语法]

Markdown 风格语法糖、一般表达式、变量、User Function、控制流、import、完整 Label resolution、插件自定义宿主 grammar、formatter trivia model 尚未进入当前规范。
