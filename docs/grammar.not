= Notist Grammar

本文是语法面的概览与索引。语言有三个模式：文档默认处于 Markup，`#` 进入 Code 求值，`@` 进入 Annotation 求值；Code 与 Annotation 中的 `[...]` 创建 Item literal 并切回 Markup。概念说明见 #<vault::intro>，类型与求值规则见 #<vault::types>，内置构造器见 #<vault::functions>，日常速查见 #<vault::cheatsheet>。精确规则在三个子模块中：

```text
#<vault::grammar::markup>       Markup：最小骨架、嵌入边界、scope 书写形态、语法糖
#<vault::grammar::code>         Code：字面量、运算符、表达式、声明、类型语法、Target 引用
#<vault::grammar::annotation>   Annotation：@/@! 求值、两种挂载位置、保留键
```

== 模式

Markup 与 Code 是两个内容模式；Annotation 是标注求值模式——与 Code 共享表达式文法，但顶层只接受单个表达式（无 let、无语句序列）：

```text
Markup     —— 产生 Item 片段（文档根；Code 与 Annotation 中的 [...] 回到这里）
Code       —— 产生 Value（#expr 进入；{...} Code block，支持 let 与多语句）
Annotation —— 产生 Dict 并绑定为标注（@expr 进入）
```

围栏代码块与 String 字面量的内部是词法层的字面量载荷，不识别任何模式构造，不参与 quotation。

`#` 与 `@` 是进入 Code 与 Annotation 的前缀，差别只在结果义务：`#expr` 的结果按插入规则拼入内容，`@expr` 的结果必须是 Dict 并绑定为标注。Annotation 的载荷文法与挂载位置见 #<vault::grammar::annotation>。

== 引用索引

Markup 与 Code 中一切构造的定义处：

```text
= Title / == Title         → #<vault::grammar::markup/Markup/语法糖/heading>（含默认 id）
`code` / ```lang```        → #<vault::grammar::markup/Markup/语法糖/raw>
---                        → #<vault::grammar::markup/rule>
- A / + A                  → #<vault::grammar::markup/item>
| a | b | / | - | - |      → #<vault::grammar::markup/table>
#<target>                  → #<vault::grammar::markup/link>
*text* / _text_ / __text__ / ~~text~~
                           → #<vault::grammar::markup/Markup/语法糖/strong / emph / underline / strike>
{...} / #[...]             → #<vault::grammar::markup/Markup/最小骨架>
# 表达式边界               → #<vault::grammar::markup/Markup/最小骨架/嵌入的边界>
字面量 / 运算符 / if / lambda / let / import
                           → #<vault::grammar::code/Code/Code 核心语法>
#<target> / Target 字面量  → #<vault::grammar::code/Code/Target 引用>
@expr / @!expr             → #<vault::grammar::annotation>
Dict / Array 字面量        → #<vault::types/Type System/集合类型>
```

== 错误与恢复

未闭合或非法的引用、Item block、实参、String、Raw 语法与标注产生 SyntaxError。Code 表达式缺失或无法识别时必须产生诊断，不得静默丢弃后回退到默认值。

未知函数不是语法错误：调用按内容节点保留（渲染有 fallback 标签兜底），check 层同时产生 unknown function 诊断——预览与工具恢复都能继续处理其余源码。

Markup 中的 `\` 后接标点字符时，二者共同产生一个字面标点 Text；该标点不再参与任何 sugar 识别。Markup、Code 与 Raw 的边界由 parser 在本地确定，不依赖函数注册表或插件运行时。
