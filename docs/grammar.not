= Notist Grammar

本文定义当前工具与写作者可以依赖的语法 surface。语言包含 Markup 与 Code 两种模式：文档默认处于 Markup，`#` 把一个 Code 表达式嵌入当前内容，Code 中的 `[...]` 创建 Content literal 并暂时切回 Markup。概念说明见 [[vault::intro]]，类型与求值规则见 [[vault::types]]，内置构造器见 [[vault::functions]]，日常速查见 [[vault::cheatsheet]]。

Markup 模式只定义最小骨架——文本流、段落分隔、`#` 嵌入与转义；其余一切 Markup 构造（标题、围栏、分割线、列表、表格、引用、强调等）都是语法糖或标注，见文末的引用索引。Code 模式是本文的主体，定义完整的字面量、运算符、表达式、声明与类型语法。

== 两种 lexical context

语义模式只有两个，递归 quotation：

```text
Markup —— 产生 Content 片段（文档根；Code 中的 [...] 回到这里）
Code   —— 产生 Value（Markup 中的 # 进入这里；{...} Code block）
```

不存在第三种语义 context。围栏代码块与 String 字面量的内部是词法层的字面量载荷，不识别任何 Markup/Code 构造，不参与 quotation。

== Markup 最小骨架

- **文本流**：连续文本成为 `Text` 元素——表达式嵌入、标注、元素构造与段间分隔处切开；
- **段落分隔**：空行产生 `Parbreak`；单个换行仍属于同一段；
- **`#` 嵌入式表达式**：边界规则见下；
- **块构造**：`{...}` Code block 与 `#[...]` 手动 scope（Markup 中 Content literal 的书写形式），见 [[vault::designs::language::annotation-syntax]]；裸 `[` 与 `]` 是普通文本；
- **转义**：`\` 转义 `#`、`[`、`]`、`@` 与 `\` 本身；转义不产生额外输出（`\#` 输出一个 `#`）；
- **注释**：Markup 文本流中 `//` 与 `/*` 是普通文本——注释是 Code 上下文的词法 trivia（见下）。

=== `#` 嵌入的边界

`#` 后的表达式有自己的边界规则：

- 标识符由 Unicode 字母（含中文）、数字、`-`、`_` 组成，表达式到标识符字符集结束处为止——`#ver。` 中全角标点不是标识符字符，`。` 作为文本继续（中文标点天然断开表达式）；
- 紧跟汉字、字母或数字时需要 `;` 或括号结束表达式：`第一段#accent;文字`、`第一段#(accent)文字`——`;` 被消费但不产生输出；
- 空格不是语法工具：它终止表达式，同时自身成为文本内容——`#1 + 2` 的 `1` 在空格处结束，` + 2` 是文本；二元运算等完整表达式用括号嵌入：`#(1 + 2)` 输出 3；
- 表达式的结果按插入规则进入外层 Content（见 [[vault::types]]）。

```not
#let accent = "violet"

= #accent

第一段#accent;文字 与 第一段#(accent)文字 输出相同的标题文本。
```

== Code 核心语法

=== 标识符

标识符由 Unicode 字母（含中文）、数字、`-`、`_` 组成，首字符不能是数字或 `-`。`-` 属于标识符字符：`my-var` 是一个名字，不是减法——想写减法要加空格（`my - var`）。`::` 两侧不允许空白，用于限定名（`vault::path`）与函数命名空间。

=== 注释

`//` 行注释与 `/* ... */` 嵌套块注释是 Code 上下文的词法 trivia，不进入值或 Content：

```not
#let total = 1 + /* 外层 /* 嵌套 */ 注释 */ 2 // 行注释
```

=== 字面量

```text
Int       十进制数字序列
Float     数字 + . + 数字（0.5；.5、1. 不合法，写 0.5、1.0）
Bool      true | false
None      none
String    见下（四种形态）
Content   [...]（回到 Markup）
```

负数是一元 `-` 运算符（字面量本身无符号）。String 字面量四种形态：

```text
"..."          转义 inline
"""..."""      转义多行（opening 三引号后立即换行才进入多行形态）
r#"..."#       raw inline（hash 级别可升级：r##"..."##）
r#"""..."""#   raw 多行（同样要求 opening 后立即换行）
```

- 转义集：`\"` `\\` `\n` `\r` `\t`；raw 形态不做任何转义处理；
- 多行形态的 opening framing newline 不属于值；closing 前紧邻的一个 framing newline 被裁掉；除此之外不裁空白、不 dedent；
- 四种形态产生同一个 `String` 类型——form/style 是字面量 provenance，不影响值语义。

=== 运算符

```text
一元：-    not
乘除：*    /
加减：+    -
比较：<   <=   >   >=
相等：==   !=
逻辑：and  or（短路）
```

优先级从高到低：一元（`-`、`not`）→ `* /` → `+ -` → 比较与相等 → `and` → `or`：

- `-a * b` 是 `(-a) * b`；
- `not a == b` 是 `not (a == b)`；
- `a and not b` 是 `a and (not b)`；
- 相等定义在 None/Bool/Int/Float/String 上（同族值比较，跨族不等）；Content 与 Function 没有自动相等。

=== 调用

`callee(实参)` 加可选 trailing block `[...]`。命名实参写作 `name: value`；位置实参必须全部出现在具名实参之前，trailing Content block 是唯一例外——它是恒为最后的 positional 实参。绑定、默认值、coercion 与调用点检查见 [[vault::types]]。

=== if

```not
#if 条件 { ... } else { ... }
#if 条件 [内容] else [内容]
```

- 分支是 Code block（求值为值）或 Content block（求值为内容），复用既有块语法；
- `else` 可省略：无 `else` 时条件为假产生 `None`——Code block 分支的类型 `T` 提升为 `T?`（Content 分支无 else 时类型仍是 `Content`，空内容合法）；
- 条件必须是 `Bool`。

=== lambda

```not
#let double = (x: Int) => x * 2
#let add = (a: Int, b: Int) => a + b
```

- 形参必须声明类型；返回类型由 body 推断；
- 参数列表括号必写（单参数省略括号暂不支持）；
- lambda 是普通 Function 值：可以绑定、传递、调用。

=== 声明

**let**：`let name = 表达式` 或 `let name: T = 表达式`——右侧先在旧环境中求值，绑定对后续可见；类型标注可选（未标注时由右侧推断）。同名遮蔽允许（最近绑定者胜出）；没有全局 hoist——名字按声明顺序可见，前向引用产生诊断。

**函数定义糖**：`let name(形参表) -> R = body`，与 `let name = (形参表) => body` 等价。形参带 `=` 的是 named 可省略，不带的是 positional 必填。

```not
#let double(x: Int) -> Int = x * 2
#let double = (x: Int) => x * 2   // 与上一行等价
```

**代码块**：`{...}` 是多语句的 Code 表达式——语句以 `;` 或换行分隔（同一行多个语句必须用 `;`，尾随 `;` 合法）。块的值采用 join 语义：块内各表达式的值按插入规则组合，`let` 产生 `None`、不参与组合。块内绑定挂在块节点上，不出块。

**import**：`#import path::{name, name as alias}`——显式选择器，无 wildcard；花括号左侧是 ModulePath，`as` 把名字映射为合法 Code 标识符。import 扩展当前根 scope 后续节点的环境，产生空 Content；依赖边在求值前进入 import graph，import 环一律是诊断。见 [[vault::designs::world::import]]。

```not
#import self::theme::{warning, accent as highlight}
```

**模块属性**：`@![...]` 见 [[vault::designs::language::annotation-syntax]]。

=== 类型语法

```text
Type
├─ None / Bool / Int / Float / String / Content
├─ T?                    # 可空
└─ fn(形参表) -> R       # 形参：名字: 类型，带 = 表示可省略
```

标注位置：`let name: T = ...`、函数形参、函数返回类型、lambda 形参。函数类型就是定义签名去掉 default 表达式：

```text
定义签名:  fn(kind: String = "note", title: Content? = none, trailing body: Content) -> Content
函数类型:  fn(kind: String =, title: Content? =, trailing body: Content) -> Content
```

`trailing` 前缀标记末尾 Content 形参。`(T?)?` 折叠为 `T?`。可空与可省略的完整规则见 [[vault::types]]。

== Wiki 引用

```not
[[setup]]                        # 裸名：从当前模块解析
[[self::setup]]                  # 当前模块路径前缀
[[super::intro]]                 # 父级
[[vault::guide#install]]         # 绝对路径 + scope id
[[vault::guide::assets#logo.png]] # 资源文件（完整文件名）
```

- `[[` 与 `]]` 之间是字面 url 字符串——不解析 markup、不嵌套 `[[`；
- `#id` 后缀是「模块内目标」：文档模块中是带 id 的 scope，资源目录中是完整文件名；引用最多包含一个 `#`，id 不能为空；
- `vault`、`self`、`super` 是只允许出现在路径开头的保留段；路径段不能包含 control character、`/`、`\`、`#`、`[` 或 `]`；
- 标题拥有默认 id：`[[vault::guide#安装]]` 按标题文本精确匹配，重复标题产生歧义诊断；
- 带 scheme（`https://` 等）的外部目标语法合法：解析为 External 目标，当前渲染为未解析的可见文本并产生 info 级 `external-reference-unsupported` 诊断——不是语法错误。

显式构造 `ref(url: String)` 使用同一个引用解析入口：`[[vault::guide]]` 与 `#ref("vault::guide")` 产生同一种 Reference 元素。

== 行内 Raw 与围栏

Backtick 语法在 Markup 中直接构造 Raw 元素：

- 行内：一对相同长度的 backtick run 包裹 payload——``Run `cargo test` before publishing.`` 中的反引号片段是行内 Raw（block 为 false）；payload 含反引号时用更长的 run：```Use ``two ` delimiters`` when the payload contains a backtick.```；
- 围栏：行首三个以上反引号开始，opening line 上的非空文本经横向 trim 后成为语言标注；closing fence 长度不得小于 opener，之后只能有横向空白或行尾；opening 后与 closing 前各一个 framing newline 不属于 payload；围栏产生 block Raw（block 为 true）；
- Raw 语法内不扫描 Notist Markup 或 Code——payload 中的 `#`、`[[...]]`、`[...]`、`@...` 都保持原文。

````not
Run `cargo test` before publishing.

```rust
fn main() {}
```
````

需要以普通函数方式构造 Raw 时，把源码作为 String 实参传给 `raw`（见 [[vault::functions]]）。

== 标注与 scope 形态

属性列表只有一套语法，挂载位置是变体：

```text
属性列表 = 条目（逗号分隔）
条目     = id | #tag | .class | key = value
```

- 行内 postfix：`@...` 贴在行内元素或表达式之后，绑定到前面紧接着的求值结果；
- 块级前缀：`@[...]` 独立成行或行首，绑定到紧接着的下一个块级节点；
- 模块：`@![...]` 文件开头，绑定为模块属性（根 scope）；
- 手动 scope：`#[...]` 显式创建 scope 节点——封装一段内容、自带词法边界、可承载属性、渲染上不产生可见元素。

```not
文字内容@note

@[wip]
= 标题

@![#design, #wip, status = "draft"]

#[一段内容]@install
```

完整规则见 [[vault::designs::language::annotation-syntax]]。

== 引用索引

Markup 中除最小骨架外的一切构造，定义处如下：

```text
= Title / == Title         → heading（syntax-sugar heading 节，含默认 id）
```lang ... ```            → raw（syntax-sugar raw 节）
---                        → rule（syntax-sugar rule 节）
- A / + A                  → item（syntax-sugar item 节）
| a | b | / | - | - |      → table（syntax-sugar table 节）
[[target]]                 → ref（syntax-sugar ref 节）
*text* / _text_            → strong / emph（syntax-sugar）
__text__ / ~~text~~        → underline / strike（syntax-sugar）
@id,#tag,.class,key=value  → 标注（annotation-syntax）
@[...] / @![...]           → 标注（annotation-syntax）
{...} / #[...]             → scope 语法形态（markup-surface）
#scope-id 引用寻址         → reference-ref-target
# 表达式边界               → markup-surface
```

== 错误与恢复

未闭合或非法的引用、Content block、实参、String、Raw 语法与标注产生 SyntaxError。Code 表达式缺失或无法识别时必须产生诊断，不得静默丢弃后回退到默认值。

未知函数不是语法错误：调用按内容节点保留（渲染有 fallback 标签兜底），check 层同时产生 unknown function 诊断——预览与工具恢复都能继续处理其余源码。

Markup 中的 `\` 后接标点字符时，二者共同产生一个字面标点 Text；该标点不再参与任何 sugar 识别。Markup、Code 与 Raw 的边界由 parser 在本地确定，不依赖函数注册表或插件运行时。
