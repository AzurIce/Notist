#heading(level=1)[认识 Notist]

Notist 是一种面向知识库的结构化文本语言。普通文字可以直接书写；Module 引用把多篇文档连接起来；Function 构造标题、引用、代码等内容；Scope 和 Attributes 为内容附加可查询的元数据。

一个 `.not` 文件既是可读的文本，也是一个有类型的文档程序。Notist 会把它求值为 `Content`，再用于检查、预览或构建静态页面。

本文从写作者的角度介绍 Notist 的核心机制。精确的字符、空白和 delimiter 规则见 [[vault::grammar]]。

#heading(level=2)[一篇 Notist 文档]

下面的示例包含标题、Module 引用、元数据、引用块和代码：

#raw(lang="not")!![
#heading(level=1)[Project Notes]

The language reference is [[vault::grammar]].

#heading(level=2)[Status]

#[The documentation build passes.]@status,#verified,owner="Alice"

#quote[
Run #raw![cargo test]! before publishing.
]

#heading(level=2)[Example]

#raw(lang="rust")![
fn main() {
    println!("hello");
}
]!@example,#code
]!!

这仍然是一篇以普通文字为主体的文档。`#heading`、`#quote` 和 `#raw` 是产生内容的 Function；`[[vault::grammar]]` 是跨文档引用；`#[...]@...` 为一段内容附加元数据。

#heading(level=2)[Vault 与 Module]

一个目录就是一个 Vault。Vault 中的每个 `.not` 文件对应一个逻辑 Module：

#raw(lang="text")![
docs/README.not          -> vault
docs/intro.not           -> vault::intro
docs/guide/setup.not     -> vault::guide::setup
docs/guide/README.not    -> vault::guide
]!

普通文件名成为 ModulePath 的最后一段。`README.not` 表示它所在的目录，因此 `guide.not` 与 `guide/README.not` 都会映射到 `vault::guide`，不能同时存在。

只有子 Module、没有 `README.not` 的目录仍然可以出现在 Module 树中。例如存在 `guide/setup.not` 时，`vault::guide` 可以作为承载 `vault::guide::setup` 的目录 Module。

ModulePath 是文档的稳定逻辑名称。引用和工具使用 `vault::guide::setup`，而不是依赖操作系统路径写法。

#heading(level=2)[引用其他 Module]

使用 `[[...]]` 引用 Module。假设当前文档是 `vault::guide`：

#raw(lang="not")![
[[setup]]
[[self::setup]]
[[super::intro]]
[[vault::grammar]]
]!

- `[[setup]]` 从当前 Module 查找 child，即 `vault::guide::setup`。
- `[[self::setup]]` 同样从当前 Module 开始，但把起点写得更明确。
- `[[super::intro]]` 先回到父 Module，再找到 `vault::intro`。
- `[[vault::grammar]]` 从 Vault 根开始，不受当前文档位置影响。

路径使用 `::` 分段。连续的 `super` 可以继续向上，例如 `[[super::super::shared]]`。引用还可以带 label：`[[vault::guide#install]]` 指向目标 Module 内的 `install`。

#heading(level=2)[Function 与 Body]

Function 是 Notist 的内容处理机制。它接收参数和一个 body，产生可以进入文档的 Content。按照签名声明的 body 类型，Function 分为两类：接受 Content 的 Function，以及接受 RawSource 的 Function。

| Function 类型 | 调用形式 | Function 收到的 body |
| --- | --- | --- |
| 接受 Content 的 Function | `#name[...]` | body 先作为 Notist 内容求值，Function 收到结构化的 `Content` |
| 接受 RawSource 的 Function | `#name![...]!` | body 不经过 Notist 解释，Function 收到 delimiter 之间的 `RawSource` |

一个具体 Function 只声明其中一种 body 类型，调用者不能为同一个 Function 随意选择两种形式。调用语法必须与签名匹配：例如 `quote` 接受 Content，所以写成 `#quote[...]`；`raw` 接受 RawSource，所以写成 `#raw![...]!`。

这里的区别属于完整 Function call，而不是方括号本身的固定含义。Transparent Scope `#[...]` 是另一种语法，后文单独介绍。

Content 与 RawSource 描述两类 Function 接收的 body 值。除此之外，两类 Function 的 body 都可以采用 inline 或 block 源码形式：`[` 后直接出现内容时是 inline，`[` 后立即换行时是 block。

| body 类型 | Inline | Block |
| --- | --- | --- |
| Content | `#quote[Quoted content.]` | `#quote[` 换行后书写多行 Content，再以 `]` 结束 |
| RawSource | `#raw![cargo test]!` | `#raw![` 换行后书写多行源码，再以 `]!` 结束 |

完整写法如下：

#raw(lang="not")!![
#quote[Quoted content.]

#quote[
First paragraph.

Second paragraph.
]

#raw![cargo test]!

#raw(lang="rust")![
fn main() {}
]!
]!!

inline/block 与 Content/RawSource 是两个不同维度。前者描述 body 在源码中的行形式，后者决定 Function 实际收到的值。Function 可以根据 inline/block 选择合适的输出形式；例如内置 `raw` 会把 inline RawSource 留在段落中，把 block RawSource 作为独立代码块。

RawSource delimiter 中 `!` 的数量可以增加。一级 delimiter 是 `![...]!`，二级是 `!![...]!!`，三级是 `!!![...]!!!`。opening 和 closing 必须使用相同数量的 `!`。

#raw(lang="not")!!![
#raw!![
literal ]! remains inside the body
]!!
]!!!

一级 body 遇到 `]!` 就会结束；如果正文需要包含 `]!`，改用 `!![...]!!`。同理，正文需要包含 `]!!` 时使用更高一级 delimiter。

多行 RawSource 的 opening 和 closing delimiter 各自占一行。opening 后和 closing 前用于分隔 delimiter 行的换行不属于 RawSource，因此：

#raw(lang="not")!![
#raw(lang="rust")![
fn main() {}
]!
]!!

传给 `raw` 的文本从 `fn` 开始，在 `}` 结束，不会额外带一个首尾空行。精确的 delimiter 匹配规则见 [[vault::grammar]]。

Content body 适合接收正常的 Notist 文档：

#raw(lang="not")!![
#quote[
Read [[vault::grammar]] before editing the syntax.
Run #raw![notist check docs]! after editing.
]
]!!

`quote` 收到的不是方括号之间的一整段字符串，而是已经求值的 Content。其中包含普通文字、一个 Module 引用，以及内层 `raw` 产生的 inline content。Function 可以直接组合这些内容，不需要重新解析源码。

RawSource body 适合把另一种语言或任意文本交给 Function 自行处理：

#raw(lang="not")!![
#raw(lang="text")![
[[this is not a reference]]
#heading[this is not a heading]
]!
]!!

这里 `raw` 收到 delimiter 之间的原始文本。`[[...]]` 和 `#heading[...]` 都只是字符，不会被 Notist 解释。

每个 Function 的签名声明它接受哪种 body：

#raw(lang="text")![
heading(level: Int = 1, body: Content) -> Content
quote(body: Content) -> Content
raw(lang: String? = none, body: RawSource) -> Content
]!

因此 `#raw[fn main() {}]` 传入了错误的 Content body，`#quote![text]!` 传入了错误的 RawSource body；两者都会产生类型错误。

Function 名称可以使用 `::` 分段。插件可以提供 `#diagram::mermaid![...]!`、`#math::block![...]!` 这样的名称，而不占用所有 Function 共用的短名称。

#heading(level=2)[类型系统]

Function 的参数、body 和返回值都由同一套类型系统描述。Notist 的核心类型包括：

| 类型 | 常见值或来源 | 含义 |
| --- | --- | --- |
| `None` | `none` | 没有值 |
| `Bool` | `true`, `false` | 布尔值 |
| `Int` | `2`, `-1` | 整数 |
| `Float` | `1.5` | 浮点数 |
| `String` | `"rust"` | 普通 UTF-8 字符串 |
| `Content` | Module 的求值结果、Function 的 Content body 或返回值 | 结构化的 Notist 文档内容 |
| `RawSource` | Function 的 RawSource body | 未经 Notist 解释的原始源码片段 |
| `Optional(T)` | `none` 或一个 `T` | 可选值，例如 `String?` |

`Content` 是文档的基础类型。一篇 Module 求值后得到 Content；普通文字、Reference 和 Function 结果按顺序组成它。Content 不是源码字符串，而是已经具有文档意义、可以继续组合和渲染的内容。

`RawSource` 可以理解为一种专门用于 Function body 的 raw string literal。它与 String 都承载文本，但用途不同：

| `String` | `RawSource` |
| --- | --- |
| 作为参数表达式，例如 `lang="rust"` | 作为 Function 的完整 RawSource body |
| 使用引号，并支持 `\n`、`\"`、`\\` 等 escape | 保留 body 字符，不执行 String escape |
| 表示一个普通字符串值 | 表示一段等待 Function 自行处理的源码 |

RawSource 在字面量形态上类似原始块字符串，但保持独立类型。这样调用者明确选择“正常求值为 Content”或“保留为源码”，Function 也明确声明自己接受哪一种输入。

#heading(level=2)[参数与绑定]

参数写在 Function 名称后的圆括号中：

#raw(lang="not")!![
#heading(2)[Positional argument]
#heading(level=2)[Named argument]
#raw(lang="rust")![fn main() {}]!
]!!

位置参数按签名中的顺序绑定，命名参数按名称绑定。命名参数之后不能再出现位置参数。Function 可以为参数提供默认值，因此 `#heading[Title]` 会使用默认的 `level=1`，`#raw![text]!` 会使用默认的 `lang=none`。

参数同样经过类型检查。未知参数、重复参数、缺少必需参数或类型不匹配都会指向对应的调用位置。Int 可以传给要求 Float 的参数，`Optional(T)` 接受 `none` 或 `T`。

#heading(level=2)[Scope 与 Attributes]

Function 用来产生内容；Transparent Scope `#[...]` 用来标注文档中的一段范围。Scope 不改变内部 Content，只为它附加 Attributes：

#raw(lang="not")![
#[The documentation build passes.]@status,#verified,.highlight,owner="Alice"
]!

上例包含四种元数据：

- `status` 是 ID，用于稳定标识这段内容。
- `#verified` 是 tag，用于分类和查询。
- `.highlight` 是 class，可供 renderer 选择展示样式。
- `owner="Alice"` 是 key-value property。

Attributes 也可以跟在 Function 结果后：

#raw(lang="not")![
#quote[Keep references stable.]@principle,#design,owner="Alice"
]!

圆括号中的 arguments 控制 Function 如何产生内容；结尾的 attributes 描述这段内容是什么。`level=2` 是 `heading` 的参数，而 `#design` 是输出内容的 tag。

#heading(level=2)[段落与块]

普通文字和 inline content 会组成段落。单个换行仍属于同一段，空行结束当前段落：

#raw(lang="text")![
first line
second line

new paragraph
]!

Heading、Quote 和多行 Raw 等 block content 会结束当前段落并独立展示。这样文档结构由 Content 中各元素的意义决定，而不只是由最终 HTML 决定。

#heading(level=2)[检查、构建与预览]

Notist CLI 以一个 Vault 目录为输入：

#raw(lang="text")![
notist check docs
notist build docs -o dist
notist preview docs
]!

`check` 检查 Module、引用、调用和类型问题。`build` 把整个 Vault 构建为静态站点。`preview` 在本地展示相同的页面，并在文档变化后刷新。

日常写作主要围绕四个概念：用 Module 组织知识，用 Reference 连接文档，用 Function 从有类型的输入产生 Content，用 Scope 和 Attributes 描述内容。需要确认某一语法形式的严格定义时，查阅 [[vault::grammar]]。
