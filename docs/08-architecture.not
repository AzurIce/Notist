= Crate Architecture

本文是实现层地图：crate 如何分层、依赖朝哪个方向流、一次请求经过哪些层、同一份核如何服务不同宿主。语言语义见 #<vault::03-language>，数据模型见 #<vault::04-world>，进程拓扑与本地协议见 #<vault::05-cli::daemon> 与 #<vault::05-cli::protocol>，LSP 契约见 #<vault::06-lsp>，插件系统见 #<vault::07-plugins>。本文不重复这些内容，只固定"代码住在哪里、依赖朝哪流"。

== 分层

#mermaid(
  source: r#"""
flowchart TB
  subgraph L4["第 4 层 入口 notist-cli 组合根"]
    CLI["inspect check build preview lsp daemon"]
  end
  subgraph L3["第 3 层 服务 notist-service 平台宿主"]
    SVC["VaultEngine CoreRequest overlay snapshot watcher 索引 transport"]
  end
  subgraph L2["第 2 层 分析与投影"]
    ANA["notist-analysis Analyzer module 图 引用解析 诊断"]
    HTML["notist-html HTML 投影"]
  end
  subgraph L1["第 1 层 语言与插件"]
    EVAL["notist-eval lower reduce"]
    PH["notist-plugin-host wasmi ABI fuel"]
    PC["notist-plugin-core core 标准包"]
  end
  subgraph L0["第 0 层 词汇"]
    SYN["notist-syntax parse"]
    MODEL["notist-model ModulePath ItemId TextRange"]
  end
  CLI --> SVC
  SVC --> ANA
  SVC --> HTML
  ANA --> EVAL
  ANA --> PH
  ANA --> PC
  HTML --> EVAL
  PH --> EVAL
  PC --> EVAL
  EVAL --> SYN
  SYN --> MODEL
"""#,
)[crate 依赖主链：每个箭头是一条 cargo 依赖边]

图为依赖主链：每个箭头都是一条 cargo 依赖边。省略的两类边：第 0-2 层的 crate 都直接依赖 model（词汇层）；cli 为 build/preview 与 pipeline 调试视图直接消费 eval/html/analysis 的产物。

== 依赖规则

- *单向向下，无环*：上层依赖下层，第 0-2 层不存在指回上层的边。
- *第 0-2 层是纯逻辑核*：不允许依赖异步运行时、OS watcher、网络客户端、全文索引引擎这类平台设施。现存的唯一例外是 analysis 依赖 `dunce` 做路径规范化——非 native 宿主需要把它替换掉。
- *平台依赖只出现在 service 与 cli*：tokio、notify、tantivy、embedding endpoint 客户端、UDS/named pipe 全部集中在 service；clap、axum、lsp-server 集中在 cli。
- *词汇类型只定义一次*：`ModulePath`、`ItemId`、`TextRange` 等被所有层共用的类型住在 model，是各层的公共语言。
- *分析身份不进词汇层*：`FileId`、`ModuleId`、`ViewId`、`Revision`、`LineIndex` 描述一次分析运行的状态，住在 analysis；它们不是语言词汇。

== 各 crate 职责

| crate | 职责 | 依赖 |
| - | - | - |
| `notist-model` | 共享词汇与数据形状：ModulePath、ItemId、TextRange、Item 树 | postcard、serde |
| `notist-syntax` | parse：源文本到语法树 | model |
| `notist-eval` | lower 与 reduce：语法树到调用森林，带函数环境规约 | model、syntax |
| `notist-plugin-core` | core 标准包：默认预装的函数/成型/投影贡献，native backend | eval、model、syntax |
| `notist-plugin-host` | wasmi 实例化、ABI 与 fuel：把 wasm 包装载为 PluginContribution | eval、model |
| `notist-analysis` | Analyzer：source 表、module 图、import 边、引用解析（RefTarget）、诊断、符号 | 下三层全部 + plugin-core/host |
| `notist-html` | 投影：规约/成型结果到带 `data-notist-*` 的 HTML 片段 | eval、model、syntax |
| `notist-service` | 服务层：CoreRequest/CoreReply、View 与 overlay、WorkspaceSnapshot 发布、watcher、全文与向量索引、本地 transport 与 daemon 协议 | 核全部 + 平台依赖 |
| `notist-cli` | 组合根：inspect/check 入口、build/preview（axum）、lsp、daemon 拉起、skill、官方 docs 同步 | service + 直接消费核 |

== 运行期：一次请求的路径

```text
编译期方向（纯函数，一遍到底）：
  源文本
    -> syntax parse
    -> eval lower（调用森林）-> reduce（函数环境来自 plugin 贡献的 registry）
    -> analysis：module 图、import 边、RefTarget 解析、诊断
    -> html 投影（build/preview 与渲染请求消费）

请求方向（client 只对 service 说话）：
  client（CLI / LSP / preview）
    -> CoreRequest
    -> service 捕获当时的 WorkspaceSnapshot
    -> 在 Analyzer 上执行
    -> CoreReply -> client 各自投影
```

== LSP 的位置

`crates/notist-cli/src/lsp.rs` 是第 4 层的一个入口：协议适配（JSON-RPC 循环、capabilities 协商、PositionCodec、方法路由）加会话编排（文档注册表、single-flight builder、诊断发布）。它只对 service 说话——CoreRequest 加 daemon transport，或 `--no-daemon` 的 embedded service——不直接触碰 analysis/eval 的内部结构。对外行为的权威是 #<vault::06-lsp>。

== 宿主矩阵

纯逻辑核的平台无关性是约束的结果，不是巧合：依赖清单保持干净，同一份核才可能编成不同 target。

| 宿主 | 状态 | 说明 |
| - | - | - |
| 本地 daemon | 现行 | cli 拉起，service 全量；每 Vault 一个实例（#<vault::05-cli::daemon>） |
| embedded | 现行 | `--no-daemon`，进程内直接承载同一 core service |
| LSP gateway | 现行 | stdio 编辑器入口（#<vault::06-lsp>） |
| wasm kernel | 未实施 | 第 0-2 层与 plugin-host 的依赖均为纯 Rust（wasmi、zip 不带平台绑定），可编 wasm32 进浏览器作为语义核；service 的平台面不随行，浏览器宿主与 vault 快照同步尚不存在 |
| SaaS | 未实施 | 复用同一请求面与事件模型的远端宿主；身份、权限与存储边界见对应设计讨论 |
