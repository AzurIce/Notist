= CLI 只读能力迭代日志

按 #<vault::ai::2026-08-27 cli readonly query eval harness> 的框架自主迭代只读查询面。终止规则：连续 5 次改进尝试在固定任务集（T1–T6，4 条件）上均无显著成绩改善则收口。每轮迭代在本文件追加一节：思路、改动、跑分、结论。

== 假设库（来自 docs/ai 先行研究的落地差距清单）

依赖综合审计结论（三篇 CLI 研究 + designs/host 规范 vs 当前二进制逐条核对），按预期收益排序：

1. A2 节选择器：`read module#标题默认id` 只返回标题行而非整节子树——取证第二跳系统性失败。修复点 analysis `label_scope_range` 回退分支 / service `resolve_source`。
2. A4 计数暴露：tantivy `Count` 已算出 total 但不对外；加 matched_modules/matched_units 让"有没有 X"不必翻页到 complete。
3. A1 检索单位：operator=all 是 per-unit AND（10k 文档单元粒度），跨节概念查询零命中（实测 "游标 续读" 0 hit 而 query-contract 同时覆盖两词）。改为模块级候选交集 + Should 证据分重排。
4. A7 SearchHit 附最近 heading 上下文；A8 outline/read 页级 source 去重；A9 公开投影去 canonical_root；A3 --exclude-scope；A5 归档先验；A6 exact 字段语义对齐。

== 迭代 0：基线（baseline）

6 任务 × 4 条件 × n=1，结果 **19/24**。判定矩阵：工具三条件（md-bash / notist-bash / notist-skill）**18/18 全部通过**；null-notools 仅 T2 靠参数化常识侥幸命中、T3–T6 如预期失败（notist 无参数化知识得到校准确认）。

**关键结论：pass 指标在当前任务集上饱和**——检索类问题对这个规模的语料和这批查询并不构成成功率瓶颈，宏评测的区分度转移到效率面（时长/token）与更难任务。

另记：语料冻结时已知缺陷——README.not 仍引用被剔除的 `#<ai>` 目录产生 1 条 error 级诊断，为保持可比性不中途修语料。

== 噪声与协议修正（iteration 1 教训）

baseline 与 it1 同协议对比暴露：**md-bash / null 组完全不受 CLI 改动影响，其逐格波动就是纯运行噪声**——T6 在 md 组 242s→574s、token ±100% 级摆动；null/T2 出现 pass→fail"退化"。据此修正协议：

1. 成效判定只看受 CLI 影响的条件格；md/null 组作为噪声对照。
2. n=1 下 token/时长差异只有 >3× 且方向跨组一致时才计入证据，否则视为噪声。
3. task 措辞收敛（"只回答要点，不要展开解释"）一次性收紧后不再变更，防止任务漂移污染后续对比。
4. 宏评测转为确认手段（每迭代至多一轮）；日常改进步进由确定性微基准（sanity_probes / byte fixture）承担。

== 迭代 1：模块级候选召回 + 节选择器展开 + 计数透出（bm25-v4）

**改动**（均落 service/cli 层，schema 向后兼容新增可选字段）：
- A1 `operator=all` 多词条查询从 per-unit AND 改为：任一词条取并集 → 各词条独立查询求模块级交集 → 交集过滤后按 Should 的 BM25 重排。`ranking_version: bm25-v4` 标注该路径。单词条与 any 路径行为不变。
- A4 coverage 新增 `matched_modules` / `matched_units`，lexical 执行器统一填充；text 模式输出 "matched N modules / M units"。
- A2 heading 默认 id 选择器（`path#标题文本`）展开到整节子树（下一个同级/更高级 heading 起点），原本只返回标题一行（21 字节 → 实测整节 793 字节）。

**微基准（确定性）**："cursor 续读"AND 查询 0 hit → 3 modules/27 units；四类边界探针全过；service 31 + analysis 59 + cli 3 单测全绿。

**宏评测 verdict：无显著改进（attempt 1/5）。** 工具组 18/18 持平（本就饱和）；total tokens 409k→537k 属噪声带内波动（md 对照组同样涨 30%+）。唯一 fail→pass 未出现；唯一 pass→fail 在 null 组（CLI 不在其回路，判采样噪声不计）。**确认饱和假设：成功率的提升必须靠加难任务或换效率指标才能显影。**

== 迭代 2：SearchHit 节归属 + check 渲染卫生

改动：
- A7 SearchHit 新增 `section_id`（覆盖命中的显式 scope label）/`section_title`（最近前序 heading 标题），两个执行器（lexical/exact/regex）都填充；agent 引用 `module#id` 可直接来自首跳结果，省一次 outline 往返。
- check 文本渲染两处修复：诊断 severity 真实映射（此前 info 一律标成 error[...]，现在 warning 黄 / info 青）；默认 severity=error 过滤导致 showing 0 时明确输出 "showing X of Y (severity/error filters may hide the rest)"。

**verdict：无显著改进（attempt 2/5）。** 工具组 18/18 维持饱和；total tokens 409k→397k 平稳；唯一翻转仍是 null-notools/T2（连续第二轮同格反向，进一步确证该格噪声本质）。受 CLI 影响的条件格零变化。

附带实证：md 对照组 T5 本轮 22.7k→9.4k tokens、T6 72k→42k，幅度超过 CLI 影响组多数格子——再次印证 n=1 单元格置信度极低，宏评测只能当确认层。

== 迭代 3：字节成本层 + 输出引用卫生（微基准主战场）

假设库余项里 A9（公开投影剥离 canonical 绝对路径）与"含空格 ModulePath 无法引用"两条都具备确定性判据且低风险，组成第三轮：

改动目标：
1. 快照投影不再携带宿主绝对路径（跨 checkout 输出确定性；每响应省 ~70B 且消除机器信息泄漏）。
2. 文本模式中 ModulePath 含空格时加引号，使其成为可整体复制引用的 token。

验收：sanity probe 断言标准查询 JSON 中不再出现宿主路径前缀；modules/read 的文本行对含空格 id 加引号；单测全绿；一轮快速确认评测无 pass 回退。

**verdict：无显著改进（attempt 3/5）。** 工具格 18/18 维持；total 409k→517k 属噪声带。两处"退化"均在 CLI 无关格：null/T2 连续三翻（此后永久隔离该格），md/T6 撞 600s 超时上限。微层确定性产出已验证（无路径泄漏、带空格 id 带引号，实测于主仓库 docs）。

== 协议第 2 阶段（p2）

三轮教训固化为新协议：null 组退出判定；超时提到 900s；T6 措辞收紧为要点式并修正判分（旧判分仍要求提及"停止规则"，与新措辞矛盾造成 p2base 初跑三连误判，修正后重判）；此后对比一律用工具组 18 格对位网格，p2base 与 p2itN 必须同任务集、同判分、n=1 并承认噪声底。

p2base（iteration 3 后二进制的锚）：18/18 全过。

== 迭代 4：outline 字节跨度 + references 摘录行

改动：outline 文本行附 `span=start..end` 节字节区间（引用定位免二次 read）；references 文本每行下输出摘录内容。

verdict：无显著改进（attempt 4/5）。对位 18→18 全平，tokens 302k→318k 持平，零翻转。微层价值确定成立（实测 `span=483..1707` 等）。

== 迭代 5：search 文本行 §节归属

改动：命中行追加最近 heading 标题（`§…`）并把 module 引号化，与 JSON section_title 对齐——首跳即得引用句柄（实测 `§Snapshot Stamp 与可恢复 Cursor`）。

verdict：无显著改进（attempt 5/5）。18 格对位：notist 两组全过且 T6 效率向优（14.4k→10.5k / 26.5k→14.2k tokens），但唯一翻转是 md 组 T6 第二次撞满 900s 超时（4 跑 2 超时，CLI 无关格）——判任务设计缺陷，不计回归。

== 终止判定与总结

五轮尝试均未在宏评测上产生显著提升，触发预定终止规则。整体结论：

1. **饱和是根因，不是失败**。固定 6 任务对有工具的 agent 是"全都能做"的难度，pass 指标从基线起就顶在 18/18；CLI 改进只能显影在效率/字节层。后续要继续用宏评测，必须换任务家族（多跳引用链、需排除归档的否定证明、跨 3+ 文档的综合题）或把 token 效率做成 n≥3 的主指标。
2. **确定性改进被微基准完整证实并保留**（每条都有 probe 证据）：bm25-v4 使 "cursor 续读" 类跨节 AND 查询从 0 hit 变 3 modules/27 units；read 标题选择器从标题一行（21B）变整节（793B）；coverage 计数让存在性判断免翻页；outline span / references 摘录 / search §归属把引用句柄压进首跳；check 渲染如实标注 severity 与隐藏计数；投影剥离宿主绝对路径；含空格 ModulePath 可引用化。
3. **协议教训已固化**：null 校准组只在建基线时跑；判分必须与任务措辞同步修订；单元格置信度与所用时长上限正相关（md/T6 两类长尾说明超时也要按任务分级设定）；CLI 无关组是免费的噪声对照，永远先看它再下结论。
4. 未尽的假设库余项（A8 页级去重、A3 --exclude-scope、A5 归档先验、export search）需要契约/规范联动而非纯实现，留给下一阶段配套 spec 修订一起做。
