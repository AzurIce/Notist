= ranim 两条 Scene Session 路径对比与 samply 开销归因

ranim M2（LogicWorld）引入的 ECS 路径（`ScenePlayer`）与既有纯求值路径（`SceneEvaluator`）实现同一个 `SceneSession` trait，输出逐帧一致。本文记录两条路径的完整对照、每帧 clone 的精确记账，以及用 samply 对 player 开销的实测归因。代码位于 ranim 仓库 `m2-logic-world` 分支（`packages/ranim-core/src/logic.rs` 与 `scene_evaluator.rs`），profiling 目标是 `benches/src/bin/profile_session.rs`。

== 两条路径

两者共享 sealed scene 的顶层 cells、整棵类型擦除动画树（同一份 `eval_at` 路由、同一个 `Iterative` RefCell 快照）与 `(animation_id, part)` 身份体系。分歧从求值结果落地开始：

- 纯路径 `SceneEvaluator::sample_at`：`eval_at → Vec<DynItem> → 每个 DynItem.extract() → move 进帧缓冲`。无驻留状态，帧是一次性产物。
- World 路径 `ScenePlayer::frame` = 三段式：`materialize_at`（`DynItem` 用自带钩子 upsert 进 World，帧末 despawn 消失的 key）→ `extract`（fn 指针读组件写回 `ExtractedItems`）→ `collect`（按 `SceneOrder` 排序后 clone 展开进帧缓冲）。

== clone 的精确记账

一个常见的错误说法是"player 多了 materialize 和 extract 两次 clone"。实际记账：

- **materialize 不 clone**：`DynItem` 钩子是 `Box<dyn Any>::downcast` 移动取值，组件 insert 也是 move；这一段的成本是 bevy storage 写入与 HashMap 簿记，不是数据拷贝。
- **extract 两边都有一次 clone**：`VItem::extract_into` 是 `buf.push(CoreItem::VItem(self.clone()))`，纯路径同样要付（evaluator profile 中 `extract_into` 占 40.6%，player 中 24.9%，绝对样本数相当）。
- **player 独有的是 collect 的第二次 clone**：extract 结果先存进 entity 的 `ExtractedItems`（retained），collect 再逐个 clone 进帧缓冲。纯路径 extract 产物直接 move 进帧缓冲，没有这一跳。

所以正确表述：纯路径 = 1 次 clone（extract 时）+ move；world 路径 = 1 次 upsert 簿记（无拷贝）+ 2 次 clone（extract 存储 + collect 展开）。

== samply 实测

方法：`profile_session` 以 release + debuginfo 构建，`transform_squares(50)`（2500 个 morph VItem + camera）扫 60 帧 × 50 轮（共 3000 帧）；`samply record --save-only --unstable-presymbolicate` 分别采样两条路径（1000Hz），离线用 `addr2line` 符号化后按函数聚合，并按样本占比 × 采样时间窗换算绝对时长。

criterion 基准（60 帧扫描总耗时，稳态）：evaluator 56.3ms（0.94ms/帧）/ player 85.7ms（1.43ms/帧）（transform，~1.5×）；static 场景 73.0ms / 90.3ms（~1.2×）；小规模 10×10 约 1.5–1.6×。

samply 全程时间窗：evaluator 2406ms / player 4209ms（含场景构建启动开销；run 循环占比分别 87.3% / 93.6%）。换算每帧：evaluator ≈0.70ms / player ≈1.31ms，与 criterion 的 0.94 / 1.43 同量级（采样开销摊高了两者的绝对值，比值一致）。

profile 归因（占比与换算每帧耗时）：

| 阶段 | evaluator | player |
|---|---|---|
| eval_at（求值，共有） | 27.2% ≈ 0.22ms/帧 | 20.1% ≈ 0.28ms/帧 |
| extract_into（共有的一次 clone） | 44.6% ≈ 0.36ms/帧 | 24.9% ≈ 0.35ms/帧（player extract 阶段合计 31.7% ≈ 0.45ms/帧） |
| materialize（upsert 簿记） | – | 19.7% ≈ 0.28ms/帧（bevy `Column::replace` 6.8%、HashSet 4.4%） |
| collect（二次 clone） | – | ~7.5% ≈ 0.11ms/帧 |

player 相对 evaluator 每帧多花的 ~0.5ms（criterion 口径）中：upsert 簿记 ≈0.28ms、collect 二次 clone ≈0.11ms，其余为 extract 存储路径差异与簿记常数项；求值与 extract 的 clone 两边同价。

== 结论

- player 每帧 ~1.2–1.6× 的开销结构清晰：约 20% 在 world upsert 簿记（无数据拷贝），约 7% 在 collect 的第二次 clone；求值与 extract 与纯路径同价。
- 这些都是实现开销而非结构差异，既定优化方向：collect 改 buffer swap 消掉第二次 clone；变更检测跳过未变 entity 的 extract；`ExtractedItems` 在 stage 2 泛化为 `Extracted<T>` 后退化成本也会变化。
- 优化前不建议把渲染路径切到 player；stage 2 让消费侧能按类型直接查询组件时，这些开销才换来实际收益。

== 后续（同日）：extract 融合进 materialize

按"extract 能否同时承担 materialize"的追问实施了融合：upsert 在仍持有 owned 值时顺手 extract 进 entity 的 `ExtractedItems`（`clear` + `extract_into`，分配跨帧复用），`ItemExtractor` fn 指针与独立 extract 遍历整个删除；collect 从 clone 改为 drain（元素 move 进帧缓冲，buffer 留在 world 复用容量）。由此 player 与 evaluator 的 clone 次数打平（各 1 次），差距只剩 world 簿记。

复测 criterion（60 帧扫描，transform_squares 50×50）：player 85.7ms → **72.7ms**（-15%），与 evaluator（55.7ms）的比值 1.52× → **1.31×**；static_squares 50×50 为 90.3 → 84.7ms（1.24× → 1.12×）。剩余差距主要是 upsert 的 bevy storage 写入与 HashMap 簿记。

权衡记录：drain 意味着 world 里的提取产物不跨帧保留，未来"值未变就跳过 extract"的变更检测需要重新取舍（collect 回到 clone 保留数据，或双层缓冲）。帧一致性 5 个 logic 测试全部保持通过。

== 后续（同日）：直接提取进渲染世界与 dylib TypeId 实证

实现了 player 的 render 侧直接提取（world exchange）：`ScenePlayer::take_world` 把 LogicWorld 交给渲染线程，`reconcile_logic` 按逻辑 SceneOrder 排序、drain `ExtractedItems` 以 move 语义直接 upsert 渲染组件（保留 Changed<T>），不经过 EvaluatedFrame/RenderFrame；离线渲染 `RANIM_SESSION=player` 切换，evaluator 缓冲路径不动。

**dylib TypeId 实证**（重要）：bevy 组件注册以 TypeId 为键、跨 dylib 不同。首版钩子在 dylib 内单态化并直接写 world，注册的组件宿主不可见（world 有 2 entity、宿主查询命中 0，渲染 0 item 触发相机断言）。修法即 stage 3 雏形——分侧：`MaterializeCtx.upsert` 宿主 fn 指针写身份/顺序/提取产物（宿主注册），钩子侧只做提取与 typed 组件 T 的 insert/contains（TypeId 敏感操作只在认识 T 的一侧有效）。dylib 定义的 item 由此能进 world 被宿主消费，但其 typed 组件宿主不可按类型查询（stage 3 registry 的正题）。

e2e 验证：basic 场景 192 帧渲染成功（1.57s vs evaluator 1.03s，dev profile）；mp4 帧级对比与 evaluator 自比噪声同量级（~5.8% 像素、max delta ~125，两次 evaluator 渲染互比为 5.86%/123——lossy 编码噪声而非路径差异）。

复测 criterion（60 帧扫描；hook 分侧后每 item 多一次 Vec 分配，player 略有回退）：

| bench | 场景 | 基线 | 新路径 | 比值 |
|---|---|---|---|---|
| session | transform 50×50 | evaluator 58.0ms | player 82.4ms | 1.42× |
| session | static 50×50 | 75.6ms | 97.2ms | 1.29× |
| extract_path | transform 50×50 | buffered 85.1ms | direct 100.4ms | 1.18× |
| extract_path | static 50×50 | 88.8ms | direct 107.3ms | 1.21× |

读法：direct（materialize+reconcile_logic）优于 player 走缓冲（82.4+reconcile≈109ms），但仍慢于 evaluator 缓冲路径 ~1.2×；差距全部在 LogicWorld 簿记与每 item 提取分配上，属于实现开销。结论不变：性能不是当前切换理由，world 的价值在 retained/typed（stage 2/3）。
