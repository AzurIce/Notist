= AI 整理文档索引

- #<self::2026-08-26 obsidian-notist lsp diagnostics design>：obsidian-notist LSP 诊断功能的完整设计——审计 2026-08-26 已落地的 diagnostics 链路（delta push、setDiagnostics 推送模型、linter(null)+lintGutter），给出未打开文件的全局面板+状态栏计数方案、version 门控陈旧丢弃、F8/Shift-F8 CM 原生导航、session 代数 token 重启一致性、FULL sync 静默拒绝的开发期自检与重同步、多 leaf 文本匹配渲染、code action 确认 server 无 capability、降级路径设计；分阶段落地并列争议点。
- #<self::2026-08-26 obsidian-notist semantic panels design>：obsidian-notist 语义面板设计——隐藏 md 面板的两层策略（purgeForeignLeaves 扩展 + CSS guardrail + layout-change 阻止重入）、Quick Switcher 命令接管、大纲 panel 由 documentSymbol 拉取刷新、入链以 (0,0) references 查当前模块（含偏移条件限制）、出链结论是不得客户端解析而应推 Notist 扩展协议（notist/documentReferences 方向可选）、workspace/symbol Mod+O 青史 picker、诊断推送的机会性失效信号模型、分层纪律与阶段路线。
- #<self::2026-08-26 tracing instrumentation and plugin reregistration root cause>：mermaid 插件调试案例（渲染期零 dispatch、HTML 出现 `<mermaid>`/`<mermaid-mermaid>` fallback 标签而 shader 照常）的根因定位与全项目结构化追踪落地——根因是 `runtime_plugins()` 先卸载 computed 函数+裸名 alias 再重装，但 data-only 元素签名（`mermaid::diagram` 等）不在卸载范围，`install_contribution` 查重报 Duplicate 后被 `let _ =` 吞掉，render evaluator 拿到残缺 registry 致所有插件调用不求值；shader「正常」是内置 ShaderHtmlProjectionHandler 按裸名匹配未规约 leaf 的 masking，声明式 WebComponent renderer 只匹配已规约 leaf 从未命中；两组 dispatch pass 分别归属 D0004 seeds pass 与 `safe_evaluation_diagnostics` check pass（后者也打真实 wasm 组件）；tracing 设计：tracing+EnvFilter 默认关闭，`NOTIST_LOG` 裸级别自动限定 `notist_*` targets，覆盖装载/guest init fuel/dispatch 计时与剩余 fuel/contribution 注册/seeds 与 check pass/投影 handler 选择/资产拷贝；修复方向（卸载范围补全或按包替换 API + 失败回退保留旧 handler）已记录未实施。
- #<self::2026-08-26 mermaid rendering investigation>：对照 Notist 与 Zed 的 Mermaid 实际渲染链：Notist 当前由 `mermaid-rs-renderer` + 浏览器 Wasm + Web Component 直接输出 SVG；Zed 使用原生 Rust `merman`（Cytoscape layout）并叠加 vendored 字体测量、编辑器主题、accent 颜色、resvg-safe 管线与 GPUI 缓存/缩放。结论是图一的主要问题来自布局/文本测量/后处理栈不同，不是简单 CSS；另记录 `dist` 可能残留旧 CDN Mermaid.js 的构建一致性风险，以及 P0 基线、P1 后端选择、P2 宿主配置与回归样本路线。
- #<self::2026-08-26 os filesystem forbidden characters>：跨平台文件系统禁止字符的来源/层级调查——按 FS 层（FAT/NTFS/exFAT/ReFS/POSIX/ext*/XFS/Btrfs/ZFS/HFS+/APFS）、OS 层（Win32、macOS 用户态 `:` → `/`、POSIX shell 元字符）、工具层（Git core.protectHFS/NTFS、tar/zip 编码、notist 自身 `filesystem_segment`）拆开梳理；并以 UAX #15 NFD/NFC、UAX #39 Unicode Security Mechanisms 切入 HFS+ NFD 与 Trojan Source 风险。结论：POSIX 唯一 FS 真禁止 `/` 与 `0x00`；Win32 额外拒 0x00–0x1F 与 `<>:"/\|?*` 与设备名 CON/PRN/AUX/NUL/COM1-9/LPT1-9（不区分大小写、可带扩展）；macOS 应用层把 `:` 替为 `/`；HFS+ 强制 NFD 引发的 Git 跨平台同步分裂是真坑。Notist 自身 `filesystem_segment` 策略（`crates/notist-cli/src/build.rs:723` 与 `crates/notist-service/src/request.rs:2406`）覆盖了 Win32 9 字符 + 控制字符 + `~` 与 `.` 编码 + 整段设备名前缀 `~00`，但未覆盖 Unicode 归一化（建议 service 入口 NFC）、`CON.txt` 类带扩展设备名、零宽字符与 Bidi 控制符（U+202E 等 Trojan Source 风险）。
- #<self::2026-08-26 typst import package cache mechanism>：源码级调查 Typst 0.14 import/包/缓存机制（typst-kit crate, 2026-08-26 SHA 41e955d2）。核心抽象：`FileLoader` trait（1 个方法，按 `VirtualRoot::Project|Package(spec)` 分发）、`Downloader` trait（`&dyn Any` key 分类 + `ProgressDownloader` 装饰器模式）、`SystemPackages { data, cache, universe }` 查找顺序（data → cache → network download to cache）、`FsPackages::store` 的「同 mount point `Tempdir + rename` + `DirectoryNotEmpty` 容忍」原子写入、`FileStore<L>` 的 `FileSlot { Empty, Loaded, Parsed }` 状态机（含 BOM 处理 + `Bytes/Source` 共享指针）、`Module = Arc<ModuleInner>` 收敛 4 种来源（builtin/file/package/wasm plugin）。与 Notist 对照：plugin 走 SDK 编译 wasm 通道、不直接同构；但 P0 建议把 import/资源加载拆 `notist-kit` crate + 全 feature-flag，P1 建议 cache 写入用 Tempdir+rename 范式（适用 8-22 路线 Step 2 进程级 Engine 缓存、8-26 reload 失效、字体/资源未来），P1 建议 FileStore 状态机下沉 analysis 层（`reset()` 触发 plugin 重载、staleness 与 8-18 `FunctionEnvironmentId` 联动），P2 建议 path 错误按 root 分类诊断（参考 typst `format_resolve_error` 的 `--root` hint 模式）。所有引用源码含仓库根 + 文件 + 行号定位。
- #<self::2026-08-26 build wasm plugin reload regression>：规约插件系统重构后 build 性能回归的调查（main @ f4b976a，对照 ab8bc43）——实测 wall 0.12s→1.40s、user CPU 0.13s→12.03s（882%）、peak RSS 52MB→154MB；samply 剖析显示 ~92% CPU 在 wasmtime/cranelift 组件编译栈（rayon 并行），探针计数证实一次 build 触发 90 次 `load_package`（每模块一次 `structured_module_with_runtime_plugins` + snapshot 构建一次），单次 `WasmComponent::new` 编译 118KB shader 组件 ~10.8ms；根因是「每模块重载插件」的旧设计撞上 45a5bf3 后的真实组件（此前是 160B stub 故无害），控制实验（移除 `[plugins.shader]` 即回基线）排除其他因素；修复方向按性价比排序为请求级提升装载、进程级共享 Engine、编译产物缓存（serialize/cache/.cwasm），并明确 per-call fuel/postcard 边界/init 注册不在嫌疑清单；请求级提升已于当日落地（`WorkspaceSnapshot::runtime_plugins()` 组合根句柄 + render pass 复用，实测 CPU -97%、wall 1.40s→0.20–0.43s，组合根留 analysis 对齐 runtime-composition 裁定）。
- #<self::2026-08-26 LSP 无头测试构思>：回应 thoughts「LSP 无头测试」的缺口定位与 harness 设计——盘点发现并非零测试而是缺黑箱层（lsp.rs 13 个测试按观察点分层后仅 1 个走完整 main_loop 且绕过 initialize 握手）；给出对外接口覆盖矩阵（references/didSave/lifecycle/cancel/UTF-16 端到端为零覆盖、didChange 仅 session 方法级）；方案对比后选定 B：`tests/` 下以 `CARGO_BIN_EXE_notist` 拉真实二进制 + stdio，复用 `lsp_server::Message::read/write` 免手写帧，核心原语是 `expect_diagnostics(谓词, timeout)` 确定性等待；附生命周期/同步契约/推送语义/编码端到端/错误规范/取消六类场景清单与 daemon 模式二阶段计划。同日方案 B 已在 feat/lsp-headless-tests 分支落地（7 场景全绿），并逼出 shutdown/exit 双消费者竞态、Connection 未释放致 io_threads.join 挂死两个真实缺陷；harness 教训：backlog 轮转会饿死收包、stderr 须增量读、嵌套模块的 `#<link>` 解析为子路径故夹具须用根模块 README。规格已提炼为现行设计 #<vault::designs::lsp::test>（partial），本文为过程记录；因 main 有重叠的未提交 lsp.rs WIP，当日裁决暂不合并。
- #<self::2026-08-26 obsidian-notist lsp client implementation>：obsidian-notist 接入 notist LSP 的实现记录——`src/lsp/` 三层（手写最小 protocol 类型 / stdio Content-Length framing+惰性 require / session 文档注册表+latest-wins 取消）+ per-view CM 扩展工厂，diagnostics/completion/hover/definition 四项一次落地并经 obsidian CLI 实机验证（基线/delta 推送、编辑联动、F12 跳转 reveal）；高复用价值踩坑：推送诊断必须配 `linter(null)`（`linter(()=>[])` 会把 setDiagnostics 推入结果刷空）、setViewData 回声与外部修改用*内容比对*区分（server 端 overlay 永远压过磁盘，外部改动必须走 didChange）、插件 reload 时异步 stopLsp 触发的 onExit 会让*已卸载实例*自重启泄漏 server 进程（onunload 先置 unloaded 标志）、shutdown 要先注册 exit 监听再发 exit 通知、spawn 成败用 `spawn`/`error` 事件判定、obsidian CLI 的 async eval 返回值偶发丢失（同步返回+轮询更稳）、bun add 不写 package.json 的已知坑复现。当日补记（Zed 式 path+argv 启动配置）：设置 onChange 逐击键引发重启风暴（800ms 防抖）、cwd 语义定稿为 spawn 固定 vault 根 + 启动器需求写进 argv（`nix develop`/`direnv exec` 都不 cd，cargo 需 `--manifest-path`，否则在 Obsidian 的 cwd 找不到 Cargo.toml 报 exit 101）、restartLsp 双开 server（lspStarting 守卫）、stderr 环形缓冲进 error detail、server 的 `window/logMessage` 协议拒绝警告接入客户端 console。
- #<self::2026-08-25 notist lsp audit follow-up>：对 #<self::2026-08-24 notist lsp implementation audit> 的逐条核实（main @ 39f6086 重写后）——已闭环：severity 透传、错误码四分、version 回退拒绝、诊断批量化+签名 diffing+baseline、latest-wins 调度器（single-flight builder+dirty 合并）、全局写锁消除、2-8 有界 worker 池、FULL sync 单 change 强制、URI 多级 parent、watcher 自通知发往客户端出站流的隐藏 bug 修复（换 PassiveDebouncedWatcher 走进程内事件总线）、embedded 真取消（execute_cancellable）、协议拒绝经 `window/logMessage` 推送客户端（2026-08-26，didChange 违规 WARNING / rebuild 失败 ERROR）；残留：daemon 路径仍 per-vault Mutex 串行+假取消（服务端已支持多路复用与 Cancel 但 CLI 未接线）、Sources/diagnostics 全量文本传输；新发现：畸形 notification/daemon 瞬断杀死整个 server 主循环、worker 池无优先级分层、诊断发布在主循环同步执行可被在途重请求阻塞。对 Obsidian 客户端：debounce 从必需降为可选、FULL sync 违规会被拒且客户端收到 logMessage 警告（仍须发送端合规）、诊断是 delta 推送、崩溃重启逻辑成必需项。
- #<self::2026-08-25 lsp architecture redesign>：基于当前 LSP、service、analysis、syntax、eval 真实实现的系统性架构重设计；结论是保留不可变 `WorkspaceSnapshot` 与 VaultEngine/View 边界，重建 LSP session/query 层，补齐 generation/latest-wins、snapshot pinning、可传播取消、批量 diagnostics 和统一 `SymbolId` 语义索引，并给出 UTF-16/UTF-8 契约、模块责任、分阶段迁移、不变量、测试矩阵与并发性能验收标准。含对照 LSP 3.17 与 rust-analyzer/gopls 的生产级协议基线（取消错误码、编码协商、watcher 注册、诊断分层节奏、线程模型）。阶段 A 契约止血（severity 透传、批量诊断、URI 多级 parent、FULL sync 单 change 校验、初始发布、错误码区分、didChange 版本校验）、阶段 C session/scheduler 核心（`LspSession` 单 mutator、进程内事件总线修复 watcher 自通知发往出站流的死 bug、单飞 latest-wins 构建、有界请求池、embedded 并发与真实取消传播）、阶段 D 的 resolver 子集（Let/LambdaParameter/ImportBinding 身份+文档级作用域）与阶段 E 的 diagnostics delta 及 structured_module 安全拆分均已落地；待做：daemon 取消多路复用、checker/resolver 合并、service 侧 SnapshotHandle 坐标表。
- #<self::2026-08-24 notist lsp implementation audit>：notist LSP 实现（`crates/notist-cli/src/lsp.rs`）审计与改进空间——契约问题（severity 硬编码 ERROR 丢弃真实标签、错误码一律 InvalidParams、$/cancelRequest 只在跑完后替换响应不真取消、didChange 无 version 校验）；性能问题按序：publishDiagnostics 对每 path 重复请求全 vault Diagnostics 再 filter 的 O(N) 往返（P0）、每 notification 全量 rebuild（fs vault 发现+全量 overlay+全量 Sources）无 coalesce 与设计文档 latest-wins revision 承诺的差距（P0）、rebuild 全程持全局写锁堵读请求、daemon 请求被 Arc<Mutex> 串行化消解每请求一线程模型、Sources 全量载入只为 position 换算、publish 无 diffing、watcher 无过滤递归 watch 整个 workspace；改进路线：诊断批量化与契约修复先行，rebuild 调度器+去全局锁+取消传播合并为一次并发改造，position 换算下移 service 需先改 CoreResponse 契约。客户端接入设计见 #<vault::obsidian-notist::lsp>。
- #<self::2026-08-24 obsidian-notist syntax highlighting implementation>：obsidian-notist 语法高亮实现记录——tree-sitter-notist 语法（钉 bdeab509）经 web-tree-sitter 跑在插件内，captures 经 boundary sweep 映射为 CM6 mark decoration（方案见 #<vault::obsidian-notist::highlight>）；高复用价值踩坑：zed-notist 的 notist.wasm 是 Zed 工具链产物、dylink 依赖 libc.so 致 web-tree-sitter 加载失败（用 `tree-sitter build --wasm` 经 wasi-sdk 重出自包含产物）、esbuild CJS bundle 里 import.meta.url 为空致 emscripten `new URL` 炸（Parser.init 必须传 locateFile 占位 + wasmBinary 直给字节）、esbuild target 需 es2020（BigInt）、web-tree-sitter 以 UTF-16 code unit 计 offset 与 CM 天然对齐（中文实测）、heading_marker 含尾空格（级别数 `=` 字符）、capture 名混用 `.`/`_`、RangeSetBuilder 要求不相交故 sweep 切段取类名并集、多 change 事务放弃增量直接全量 reparse。
- #<self::2026-08-24 obsidian-notist editor cm6 migration>：obsidian-notist 编辑器从 textarea 占位迁移 CodeMirror 6 的实现记录——宿主提供 CM 运行时下的最小扩展组合（行号/软换行/history）、vim 键位经 `@replit/codemirror-vim` 打包接入（Compartment 热切换；normal/visual 置 `editable=false` 抑制 IME 的 Zed 式方案；`drawSelection` 与 `allowMultipleSelections` 是 replit-vim 的两个隐性宿主依赖，缺前者 insert 光标消失、缺后者 visual block 被裁剪）、对齐 Zed 精致度的全主题变量样式（当前行带连续覆盖 gutter、vim ex 面板并入主题底栏）；高复用价值踩坑：update 周期内 dispatch 抛异常致 editable 与 vim 模式脱钩卡死（setTimeout 延后）、tabindex 移除致 Chrome blur（常驻 + 焦点恢复）、拷贝安装必须 `bun run sync <vault>` 再 reload、Prec.highest 主题需 `!important`、`.cm-content` 水平 padding 致当前行断裂、core `.view-content` padding 需更高特异性、bun add 因 install 钩子失败不写 package.json。
- #<self::2026-08-24 obsidian-notist plugin mvp implementation>：obsidian-notist 插件 MVP 实现记录——bun 工程化与 sync/install 安装脚本、双 World 机制（body class + getLayout/changeLayout 布局隔离 + purgeForeignLeaves 自愈快照、CSS data-path 文件树过滤、ribbon 白名单）、文件创建的 chokepoint 方案（patch `FileManager.createNewMarkdownFile` 单点覆盖按钮/右键/命令/快捷键，附 create 事件兜底误伤用户文件等三条废弃弯路）、NotistTextView 内联标题改名；高复用价值踩坑：`titleEl` 字段遮蔽 ItemView 内建字段致 view 打不开、`input[type=text]` 特异性压插件样式、obsidian CLI 驱动运行中实例做调试与实机验证。
- #<self::2026-08-24 obsidian plugin feasibility research>：notist 以 Obsidian 插件接入的可行性调研与立项记录——notist 侧资产（stdio LSP、JSON CLI、notist-html fragment + data-notist-* 投影、协议无关 service）与缺口（无 HTTP/WS、无 JS 语法解析、无跨平台分发、MCP 不存在）、Obsidian API 能力边界（registerExtensions 一等支持、CM6 由宿主提供、桌面可 spawn 外部 LSP、wasm-bindgen 两端通用、非 md 不进核心索引无双链/图谱）、三层推荐架构（wasm 基础层 / 桌面 LSP 增强层 / 自研互联层）与残余风险；末尾记录同日「借壳架空」立项决策（两 world 隔离、语义组件不 import obsidian API），立项文档在 #<vault::obsidian-notist>。
- #<self::2026-08-23 plugin system design evolution and roadmap summary>：两日会话的收口总结——六次设计转向（init 自描述形态、wasm 即 dylib 哲学、能力系统删除、一切皆 call 统一 IR、bytes+可替换序列化边界、双通道注册方向）与实施路线对照表：已完成 10 项（SDK/装载/统一 IR/node 引擎/数据型声明/能力移除/基准归档等），未完成含 A2 残余四步（core 构造器→Nodes、html legacy 路径删除、eval 值域重写、桥接层清除）、C 类欠账（postcard/shader v0/api-version/spike 归档）与 B 阶段（core wasm 化实验 + 基准定案）；末尾固化关键不变量备忘。
- #<self::2026-08-23 boundary transport benchmark>：插件 ABI 边界表示三方基准与裁决：真实 wasip2 组件 echo dispatch 下，postcard 全面胜出（e2e 比 JSON 快 2.3–3.3×、payload 小 2.9–4×）；反直觉发现 typed-WIT arena 比 postcard 慢约 2×（字符串密集堆结构下 canonical ABI 逐字段 lift/lower 的分配拷贝开销超过一次紧凑二进制解码）——arena 从性能候选降级为语言开放性候选；实测迁移阻塞点：postcard 不支持 internally/adjacently-tagged 枚举（WontImplement），切换前提是 WireNode 改外部标签 + version byte；绝对值层面 medium 树单次 dispatch 仅 6–22µs，唯一放大场景是 core wasm 化（千次 dispatch 时 JSON 22ms vs postcard 6.7ms），不再单独构成否决项。裁决：维持 opaque bytes，ABI 稳定后一步切 postcard。
- #<self::2026-08-22 plugin form and unified ir roadmap>：插件形态集中收敛的裁决与实证记录：形态定为「Rust crate → wasip2 组件 + SDK `init` 注册自描述」（schema 真相来源是 guest 注册、manifest 缩水为信封、装载统一为实例化+init 单路径）；双端 bindgen spike 实证 WIT 递归类型不可行（"depends on itself"）而 arena 拍平端到端可行，与本日 #<self::2026-08-22 wit recursive types best practices> 的上游调查互证并修正其「arena ergonomics 差」论据（SDK 包装消解）；裁定 bytes 边界维持但 codec 解耦（JSON→postcard 留作 ABI 稳定后的一行级演进）；确立 native rlib / wasm component 双注册通道（first-party=native、third-party=wasm 信任分层）与三步路线：Box 单一 Node IR 统一 → core 运行时加载实验（头号风险 body 重序列化 O(深度)）→ 基准定案（不达标退路是 hot 构造器留 native 的分层混合）。

- #<self::2026-08-22 wit recursive types best practices>：调查 WIT 表示递归类型（notist 的用例是插件 ABI 的 `Stream<Call | Leaf>` 规约树）的现状与最佳实践：WIT 值类型必须无环（wasm-tools 1.256.0 实测 `list<>` 间接递归同样报 "depends on itself"），环只能经 resource 句柄形成；上游 component-model#56 仍 open，方向是 wasm-gc 递归类型或显式 `box<T>`。三种官方认可的绕行——arena/索引拍平、bytes+serde、resource 句柄——中，结论维持现状的 bytes+JSON+宿主校验（schema 演化宽松、guest 语言无关、不可信输入本就要校验），未来要类型化边界选 arena 不选 resource；issue 中提出的递归值 DoS 隐患与现有 depth/fuel/max_nodes 预算对应。
- #<self::2026-08-18 notist plugin system design>：把插件系统范围收敛为「eval 面 + html 展示面」的双面 ABI 设计：内置无糖内容函数视为 eval 贡献 + html 贡献组成的插件，`notist-html` 定位为 html target 的投影宿主与 Custom renderer registry；`ElementSchema/Custom.fields/ElementValue` 作为两侧稳定契约，renderer 缺失时 total fallback。含跨系统插件形态调研（VS Code/Zed/Neovim/Obsidian/Typst/Helix/Figma/Blender/Jupyter/VST）：插件最终存在形式定为「包 = manifest + 按 interface 拆分的 Wasm component + assets」而非单一 .wasm；三条收敛规律（包+manifest、信任光谱上 wasm 是唯一成熟的不可信插件答案、跨 target 渲染必须「插件产出声明/宿主渲染」）；关键张力——render 契约若返回 HTML 字符串会锁死 html target，应演进为后端无关渲染描述，已列入待决事项。分析 Cordis 的 Context/Service/Fiber/Events 可借鉴点与不宜移植部分，并明确成型阶段由 core 拥有、插件只通过 block 声明参与。SSG、模板、gallery、native 多目标等后续消费者明确排除。现行插件系统规范见 #<vault::designs::plugin-system>。
- #<self::2026-08-17 web ui obsidian-like editor research>：回应 `vault::designs::thoughts` 的 Web Editor/Web UI 信息增强想法，预调研基于 Web UI 做类 Obsidian 编辑器的方案：编辑器内核分「源码+装饰」与「文档模型」两范式，Obsidian Live Preview 即 CM6 ViewPlugin+Decoration 机制，结论选 CodeMirror 6（Tiptap/BlockNote/Vditor 等为 WYSIWYG 备选）；.not 语法接入需独立 Lezer grammar 或 notist-syntax 编 WASM 二选一（各有漂移/增量解析风险，建议 spike 对比）；架构主路线为思源/SilverBullet 模式（daemon 加 HTTP/WS 服务层 + PWA 前端，Tauri 壳备选，LSP-over-WS 复用语义能力，Yjs/yrs 协同为二期）；给出 spike → 只读浏览 → 编辑接入 → 信息增强四阶段路线与风险清单。与 #<self::2026-07-20 notist native desktop architecture research> 在 CM6 内核与「前端不重解释 .not」两点收敛。
- #<self::2026-08-17 cli interface design direction>：综合 docs/ai 全部六份 CLI 文档、2025–2026 外部 Agent CLI 实践调查（gh/kubectl/ripgrep/QMD/RTK/cli-output-spec 等）与当前代码现状的方向分析：确认 v3 三层分离（语义命令/运行时策略/输出投影）、唯一 next、极简信封获外部充分印证；指出地基已在（预算执行早在 service 层 page<T>()、cursor 自包含）；关键裁决建议——`next` 用现有无状态自包含 cursor 实现即可，砍掉 daemon page-session LRU；另补 schema 自描述、next 失效语义、确定性排序三个契约细节，并给出按 Phase A–D 的落地要点。
- #<self::2026-08-15 cli v3 clean interface redesign>：回应“现有命令被 token 预算与翻页参数搅乱”的判断，推翻同日上一稿“保持命令面 + 增量补 flag”的结论，提出 CLI v3：语义命令只表达意图，删除所有 `--limit/--max-bytes/--cursor/--mode/--format` 型策略参数；search 与 locate 拆分 recall/precision；分页收敛为唯一 `notist next <TOKEN>`；JSON v3 只保留 items/next/complete/matched/hints；运行时预算与 page-session 下沉 daemon；并给出迁移、规范修订与验收标准。
- #<self::2026-08-15 cli agent navigation and retrieval interface>（已被同日 #<self::2026-08-15 cli v3 clean interface redesign> 取代“保持命令面”的结论，检索正确性发现仍有效）：调查 Notist CLI 面向 Agent 的最佳接口形式：确认现有 status/modules/search/outline/read/references/check/edit 导航原语与 bounded page contract 是正确的认知模型；以当前 75 source docs Vault 实测 6 条 Agent 轨迹与 14 个概念查询，发现标题默认 id 只返回标题行、多概念 AND 作用在段落级导致漏检、archive 压过现行规范、exact 与 lexical 字段语义不一致等 9 个差距；提出模块级候选生成 + 单元级证据排序的 ranking v4、section context、--exclude-scope、公开 SnapshotStamp 去 canonical root 与 schema v3 页级去重，并给出 P0/P1/P2 与验收标准。
- #<self::2026-08-15 design record separation audit>：对 #<vault::designs> 当前 15 篇设计稿与 L/V/A 分层地图做结构分离审查：已完全分离 7 篇（L3/L4/L5/L6/L9/A3/集合草案），部分分离 3 组（D0001/D0002/D0006），未分离 4 组（D0004 的 V1–V4，D0008 的 A2/A4，D0009 的 A1/A5，D0010 的 A6/A7）；当前无整篇可归档，仅 D0002\#函数类型已节级重定向。给出 D0016 起按 Core Type System → Property Table → V1–V4 → A1–A7 的拆分顺序。已执行：D0002 → D0016/D0017，D0006 → D0017/D0018/D0033，D0007 → D0033/D0034，D0004 → D0019–D0022，D0008 → D0023/D0024（并继续拆出 D0032 检索语义），D0009 → D0025/D0026，D0010 → D0027/D0028，D0003 → D0030/D0031，D0005 → D0035/D0036；D0001 边界规范迁入 D0007；V5 → D0029 并归档旧 D0004。对应旧编号的 redirect 壳已移入 `vault::designs::archive`（该目录现已删除，见 Git 历史）。最终 active designs 进一步改为无编号语义目录：`language/`、`world/`、`host/` 共 26 篇概念文档，另有 `overview.not`；每篇用模块属性 `@![implementation = "..."]` 声明实现对齐状态，不再使用文件名编号或 `Design:` trailer。
- #<self::2026-08-14 implementation design audit and rulings>：对新一代设计序列 D0001–D0010 与当前实现及官方文档做完整一致性审查（7 个并行审计 + 端到端验证）：约 42 个根因，核心结论是 syntax/eval/model 语言核心仍是旧世代实现（无一般 let/if/lambda/闭包/import/RefTarget/Section 归组，求值器重扫源码识别糖），service/CLI/构建层已对齐 D0005/D0008/D0009/D0010；MCP 是唯一设计禁止但完整存在的功能。记录 24 项设计歧义裁决（R01–R15、E01–E09，含命名实参用冒号、禁止实参交错、Quote/Table 排除出 v1、类型表面收敛、外部 URL 语法合法等），设计文档已按裁决修订（9 文件 35 处编辑）。
- #<self::2026-07-31 programmable document language redesign>：审计当前 `let`/Function、scope、Call binding、Type、Raw provenance、evaluator reparse 与 Analyzer 路径为何尚未形成统一 PL 语义，归纳 ModuleResult、Content quotation/monoid、lexical closure、first-class Function 和 typed CallPlan 的目标模型，并给出 conformance-first 的九段重构路线与按问题推进的 PL 学习顺序。
- #<self::2026-07-27 cli token cost and search ergonomics evaluation>：实现并评估 CLI/MCP 的 token 成本优化：统一保留 `--limit`，将 search 默认缩到 8 个 256-byte excerpt，以 source/section/match grouping 提升首页多样性，把 index provenance 移到页级，压缩 cursor，修正 Agent 翻页停止规则与 human text/help，并提供 MCP full/brief mirror；同一 docs query 的默认 search JSON 从 23,974 B 降到 6,647 B。
- #<self::2026-07-27 agent-facing CLI and MCP interface redesign>：在成本取证之上重新设计 Agent 与人共用的 query contract：拒绝隐式 agent/human profile，采用“默认预算 + 参数可调 + 服务端硬上界 + 显式 export artifact”，并给出统一 PageRequest、snapshot cursor、CLI 命令面、十个 MCP tools、Resource Template、typed error、编辑计划、兼容迁移和可量化验收。
- #<self::2026-07-26 agent-friendly CLI output and Notist improvements>：基于 v0.3 的 720 次 Agent run 成本取证，对照 QMD、GitHub CLI、AWS CLI、Kubernetes API、ripgrep、RTK、Repomix 与 MCP 规范，提出默认有界、snapshot cursor、字段投影、渐进式 source read、32 KiB response budget、MCP Resource Template、typed error 和输出成本回归方案。
- #<self::2026-07-21 notist agent mcp semantic retrieval and vector indexing>：从零梳理 Notist 作为 Agent 输入/输出知识库的完整方向，说明 MCP resources/tools 与 application core 的边界，并综述 BM25、dense/sparse embedding、ColBERT、多向量、HNSW/IVF/PQ/DiskANN、hybrid/rerank、RAPTOR/GraphRAG、记忆、引用评测和安全写入；给出从稳定 ID、全文索引和只读 MCP 到向量、图检索、事务化 Agent 编辑及远程多租户的渐进路线。
- #<self::2026-07-20 notist native desktop architecture research>：系统比较原生平台 UI、Qt、Floem、Slint、GPUI、Tauri 与 Wry 等路线，推荐采用原生源码编辑器、pane 级 WebView、不可变 WorkspaceSnapshot 和 Wasmtime/WASI capability 插件宿主，并列出 Linux Wayland、安全隔离及四个落地 spike。
- #<self::2026-07-17 typed document analysis and lsp migration>：静态求值模型迁移收尾。记录 mode-aware 语法树之后的五项决策（元素 range 含 `#`、Annotation 移出 evaluator、注册时签名校验、签名数据上移 model、analysis 静态 type checking 与 eval 的错误对齐规则）,LSP 诊断去 runtime 与最内层 call 补全，以及 typst CST/AST 设计对照与遗留事项。
- #<self::2026-07-13 tinymist preview and notist html>：调查 Tinymist 如何组合 project/compiler、CompileWatcher、HTTP/WebSocket、增量矢量协议、内嵌前端和 LSP 控制，并说明其分页排版模型的适用边界。Notist 自身的设计见 `vault::old-designs::archive::D0009-preview-and-html`。
- #<self::2026-07-11 typst element function and syntax sugar>：梳理 Typst 如何将 Heading、Strong、ListItem、Reference 等标记语法映射到统一的 Content/Element 模型，区分 Element sugar、结构语法糖、值语法糖和语言级构造，并总结 Notist 可复用的强类型 Element 与函数构造器设计。
