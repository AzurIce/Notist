= 向量化检索实验对比

承接 #<vault::ai::2026-09-08 向量化检索系统调查> 的因子化实验裁决与 `vsearch` 实验 lane（分支 `research/vector-retrieval`）的实测报告。多臂标注 bench：切块粒度 × 上下文前缀 × 模型运行时/硬件，46 条标注查询，Recall\@k 与 MRR 打分。本文是实验记录，不是规范；结论支撑选型，不构成契约变更。

== 实验设置

- 语料：docs vault 副本。section 粒度 1228 块，paragraph 粒度 6910 块。
- 模型：bge-small-zh-v1.5（24M 参数、512 维、512 token 上下文），多运行时同一套权重：fastembed ONNX（内置）、llama.cpp 10408 f16 GGUF（CPU 4 线程与 CUDA 两种执行）。
- 硬件：Ryzen 9 9950X3D（嵌入统一 4 线程）+ RTX 4070 Ti SUPER 16GB。
- 查询集：46 条标注查询（36 概念、4 改述、6 标识符守护），由 Agent 对照节清单盲写；目标为具体规范节。
- 判定：strict = 命中目标节或其子节；lenient = 另计祖先节。指标 Recall\@5/\@10、MRR\@10。打分器与查询集随分支（`vsearch-bench` bin + `score.py`）。

== 总表（strict）

| 臂 | 块数 | 全库嵌入 | 查询延迟 | R\@5 概念 | R\@10 全部 | MRR | 标识符 R\@10 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| fastembed ONNX，无前缀 | 1228 | 52s | 13ms | 0.19 | 0.39 | 0.188 | 0.50 |
| fastembed ONNX，链+属性前缀 | 1228 | 52s | 14ms | *0.42* | *0.41* | 0.228 | 0.50 |
| llama.cpp CPU 4 线程，无前缀 | 1228 | 10s | 14ms | 0.31 | 0.43 | 0.250 | *0.83* |
| llama.cpp CUDA `-ngl 99`，无前缀 | 1228 | *2.8s* | 14ms | 0.31 | 0.43 | 0.250 | 0.83 |
| ollama GPU（同 GGUF） | 1228 | 17.5s | 172ms | 0.31 | 0.43 | 0.250 | 0.83 |
| fastembed 段落切块，无前缀 | 6910 | 194s | 68ms | 0.25 | 0.26 | 0.196 | 0.50 |

lenient 判据下排序一致。scope 臂（两个）与 section 臂逐位相同。

== 因子结论

=== 切块粒度：section 决定性胜出

paragraph 的 strict R\@10 0.26 对 section 的 0.39，代价是 5.6 倍块数与 4 倍构建时间。失败机制：短碎片相似度虚高挤占 top-k（10 个命中只覆盖 2–4 个不同节），单行块可排到第 1。paragraph 在 lenient 下不差（0.46），说明碎片常落在正确节内——若将来需要细粒度命中，走「小块检索、返回整节」（small-to-big），不用小块直接排序。

=== 上下文前缀：属性注解有效，是最大单项发现

概念查询 R\@5：无前缀 0.19 → 标题链 0.33 → *标题链+属性 0.42*。链+属性在全部指标上为诸臂最佳。机制：嵌入文本前拼标题链行与生效属性 token（如 `@implementation = aligned`，来源为模块 `@!` 条目与管辖节注解），spec 文档因此能压过内容相近的 ai/ 笔记。链前缀单独使用只在概念\@5 有提升、整体\@10 略降；属性 token 是净收益来源。

=== scope 切块：因子未被本语料检验

docs vault 只有 3 个真 scope 块，scope 切分退化为 section，两 臂逐位相同。切分器已实现（节按直接 `#[...]` scope 子块分区），结论是语料级缺口而非方案级否决；需要重度使用 scope 的语料复测。

=== 运行时与硬件：GPU 赢在批量构建，查询无差别

同一 llama.cpp CUDA 二进制切 `-ngl 0`/`-ngl 99`：全库嵌入 10s → *2.8s*（440 块/秒，3.6 倍）；单条查询 14ms → 14ms（单文本推理太小，开销在加载与网络）；质量逐位一致（46 条中仅 3 条命中序列有数值抖动级差异）。ollama 的 17.5s 是其调度开销，非 GPU 速度。fastembed ONNX CPU 路径明显最慢（52s/4 线程；17 线程 29s 但满核突发）。GPU 的价值场景：万级块以上、大模型、常驻低延迟服务；千级块 + small 模型下 CPU 4 线程已足够。

=== 标识符查询：向量 lane 不解决

fastembed 六条守护命中 3（`SearchUnitBuilder`、`index_not_ready` 所在节完全丢失）；llama.cpp 运行时命中 5（同权重，运行时差异所致）。最优仍丢 1/6。标识符与精确匹配归 exact/grep，向量 lane 定位概念检索——工具箱分工获得实测支撑。

=== 改述型查询：dense-only 的实测短板

4 条无关键词重叠的改述查询在所有臂上全部未命中（0/4），返回仅话题相邻。样本小不下硬结论，但方向明确：这是 hybrid（+词法）最直接的实证需求。

== 推论

- 数据支持的内置默认：`bge-small-zh-v1.5` + `granularity = "section"` + `context = "chain-attrs"` + 4 线程。bge-m3 等大检查点按性能裁决移出内置模型表，大模型走 `endpoint`。
- endpoint 通道（llama.cpp/ollama）实测比内置更快且质量略好（f16 GGUF + llama.cpp 分词/池化路径），适合愿跑 server 的用户；内置通道价值为零依赖零设置。
- absolute 数字受限于 46 条 Agent 盲写查询与 strict 判据，支撑相对选型而非绝对质量承诺；进入默认契约仍须按 search 规范的向量门槛扩查询集并加词法基线对照。
