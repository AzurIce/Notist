#heading(level=1)[Tinymist Preview 调查与 Notist 预览设计]

本文调查 Tinymist 的预览能力，并据此设计 Notist 的 HTML 渲染层和未来的 `notist preview` 命令。调查基于 Tinymist 官方仓库 2026-07-12 的主分支代码和 crates.io 上的 `0.15.2` 版本。

#heading(level=2)[结论]

Tinymist 自带完整预览能力。它不是只通过编辑器扩展调用外部编译器，而是在默认 CLI 构建中启用 `preview` feature，并提供 `tinymist preview` 子命令。

Tinymist 的预览栈包括：

- Typst project 和增量编译服务。
- 嵌入 CLI 二进制的预览前端 HTML。
- HTTP 服务和 WebSocket data plane。
- 增量矢量文档更新协议。
- 浏览器自动打开。
- 文档与幻灯片预览模式。
- 源码和预览之间的双向定位控制。
- 由 LSP 启动、停止和滚动预览的命令。

典型调用为：

#raw(lang="shell")![
tinymist preview main.typ
tinymist preview main.typ --no-open
]!

其主流程可以概括为：

#raw(lang="text")![
tinymist preview
  -> 启动 Typst project/compiler
  -> CompileWatcher 接收编译结果
  -> 增量矢量渲染
  -> WebSocket data plane
  -> 浏览器中的内嵌预览前端
]!

#heading(level=2)[Tinymist 中值得借鉴的设计]

#heading(level=3)[预览是编译管线的消费者]

Tinymist 没有让预览前端直接解释 Typst 源码。编译器先生成语义明确的文档产物，预览层再消费该产物。这样编辑器、导出和预览共享同一套语言语义。

Notist 也应保持同样的边界：

#raw(lang="text")![
.not source
  -> notist-syntax
  -> notist-eval
  -> StructuredDocument
  -> notist-html
]!

HTML renderer 不应直接扫描 `.not` 源码，也不应重新实现 call、scope 或 reference 语义。

#heading(level=3)[前端资源随二进制分发]

Tinymist 将预览 HTML 作为 asset 嵌入二进制。Notist 也应使用 `include_str!` 或等价方式嵌入小型 HTML、CSS 和 JavaScript，避免安装 CLI 后还要寻找外部静态资源目录。

#heading(level=3)[默认只监听 loopback]

本地预览会暴露正在编辑的文档内容。Tinymist 默认使用 `127.0.0.1`，并检查 WebSocket Origin。Notist 第一版同样只应监听 loopback。

如果未来允许 `--host 0.0.0.0`，必须显式警告，并在暴露写操作或源码接口前加入随机访问 token。

#heading(level=3)[最后一次成功结果应继续可见]

编辑中的文档经常短暂处于语法错误状态。预览服务不应在一次失败后清空页面，而应保留最后一次成功渲染，并单独推送新的 diagnostics。

#heading(level=2)[不直接复用 tinymist-preview 的原因]

`tinymist-preview` 的核心输入是 Typst 已排版文档，输出是 Typst 专用的增量矢量表示。它还包含页面定位、帧坐标、部分渲染和 Typst source span 映射。

Notist 当前是结构化知识文档系统，不是分页排版系统。直接依赖 `tinymist-preview` 会引入错误的文档模型和大量无关复杂度。

可复用的是分层思路，而不是 crate 或通信协议。

#heading(level=2)[Notist Preview 的产品形态]

Notist 的基本单位是 vault/module，因此预览应表现为本地知识库浏览器，而不只是孤立文件页面。

建议的 CLI：

#raw(lang="shell")![
notist preview .
notist preview . --entry vault::grammar
notist preview . --port 3000 --no-open
]!

默认行为：

- 加载整个 Workspace。
- 默认打开根模块 `vault`，即根目录的 `README.not`。
- Wiki Reference 渲染成模块导航链接。
- 文件变化后自动刷新当前模块。
- 页面同时显示 analysis 和 evaluation diagnostics。
- 只监听 `127.0.0.1`，端口默认使用系统分配的空闲端口。

#heading(level=2)[Notist Build 的静态站点形态]

单文档 `compile` 不符合 Notist 的 vault/module 模型。静态导出应使用 workspace 级 `build` 命令：

#raw(lang="shell")![
notist build docs -o dist
]!

构建器遍历整个 Workspace，并把每个模块映射为 clean URL 目录：

- `vault` -> `index.html`
- `vault::grammar` -> `grammar/index.html`
- `vault::designs::type system` -> 磁盘上的 `designs/type system/index.html`，对应 URL `/designs/type%20system/`

没有 `README.not` 的虚拟目录模块也生成索引页，因此模块树中的每个节点都有稳定页面。所有页面共享 `_notist/style.css`，并包含整个 vault 的模块导航。

Wiki Reference 必须在构建时解析为从当前页面到目标模块页面的相对 URL。目标不存在时保留可见文本和 unresolved 样式，但不生成可点击链接。构建仍输出可用页面，同时通过 diagnostics 和非零退出码报告语法、求值或引用错误。

第一版 build 不主动清空输出目录，避免误删用户文件。它只创建或覆盖当前 Workspace 对应的页面和自有 `_notist` 资源；未来可增加显式 `--clean`。

#heading(level=2)[建议架构]

#raw(lang="text")![
notist-analysis
  WorkspaceSnapshot: source、module index、resolved references
       |
       v
notist-eval
  Evaluation -> StructuredDocument
       |
       v
notist-html
  semantic HTML fragment
       |
       v
notist-cli preview
  watcher + HTTP + SSE + embedded frontend
]!

#heading(level=3)[notist-html]

`notist-html` 是纯渲染 crate，只依赖 `notist-model`。它负责：

- 将 `StructuredDocument` 渲染为 HTML fragment。
- 对所有文本、属性和 URL 数据进行转义或编码。
- 在元素上保留 source byte range，供未来源码同步使用。
- 根据当前模块解析 Wiki Reference 的目标模块。
- 对 Custom 和 UnresolvedCall 提供安全、可见的降级表示。

它不负责：

- 读取文件或构建 Workspace。
- 判断目标模块是否真实存在。
- 启动 HTTP 服务。
- 监听文件变化。
- 注入完整页面 shell、主题或交互脚本。

把 renderer 限定为 HTML fragment 可以让 preview、静态导出、测试和编辑器 hover 复用同一层。

#heading(level=3)[notist-analysis 的后续改造]

当前 `Workspace` 中的 Module 保存 source path 和 Parse，但不保存与 Parse 对应的源文本。预览实现前应增加稳定的 Workspace snapshot：

- Module 保存 `Arc<str>` 或等价的不可变 source。
- 提供按 `ModulePath` 查找 Module 的公开接口。
- 提供引用解析结果查询，或让渲染调用方提供 resolved target。
- 每次 reload 构造一个完整新 snapshot，成功后原子替换旧 snapshot。

Parse 的 byte range 必须始终和同一 snapshot 中的 source 对应，不能在渲染过程中再次读取可能已经变化的文件。

#heading(level=3)[notist-cli preview]

预览服务第一版建议使用：

- `axum` 和 `tokio` 提供 HTTP 服务。
- `notify-debouncer-mini` 监听并合并文件系统事件。
- `open` 打开系统默认浏览器。
- Server-Sent Events 通知浏览器 workspace revision 已变化。

建议端点：

- `GET /`：返回嵌入式预览 shell。
- `GET /api/module?path=vault::grammar`：返回当前模块 HTML、目录和 diagnostics。
- `GET /events`：发送 workspace revision 变化事件。

浏览器收到 revision 后重新获取当前模块。第一版不需要 WebSocket，也不需要自定义 DOM diff 协议。

#heading(level=2)[HTML 映射]

当前模型建议映射如下：

- `Block::Paragraph` -> `p`
- `Block::List` -> `ul` 和 `li`
- `Text` -> HTML escaped text
- `Strong` -> `strong`
- `Heading` -> `h1` 到 `h6`
- `Quote` -> `blockquote`
- inline `Raw` -> `code`
- block `Raw` -> `pre > code`
- `Reference` -> `a.notist-reference`
- `Custom` -> 带 `data-notist-name` 的 `span` 或 `div`
- `UnresolvedCall` -> 带函数名和原 body 的降级容器

Raw 和 Custom 内容不能被当作可信 HTML 注入。未来如需插件输出 HTML，应设计显式的 trusted renderer 注册边界，而不是复用 RawSource。

#heading(level=2)[Source Range 和 Metadata]

第一版 renderer 应在语义元素上输出：

#raw(lang="html")![
data-notist-start="12" data-notist-end="28"
]!

这让后续点击预览定位源码不需要改变 HTML renderer 的公共模型。

StructuredDocument 中的 Annotation 可能覆盖多个嵌套元素，不能简单地转换为单个 HTML class。第一版保留 source range，但暂不把任意区间 annotation 强行投影到 DOM。后续可以在渲染前构建 range event，并将 Content 切分为正确嵌套的 annotated spans。

#heading(level=2)[刷新和错误策略]

推荐刷新流程：

#raw(lang="text")![
filesystem event
  -> debounce
  -> rebuild complete WorkspaceSnapshot
  -> evaluate affected/current module
  -> render HTML
  -> publish new revision
]!

处理原则：

- Workspace 构建成功但存在 recoverable diagnostics：发布新页面和 diagnostics。
- 文件读取等致命错误：保留旧 snapshot，发布错误状态。
- 当前模块被删除：显示模块不存在，并提供返回根模块的入口。
- 浏览器断开后由 SSE 自动重连，再根据 revision 获取最新状态。

#heading(level=2)[实施顺序]

1. 新增 `notist-html`，覆盖现有 StructuredDocument 和 Element。
2. 增加 workspace 级 `notist build`，验证模块路径、引用和多页面 HTML 输出。
3. 扩展 Workspace snapshot，使 source、Parse 和引用结果保持一致。
4. 增加 `notist preview` 的单次 HTTP 渲染。
5. 加入 watcher、debounce、SSE 和浏览器自动打开。
6. 加入 vault 导航、目录和 diagnostics panel。
7. 最后实现编辑器光标同步和点击预览跳转源码。

#heading(level=2)[资料]

- Tinymist repository: https://github.com/Myriad-Dreamin/tinymist
- Tinymist preview CLI: `crates/tinymist-cli/src/cmd/preview.rs`
- Tinymist preview server: `crates/tinymist/src/tool/preview.rs`
- Tinymist preview protocol: `crates/typst-preview/src/protocol.rs`
- tinymist-preview crate: https://crates.io/crates/tinymist-preview
