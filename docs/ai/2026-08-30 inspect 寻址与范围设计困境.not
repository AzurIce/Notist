= inspect 寻址与范围：设计困境记录

统合裁决（#<vault::ai::2026-08-30 inspect 命令面统合设计>）确立 R1–R10 之后，寻址问题在同日更晚的讨论中继续演化：R1（输入只认 ModulePath）与 R3（locate 桥）被推翻，输入面转向 path 原生；随后 stem 布局的反例又暴露出纯路径范围的缺口。本文如实记录这场**尚未收口**的困境——张力是什么、候选方案各自输在哪赢在哪、目前浮现的终态草案与未决清单。它是裁决前的工作存档：R11 定稿落地后，应在本文标注收口位置；与统合裁决冲突之处，以更新后的裁决为准。

== 困境一：输入地址到底是谁——ModulePath 还是路径？

- R1 的动机：内容引用语法用 ModulePath，当时认为它是稳定身份、路径是易变坐标。
- 推翻的理由：D0004 的 ModulePath 从文件路径**派生**（stem/README 规则），移动文件模块即变——它不是独立于路径的稳定 ID，两者 1:1 机械对应；而 read/ls/grep 无从获得 ModulePath，输入面强求它等于强迫调用者做机械换算。对照一个精确的类比：字节偏移必须由 notist 发放，因为它需要*计数*（多字节/编码陷阱）；路径与名字的*拼接*零计数零歧义，模型与人都不会错。
- R1 的药也下错了：病是三种 selector 形态并存（ModulePath、path、`path#name`），药应该是"只留一种"，而不是"只留 ModulePath 那一种"。
- 结论方向：path 原生；ModulePath 只在内容引用语法 `#<...>` 中出现（写链接时按 README 一句话规则推导，`check` 兜底）。

== 困境二：范围——stem 布局反例

初版方案"DIR = 递归子树即模块完整足迹"只对 README 布局成立。反例：`module.not` 与 `module/sub.not` 并存（D0004 允许）时，文档与命名空间各是足迹的一半——文件漏子模块，目录漏文档。

候选方案：

- **A. 范围改用 ModulePath 前缀**（`vault::module` = 文档 + 命名空间，天然完整）。代价：输入面重新出现两种地址，与推翻 R1 的初衷矛盾。
- **B. 兄弟提升规则**：DIR 范围 = 目录内全部文件 + 同名兄弟 `DIR.not`（若存在）。依据：D0004 的单 source 保证使该集合恰好等于一个模块的足迹——README 布局下兄弟文件不可能存在（`cli/README.not` 与 `cli.not` 冲突），stem 布局下恰好补全。代价：范围语义多一条特殊规则，必须写进文档与 `--help`。
- **C. 双形式 scope**（ModulePath 与路径并存，按需选用）。代价：最大，回到双文法。

当前倾向 B；若未来出现 B 表达不了的范围需求，再评估 A/C。FILE 范围 = 单个文档（"只看这篇"）。

== 困境三：Item 的名字怎么写——`#` 还是 `--item`

ItemName 是任意字面字符串，任何内联分隔符（`#`、`:`）都有碰撞面；坐标已定"不做内联语法、一律 flag"的纪律。倾向 `--item <NAME>`（显式、可发现、零冲突），`path#name` 内联形式移除。

== 困境四：ancestors 与 attrs 是否两个命令

ancestors 已经输出"重叠子树 + 每层属性"，计划中 attrs 的增量只剩模块继承链（R6 实施后体现为根行的模块链）。裁决倾向：**合并为一个命令**（保留已实现的 ancestors），attrs 从实施清单划掉，避免同一份数据的两个投影变成两条命令。

== 终态草案（R11，未裁决）

- 地址：`DIR | FILE [--item NAME]`（可选叠加 `--from-line/--lines/--byte-range`）。
- DIR 范围 = 目录内全部 + 同名兄弟 `DIR.not`（困境二方案 B）；FILE 范围 = 单文档；缺省 = 全库。
- 身份命令（items / ancestors / 合并后的 attrs 职责）行内零文件路径；行区间直接喂宿主 Read。
- ancestors 承担原 attrs 职责；locate 移出实施清单（R3 同步作废）。
- `--attr <SPEC>` 筛选 Item，SPEC 用注解面拼写（`#tag` / `.class` / `key = value` / 裸 `id`——与源文件书写及 ancestors 输出显示同一套拼写，所见即所查）；只匹配 Item 自身挂载的注解，不含继承（继承环境筛选留作 v2）。
- `--attr` 的硬前置 = 注解覆盖缺口修复（见下）。
- search 保留：存在理由（排序 / 容错 / 求值后字段 heading+tag）显式成文，附复核条件。

== 未决清单

- R11 草案整体未裁决——尤其是兄弟提升规则（方案 B）与 `--item` 命名。
- R6 `\@!(class: ..)` 沿 Module 树继承未实施；实施后 ancestors 根行体现模块链。
- 注解覆盖缺口未修复：`parse.annotations()` 只收集后缀注解——块级 `\@(id: ...)` 不可寻址、块级 `\@(tag: true)` 不进检索 tag 字段、`--attr` 依赖同一修复。
- search 复核条件：使用数据表明 Agent 均以 grep 自足则退场。
- items/ancestors 行内零路径已执行；若将来需要坐标回声再议。
