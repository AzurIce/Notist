@!(implementation: "partial")
@!(agents: "所有 CLI 命令的输出示例必须来自实测，并完整体现字段说明声称的每一种形态——虚拟模块、@id 标记、ambiguous、definition 标记、省略等边界；能共现的形态在同一样例中出现，互斥形态以括注说明")

= Notist CLI: Command Design and Reference

@(type: "user")
#[

]

本文是 `notist` CLI 命令面的单一真相，由两篇组成：本文固定全局治理与跨命令契约（三层分离、命令分组、全局参数、输出投影、错误与退出状态，以及 Validate/Maintenance/Runtime/Publishing 各组命令）；`inspect` 调查组（status/ls/search/locate/items/read/ancestors/refs/definition）的逐命令规范与身份/坐标寻址契约在 #<vault::05-cli::inspect>。高层分发边界（Skill、官方 docs Vault、发布边界）在本文「高层分发边界」一节；检索语义与派生索引见 #<vault::05-cli::search>；daemon 运行时见 #<vault::05-cli::daemon>，core request 与本地协议见 #<vault::05-cli::protocol>。

当前实现状态：`partial`。本文描述*目标命令面*：完整结果契约（无预算、无翻页）、顶层命令收敛为 inspect/check/index/daemon/lsp/skill/build/preview，以及 inspect 统合（身份/坐标分离、locate 桥、items/attrs 四视角、`@!(class: ..)` 继承）。实现补齐之前，已安装二进制的 `--help` 是实际可执行面的最终依据。notist 从未发布过版本，命令面不保留兼容窗口。

== 设计总览

=== 三层分离

CLI 只保留三层，且每层只有一个入口：

```text
语义命令    我想做什么            inspect status/ls/search/locate/items/read/ancestors/refs/definition、check
运行时策略  服务如何执行          daemon 连接、index、语义窗口（snippet bytes）
输出投影    结果如何显示          完整 text；颜色与 TTY 只影响渲染
```

命令参数只表达语义意图与语义窗口。截断策略（limit/max_bytes/cursor 型 flag）不进入命令语法；`--from-line/--lines/--byte-range/--depth/--snippet-bytes` 是调用者选择读取哪段的语义窗口，不是服务端截断。

CLI 是命令面与 daemon 之间的最薄适配层：参数解析 → core request（#<vault::05-cli::protocol>）→ daemon 或 embedded service → text 投影。CLI 不做任何语义猜测：selector 解析、fingerprint 检查都在 core 侧完成，CLI 只投影与展示。

=== 命令分组

```text
Inspect
  status ls search locate items read ancestors refs definition
Validate
  check
Maintenance
  index
Runtime & tooling
  daemon lsp skill
Publishing
  build preview
Experimental
  vsearch
```

`inspect --help` 是调查命令的唯一发现入口；普通知识工作只使用 inspect 与 check。`vsearch` 是实验性语义向量检索命令（配置见 #<vault::05-cli::search> 的向量 lane 节），不进入稳定分组，过 benchmark 门槛裁决前不构成契约。内容访问与目录列举由宿主 Read/ls 承担——模块映射是机械规则：Vault 根下的路径逐段对应 `::`，`X/Y.not` 即模块 `vault::X::Y`；`README.not` 即其所在目录的模块（`X/README.not` 即 `vault::X`，`X/Y/README.not` 即 `vault::X::Y`）；inspect 只收录语义查询。命令面没有写命令：磁盘写入来自宿主编辑工具或其他外部进程，daemon 通过 watcher 观察并发布新 snapshot；修改 Vault 后运行 `notist check` 验证。

=== Vault 发现与 --vault

需要 Vault 的命令通过全局 `--vault DIR` 选择 Vault，默认 `.`（当前目录）。DIR 可以是 Vault 内文件或目录；Notist 按 #<vault::04-world::boundary> 的发现规则向上查找最近的 `Notist.toml`，canonicalize 后连接该 root 对应的 daemon。Nested Vault 不进入外层扫描。命令的其他 positional 全部是语义参数（query、selector、path、offset），不再包含 root。

```shell
notist inspect status
notist inspect search "type system" --vault docs
notist check --vault docs
```

=== Selector 与 Scope

Selector 是精确身份，Scope 是集合过滤，两者不互相替代；完整寻址契约（身份/坐标两个世界、ModulePath 文法与 --item 区域、citation 统一位置）在 #<vault::05-cli::inspect/CLI Inspect: Agent Query and Navigation/身份与坐标>。

=== 全局参数

```text
notist [--color auto|always|never] [--no-daemon] [--vault DIR] <COMMAND>
```

- `--color`：只影响 human diagnostic 与 source frame。
- `--no-daemon`：在当前进程创建临时 VaultEngine 与 disk View，执行同一 core operation；不关闭 analysis、snapshot 与校验。
- `--vault DIR`：选择目标 Vault，默认 `.`；规则见「Vault 发现与 --vault」。
- `--help`、`--version` 各自打印当前安装版本信息。

全局参数可放 subcommand 前后，但脚本建议放在命令前。子命令 help 不重复展示这些全局参数。

`daemon` 与 `lsp` 只允许自己的 framing：传 `--color` 是 usage error。`preview` 的 stdout 是启动状态与 SSE revision stream，不是有限 query text。

=== 输出投影

有限 CLI 命令只有一种公开投影：完整 text。没有 `--format text|json`，也没有 schema-versioned JSON envelope；LSP/daemon 使用各自协议 framing。

Text 投影契约：

- stdout 包含当前查询的完整结果，一行一记录；输出正比于被查询的数据集，没有 item/byte 截断。
- collection 命令以计数头开头（如 `4 modules`、`1 reference (incoming)`）；计数头是元信息行，不是记录。
- 错误写 stderr：`error[code]: message`，hint 与 candidates 逐行跟随。
- 颜色、TTY 只能改变渲染，不能改变 logical result、排序或 exit status。
- excerpt 只用于挑选候选；形成事实结论前用宿主 Read 读取 authored source。

=== 语义窗口与截断的边界

`--snippet-bytes` 是语义窗口：调用者选择摘录大小，服务端不额外截断。截断策略（limit/max_bytes/cursor）不属于命令语法。

=== Error 与退出状态

```text
exit 0   成功；零 search result 也属于成功
exit 1   命令有效完成但业务结论失败：check 有 error
exit 2   usage/schema error
exit 3   可纠正 query error：not_found、ambiguous_selector、snapshot_changed、index_not_ready...
exit 4   service/protocol unavailable
exit 70  internal failure
```

稳定 error code 至少包括 `not_found`、`ambiguous_selector`、`invalid_selector`、`snapshot_changed`、`index_not_ready`、`query_timeout`、`invalid_regex`、`invalid_argument`。调用者按 exit status 粗分、按 error code 恢复，不解析 message 子串。

== Inspect

Inspect 组是 Agent 的查询与导航原语：status、ls、search、locate、items、read、ancestors、refs、definition；`inspect --help` 是唯一发现入口。逐命令规范、身份/坐标寻址契约与 Agent 工作流见 #<vault::05-cli::inspect>。

== Validate

=== check

用途：对完整 Vault 下健康结论，不提供 module scope。跨模块 import/reference 的语义世界不能靠局部 scope 变成"检查通过"。

```shell
notist check [--vault DIR]
  [--summary]
  [--severity error|warning|info]
```

- 始终完整分析整个 Vault；`--severity` 之外的诊断全部可见，无分页截断。
- `--summary`：只打印 `N sources, N diagnostics, N errors` 与按 code 计数。
- `--severity`：只过滤 detail 展示；summary 与 exit status 永远反映全部 diagnostics。
- 只要存在 error，exit 1；只有 warning/info 时 exit 0 但显示 summary。
- 单模块诊断通过 LSP 获得；CLI 不提供 check scope。

text source frame：

```text
error[unresolved-module]: target module was not found
  --> designs/host/search-retrieval-index.not:12
   |
12 | #<vault::does::not::exist>
   |    ^^^^^^^^^^^^^^^^^^^^^^
hint: create the module or correct the reference

2 diagnostics in 1 source
```

== Publishing

=== build

用途：从一个 captured snapshot 生成多页静态 HTML artifact。

```shell
notist build [--vault DIR] --output DIR [--clean]
```

- `--output` 默认 `dist`。
- `--clean` 只清理已验证位于目标内的 build artifact；不得借此删除任意目录。
- 构建诊断与页面来自同一 snapshot；存在 error 时 exit 1。
- stdout 只回 page count、输出目录与 error count。

=== preview

用途：本地预览与 live reload；是长时间 runtime，不是有限 query。

```shell
notist preview [--host IP] [--port N] [--open] [--vault DIR]
```

- 默认 host `127.0.0.1`，port `3250`；端口被占用时自动回退到系统分配端口。
- `--open` 显式打开浏览器；默认不打开，适合脚本与远端端口转发场景。
- 非 loopback host 需要显式选择并显示安全 warning。
- stdout 是启动状态 + SSE revision stream；不要混入普通查询语义。
- Enhanced 模式：页面侧边栏显示 Symbols（锚点、根 `let` 绑定、标注区域），标注区域高亮并可跳转；状态在同一标签页 live reload 间保持。

== Runtime & tooling

=== index

用途：检查或重建派生检索索引；不修改 authored Vault。

```shell
notist index status [--vault DIR]
notist index rebuild [--wait] [--vault DIR]
```

- `status`：health `ready|building|stale|error|not_built`、schema/tokenizer/ranking version、indexed source fingerprint、unit count、最近错误。
- `rebuild`：只替换当前 snapshot 的可重建 generation；默认返回后台 task handle，`--wait` 等待完成。
- `--no-daemon` 下 rebuild 隐式等待，避免后台任务随进程退出丢失。
- search 不依赖调用者手动 rebuild；首次 search 会按 snapshot 惰性提交 build task。

=== daemon

```shell
notist daemon [--vault DIR]
notist daemon stop [--vault DIR]
```

- `daemon`：为一个 canonical Vault root 持有长期 Engine、watcher、cache 与 disk View；前台运行，stdout 只允许 daemon framing。
- `daemon stop`：显式停止 root 对应 daemon；没有 daemon 时成功退出，脚本可幂等调用。
- CLI 默认惰性连接或启动 daemon；`--no-daemon` 是隔离部署形态，不是第二套语义。
- stale daemon 的 binary stamp 回收规则见 #<vault::05-cli::protocol>。

=== lsp

```shell
notist lsp
```

- 通过 stdin/stdout 提供 LSP JSON-RPC；root 由 editor workspace 动态发现。
- 只允许 LSP framing；不接受 `--color`。
- LSP 维护 editor overlay View；CLI 普通命令查询 disk View，二者不共享未保存文本。

=== skill

```shell
notist skill init <SKILLS_ROOT> [--force]
```

- 在 SKILLS_ROOT 下物化名字固定的 `notist/SKILL.md`；SKILLS_ROOT 不存在时创建。
- 默认要求 `<SKILLS_ROOT>/notist` 尚不存在，只生成确定性的 `SKILL.md`。
- `--force`：`<SKILLS_ROOT>/notist` 必须是已有目录，原子替换其中的 `SKILL.md`，保留其他文件。
- CLI 共享启动路径对 `~/.agents/skills` 与 `./.agents/skills` 做无状态安装探测：存在 `notist/SKILL.md` 则与内嵌 fingerprint 比较，缺失或失配只在 stderr 提示对应 init 命令，不改变退出码；安装在自定义位置的 Skill 不在探测范围；`skill init` 自身不探测。
- 不修改 Host registry，不接受把当前 Vault 导出为 Skill。
- 分发与 docs Vault 边界见「高层分发边界」。

== 高层分发边界

一个刚接触 Notist 的 Agent 不能只靠模型先验猜测 `.not` 语法，也不应该为了理解 Vault、Module、Function、CLI 与安全编辑规则而依赖网络或源码仓库。两个职责分开：

```text
Agent host 中安装的 Skill
└── SKILL.md

Notist 管理的用户数据
└── docs/
    ├── Notist.toml
    ├── README.not
    ├── language/
    ├── cli/
    └── ...
```

`SKILL.md` 是很小、可复制、可由 Agent host 管理的只读说明入口；docs Vault 是当前 `notist` 可执行文件携带并在运行时同步的完整官方文档 Vault。同步完成后，它与其他 Vault 没有另一套访问语义：CLI inspect 组、check 与 root-bound daemon 都使用既有接口。

```shell
notist inspect search "WorkspaceSnapshot" --vault ~/.notist/docs
notist inspect refs vault::05-cli::daemon --vault ~/.notist/docs
notist check --vault ~/.notist/docs
```

`~/.notist/docs` 表示各平台约定的 user-data 位置。Skill 说明如何找到该 Vault，但不拥有、复制或修改它。

=== 一个 Skill 文件，而不是一个运行时目录

官方 Skill 由 `notist skill init`（见「Runtime & tooling」）物化为名字固定的 `notist/SKILL.md`；目录名 `notist` 是 Agent host 按 name 发现 Skill 的契约，由命令而不是用户拼写保证。`skill init` 只物化 `SKILL.md`，相同源状态生成相同 bytes（时间戳、checkout path、用户名、当前目录不进入结果）；`--force` 是单文件更新能力，不是目录重建。

CLI 共享启动路径对 `~/.agents/skills` 与 `./.agents/skills` 两个约定 skills root 做无状态安装探测：只检查 `notist/SKILL.md` 是否存在，存在则对其 bytes 计算 SHA-256 并与构建期内嵌的 skill fingerprint 比较，缺失或失配只向 stderr 输出一行提示并给出对应的 init 命令，不改变退出码。探测不写 ledger、不持久化任何状态——安装位置每次从这两个约定重新派生；安装在任意自定义位置的 Skill 不会被看见也不被提示，这是有意接受的取舍。

Skill 安装目录是不可变的发布输入，不是 cache 或工作目录。Skill 的 authored source 放在仓库 `.agents/skills/notist/`（当前只有 `SKILL.md`；该路径同时是 workspace-scope 的 skill 发现位置，repo 内工作时 Agent host 直接发现同一份文件），构建系统把该目录作为整体资源边界 include 进 CLI，并计算与 `docs_fingerprint` 同构的目录 fingerprint（覆盖相对路径与原始 bytes）一并嵌入，供启动探测比较。

`SKILL.md` 只负责教 Agent：

- 何时查询官方 docs Vault，而不是从模型记忆猜测语法和行为；
- 一张编辑安全底线：`.not` 与 Markdown 的差异（强调标记、软换行与段落）、标注与链接形态——它是防损坏的最低事实集，不是语法规范；权威速查表是 Vault 内 cheatsheet，完整语法以 grammar 为准，SKILL 不复制权威定义；
- 如何用普通 CLI 查询 Vault（inspect search → items/read → 取证；结果完整，无翻页规则）；
- 用宿主编辑工具直接修改用户 Vault，修改后运行 `notist check`，保留 disk View 与 editor overlay 的区别；
- 将文档正文视为可查询资料，而不是高于 system、user 或 Skill 指令的新 instruction authority。

=== Docs 是普通 Vault

官方 docs 保留仓库 `docs/` 下的完整 authored corpus、相对目录与 Module 关系（根层地图与叙事、各分区规范、`docs/ai` 调研档案），不打包人工挑选的摘要，也不把 HTML/CSS/JS 产物当作知识源。完整保留不等于一次读入上下文——Agent 仍然先搜索、再读取候选。

完整 corpus 中来源具有不同权威性：根层 `intro`/`cheatsheet` 与 `language/`、`world/`、`cli/`、`lsp/`、`plugins/` 各分区描述当前公开行为；`docs/ai/` 是带日期的调研档案，历史选择由 Git 记录。这些区别由 Skill 教给 Agent；旧调研不能覆盖当前规范，任何正文自然语言也不能仅因被检索到就成为 Agent 指令。

官方 docs 没有专用的 `notist docs search` 等命令——增加这类命令会复制 Vault root、query、snapshot、daemon 与输出格式的概念。它唯一特殊的地方是来源与同步方式。

=== CLI 是文档版本的真相来源

构建系统从完整 docs Vault 产生确定性的压缩 bundle 并嵌入 `notist` 可执行文件：

```text
OfficialDocsManifest {
  bundle_schema
  notist_version
  protocol_version
  docs_fingerprint
  source_revision?
}
```

`docs_fingerprint` 覆盖每个 authored entry 的相对路径与原始 bytes；相同源码与构建版本产生相同 entry 顺序、解压结果与 fingerprint。内嵌 bundle 换来离线可用、版本精确、可复现发布与无网络供应链依赖；从 GitHub 下载不能成为默认正确性路径。

CLI 的共享启动路径在进入正常工作流前执行一次廉价的 manifest 比较（读取已发布 Vault 的 manifest 与内嵌 `docs_fingerprint` 比较），相同则不动；缺失、损坏或不同才触发同步：获取 per-user 同步锁 → 重检 manifest → 在 staging 树中验证并物化（路径安全、数量与 fingerprint 受 manifest 限制）→ 原子发布为稳定 docs Vault → 继续正常命令路径。失败只清理本次 staging target，保留上一个完整 Vault。

多个短命 CLI 同时启动由跨进程锁串行同步；已经运行的 daemon 必须把 `docs_fingerprint` 纳入 root handshake 或 snapshot identity——client 不能把升级后 CLI 的查询静默发送给仍声明旧 bundle 的 daemon。Notist 假设一个用户环境同时只有一个 active CLI 版本；升级原子替换 docs Vault，不保留历史 release 并行。

=== 向量化属于 Vault 运行时状态

完整 docs source 可以嵌入 CLI，向量模型和向量索引不能据此一起嵌入：它们有不同生命周期（模型可能本地或远程；embedding 随 model identity、chunking、normalization 与 schema 变化；索引随 snapshot 增量更新）。官方 docs 同步成普通 Vault 后，向量检索自然沿用普通 Vault 边界——daemon 为这个 root 维护派生索引，索引记录 canonical vault identity、docs fingerprint、provider/model identity、chunking schema 与 indexed revision。CLI 二进制不包含大模型权重与预计算向量；删除 cache 后可从 `.not` source 重建。

=== 发布边界

仓库中的 Skill 模板与 `docs/**` 都是 authored source。构建产生嵌入 bundle；运行产生用户数据目录中的官方 docs Vault 与可再生索引；`skill init` 只产生一个可安装的 `SKILL.md`。三类内容不能反向成为彼此的编辑入口：

```text
.agents/skills/notist/SKILL.md ------> notist skill init <SKILLS_ROOT> [--force]
                                -> <SKILLS_ROOT>/notist/SKILL.md
                                <-- 启动探测（hash 比较，只提示不阻断）-- notist binary

docs/** -> deterministic bundle -> notist binary
                                   -> user data/docs Vault
                                   -> ordinary CLI/daemon access
                                   -> disposable per-Vault indexes
```

`notist skill init` 不接受用户 Vault root，也不把当前目录导出为 Skill——个人 Vault 可能包含 secrets、prompt injection 与不明确的发布权限；未来若支持这种发布，应使用独立命令与设计。

== Conformance

一个命令要进入本文公开 surface，必须同时满足：

- 语义参数完整写入本文与 `--help`；help 展示 default 与 validation。
- 没有 page/budget/rendering 策略 flag 泄漏进语义命令；语义窗口与截断策略按「语义窗口与截断的边界」区分。
- 有限 query 只输出完整 text，一行一记录；相同参数与 snapshot 产生逐字节相同的排序与输出。
- `--color`、TTY 不改变 logical result。
- 零结果成功；错误只按 typed code 与 exit status 恢复。
