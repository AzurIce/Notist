@!(implementation: "partial")
= vsearch: Semantic Vector Query

`notist inspect vsearch` 是 inspect 组的实验性语义向量命令：查询文本经用户提供的 embedding endpoint 嵌入为向量，与按结构切块的全库内容块算余弦相似度，按分数返回前 k 条。它与 #<vault::05-cli::search> 的四模式检索不共享命令、排名契约或索引；进入稳定命令面须先在版本化 benchmark 上证明相对词法基线的明确提升，且不破坏 citation 与 snapshot 一致性。inspect 组的跨命令契约见 #<vault::05-cli::inspect>；命令面治理、全局参数与错误退出状态见 #<vault::05-cli>。

```shell
notist inspect vsearch "属性的继承语义是怎么定义的"          # 默认 k=20
notist inspect vsearch "..." -k 5                          # 召回深度
```

== 嵌入由用户提供：endpoint 必需

本命令不内置推理引擎，也不内置模型——嵌入一律来自 `[embedding].endpoint` 指向的 OpenAI 兼容 `/v1/embeddings` 服务（ollama、llama-server、云端 API 均可），server 的部署、模型选择与硬件加速完全由用户决定。`[embedding]` 表缺席或 endpoint 缺失时整条命令关闭，报 typed `embedding_not_configured`，hint 给出最小配置。

```toml
[embedding]
model    = "bge-small-zh-v1.5"     # 转发给 endpoint 的模型名
endpoint = "http://127.0.0.1:11434/v1"
# dims        = 512                # 可选；缺省时从首个嵌入响应学习
# api_key_env = "OPENAI_API_KEY"
# granularity = "section"          # section | paragraph | scope
# context     = "chain-attrs"      # none | chain | chain-attrs
```

`dims` 是校验不是义务：不填时从首个嵌入响应学习并记入索引 manifest，此后每次查询的返回宽度都与之核对。`api_key_env` 命名的环境变量持有 endpoint 的 bearer token。

== 检索单元与上下文

检索单元从求值后 Item 树切出，边界是作者声明的结构边界而非固定窗口：

- `granularity = section`：一块一节，默认。
- `granularity = paragraph`：节内按空行细分，块携带标题链；碎片分数虚高，实测劣于 section。
- `granularity = scope`：节按其直接 `#[...]` scope 子块分区，无 scope 时退化为 section。

`context` 决定嵌入文本的确定性前缀：`none` 为块正文；`chain` 加标题链行；`chain-attrs` 再加生效属性 token（模块 `\@!` 条目与管辖节注解，经求值后属性表取得）。实测 `chain-attrs` 使概念查询 R\@5 翻倍，是推荐默认。

== 存储与增量

向量索引存用户缓存、按 Vault 身份分区；manifest 记录 model、chunker 版本、上下文、source fingerprint，任一不匹配即弃置重建。块内容 hash 未变者复用已缓存向量，仅变更块重嵌；索引滞后或缺失时查询内联重建（阻塞），endpoint 不可用时报 typed error，绝不返回降质的静默结果。`index status` 附带 dense 通道行（状态/模型/维度/块数）。

== 输出契约

一行一 hit：名次、余弦分、相对路径、1-based 行区间、一行摘录。摘录与 `-k` 的语义同 #<vault::05-cli::search> 的纪律：摘录是候选信息不是证据，形成结论前用 read 取证；`-k` 是排名语义的召回深度，不是输出截断。零命中输出 `No matches.`。

== 实测基线

docs vault（1228 块、46 条标注查询、2026-09-08）：section 优于 paragraph（strict R\@10 0.39 对 0.26）；`chain-attrs` 使概念查询 R\@5 由 0.19 升至 0.42；标识符类查询最好 0.83 仍不达标，归 exact/grep。实验当日在内置 ONNX 与 llama.cpp（CPU/GPU）三类运行时上测得，结构因子跨运行时成立；数据与 bench 工具见实验记录，不随契约发布。
