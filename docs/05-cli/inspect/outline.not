= Inspect Outline: Item 树索引

`inspect outline` 交付一个模块的 *Item 树*：每个可寻址 Item 的 ItemId、它的子树行区间、以及它首个字节处的*求值后属性环境*。它是索引，不是内容——不内嵌任何源文行。

它存在的理由是两条路之间的空档：想摸清一个模块，`read` 要给整篇源文，grep 只给行号与文本而没有身份。outline 补上"有哪些 Item、在哪几行、处在什么环境里"，让调用者先挑再读：

```text
outline  → 我该读哪个 Item     （身份 + 坐标 + 环境，无内容）
read --item NAME → 那个 Item 是什么   （内容）
```

范围纪律：outline 只列*可寻址* Item（有 ItemId 的 Item 与 Module）。匿名 Item 不可寻址，列出来调用者也无法动作；它们的属性由 `read` 的分段投影呈现。outline 也不建语义图——引用边是 #<vault::05-cli::inspect::refs> 的事。


== 命令面

```shell
notist inspect outline <MODULE> [--depth N] [--vault DIR]
```

- 位置参数为绝对 ModulePath，与 `read` 同一选择器文法；OS path 报 `invalid_selector`。
- `--depth N` 只列到第 N 层（模块顶层的 Item 为第 1 层）；缺省列全树。
- 不接任何坐标 flag（`--line`/`--offset`/`--byte-range`）：outline 的回答单位是 Item，不是区域。要看某个区域是什么，用 `read --line`。


== 输出逐行契约

header 一行（module、相对路径、行区间、fingerprint）；`items N` 一行；随后每个 Item 一行，形如：

```text
<缩进><ItemId>[ @]  <行区间>  <环境 Dict>
```

- *缩进*为两空格 × (层级 − 1)，层级由树结构决定（不是字符串里 `/` 的个数）：`@id` 绑定的行内 span 处在包裹它的 Item 之内，因此缩进到那一层。
- *ItemId* 是完整标题链（根到自身），即 `read --item` 接受的那种拼写。链上的标题是*纯文本*：行内代码标记被剥掉，所以标题 `Heading with code inside` 即使源文里给 code 加了标记，它的 ItemId 也不含标记。因此 outline 里复制出来的任何一行都能直接喂回 `read --item`（见下方常驻测试）。
- 行尾的 `@` 标记该 Item 的身份来自显式 `@(id: "…")` 标注而非标题链。
- *行区间*是该 Item 的整棵子树（与 `read --item` 返回的范围一致），不是标题行。坐标只给行号：行号是 grep/宿主 Read 的原生产出，字节偏移是协议内部坐标，不在 Agent 面上交付。
- *环境 Dict* 是 notist 字面量 `(k1: "v1", k2: "v2")`，key 排序、字符串值带引号、空环境为 `(:)`；内容与该 Item 在 `read` 里的 Dict 逐字节一致——两处求值同源，不一致即为缺陷。
- 行内不出现文件路径（与 items/ancestors 同规）；路径只在 header 交付，作为身份→路径的唯一反向出口。
- 结尾一行 `hint:` 给出下一条命令（`read <MODULE> --item "<ItemId>"`），把"挑完读哪个"这个决策显式交出去，调用者不必自行拼命令。颜色由全局 `--color` 控制且只影响渲染（见 #<vault::05-cli>）。


== 与其它命令的分工

| 命令 | 回答 | 单位 |
| --- | --- | --- |
| `outline MODULE` | 这个模块里有哪些 Item、在哪、什么环境 | Item |
| `read MODULE --item NAME` | 这个 Item 里写了什么 | Item |
| `read MODULE --line A..B` | 这几行是什么（原文 + 环境） | 区域 |
| `refs MODULE [--out]` | 谁引用它 / 它引用谁 | 引用边 |
| `check` | 它对不对 | 诊断 |

outline 与 read 共享身份与环境的求值口径：名字来自标题默认 ItemId 的同一条推导，环境来自 `region_info` 的同一套管辖区间与逐 key 最内层值胜规则。因此"outline 说的环境"与"read 说的环境"是同一个值，不需要调用者交叉核对。


== 实测示例

模块 `vault::example::annotations` 的 Item 树（从仓库根目录运行）：

```shell
notist inspect outline vault::example::annotations --vault docs
```

```text
module <vault::example::annotations> example/annotations.not lines 1..19 fingerprint 870003d1453dbd0b
items 5
Scope 与标注  2..19  (kind: "demo", reference: true)
  Scope 与标注/Scope id 与锚点引用  6..10  (kind: "demo", reference: true)
    Scope 与标注/Scope id 与锚点引用/显式 id 的标题  8..10  (id: "explicit-id", kind: "demo", reference: true)
  Scope 与标注/标注属性  12..19  (class: "wide", done: false, draft: true, kind: "demo", priority: 1, reference: true, reviewed: true, title: "块级标注")
    inline-id @  17..17  (class: "wide", done: false, draft: true, id: "inline-id", inline: true, kind: "demo", level: 2, priority: 1, reference: true, reviewed: true, title: "块级标注")
hint: read one with `read vault::example::annotations --item "<ItemId>"`
```

示例阅读：`显式 id 的标题` 的 ItemId 取自 `@(id: "explicit-id")` 所绑定的标题链，其环境含 `explicit-id`；`inline-id` 是行内 span 上的 `@id`，缩进在包裹它的 `标注属性` 之内，环境比同一行的 `标注属性` 多出 `id`/`inline`/`level`；模块 `@!` 的 `kind`/`reference` 下传到每一个 Item，因为它们在整篇文件范围内管辖。
