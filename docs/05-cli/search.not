@!(implementation: "partial")
= Search Semantics and Retrieval Index

本文定义检索的四种模式、可检索字段、tokenizer、scope 过滤与派生索引的职责边界。CLI 命令参数与 Agent 工作流见 #<vault::05-cli::inspect>；命令面治理与输出契约见 #<vault::05-cli>；snapshot 与 WorkspaceDelta 的来源见 #<vault::05-cli::daemon>。

== Search 不是一种模式

Core query 有四种显式模式：

| Mode | 语义 | 排序 | 典型用途 |
| --- | --- | --- | --- |
| `lexical` | fielded token match + BM25 | relevance | 默认自然语言与主题检索 |
| `exact` | literal UTF-8 substring | source order | Module 名、错误消息、源码片段 |
| `fuzzy` | lexical BM25 + bounded term expansion | relevance | 拼写错误（`langauge`、`WorkspaceSnaphot`） |
| `regex` | Rust-compatible regular expression | source order | CLI 专家级源码调查 |

CLI 把模式投影为两个命令而不暴露 `--mode`：lexical/fuzzy 投影为 `notist inspect search`，exact/regex 投影为 `notist inspect locate`；参数面见 #<vault::05-cli::inspect>。默认 `lexical`。Lexical 不提供隐藏 query mini-language——引号、冒号、减号默认是待分词文本；默认 `operator=all`，CLI 用 `--any` 切换到 any；不做 stop-word 删除与 stemming，否则 `not`、类型名、数字和缩写会在知识库中丢失真实含义。

== 可检索字段

字段权重属于 ranking version：

| Field | 内容 | 默认权重 |
| --- | --- | ---: |
| `title` | Module 的语义标题 | 5.0 |
| `heading` | 当前 Heading 与祖先 Heading | 4.0 |
| `id` | 显式 scope id 与标题默认 id | 6.0 |
| `tag` | 属性 tag | 4.0 |
| `module` | ModulePath segment 与完整路径 | 6.0 |
| `path` | Vault-relative path 与文件 stem | 3.0 |
| `body` | 普通 authored Item | 1.0 |
| `raw` | Raw 内容 | 0.7 |
| `comment` | source comment | 不默认检索 |

`--field` 控制参与匹配的字段，不控制 response projection；未知字段立即报 usage error。属性检索字段（class、key=value）随属性索引扩展。

== Tokenizer

Tokenizer 对 index 与 query 使用同一套 versioned pipeline：Unicode NFKC 与 case folding 生成匹配 term 但保留原 byte offsets；Latin/数字按 punctuation、snake/kebab/camelCase 边界拆分，并保留完整 identifier 与组成部分为同一 term group 的 variant；连续 Han 字符用 overlapping bigram（混合中英文在 script boundary 分开）；路径与 ModulePath 额外保留 path/segment/stem 分段。不做语言猜测、远程分词或词干还原；替换 tokenizer 必须提升 version 并使已持久化索引失效重建。

== exact、fuzzy 与 regex 的边界

Exact（CLI `locate`）默认大小写敏感，`--ignore-case` 显式启用；它不依赖全文索引，index 重建期间仍可用。

Fuzzy 先执行与 lexical 相同的分词和 boolean 语义，再对适合纠错的 term 查询词典：默认 Damerau-Levenshtein distance 1（runtime policy 上限 2）；长度 <4 的 term、纯数字、单个 Han 字符与结构 delimiter 不扩展；每 term 至多 32 候选、每 query 至多 128，达到上限返回 `expansion_limited=true`。

Regex 使用 Rust regex 语义（无 backreference/look-around）；pattern 长度、编译大小与扫描时间都有上限，分别以 typed error（`invalid_regex`、`query_timeout`）拒绝。它是 CLI 专家能力，官方 Skill 引导 Agent 优先使用 exact/lexical/fuzzy。

== Scope、Excerpt 与分组

`--scope` 与 `--exclude-scope` 都接受精确绝对 ModulePath prefix，可重复：include 取并集，exclude 在 include 之后生效。匹配按段边界进行——`vault::foo` 只命中自身与 `vault::foo::*`，不命中 `vault::foobar`；寻址文法见 #<vault::05-cli::inspect>。`--field`、scope 与访问控制在 candidate generation 前生效（Vault 外路径永远不进入 index 或 result），不能先取全 Vault 候选再过滤。

```text
SearchHit {
  location
  matched_field
  match_range?
  unit_range
  excerpt
  excerpt_range
  excerpt_truncated
  score?
}
```

默认 excerpt 上限 256 bytes（runtime policy，不进入 CLI 参数），截取优先以实际 query term 的 source range 为中心；单行超限在 UTF-8 scalar boundary 截断。excerpt 是候选选择信息：形成事实结论前用宿主 Read 读取 authored source。

`group_by` 控制结果多样性：`source` 每 Module 保留最佳 hit（lexical/fuzzy 默认）、`section` 以最近 Heading 为边界、`match` 返回每个独立 region（exact/regex 默认）。scope、exclude-scope、grouping 与 fields 都是查询语义的一部分：相同参数与相同 snapshot 产生逐字节相同的排序与输出。

零结果仍是成功，hint 只建议（如 `--any`、改用 `locate`），不自动重试、不把零结果改成 error。

== 检索单元与索引

Notist 使用独立的 `SearchIndex` 边界，不把 Tantivy type 暴露进 core request。默认实现选择 Tantivy：Rust-native、fielded BM25、term dictionary、fuzzy term query、增量 segment 与 immutable reader，与 VaultEngine 的 publication 模型契合。

```text
WorkspaceSnapshot
  -> SearchUnitBuilder
  -> SearchIndexWriter (Tantivy)
  -> immutable SearchIndexReader + IndexStamp
  -> SearchService
```

`SearchUnitBuilder` 从 snapshot 已捕获的 source、parse、Module/Heading/属性 facts 构造检索单元，不执行有外部副作用的 plugin。普通 paragraph、list item、Heading section 与 Raw block 优先成为独立 unit；超过 4 KiB 的单元按 source-safe boundary 切分（至多 256 bytes overlap）。每个 unit 存储 FileId、ModuleId、ModulePath、relative path、source fingerprint、source range、Heading ancestry 与 typed field text；完整 source 不复制进 index，excerpt 始终从捕获的 snapshot 读取。

Disk View 的 index 用 WorkspaceDelta 增量更新，写入新 generation 后原子发布 reader；删除 source 产生 tombstone。持久化 index 位于 Notist user cache（按 Vault identity 分区），manifest 记录 index schema、tokenizer、ranking version、Vault identity、source-set fingerprint 与每个 source fingerprint——任何一项在加载时校验不匹配都触发丢弃或重建。Session View 在 disk generation 上叠加小型 in-memory overlay segment，用 FileId tombstone 屏蔽被 overlay 替换或删除的 unit；overlay 释放后整个派生 segment 可丢弃。

`notist index status` / `index rebuild`（#<vault::05-cli>）只检查或替换这个可重建 generation，不修改 authored Vault；search 不依赖调用者手动 rebuild，首次 search 会按 snapshot 惰性提交 build task。

== Index 就绪与一致性

Lexical/fuzzy query 只在 `IndexStamp.source_set_fingerprint` 与捕获 snapshot 相同时执行；index lag 时按 runtime policy 在短 deadline（默认 2s、最大 10s）内等待，超时返回 typed `index_not_ready` 与 exact fallback hint，绝不把 stale result 当成当前 snapshot。Exact/regex 直接扫描 snapshot，不受 index health 影响。

== 向量与 semantic mode 的门槛

向量、semantic 与 hybrid mode 不属于默认契约：只有在版本化 benchmark 上相对 lexical 明确提升 Recall、nDCG 与 Agent task success，并保留 citation、ACL 与 snapshot 一致性之后，才能增加显式 mode。
