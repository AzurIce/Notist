@!(ai: true, desc: "纯 Item 内容树、函数与节点、属性及引用模型")

= Content 与 Item

== 统一的内容表示

Content 是内容值的类型，每个 Content 持有一个根 Item。全部内容节点采用 Item 结构：

```text
Item {
    name: ElementName,
    args: Dict<Value>,
    attributes: Dict<Value>,
}

text("hello")                  : Content
seq([text("hello"), link(...)]) : Content
item("paragraph", { body })    : Content
```

以上为模型伪代码。args 保存结构字段，语言可读取 `content.name`、`content.args` 和 `content.attributes`。名称按完整字符串匹配。宿主节点另外保存创建位置、可选源码范围及来源种类；这些诊断元数据不进入插件传输。

text、space、seq、paragraph、strong、math、link、error 和 annotation 都是 Item。语义定义各不相同，树表示保持统一。文本与空格可以由程序构造、观察和组合；自动 paragraph 聚合保留其内容。

字段中的 Content，以及经 List、Dict 嵌套的 Content，组成节点的子树。属性属于独立元数据，不进入结构子树。通用树遍历与特定内容槽的成型具有不同职责：遍历能发现一个 Content 字段，不代表该字段应按普通段落 flow 处理。

== 函数与节点

`item(name, fields)` 保留为显式节点构造入口，返回 Content。普通函数仅执行计算，其调用本身不增加内容层级：

```notc
let paragraph = (body: Content) => item("paragraph", (body: body));
let foo = (body: Content) => paragraph(body);
let badge = (body: Content) => item("badge", (body: body));
```

foo 返回 paragraph；badge 返回 badge。需要对 foo 的结果整体赋予独立身份时，函数可以显式构造节点。为每次调用保留包装会使辅助函数影响结构深度，因此包装由内容构造决定。

函数参数与 Item 字段可以具有相似形状，但两者无需一一对应。函数可以计算、忽略或合并参数，也可返回多个内容的 seq。属性不隐式投影为函数参数；属性继承也不属于函数调用规则。

完整的 call origin 树不属于内容语义的必需数据。源码诊断与调试仍可保留调用事件和位置，避免用额外内容节点承担这些职责。

== seq 与属性

Code `[]`、Markup `#[]` 和尾随内容参数均构造 [[vault::designs::plugins::core::seq]]，空内容和单元素内容也保留该节点。

`@expr` 求值为属性标注 Item，记录 Dict；标准文档成型决定目标并附着属性。`@!expr` 的目标已由语法确定为当前 Module。具体附着规则定义于 [[vault::designs::plugins::core::annotation]]。

属性独立于 args。连续标注按源码顺序合并，同名键后者覆盖，属性不继承。读取一个原始 Item 的已有属性，与查询语义文档中完成附着的属性，属于不同阶段的观察。

== Item 树与 label

模块输出的每个内容节点都进入树，包括 seq、text、link 和 error。seq 的 children 保持顺序。其他字段按模型规定的确定遍历顺序进入子树；Dict 的遍历顺序不应被解释为自然语言内容顺序。

同一 Content 值被嵌入多次时，每个出现位置都是独立节点。创建来源与共享存储地址均不能代替节点身份。新增包装节点会改变父子路径；label 的祖先匹配允许跳过未命名层级。

标准文档的 Item 索引建立在属性归属和语义单元确定之后。原始树也可以提供调试遍历，其节点身份不与语义树混用。

Item 通过 label 提供可引用名称。显式属性 label 必须为非空 String，并覆盖默认 label。section 的默认 label 取 title 的文本；空标题不产生默认 label，其他 Item 无默认 label。同一模块和同一父节点下均允许重复 label，重复本身不产生诊断。

标题文本由 String、Content 和 List 的文本按顺序拼接。text 贡献自身文本，seq 按 children 顺序提取，space 贡献相应空白。其他嵌套 Item 依次取首个存在的 body、content、title 字段继续提取；linebreak 和 smartquote 分别贡献换行与对应引号；目标链接使用目标字符串表示。error 和其余无文本值不贡献文本。拼接保留大小写和空白。

label 是名称，不承诺跨修改、移动或重命名的稳定身份。显式 label 参与所在节点的结构路径。

== 模块与 Item 引用

引用由精确定位模块的 ModulePath 和可选的 LabelPath 组成。LabelPath 使用一个或多个以 `::` 分隔的字符串段：

```not
[[vault::guide]]
[[vault::guide::"例子"]]
[[vault::guide::"第一章"::"例子"]]
```

LabelPath 末段必须等于目标节点自身的 label；之前各段按从外到内的顺序匹配严格祖先的 label，可以跳过中间层级。名称逐字匹配。匹配在指定模块内进行，与引用所在章节无关。

结构为 `第一章 → 安装 → 例子` 时，`"例子"`、`"安装"::"例子"`、`"第一章"::"例子"` 和 `"第一章"::"安装"::"例子"` 都可匹配末端节点；逆序不匹配。单独 `"第一章"` 只匹配该名称的节点自身。

解析按不同目标节点计数，同一节点满足多个祖先匹配组合只计一次。一个候选表示成功，零个候选产生目标缺失诊断，多个候选产生引用歧义诊断并列出候选。完整 label 链相同的节点仍是不同目标；分别引用它们需要调整 label。

构造 Target 时按表达式所在模块和作用域解析 ModulePath，保存确定的模块地址，不立即求值目标模块。Target 被传递后保持含义。查询与引用检查按需取得目标模块的语义文档并匹配 Item 树，解析目标时不递归追踪其链接。

package 检查覆盖求值过程中已遇到的引用，包括仅绑定到变量的 Target；未执行代码中的引用检查覆盖范围由静态分析能力决定。

== 错误与消费者

error 是能够与正常内容共存的 Item，并保留消息、诊断类别和来源。解析恢复、模块加载、类型或参数绑定、插件调用、内容约束都可能产生诊断；并非每条诊断都必须占据一个内容节点。

Item 不要求存在 Web Component，也不包含强制的 HTML 标签映射。消费者可以解释特定节点、展开它或显示不支持内容的诊断。字段能否跨插件或渲染器传输由相应数据协议约束。

通用构造器允许未知名称。未知节点进入标准文档 flow 后的角色与校验规则属于文档模型，不由其名称拼写或组件 CSS 自动决定。
