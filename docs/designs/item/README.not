@!(ai: true, desc: "Content、Item、label 与引用解析")

= Content 与 Item

== Content

Content 是模块求值的输出类型：

```text
Content = Text(String)
        | Sequence(Content*)
        | Item(name, args, attributes, location)
        | Link(target, location)
        | Error(message, location)
```

文本不是 Item。Link 保存模块或 Item 目标以及引用的来源位置；构造引用值不触发目标模块求值。List、Dict、Function 等值只有在函数或表达式需要输出 Content 时才被转换；不可转换值产生 Error Content。

== Item

`item(name, args)` 返回结构化 Item。`name` 是字符串，`args` 是字段到 Value 的字典，Item 同时保留创建它的源文件和字节偏移。Item 参数可以嵌套 List、Dict、Item 和 Content。

`attributes` 独立于参数。Markup 的 `@expr` 将 Dict 附到后续 Item，`@!expr` 将 Dict 附到所在 Module；连续标注按源码顺序合并，同名键后者覆盖，属性不继承。Code 通过 `item.attributes` 读取 Item 属性，Module 属性随求值结果和 snapshot 输出。

```notc
item("paragraph", (body: [#text("hello")]))
```

Item 不要求存在 Web Component，也不包含 HTML 标签信息。它是语言层的中立数据；不同消费者可以采用不同投影。

== Item 树与 label

模块输出中的 Item 按结构组成树。Content Sequence 保持兄弟顺序；Item 的 args 中直接包含的 Item、Content，以及经 List、Dict 嵌套的 Item、Content，均进入子树。Text、Link 和 Error 不引入 Item 层级；attributes 是元数据，不进入子树。相同 Item 值在输出中出现多次时，每个出现位置都是独立节点；创建来源不能代替节点身份。

Item 通过 `label` 提供可引用名称。显式属性 label 的值必须是非空 String，`@(label: "名称")` 覆盖默认 label。section 的默认 label 取求值后 title 的文本；空标题不产生默认 label，其他 Item 没有默认 label。同一模块、同一父节点下都允许 label 重复；重复本身不产生诊断。

标题文本由 String、Content 和 List 的文本按顺序拼接。嵌套 Item 依次取首个存在的 body、content、title 字段继续提取；linebreak 和 smartquote 分别贡献换行和对应引号，Link 使用其目标的字符串表示，其他值和 Error 不贡献文本。拼接保留大小写和空白。

label 是名称，不承诺跨修改、移动或重命名的稳定身份。显式 label 也参与所在 Item 的结构路径。

== 模块与 Item 引用

引用由精确定位模块的 ModulePath 和可选的 LabelPath 组成。LabelPath 使用一个或多个以 `::` 分隔的字符串段：

```not
[[vault::guide]]
[[vault::guide::"例子"]]
[[vault::guide::"第一章"::"例子"]]
```

LabelPath 的末段必须等于目标节点自身的 label；之前的段按从外到内的顺序匹配严格祖先的 label，可以跳过中间层级。名称逐字匹配。匹配在指定模块内进行，与引用所在章节无关。

例如结构为 `第一章 → 安装 → 例子` 时，`"例子"`、`"安装"::"例子"`、`"第一章"::"例子"` 和 `"第一章"::"安装"::"例子"` 都能匹配末端节点；`"例子"::"第一章"` 不匹配它。单独 `"第一章"` 只匹配名为第一章的节点自身。

解析时按不同目标节点计数；同一节点满足多种祖先匹配组合只计一次。一个候选表示成功，零个候选产生目标缺失错误，多个候选产生引用歧义错误并列出候选。即使完整 label 链相同，节点也保持独立；要分别引用这些节点，需要调整其中的 label。

构造 Target 时按表达式所在模块和作用域解析 ModulePath，保存确定的模块地址，不求值目标模块；引用值被传递到其他位置后保持原有含义。查询或检查引用时按需取得目标模块输出并匹配 Item 树。`Snapshot::check` 检查求值过程中已遇到的引用，包括仅绑定到变量的 Target；解析目标时不递归追踪目标模块的链接。package `check` 检查所有模块并报告引用缺失与歧义，不将重复 label 本身判为错误。

== Error

Error 是 Content 的一等分支，可以出现在 Sequence 中并与正常 Item 共存。错误消息和来源位置随节点保留。`error`、解析恢复、模块加载、绑定、WASM 和函数调用都可以产生 Error Content。

== HTML 投影

HTML consumer 将 Item 名称映射到内置元素或自定义元素。自定义 Item `chart` 变为 `<notist-chart>`；每个可序列化字段变为 JSON 编码的 `data-<field>`。组件负责解释字段和渲染嵌套 Content。

native 与浏览器 renderer 将有效 label 输出到 `data-notist-label`，该属性可以重复。DOM 锚点和页面地址由宿主导航负责映射。

该映射是消费者约定，不是 Item 类型系统的一部分。LSP 和语言运行时不检查组件是否存在或字段是否符合组件预期。
