@!(ai: true, desc: "当前原型的运行时值域和参数类型")

= 值域与类型

== 运行时值

```text
Value = String | Int | Bool | None
      | List(Value*) | Dict(String -> Value)
      | Content | Item | Module | Target
      | Function
```

`Content` 是 `Text | Sequence(Content*) | Item | Link | Error`。`Item` 包含名称、字段字典和来源位置。`Module` 是可被 `use` 继续解析的模块引用。`Target` 保存模块路径与可选 Item ID，输出时成为 Link；当前不验证目标存在性。闭包和 WASM 导出都属于 Function。Module、Target 和函数值不能通过 WASM 或 Item 参数序列化。

`None` 表示缺省值或无值；它与空 Content 不同。将 `None`、字符串、整数或列表作为模块表达式输出时，运行时会尝试转换为 Content；函数、字典等不可转换值会产生 Error Content。

== 类型标注

参数可以标注：`String`、`Int`、`Bool`、`Content`、`Item`、`Module`、`Target`、`List`、`Dict`、`Function`、`None`、`Any`，以及在这些类型后加 `?` 的可选类型。`Content` 接受 Content 和 Item。

标注主要用于函数参数绑定和 WASM 注册结果校验。当前没有完整静态推导、用户自定义类型、Float、Array 或隐式 Int 到 Float 转换。

== 集合字面量

- `()` 是空 List。
- `(value,)` 是单元素 List。
- `(a, b)` 是 List。
- `(:)` 是空 Dict。
- `(key: value)` 是 Dict；多个具名字段组成同一个 Dict。
- `[]` 是 Content 序列。

Dict 键是字符串。字段和列表保持动态值，不进行元素类型追踪。
