@!(ai: true, desc: "统一 Content 值域、类型检查与内容约束的边界")

= 值域与类型

== 值域

```text
Value = String | Int | Bool | Unit
      | List(Value*) | Dict(String -> Value)
      | Content | Module | Target | Function

Content = 持有一个根 Item 的不可变内容值
Item    = 节点名称 + 字段 + 属性
```

Content 是语言中的内容类型，Item 是其节点结构。text、space、seq、paragraph、自定义元素、link 和 error 的构造结果统一属于 Content。节点与索引的定义见 [[vault::designs::item]]。

List 和 Dict 可以包含 Content。字段使用普通 Value；其中的 Content 构成子树。函数参数写作 `body: Content`，能够接收 seq、paragraph 或单个 text 等任意内容节点；具体内容约束由使用位置解释。

Module 是可继续解析的模块引用。Target 保存模块路径和 LabelPath；作为内容输出时构造 link Item。闭包和 WASM 导出都属于 Function。Target 和 Content 可以跨插件传输；Function 与 Module 依赖宿主状态，不能跨 WASM 边界。

Unit 只有 `()` 一个值，与包含空 children 的 seq 有区别。输出表达式将字符串和整数转换为 text，Target 转换为 link，List 逐项转换并组合为 seq；不可转换值产生 error Item。Unit 表达式不输出节点；显式空 seq 保留。

== 类型检查与构建时验证

当前求值器在实际执行时检查参数类型、返回值标注、操作数与值转换；编辑器另有有限的类型推断用于提示。未调用函数体不保证经过完整类型检查，高阶 Function 未携带完整的可静态检查签名。因此当前实现不承诺全面的执行前类型安全。

静态类型检查在执行被检查的代码之前，根据程序和声明判断名称、操作数、参数与返回值是否匹配。类型检查是否静态，取决于它能否在执行前给出保证；与最终输出是否为静态文档无关。

一个完整的提前检查方案需要检查函数体，包括尚未调用的函数；函数作为值传递时保留参数和返回类型信息。调用的实际函数值可以在求值时确定，已知签名仍能支持提前检查。仅有统一的 `Function` 标签不足以完整检查高阶调用。

Content 的普通类型信息不必包含整棵内容树的形态。一个函数可根据值产生不同结构，而保持 `Content` 返回类型。实际结果能否放入某个 paragraph，属于内容模型验证，可以在构建时成型阶段完成。

因此，普通值和函数的提前类型检查可以与求值后的内容验证同时存在。后者不自动否定前者；也不能据此宣称所有内容约束都已静态证明。语言对“静态类型”的正式承诺须以明确的检查覆盖范围为准。

| 方案 | 执行前的保证 | 执行时的检查 |
| - | - | - |
| 主要依赖动态检查 | 可先检查语法与部分名称 | 实际调用时检查参数、操作数和内容 |
| 普通值静态检查，Content 不透明 | 检查函数签名和普通值运算 | 验证实际生成内容的使用约束 |
| 连内容形态也静态表达 | 进一步证明行内性及内容槽匹配 | 仍可能保留插件边界和其他值相关检查 |

最后一种方案需要在函数签名、分支与组合中传播内容形态信息，或要求额外声明；遇到任意生成逻辑时还需采取保守限制。普通值类型与内容形态保持独立，完整的提前检查器属于独立实现工作。

== 参数与缺省值

基础标注使用 String、Int、Bool、Content、Module、Target、List、Dict、Function、Unit、Any，以及 `T?` 可选类型。Item 节点通过 Content 类型传递。

`T?` 接受 T 或 Unit。省略参数时，先使用声明的默认值；没有默认值的 `T?` 参数补入 `()`，其他参数报缺参错误。显式传入的值优先，传 `()` 不触发默认值；Any 和 Unit 参数也需要显式默认值才能省略。源码函数和 WASM 函数使用一致的绑定规则。

例如，`x: Int?` 省略时为 `()`；`x: Int? = 2` 省略时为 2，显式传 `()` 时仍为 `()`。局部绑定和返回类型的 `?` 只表示可空，不产生初始化值。

== 集合与内容字面量

- `()` 是 Unit。
- `(,)` 是空 List。
- `(value)` 是分组表达式。
- `(value,)` 是单元素 List。
- `(a, b)` 是 List。
- `(:)` 是空 Dict。
- `(key: value)` 是 Dict；多个具名字段组成同一个 Dict。
- `[]` 构造新的 seq Item，值类型为 Content。

Dict 键为字符串。集合元素类型的追踪深度与泛型能力尚待确定；这不影响 List、Dict 和 Content 在值域中的区分。

== 待定的类型能力

- Any 是受限动态出口还是允许普遍绕过检查；未知类型如何产生诊断。
- 高阶函数签名、返回类型标注与检查结果的持久表示。
- List、Dict 的元素类型追踪和用户定义类型能力。
- 是否为部分内容提供可选的行内性分析；是否需要 `Content<Inline>` 一类类型。基础模型不依赖这项能力。

实现上宜让名称解析、参数绑定规则和诊断共享定义，避免分析器与求值器独立维护两套规则。提前检查可以产出调用计划等信息，但执行 IR 的具体结构仍待设计。
