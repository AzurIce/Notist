@!(implementation: "partial")
= Trust Model

本文记录插件信任模型（原 Capability Model 的裁决更新，2026-08-23）。内容语义对所有 backend 统一；安全边界则随 backend 而不同：Wasm 通过无 IO import、fuel 与内存预算、响应大小/节点上限获得沙箱隔离，native plugin 属于宿主进程级信任（#<vault::designs::plugin-system::safety>）。

== 裁决：内容组合不做 per-call 授权

内容组合不需要 Principal/grant 模型：

1. *没有安全增量*——插件输出森林本就完全由插件控制；它可以直接写 `Call(core::x,…)`，最终语义由宿主 registry 与统一 fixpoint 决定。内容组合不因 call 的来源而获得不同权限；需要隔离时依赖 Wasm backend 的沙箱，native backend 则依赖宿主对该进程的信任。
2. *归因不可行*——透传类插件把文档正文原样带回时，朴素 fixpoint 若按来源切换 principal 会误伤透传；放宽归因又会让来源判定失去意义。

因此：引擎不做 per-call 能力检查；`Notist.toml` 不再有 capabilities 键；清单不再声明 `capabilities.request`。插件通过返回 call 森林表达跨包组合，宿主统一继续规约。若未来出现 IO 级能力需求（文件、网络），作为独立 capability 面另行设计，与内容组合正交。

== 当前实现映射

- `CapabilityPolicy` / `Principal` / caller 切换：已从 notist-eval 移除；
- 清单与 `Notist.toml` 的 capabilities 字段：已删除（未知键按预留策略忽略）；
- component ABI 不提供语义宿主回调；`evaluate` 返回的 call 森林由宿主统一 fixpoint，可自然组合 core 与其它 package；
- 当前 Wasm loader 提供沙箱预算；native contribution 的进程级信任边界尚未由统一 App API 表达。

== 已裁定

- 已裁定：内容语义和 per-call 组合规则不按 backend 区分；Wasm 沙箱与 native 进程信任只是不同的执行边界。
- 已裁定：内容组合不授权；未来文件/网络等 IO 能力另行设计，不伪装成内容函数权限。
- 已裁定：`FunctionOwner` 保留为函数来源的描述性元数据，不参与运行时判定。
- 已裁定：跨包组合通过返回森林中的 call 表达，由宿主 registry 在组件返回后统一规约。
