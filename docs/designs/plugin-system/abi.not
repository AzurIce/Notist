@!(implementation: "aligned")
= Plugin ABI: Shared Types and WIT Boundary

本文定义宿主与 Wasm guest 之间的共享类型与模块 ABI 的载荷层；调用约定（裸内存 ABI、句柄与所有权）见 #<vault::designs::plugin-system::core-wasm>。call 森林在 reduce 阶段的求值语义见 #<vault::designs::pipeline::plugin-call-reduction>；package 如何声明 Wasm module 见 #<vault::designs::plugin-system::package>。

== 共享类型

`Node` 是唯一的语义表示（#<vault::ai::2026-08-22 plugin form and unified ir roadmap> 的 IR 统一裁决）；`PluginElementDecl` / `PluginParamDecl` / `DefaultValue` 是唯一的插件声明表示。它们都定义在 `notist-model` 并由宿主与 guest 直接共享，WIT 不引入声明或节点镜像。

- WIT 的 `init` 与 `evaluate` 都只暴露 opaque bytes `result`；
- 每个 payload 的第一个字节是 wire version byte，其后是 postcard：`init` 承载 `Vec<PluginElementDecl>`，`evaluate` 的请求与响应承载 `Vec<Node>`；
- 宿主与 guest 共享 `notist_model::wire` 的 declarations / forest encode/decode API；解码严格检查 wire version byte，不提供 JSON 或旧编码 fallback；
- 参数默认值直接使用共享 `DefaultValue`，不经过 JSON 字符串；
- `range` 随共享 `Node` 传输，使插件透传的子树保留诊断位置；它只是可被 guest 伪造的定位元数据，不参与 call identity、规约或权限判定；
- *响应森林继续参与 fixpoint*：handler 返回 call 森林后，宿主统一把它重新送入不动点；其中指向已注册名字的 call 会再次派发。handler 返回与输入完全相同的 call 表示 identity；持续产生新的自返 call 才是作者错误，由 depth/fuel 兜底并给出诊断；纯数据元素不注册运行时 handler。

== init 注册语义

`init` 在实例化后、任何 `evaluate` 之前被调用恰好一次，消耗与 `evaluate` 相同的 fuel 预算：

1. 宿主按共享 wire codec 解码 `Vec<PluginElementDecl>`；
2. 整体校验：每个元素名必须落在 package 自己的 namespace 内，参数类型必须是可解析的 Notist 类型，签名合法；
3. 校验通过后按声明生成 `FunctionSignature` 与 `ElementSchema` 并注册；任一失败则整个 package 装载失败，产生 host boundary diagnostic。

注册即自描述：schema 的唯一真相来源是插件代码里的 `init` 实现，manifest 不再携带 semantic 接口。静态分析、completion 与 runtime 读到的是同一份注册结果，不存在漂移面。

== 语义 ABI

`evaluate` 是单个 dispatcher；`init` 声明的每个元素都对应它的一个可调用名字。

插件只有一种语义实现模式：`evaluate` 返回 call 森林，宿主把整个返回森林统一送回规约不动点。插件通过在森林中写 `core::details` 或其它 package 的名字表达组合，不在模块调用期间嵌套执行宿主规约。无限递归由统一 depth/call budget 与模块 fuel 预算兜底（持续自返属于作者 bug）。

=== 插件不能做什么

- 不能 import 文件系统、网络、随机数或进程；Rust std 所需的 WASI 0.2 imports 由宿主以空 `WasiCtx` 满足；
- 不能修改 parser、不能重新解释已有 token；
- 不能覆盖其他 package 的 namespace。

== 当前实现

- WIT 已由 wasmtime bindgen 编译校验；单一 `plugin` world 的 bytes `export init` 与 `export evaluate` 均已接入 loader。
- `model::wire::{encode_declarations,decode_declarations}` 与 `{encode_forest,decode_forest}` 复用同一严格 version byte + postcard frame；模块返回的 call 森林由 node engine 统一继续规约。
- 插件以 `notist-plugin-sdk` 编写：`Plugin::init(reg)` 注册元素，`ElementFn::reduce` 实现规约，`export_plugin!` 生成模块导出。
- 所有可执行 Wasm package 都实例化为上述 module；宿主不保留 core Wasm dispatcher 或旧载荷转换分支。
- JSON 只用于读取人工编辑的 `plugin.json` 信封；semantic declarations、defaults、`Node` / `Value`、request / response 全部使用共享类型的 version byte + postcard frame。
- Component response 的大小与节点上限见 #<vault::designs::pipeline::plugin-call-reduction>。

== 已裁定 / 待定

- 已裁定：模块 ABI 的载荷层是共享 `notist-model` 类型的 wire version byte + postcard frame；调用约定见 #<vault::designs::plugin-system::core-wasm>。
- 已裁定：插件形态是 Rust crate → 零导入 core wasm module；作者通过 SDK trait 与 `init` 注册贡献。
- 已裁定：schema 真相来源是 guest 的 `init` 注册；manifest 只保留信封字段，不再携带 semantic 接口。
- 已裁定：module ABI 只有 `init` 与 `evaluate` 两个入口；插件返回 call 森林后由宿主统一执行 fixpoint。
