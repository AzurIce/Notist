@![implementation = "partial"]
= Termination, Resource Limits, and Determinism

本文定义插件执行的安全边界。信任模型的裁决见 [[vault::designs::plugin-system::capability]]；reduce 阶段如何消耗预算见 [[vault::designs::pipeline::plugin-call-reduction]]。

== 终止性

- `ReduceLimits`：最大调用深度 64，单次 reduction 最多 dispatch 10 000 次。
- 每次 dispatch 消耗 fuel 并增加 depth；未知函数、类型错误与不接受 body 都产生诊断，不通过 accessor panic 假装成功。
- 不按同名函数直接判定循环：合法递归可以带不同参数收敛；可选对完全相同 `(function, 规范化参数)` 的重入做精确循环检测。
- Wasm 组件每次调用 reset fuel（50 000 000），即使 guest 不配合也能终止，不需要 timer 线程；native plugin 不获得 Wasm fuel 语义，而由宿主进程的执行治理与统一 reduction budget 约束。预算量级以真实插件冷启动成本为锚：guest 惰性初始化（如 mermaid 插件首次解析 flowchart 触发的 regex 表构建）实测约 21M fuel，预热后的常规 dispatch 在个位数 M 内；2026-08-26 由 10M 上调至 50M。

== 资源预算

- Wasm 线性内存上限 16 MiB；table 元素上限 64 Ki。
- Component response 上限 1 MiB 编码字节 / 10 000 个节点；插件请求写入内存前检查 guest memory 是否足够。
- Wasm 超出 backend 预算统一产生 host boundary diagnostic，不返回部分成功；native plugin 必须遵守同一 contribution 与 reduction 结果上限，但不假装拥有 Wasm 沙箱。


== 确定性

- backend 不改变语义确定性要求：相同输入、同一环境 identity 与同一 contribution 必须产生相同输出。Wasm 由 import 白名单与空 `WasiCtx` 约束；native plugin 的确定性由宿主信任、测试与注册契约保证，而不是由 Wasm 沙箱伪造。
- Wasm 不提供文件系统、网络、随机数、进程或 cache 等非确定性 import；WASI 0.2 imports 由宿主以空 `WasiCtx` 满足。
- component 调用期间不嵌套执行宿主规约：`evaluate` 返回 call 森林并释放 Wasm store 锁后，宿主才把结果送回统一 fixpoint，因此不存在组件回调重入 runtime 锁的路径。
- 满足以上条件后，语义函数可安全 memoize；memoize 的 key 必须包含 package/contribution 与 environment identity。

== 已裁定 / 待定

- 已裁定：所有 plugin contribution 都必须可终止、有资源上限、有确定性重放语义；Wasm 另有 backend 级沙箱上限，native 不获得同等沙箱保证。
- 待定：精确循环检测是否进入默认 budget 路径，还是作为 debug / 诊断选项。
