@![implementation = "aligned"]
= Plugin Package and Lifecycle

本文定义插件 package 的形态、manifest、namespace、core package 以及装载生命周期。插件求值语义见 [[vault::designs::pipeline::plugin-call-reduction]]；共享类型与 WIT 边界见 [[vault::designs::plugin-system::abi]]。

== Package 模型

```text
PluginPackage {
  id: PackageId              // shader
  namespace: String          // shader::
  api-version: String
  trust: Core | Wasm
  functions: Vec<FunctionDecl>
  elements: Vec<ElementDecl>
  render: Vec<RenderContribution>
  capabilities: CapabilitySet
  wasm: Option<WasmModule>
}
```

- `Core` package 是语言版本的一部分，由宿主 native 实现；它使用同一 schema，但不受 capability 限制。
- `Wasm` package 是第三方组件，只通过受限 host import 与宿主交互。
- `PackageId` 成为 `ElementName` 的 namespace：插件 Leaf 写作 `shader::canvas`。

== Manifest

插件包是目录或 zip，根下是 `plugin.json`。manifest 只是信封：身份、Wasm 装载参数、投影资产与权限请求；semantic 接口由组件的 `init` 注册提供（[[vault::designs::plugin-system::abi]]），不写入 manifest：

```json
{
  "package": "shader",
  "version": "0.1.0",
  "api-version": "0.1",
  "render": {
    "html": {
      "contributions": [
        {
          "element": "shader",
          "trusted": true,
          "web-component": {
            "tag": "notist-shader",
            "module": "assets/shader.js",
            "style": "assets/shader.css"
          }
        }
      ]
    }
  },
  "capabilities": {
    "request": ["html:render", "html:asset"]
  },
  "wasm": {
    "module": "semantic.wasm",
    "component": true,
    "host-call": false
  }
}
```

- `wasm.component` 区分 WIT component 与 legacy v0 core Wasm；`wasm.host-call` 表示组件 import `host.call`。
- `interfaces.render` 描述投影面；投影贡献是静态文件引用，保留在信封里，细节见 [[vault::designs::plugin-system::projection]]。
- manifest 的 `capabilities.request` 只声明请求；实际生效权限见 [[vault::designs::plugin-system::capability]]。

== core package

core 不写成一个 Wasm 组件，而是宿主内的特权 native package：

```text
CorePackage {
  functions: 所有 core::* 构造器实现
  elements:  core 成型 schema
  render:    core 投影（native HTML / future native render node）
  authority: host
}
```

它和插件 package 共享：

- `FunctionRegistry`；
- `ElementSchema`；
- `RendererRegistry`。

但不共享信任等级：core 的实现与 schema 随语言版本发布，宿主静态链接；`core::*` 不可被插件覆盖，边界见 [[vault::designs::world::core-namespace-plugin-boundary]]。

== Notist.toml 配置

Vault 在 `Notist.toml` 中声明要加载的 package 与站点授予：

```toml
[plugins.shader]
path = "../plugins/shader"

[plugins.component-host-call]
path = "../plugins/component-host-call"
capabilities = ["core::text"]
```

- `path` 指向相对 Vault root 的 package 目录或 zip；registry 形式的 `package` 字段为未来入口保留。
- `capabilities` 是站点 grants，与 manifest request 取交集后才是有效权限。

== 装载与生命周期

装载只有一条路径，静态分析与 runtime 不再分离：

```text
Notist.toml
  → package manifest（信封）
  → compile + instantiate Wasm component
  → init()：guest 注册元素声明
  → validate schema / capability / namespace（原子，任一失败即整包拒绝）
  → register functions + elements + renderers
  → WorkspaceSnapshot identity
```

- `init` 消耗与 `evaluate` 相同的 fuel 预算，且要求确定性；装载成本是毫秒级的一次性开销。
- 插件失败是 host boundary diagnostic，不伪装成 `None` 成功。
- 插件 namespace 冲突在加载时拒绝。
- package、manifest、assets 或权限变化进入 source-set / plugin fingerprint，分配新的 `FunctionEnvironmentId` 并发布新 snapshot revision。
- daemon 递归 watch 外部 package 目录；`plugin.json` / wasm / assets 变化触发 disk view reload。

== 已裁定 / 待定

- 已裁定：插件最终形态是 Rust crate 编译出的 wasip2 组件 package = 信封 manifest + Wasm 组件 + 可选 assets。
- 已裁定：core 是宿主静态链接的特权 native package，但共享 registry 与 schema 模型。
- 已裁定：插件加载失败是 host boundary diagnostic。
- 已裁定：semantic 接口由组件 `init` 注册提供，manifest 只保留信封字段；schema 真相来源唯一，不存在声明与实现漂移面。
- 已裁定：装载统一为实例化 + init 一条路径，不再维护不执行 Wasm 的 schema-only 快照路径。
- 待定：registry package 的解析、校验与缓存协议。
