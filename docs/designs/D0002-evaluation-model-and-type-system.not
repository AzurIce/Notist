= D0002: Evaluation Model and Type System

本文承接 [[self::D0001-language-overview]] 的最小示例：在那里，一段代码求值后得到 `bindings`、`content` 与 `annotations` 三个产物。本文定义这个结果如何产生。

一段源代码经过三个阶段：解析、静态检查、求值。解析把源码变成语法结构；静态检查解析名称、推断并检查类型，并把每个调用点解析为确定的已解析调用（resolved call）；求值器只消费静态检查的产物，在求值收尾时把内容成型为文档树，输出求值结果。传统的标记语言直接通过解析就可以得到表示文档的元素树；Notist 在解析之后多一步求值——计算是语言的固有部分，不是事后插入的占位符。

本文先从求值之前的静态检查讲起——类型系统：类型是值的静态投影，静态检查产出求值器唯一的语义输入；再讲求值阶段——求值模型：值、scope、组合、规则、结果与成型如何把代码变成 Content 树和环境。

== 类型系统

静态检查在求值之前运行：它以类型判断（type judgment）Γ ⊢ e : T 描述"表达式 e 产出什么值"，并产出求值器唯一消费的语义输入。类型是值的静态投影；名称解析沿 scope 链进行——值与 scope 的定义见求值模型一节。

=== Type 与 Value 对应

类型是值的静态投影：每个类型描述一类值；求值阶段的值宇宙与类型系统共享同一套概念，类型系统是它的静态面。

```text
Type
├─ None / Bool / Int / Float / String / Content
├─ T?        # 可空类型
└─ fn(signature) -> R
```

错误恢复使用独立的 Error marker——Error 不是 `None` 或空 Content，后两者都有各自的合法语义。（发散类型（类似 Rust 的 `!`）暂不进入语言，待出现不会正常返回的表达式时再引入。）

赋值兼容性（assignability）与 coercion 是分离的概念：相同类型直接赋值；`Int` 传给 `Float` 形参时，静态检查在解析结果中插入显式数值 coercion（coercion insertion）。不做隐式函数 subtyping，也不做全局 subtyping。（Union 暂不进入语言，待后续设计引入；可空类型 `T?` 见"可空与可省略"一节。）

=== 可空与可省略

`T?` 是可空类型（nullable type）：值空间等于 T 的值空间并上 `None`。它是独立的内置类型构造，不依赖一般 Union——语义上 `T?` 与 `T | None` 等价，但实现与规则独立。规则：

- `None` 可提升到任意 `T?`（`None` 字面量的类型是 `None`）；
- `T` → `T?` 是隐式提升（widening）；
- `T?` → `T` 没有隐式降级——使用前需要显式检查；
- `(T?)?` 折叠为 `T?`。

"可省略"（omittable）与可空正交：可省略由形参是否带默认值（default）决定。函数类型签名中，`?` 只用于可空类型；有 default 的形参用 `=`（不带值）标记：

```text
定义签名:  fn(kind: String = "note", title: Content? = none, trailing body: Content) -> Content
函数类型:  fn(kind: String =, title: Content? =, trailing body: Content) -> Content
```

四种形参形态：

- `kind: String`：必填，位置绑定（positional）；
- `kind: String =`：可省略（有 default；default 表达式不参与类型身份，见函数类型一节），具名绑定（named）；
- `kind: String?`：必填但值可为 None，位置绑定；
- `kind: String? =`：可省略且值可为 None（default 通常为 `none`），具名绑定。

可省略与位置绑定互斥：位置实参按声明顺序依次填充，无法跳过——任何可以省略的形参都必须具名绑定（可选即 named）。因此 positional 的 `T?` 是必填但可空的形参：调用必须显式传 `none` 或值；"省略即 none"的唯一写法是 `T? = none`（具名绑定）。default 表达式不限于 `none`（`lang: String? = "auto"` 合法）：可空（`?`）管值域、可省略（`=`）管能否不给，两轴正交，而"可省略"这一端与 named 绑定。

函数类型就是定义签名去掉 default 表达式。

类型描述得最精细的地方是函数——函数应用是求值中最复杂的动作，静态检查必须精确知道它的形状。

=== 函数类型

函数的可调用签名（callable signature）包含：形参（parameter）的名称与类型；形参是否带默认值（default）而可省略（omittable，记号见"可空与可省略"一节）；显式声明的末尾内容形参（trailing content parameter）；结果类型（result type）。

形参的绑定方式由是否带默认值决定，不需要额外的修饰符——分界线就是 default：

- 带默认值（`=`）的形参只能以具名实参（named argument）绑定（named-only）；
- 不带默认值的形参只能以位置实参（positional argument）绑定（positional-only）。

必填即 positional、可选即 named，两种表面不互通（Typst 采用同一条分界线）。调用点按位或按名绑定：positional 实参按声明顺序依次填充 positional 形参，named 实参按名字匹配 named 形参；两类实参可以任意交错。定义签名：

```text
fn(kind: String = "warning", trailing body: Content) -> Content
```

相应函数类型写作：

```text
fn(kind: String =, trailing body: Content) -> Content
```

默认值表达式在定义时求值——它是捕获定义处环境的闭包，同一函数的缺省形参值不随调用环境变化；默认值表达式不能引用形参（形参名字在定义处不可见）。

调用点检查（call-site checking）拒绝四类错误：

- 重复绑定（duplicate binding）：同一形参被绑定两次；
- 未知实参（unknown argument）：签名中不存在的名字——绝不静默忽略，防止拼写错误；
- 缺失实参（missing argument）：无默认值的必填形参未被提供；
- 绑定方式不匹配（binding mismatch）：positional 实参没有剩余可填充的形参，或 named 实参指向 positional 形参——绝不静默改绑。

Trailing content 是最后一个 positional 形参：定义中用显式 `trailing` 修饰符声明，至多一个，必须接受 Content；`#f(1)[内容]` 与 `#f(1, [内容])` 等价——trailing block 只是追加一个 positional 实参的语法糖，没有独立的绑定通道。其余形参没有这种紧凑写法。

函数值（function value）是闭包（closure）：携带签名、实现、捕获环境（captured environment）与所需 capability。capability 与函数类型身份正交，宿主在实际调用时检查。

函数与元素的边界在 Content 类型上汇合：builtin 元素就是 first-class 构造器函数。

=== Content 类型

`Content` 是不透明类型：一个类型涵盖所有元素组合，元素结构（种类、嵌套、参数）是运行时数据，静态检查不追踪它。树中的每个节点携带自己的构造器身份；builtin 元素以 first-class 构造器函数的形式存在——`callout(kind: ..., body: ...)` 产生一个 callout 节点，其类型就是 `Content`。（内置构造器的命名空间尚未决定，本文暂以裸名书写。）用户函数与 builtin 构造器因此同构：`warning` 与 `callout` 遵守同一套签名、调用与求值规则，渲染与查询按节点身份消费结果，不区分来源。

插入转换只在 Markup insertion 边界成立：

```text
Γ ⊢ e : String
────────────────────
Γ ⊢ #e : Content(Text(e))
```

普通函数要求 Content 参数时仍然拒绝 String——"可展示"不会渗透成所有 API 的隐式转换规则。

这些规则汇总为静态检查的三个职责。

=== 静态检查

求值之前先执行静态检查，三个职责：

1. 名称解析：名字绑定到 scope 链上的哪一个 binding——解析结果使用稳定的 Symbol identity，不反复按字符串查表；
2. 类型推断与检查：为每个表达式推断类型，检查 `let` 绑定、实参与签名是否一致；形参必须声明类型；嵌套非递归函数的结果可由 body 推断，根级函数必须声明结果类型（递归绑定暂缓，待 `let rec` 引入）；
3. 调用解析（call resolution）：把每个调用点解析为已解析调用（resolved call）——callee 身份、实参到形参的绑定（按位或按名）、默认值、coercion、trailing 目标与结果类型，并一次性完成重复绑定、未知实参、缺失实参与绑定方式不匹配的检查。

错误恢复：语法、未解析名称与类型错误允许产生 partial HIR，使编辑器与预览能继续处理其他源码；恢复节点保留自己的 Error 状态——错误实参不使用默认值，错误 callee 不伪装成返回 `None` 的成功函数。

静态检查的产物——名称解析结果、类型与已解析调用——构成求值器唯一的语义输入：求值器不重新猜测名字指向哪个函数、实参如何绑定，只执行这份输入。求值模型一节展示它如何消费这份输入。

== 求值模型

求值阶段回答一个问题：程序在环境中产生什么？答案是一棵 Content 树和一个环境。类型系统已经为它完成了检查——求值器只消费静态检查的产物。下面从求值的原料开始，依次展开值、scope、组合、规则、结果与成型。

=== 值的宇宙

求值处理的值分为几类：

```text
Value
├─ None
├─ Bool / Int / Float / String
├─ Content          # 文档内容，元素的有序组合
└─ Function         # 形参可以是普通值，也可以是 trailing Content
```

（集合类型 Array/Dict 暂不进入语言，待后续设计引入。）

每个公开类型都必须具有相应的值构造、观察与操作——进入语言的类型就是普通值，可以绑定、传递与运算（语义诚实）。类型与值一一对应，见类型系统一节。

值中与文档直接相关的是 `Content`——文档的每一个元素都是一个 Content 值。下面单独展开它。

=== Content 值

`Content` 是不透明的、已求值完成的文档子树：它是一个值的类型，内部是元素节点的递归组合（元素可以再携带 Content），元素结构是运行时数据，类型系统不追踪它。Content 值里没有占位符——求值完成时一切内容已经确定；需要延迟计算的地方使用 `fn() -> Content`。

元素按种类区分，每个元素携带自己的构造器身份与参数：

```text
Element
├─ Text          # 文本；String 经插入规则转换产生
├─ Parbreak      # 段间分隔；成型时不进入结构树
├─ Reference     # 引用（ref 构造器）：文档目标或资源文件（外部 URL 暂缓）
├─ Heading       # 标题（heading 构造器）
├─ Raw           # 代码（raw 构造器；行内/块级由 block 参数决定，行内糖暂缓）
├─ Rule          # 分割线（rule 构造器）
├─ ListItem      # 列表条目；相邻同类条目由成型组合为 List/Enum
├─ Quote         # 引用块（quote 构造器暂缓）
├─ Table         # 表格（table 构造器暂缓）
├─ Callout       # 提示框（callout 构造器）
├─ Details       # 折叠内容（details 构造器）
├─ Strong        # 加粗（strong 构造器；*...* 糖）
├─ Emph          # 斜体（emph 构造器；_..._ 糖）
├─ Underline     # 下划线（underline 构造器；__...__ 糖）
├─ Strike        # 删除线（strike 构造器；~~...~~ 糖）
├─ ……            # 其余内置元素随内置函数设计扩展
└─ Custom        # 扩展元素：携带构造器身份与参数，渲染器对未知种类有兜底
```

在任一语言版本中，内置集合是封闭的：内置元素可以穷举匹配；扩展（插件等）走 `Custom`，不改变内置集合，内置集合随语言版本演化。元素参数是普通值——例如 Heading 的 `level` 与 body、Callout 的 `kind` 与 body；容器元素的子元素不变量在成型时检查。各内置元素的构造器签名与 Markup 语法糖见 [[self::D0003-builtin-functions-and-syntax-sugar]]。

Content 值不是散落的数据：它们组合在同一棵树上——scope 树。节点既装内容，也装名字；属性不挂在树上，见"属性表"一节。

=== scope：节点即环境

值层组合树上的每个节点同时就是一个 scope，承担两种角色：

1. 内容的容器：节点包含一段 Content；
2. 名字的容器：binding 挂在节点上，名称解析沿节点链向上——词法环境不是树之外的平行结构，就是树本身。

属性不挂在节点上：标注在求值期绑定到值（语法见 [[self::D0006-scope-and-annotation-syntax]]），求值收尾转写为旁置的属性表（annotation table）——见"属性表"一节。

名称解析沿节点链自内向外进行，最近绑定者胜出（shadowing）；`let` 绑定从声明处开始对后续节点可见。求值结束后，除根 scope 的 bindings 进入求值结果外，节点上的绑定只是求值期的环境事实——成型后的结构树消费者不依赖它们。

scope 按来源分为：根（root）、Content literal、Code block、元素形成的 scope，以及作者显式创建的手动 scope。手动 scope 用 `#[...]` 显式创建：封装一段内容，自带词法边界，可承载属性（其属性以区间条目进入属性表），渲染上不产生可见元素；它与隐式形成的 scope 是同一类东西，区别只在显式与隐式。

scope 节点只存在于值层。成型时，元素形成的 scope 是实元素，保留为结构树节点；透明 scope（Content literal 与手动 scope）溶解进元素序列参与统一分组——scope 的内容可以与外部文字连排（例如一段文字内嵌一个 scope，其内容与前后的文字组成同一个段落），溶解后其内容可能分属多个父节点，树无法把它装成单个节点。溶解时 scope 的属性与身份以区间条目进入属性表，覆盖它内容所在的区域，可以跨越段落边界。结构树里没有透明 scope 节点，只有实元素；属性一律以区间命中（单元素区间是特例），没有第二种形状。

=== Content 组合

求值就在 scope 树上进行：名字沿节点链解析，内容按源码顺序组合成 `Content`。文档正文是隐式的 Content quotation——普通文字、引用、标注与嵌入的计算按源码顺序组合成 `Content`。正文不是求值之外的另一套流程，而是同一管线里的一种写法。

嵌入表达式的结果按插入规则进入外层 Content：

- `Content` 原样插入；
- `String` 转为 `Text` 元素；
- `Int` / `Float` / `Bool` 经 toString 转为 `Text`，与 `String` 同路；
- `None` 产生空内容；
- 其他类型（如 `Function`）不能插入，产生诊断。

组合的规则与顺序，加上函数调用的执行，就是求值规则。

=== 属性表

属性（标注）在求值期绑定到值，求值收尾转写为旁置的属性表（annotation table）：以元素序列区间为键、属性集为值的映射。树（scope 树、结构树）不携带属性——属性是位置注解，与结构解耦。

坐标是元素序列区间 `[start, end)`。元素是语法构造的产物，粒度由语法决定：

- `Text`：文本流中不被其他构造（表达式嵌入、标注、元素、段间分隔）打断的连续片段——`文字@note 继续` 在标注处切开，`@note` 挂在第一个 `Text` 个体上；
- `Parbreak`：段间分隔，也是元素；
- 其他元素：每个构造调用一个个体；容器元素的 body 是子序列。

序列是分层的（容器 body 是子序列），区间在同一层序列上，可以跨越段落边界。成型只重组元素，不合并、不删除个体——区间在求值、成型与投影之间稳定（Text 拼接是渲染层优化，不是成型语义）。

三类条目：

- 元素条目：实元素上的属性（`#callout[...]@danger`）→ 单元素区间；
- 区间条目：透明 scope 溶解后的属性与身份（`#[...]@install`）→ 内容覆盖的区间；
- 节条目：修饰节头部 heading 的属性（`@[#施工]`、`= 标题@安装`）→ 该节覆盖的区间——节边界由 heading 归组规则确定（见成型一节），节属性语义上作用于整个子树。

属性是值的属性：绑定在值上的属性随值复制——`#let alert = [注意]@wip` 后 `#alert #alert` 产生两个元素个体，属性表里有两条 `wip` 条目。

模块属性（`@![...]`）作为模块级条目进入属性表，随求值结果发布为模块元数据。

=== 求值规则

声明与正文按源码顺序求值：`let` 绑定扩展后续节点的环境；可展示的 Markup 与嵌入表达式通过 Content insertion 追加内容。

函数应用遵循统一的求值顺序：求值 callee 表达式 → 按源码顺序求值显式实参与 trailing Content → 按形参声明顺序建立绑定（应用解析结果中的 coercion，或代入缺省形参的默认值）→ 调用函数值 → 校验返回值满足声明的结果类型。每个表达式只求值一次。

求值结束后，程序的一面与文档的一面分离为求值结果。

=== 求值结果

求值结果是三元组：

```text
求值结果 {
  bindings:    Environment,      // 根 scope 自有的 binding
  content:     Content,          // 文档内容（值层真相），成型后供渲染、检索与查询
  annotations: AnnotationTable,  // 属性表：元素区间 → 属性集
}
```

D0001 的最小示例中：`accent` 与 `warning` 进入 `bindings`，标题与 callout 组合成 `content`——此时它还是组合形态，求值收尾把它成型为结构树。（此结构在引入 Vault 与 Module 后成为 ModuleResult，见 [[self::D0004-vault-module-and-scope]]。）

=== 成型：从组合到结构树

求值收尾时，成型规则把元素序列组成结构树：连续的行内元素组成段落，段间分隔（Parbreak）断开段落，相邻同类条目组成列表或枚举；heading 与其后内容（直到下一个同级或更高级 heading 之前）归组为 Section 节点——节归组递归作用于任何内容序列，同一规则确定属性表的节条目（见"属性表"一节）。块级元素（Heading、block: true 的 Raw、Table、Quote 等）独立存在，其到来前先收束当前段落与容器状态；行内元素（含 block: false 的 Raw）参与段落组合；容器子元素的不变量在成型时检查。成型只重组元素，不合并、不删除个体——属性表区间在成型前后稳定。

成型是对任意 `Content` 值可独立运行的规范纯函数：任何 Content 值都是一份"文档"，可以单独成型、渲染与查询。整份文档的成型是默认视野；scope 内部、单个元素等局部区域用同一套规则对相应内容成型——规则唯一，输入随视野选择。

值层（求值产生的 Content 组合）与属性表是唯一真相，成型后的结构树是它们的投影：可丢弃、可重建，任何消费者都不能把投影当作新的真相；消费端联合消费结构树与属性表。成型是确定的纯函数，因此不需要 reconcile：没有内容变换规则、没有迭代收敛、没有增量 diff。每次变更都对整份代码重新求值；增量更新只属于分析层的优化，不进入求值语义。

成型是求值的收尾。至此，代码经过静态检查与求值，变成一棵 Content 树和一个环境；求值器全程只消费静态检查的产物，不重新猜测——编辑器、LSP 与构建因此可以在不执行副作用的情况下理解文档。
