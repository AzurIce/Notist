= D0002: Type System and Property Table

[[vault::designs::D0001-language-overview]] 的最小示例给出求值结果 `bindings`、`content` 与 `annotations` 三个产物。本文定义其中两个规范面：静态类型系统，以及旁置的属性表。求值如何执行并产生 `content` 元素序列见 [[vault::designs::D0013-evaluation-model-and-content-sequence]]；scope 与词法环境见 [[vault::designs::D0012-scope-and-environment]]；成型见 [[vault::designs::D0011-shaping-and-structure-tree]]。

一段源代码经过三个阶段：解析、静态检查、求值。解析把源码变成语法结构；静态检查解析名称、推断并检查类型，并把每个调用点解析为确定的已解析调用（resolved call）；求值器只消费静态检查的产物，产生元素序列后由成型投影为结构树。传统的标记语言直接通过解析就可以得到表示文档的元素树；Notist 在解析之后多一步求值——计算是语言的固有部分，不是事后插入的占位符。

本文先定义类型系统：类型是值的静态投影，静态检查产出求值器唯一的语义输入；再定义属性表：属性如何以区间条目旁置于树外。

== 类型系统

静态检查在求值之前运行：它以类型判断（type judgment）Γ ⊢ e : T 描述"表达式 e 产出什么值"，并产出求值器唯一消费的语义输入。类型是值的静态投影；名称解析沿 scope 链进行——scope 与词法环境见 [[vault::designs::D0012-scope-and-environment]]，值的定义见 [[vault::designs::D0013-evaluation-model-and-content-sequence]]。

=== Type 与 Value 对应

类型是值的静态投影：每个类型描述一类值；求值阶段的值宇宙（[[vault::designs::D0013-evaluation-model-and-content-sequence]]）与类型系统共享同一套概念，类型系统是它的静态面。

```text
Type
├─ None / Bool / Int / Float / String / Content
├─ T?        # 可空类型
└─ fn(signature) -> R
```

错误恢复使用独立的 Error marker——Error 不是 `None` 或空 Content，后两者都有各自的合法语义。（发散类型（类似 Rust 的 `!`）暂不进入语言，待出现不会正常返回的表达式时再引入。）

赋值兼容性（assignability）与 coercion 是分离的概念：相同类型直接赋值；`Int` 传给 `Float` 形参时，静态检查在解析结果中插入显式数值 coercion（coercion insertion）。不做隐式函数 subtyping，也不做全局 subtyping。（Union 暂不进入语言，待后续设计引入；可空类型 `T?` 见"可空与可省略"一节。）

=== 可空与可省略

`T?` 是可空类型（nullable type）：值空间等于 T 的值空间并上 `None`。它是独立的内置类型构造，不依赖一般 Union——语义上 `T?` 与 `T | None` 等价，但实现与规则独立。规则：

- `None` 可提升到任意 `T?`（`None` 字面量的类型是 `None`）；
- `T` → `T?` 是隐式提升（widening）；
- `T?` → `T` 没有隐式降级——使用前需要显式检查；
- `(T?)?` 折叠为 `T?`。

"可省略"（omittable）与可空正交：可省略由形参是否带默认值（default）决定。函数类型签名中，`?` 只用于可空类型；有 default 的形参用 `=`（不带值）标记：

```text
定义签名:  fn(kind: String = "note", title: Content? = none, trailing body: Content) -> Content
函数类型:  fn(kind: String =, title: Content? =, trailing body: Content) -> Content
```

四种形参形态：

- `kind: String`：必填，位置绑定（positional）；
- `kind: String =`：可省略（有 default；default 表达式不参与类型身份，见函数类型一节），具名绑定（named）；
- `kind: String?`：必填但值可为 None，位置绑定；
- `kind: String? =`：可省略且值可为 None（default 通常为 `none`），具名绑定。

可省略与位置绑定互斥：位置实参按声明顺序依次填充，无法跳过——任何可以省略的形参都必须具名绑定（可选即 named）。因此 positional 的 `T?` 是必填但可空的形参：调用必须显式传 `none` 或值；"省略即 none"的唯一写法是 `T? = none`（具名绑定）。default 表达式不限于 `none`（`lang: String? = "auto"` 合法）：可空（`?`）管值域、可省略（`=`）管能否不给，两轴正交，而"可省略"这一端与 named 绑定。

函数类型就是定义签名去掉 default 表达式。

类型描述得最精细的地方是函数——函数应用是求值中最复杂的动作，静态检查必须精确知道它的形状。

=== 函数类型

函数的可调用签名（callable signature）包含：形参（parameter）的名称与类型；形参是否带默认值（default）而可省略（omittable，记号见"可空与可省略"一节）；显式声明的末尾内容形参（trailing content parameter）；结果类型（result type）。

形参的绑定方式由是否带默认值决定，不需要额外的修饰符——分界线就是 default：

- 带默认值（`=`）的形参只能以具名实参（named argument）绑定（named-only）；
- 不带默认值的形参只能以位置实参（positional argument）绑定（positional-only）。

必填即 positional、可选即 named，两种表面不互通（Typst 采用同一条分界线）。调用点按位或按名绑定：具名实参写作 `name: value`（与形参声明同构；`=` 只用于签名默认值与标注列表的 `key = value`）；positional 实参按声明顺序依次填充 positional 形参，named 实参按名字匹配 named 形参。位置实参必须全部出现在具名实参之前，具名实参之间可任意排序；trailing Content block 是唯一例外——它是恒为最后的 positional 实参，允许跟在具名实参之后。定义签名：

```text
fn(kind: String = "warning", trailing body: Content) -> Content
```

相应函数类型写作：

```text
fn(kind: String =, trailing body: Content) -> Content
```

默认值表达式在定义时求值——它是捕获定义处环境的闭包，同一函数的缺省形参值不随调用环境变化；默认值表达式不能引用形参（形参名字在定义处不可见）。

调用点检查（call-site checking）拒绝四类错误：

- 重复绑定（duplicate binding）：同一形参被绑定两次；
- 未知实参（unknown argument）：签名中不存在的名字——绝不静默忽略，防止拼写错误；
- 缺失实参（missing argument）：无默认值的必填形参未被提供；
- 绑定方式不匹配（binding mismatch）：positional 实参没有剩余可填充的形参，或 named 实参指向 positional 形参——绝不静默改绑。

Trailing content 是最后一个 positional 形参：定义中用显式 `trailing` 修饰符声明，至多一个，必须接受 Content；`#f(kind: "warning")[内容]` 与 `#f(kind: "warning", [内容])` 等价——trailing block 只是追加一个 positional 实参的语法糖，没有独立的绑定通道；它是唯一允许跟在具名实参之后的位置实参。其余形参没有这种紧凑写法。

函数值（function value）是闭包（closure）：携带签名、实现、捕获环境（captured environment，见 [[vault::designs::D0012-scope-and-environment]]）与所需 capability。capability 与函数类型身份正交，宿主在实际调用时检查。

函数与元素的边界在 Content 类型上汇合：builtin 元素就是 first-class 构造器函数。

=== Content 类型

`Content` 是不透明类型：一个类型涵盖所有元素组合，元素结构（种类、嵌套、参数）是运行时数据，静态检查不追踪它；元素清单见 [[vault::designs::D0013-evaluation-model-and-content-sequence]]。树中的每个节点携带自己的构造器身份；builtin 元素以 first-class 构造器函数的形式存在——`callout(kind: ..., body: ...)` 产生一个 callout 节点，其类型就是 `Content`。（内置构造器的命名空间尚未决定，本文暂以裸名书写。）用户函数与 builtin 构造器因此同构：`warning` 与 `callout` 遵守同一套签名、调用与求值规则，渲染与查询按节点身份消费结果，不区分来源。

插入转换只在 Markup insertion 边界成立：

```text
Γ ⊢ e : String
────────────────────
Γ ⊢ #e : Content(Text(e))
```

普通函数要求 Content 参数时仍然拒绝 String——"可展示"不会渗透成所有 API 的隐式转换规则；插入转换只存在于 Markup insertion 边界（[[vault::designs::D0013-evaluation-model-and-content-sequence]]）。

这些规则汇总为静态检查的三个职责。

=== 静态检查

求值之前先执行静态检查，三个职责：

1. 名称解析：名字绑定到 scope 链上的哪一个 binding——解析结果使用稳定的 Symbol identity，不反复按字符串查表；
2. 类型推断与检查：为每个表达式推断类型，检查 `let` 绑定、实参与签名是否一致；形参必须声明类型；嵌套非递归函数的结果可由 body 推断，根级函数必须声明结果类型（递归绑定暂缓，待 `let rec` 引入）；
3. 调用解析（call resolution）：把每个调用点解析为已解析调用（resolved call）——callee 身份、实参到形参的绑定（按位或按名）、默认值、coercion、trailing 目标与结果类型，并一次性完成重复绑定、未知实参、缺失实参与绑定方式不匹配的检查。

错误恢复：语法、未解析名称与类型错误允许产生 partial HIR，使编辑器与预览能继续处理其他源码；恢复节点保留自己的 Error 状态——错误实参不使用默认值，错误 callee 不伪装成返回 `None` 的成功函数。

静态检查的产物——名称解析结果、类型与已解析调用——构成求值器唯一的语义输入：求值器不重新猜测名字指向哪个函数、实参如何绑定，只执行这份输入。求值如何消费这份输入见 [[vault::designs::D0013-evaluation-model-and-content-sequence]]。

== 属性表

属性（标注）在求值期绑定到值，求值收尾转写为旁置的属性表（annotation table）：以元素序列区间为键、属性集为值的映射。树（scope 树、结构树）不携带属性——属性是位置注解，与结构解耦。

坐标是元素序列区间 `[start, end)`。元素是语法构造的产物，粒度由语法决定：

- `Text`：文本流中不被其他构造（表达式嵌入、标注、元素、段间分隔）打断的连续片段——`文字@note 继续` 在标注处切开，`@note` 挂在第一个 `Text` 个体上；
- `Parbreak`：段间分隔，也是元素；
- 其他元素：每个构造调用一个个体；容器元素的 body 是子序列。

序列是分层的（容器 body 是子序列），区间在同一层序列上，可以跨越段落边界。成型只重组元素，不合并、不删除个体（[[vault::designs::D0011-shaping-and-structure-tree]]）——区间在求值、成型与投影之间稳定（Text 拼接是渲染层优化，不是成型语义）。

三类条目：

- 元素条目：实元素上的属性（`#callout[...]@danger`）→ 单元素区间；
- 区间条目：透明 scope 溶解后的属性与身份（`#[...]@install`）→ 内容覆盖的区间；
- 节条目：修饰节头部 heading 的属性（`@[#施工]`、`= 标题@安装`）→ 该节覆盖的区间——节边界由 heading 归组规则确定（[[vault::designs::D0011-shaping-and-structure-tree]]），节属性语义上作用于整个子树。

属性是值的属性：绑定在值上的属性随值复制——`#let alert = [注意]@wip` 后 `#alert #alert` 产生两个元素个体，属性表里有两条 `wip` 条目。

模块属性（`@![...]`）作为模块级条目进入属性表，随求值结果发布为模块元数据。

== 与求值、成型的关系

求值结果三元组与 `content` 元素序列的产生见 [[vault::designs::D0013-evaluation-model-and-content-sequence]]；scope 溶解见 [[vault::designs::D0012-scope-and-environment]]；结构树投影见 [[vault::designs::D0011-shaping-and-structure-tree]]。值层与属性表是唯一真相，成型后的结构树是它们的投影：可丢弃、可重建，任何消费者都不能把投影当作新的真相；消费端联合消费结构树与属性表。
