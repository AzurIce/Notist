@![implementation = "partial"]
= Function Parameters and Call Model

本文是 L4 调用模型的独立成篇。承接 [[vault::designs::language::type-system]] 的类型系统与 [[vault::designs::language::code-grammar#声明]] 的函数声明语法。本文定义：函数签名如何记录可调用形状；positional/named/default/trailing 如何绑定；rest parameter 与 Typst 式实参交错（随 [[vault::designs::language::collection-types]] 实现进入现行）。

== 函数类型与调用模型

函数的可调用签名（callable signature）包含：形参（parameter）的名称与类型；形参是否带默认值（default）而可省略（omittable，记号见 [[vault::designs::language::type-system#可空与可省略]]）；显式声明的末尾内容形参（trailing content parameter）；结果类型（result type）。集合类型进入语言后，签名还包含可选 rest parameter（见下节）。

形参的绑定方式由是否带默认值决定，不需要额外的修饰符——分界线就是 default：

- 带默认值（`=`）的形参只能以具名实参（named argument）绑定（named-only）；
- 不带默认值的形参只能以位置实参（positional argument）绑定（positional-only）。

必填即 positional、可选即 named，两种表面不互通。调用点按位或按名绑定：具名实参写作 `name: value`（与形参声明同构；`=` 只用于签名默认值与标注列表的 `key = value`）；positional 实参按声明顺序依次填充 positional 形参，named 实参按名字匹配 named 形参。当前 surface（R05）规定：位置实参必须全部出现在具名实参之前，具名实参之间可任意排序；trailing Content block 是唯一例外——它是恒为最后的 positional 实参，允许跟在具名实参之后。该顺序在 rest 实现切片中由下节规则取代。定义签名：

```text
fn(kind: String = "warning", trailing body: Content) -> Content
```

相应函数类型写作：

```text
fn(kind: String =, trailing body: Content) -> Content
```

四种形参形态：

- `kind: String`：必填，位置绑定（positional）；
- `kind: String =`：可省略（有 default；default 表达式不参与类型身份），具名绑定（named）；
- `kind: String?`：必填但值可为 None，位置绑定；
- `kind: String? =`：可省略且值可为 None（default 通常为 `none`），具名绑定。

可省略与位置绑定互斥：位置实参按声明顺序依次填充，无法跳过——任何可以省略的形参都必须具名绑定（可选即 named）。因此 positional 的 `T?` 是必填但可空的形参：调用必须显式传 `none` 或值；"省略即 none"的唯一写法是 `T? = none`（具名绑定）。default 表达式不限于 `none`（`lang: String? = "auto"` 合法）：可空管值域、可省略管能否不给，两轴正交。

默认值表达式在定义时求值——它是捕获定义处环境的闭包，同一函数的缺省形参值不随调用环境变化；默认值表达式不能引用形参（形参名字在定义处不可见）。

调用点检查（call-site checking）拒绝四类错误：

- 重复绑定（duplicate binding）：同一形参被绑定两次；
- 未知实参（unknown argument）：签名中不存在的名字——绝不静默忽略，防止拼写错误；
- 缺失实参（missing argument）：无默认值的必填形参未被提供；
- 绑定方式不匹配（binding mismatch）：positional 实参没有剩余可填充的形参，或 named 实参指向 positional 形参——绝不静默改绑。

Trailing content 是最后一个 positional 形参：定义中用显式 `trailing` 修饰符声明，至多一个，必须接受 Content；`#f(kind: "warning")[内容]` 与 `#f(kind: "warning", [内容])` 等价——trailing block 只是追加一个 positional 实参的语法糖，没有独立的绑定通道。当前 surface 中它是唯一允许跟在具名实参之后的位置实参；其余形参没有这种紧凑写法。

函数值（function value）是闭包（closure）：携带签名、实现、捕获环境（captured environment，见 [[vault::designs::language::scope-environment]]）与所需 capability。capability 与函数类型身份正交，宿主在实际调用时检查。

函数与元素的边界在 Content 类型上汇合：builtin 元素就是 first-class 构造器函数。

== Rest 与 Typst 式实参交错

本节来自 [[vault::designs::language::collection-types]]，随集合类型实现切片进入现行；进入后替换上一节的 R05 顺序规则。

=== Rest parameter

```text
parameter = ".." name ":" Type
```

- `..name: T` 表示“零或多个位置实参，每个实参按 T 检查”；在函数体内 `name` 的静态类型是 `Array<T>`；
- rest 必须是最后一个 parameter，不带 default；
- `trailing body: Content` 与 `..name: T` 在同一个 signature 中互斥；二者都声明时注册/检查直接诊断；
- 如果签名只有 rest 且 T 接受 Content，trailing `[...]` block 是 rest 的 positional Content 实参——`#f[a][b]` 等价于 `#f([a], [b])`；
- native function 通过 `FunctionInput` 直接消费 rest Value 序列；table 迁移到 rest 后不再需要“只接受一个 trailing body”。

目标中的 table 族签名：

```text
table(columns: Int, align: Array<String>? = none, ..cells: Content) -> Content
table-header(repeat: Bool = true, ..cells: Content) -> Content
table-cell(colspan: Int = 1, rowspan: Int = 1, trailing body: Content) -> Content
```

=== 调用顺序

R05 被替换后：

- named 实参可在任意位置绑定 named-only 形参；
- positional 实参按声明顺序填充尚未绑定的 positional-only 形参；
- 固定 positional 形参都填满后，剩余 positional 实参进入 rest；没有 rest 时剩余 positional 是诊断；
- 所有显式实参与 trailing block 仍按 source order 求值一次；binding 只是静态计划，不改变求值顺序。

因此 `#table(columns: 2, align: ("left", "right"), #table-cell[A], #table-cell[B])` 合法，且不是 parser 特例，而是统一调用模型的结果。

== 静态计划与求值

静态检查为每个调用点生成唯一 binding plan：callee、parameter sources、defaults、coercions、trailing/rest target 与 result type。求值器先求值 callee，再按 source order 求值所有显式实参与 trailing block，最后按形参声明顺序建立绑定；每个表达式只求值一次。该规则继承 [[vault::designs::language::evaluation-model#求值规则]] 的 E1–E4。

== Conformance 候选

| source | 预期 | 测试名 |
|---|---|---|
| `#f(c: 1, 2, 3)` | 当前 surface：R05 诊断；实现后：合法 | `named_and_positional_arguments_interleave` |
| `#let f(a: Int, ..rest: Content) -> Content = ...` | rest 静态类型 `Array<Content>` | `rest_binds_as_array` |
| `#table(columns: 2, #table-cell[A], #table-cell[B])` | rest 收两个 cell | `table_accepts_variadic_cells` |
| `#f[a][b]`（f 只有 `..body: Content`） | 两个 trailing block 进入 rest | `trailing_blocks_feed_content_rest` |

== 已裁定 / 待定

- 已裁定：函数签名记录 parameter order/name/type/default/trailing；必填 positional、可选 named；trailing 至多一个且必须 Content。
- 待随集合类型实现生效：rest 与 trailing 互斥；R05 由 Typst 式交错规则取代；table/table-header 迁移到 rest 签名。
