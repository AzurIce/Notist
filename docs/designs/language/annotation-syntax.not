@![implementation = "aligned"]
= Annotation Syntax

本文是 L8 标注语法的独立成篇，从归档的历史混合文档中独立成篇。属性如何绑定到值、求值收尾如何转写为属性表见 [[vault::designs::language::property-table]]；scope 的语法形态见 [[vault::designs::language::markup-surface#scope 的语法形态]]。

== 属性

属性（annotation）是绑定到求值结果（值）上的标记：标注语法在求值期绑定属性，求值收尾转写为旁置的属性表（[[vault::designs::language::property-table]] 属性表一节）——树不携带属性。属性列表只有一套语法，挂载位置是变体。

=== 属性列表

```text
属性列表 = 条目（逗号分隔）
条目     = id | #tag | .class | key = value
```

- `id`：裸标识符，赋 scope id——模块内唯一，可被引用（`[[vault::guide#install]]`，见 [[vault::designs::world::reference-ref-target#scope id]]）；
- `#tag`：标签，任意多个，用于归类与查询；
- `.class`：类名，任意多个，用于样式与查询；
- `key = value`：键值属性，value 是字面量（String / Int / Bool）。

id 至多一个；tag、class 与 key=value 任意多个。示例：

```text
@install,#design,.highlight,status = "draft"
```

=== 三种挂载位置

1. 行内 postfix：`@...`

   贴在行内元素或表达式之后，绑定到**前面紧接着的求值结果（值）**上——适合行内细粒度标注，阅读流先内容后标注：

   ```not
   文字内容@note
   #warning[...]@wip
   #[一段内容]@install
   ```

   修饰对象是前面紧接着的表达式/元素的求值结果；文本流在标注处切开，标注挂在紧邻的 Text 个体上（粒度规则见 [[vault::designs::language::property-table]] 属性表一节）。

2. 块级前缀：`@[...]`

   独立成行或行首，绑定到**紧接着的下一个块级节点**——通常修饰块级节点，先声明后内容（Rust attribute 的位置）：

   ```not
   @[wip]
   = 标题

   @[install]
   #warning[...]
   ```

   - "紧接着"指语法上紧随其后的下一个块级节点（heading、段落、列表、块级元素）；
   - 多个 `@[...]` 可以堆叠，全部绑定到同一节点；
   - 内部按属性列表解析，不是 Markup 内容——`@` 前缀改变了对 `[...]` 的解读。

3. 模块：`@![...]`

   文件开头，绑定为模块属性（根 scope）：

   ```not
   @![#design, #wip, status = "draft"]

   = 标题
   ```

   - 必须出现在文件开头（第一个有意义的 token 之前）——模块元数据可被索引器快速扫描，不依赖求值；
   - `!` 表示"作用于所在模块"（对应 Rust inner attribute 的语义）；
   - 与行内/块级标注同一模型：等价于把属性列表绑定为模块属性；
   - 属性列表中不使用 id——模块本身已是可寻址目标（Module target，见 [[vault::designs::world::reference-ref-target#scope id]]），id 无意义。

== 语法边界

- `@[`、`@![` 与 `@id`（`@` + 标识符）是三种不同的 token 序列，lexer 层面区分：`@` 后跟 `[` 是块级前缀，`@!` 后必须跟 `[`，`@` 后跟标识符是行内 postfix；
- `@[...]` 内部按属性列表解析：标识符、`#tag`、`key = value`；字符串字面量内的逗号与 `]` 属于字符串；
- 错误恢复：未闭合 `@[...]` → 诊断并按文本恢复，内容到第一个 `]` 为止；悬空的 `@[...]`（其后没有块级节点，如文件或容器在此结束）→ 诊断，属性不绑定；`@!` 后不是 `[` → 诊断；
- 转义：正文中字面 `@`（邮箱、提及等）与 Markup 其他符号一致用 `\` 转义（`\@`），与 `#`、`[` 的转义规则统一。

== 与查询的关系

属性表是查询的唯一入口：id 是模块内唯一身份（[[vault::designs::world::reference-ref-target#scope id]]），tag 与 key=value 支持按属性查询——编辑器与 Agent 借此检索"打了某个标签/某个状态的内容"。模块属性是模块的稳定元数据，随 ModuleResult 发布，`@![...]` 的"文件开头"约束保证它可以不求值地读取。
