@![implementation = "aligned"]
= Type System

本文是 L2 核心类型系统的独立成篇，从归档的历史混合文档中独立成篇。函数签名与调用模型见 [[vault::designs::language::call-model]]；集合类型目标设计见 [[vault::designs::language::collection-types]]；属性表见 [[vault::designs::language::property-table]]。

一段源代码经过三个阶段：解析、静态检查、求值。解析把源码变成语法结构；静态检查解析名称、推断并检查类型，并把每个调用点解析为确定的已解析调用；求值器只消费静态检查的产物，产生元素序列后由成型投影为结构树。本文只定义类型系统：类型是值的静态投影，静态检查产出求值器唯一的语义输入。

== 类型系统

静态检查在求值之前运行：它以类型判断（type judgment）Γ ⊢ e : T 描述"表达式 e 产出什么值"，并产出求值器唯一消费的语义输入。类型是值的静态投影；名称解析沿 scope 链进行——scope 与词法环境见 [[vault::designs::language::scope-environment#scope 是值层节点]]，值的定义见 [[vault::designs::language::evaluation-model#值的宇宙]]。

=== Type 与 Value 对应

类型是值的静态投影：每个类型描述一类值；求值阶段的值宇宙（[[vault::designs::language::evaluation-model#值的宇宙]]）与类型系统共享同一套概念，类型系统是它的静态面。

```text
Type
├─ None / Bool / Int / Float / String / Content
├─ T?        # 可空类型
└─ fn(signature) -> R
```

错误恢复使用独立的 Error marker——Error 不是 `None` 或空 Content，后两者都有各自的合法语义。（发散类型（类似 Rust 的 `!`）暂不进入语言，待出现不会正常返回的表达式时再引入。）

赋值兼容性（assignability）与 coercion 是分离的概念：相同类型直接赋值；`Int` 传给 `Float` 形参时，静态检查在解析结果中插入显式数值 coercion（coercion insertion）。不做隐式函数 subtyping，也不做全局 subtyping。（集合类型的 Union/Never 目标设计见 [[vault::designs::language::collection-types]]；未进入现行 surface 前，Union 暂不进入语言。）

=== 可空与可省略

`T?` 是可空类型（nullable type）：值空间等于 T 的值空间并上 `None`。它是独立的内置类型构造，不依赖一般 Union——语义上 `T?` 与 `T | None` 等价，但现行实现与规则独立。规则：

- `None` 可提升到任意 `T?`（`None` 字面量的类型是 `None`）；
- `T` → `T?` 是隐式提升（widening）；
- `T?` → `T` 没有隐式降级——使用前需要显式检查；
- `(T?)?` 折叠为 `T?`。

"可省略"（omittable）是函数签名概念：由形参是否带 default 决定，与值域上的可空正交。函数类型、四种形参形态与 default 规则见 [[vault::designs::language::call-model]]。

=== Content 类型

`Content` 是不透明类型：一个类型涵盖所有元素组合，元素结构（种类、嵌套、参数）是运行时数据，静态检查不追踪它；元素清单见 [[vault::designs::language::evaluation-model#Content 值]]。树中的每个节点携带自己的构造器身份；builtin 元素以 first-class 构造器函数的形式存在——`callout(kind: ..., body: ...)` 产生一个 callout 节点，其类型就是 `Content`。（内置构造器的命名空间尚未决定，本文暂以裸名书写。）用户函数与 builtin 构造器因此同构：`warning` 与 `callout` 遵守同一套签名、调用与求值规则，渲染与查询按节点身份消费结果，不区分来源。

插入转换只在 Markup insertion 边界成立：

```text
Γ ⊢ e : String
────────────────────
Γ ⊢ #e : Content(Text(e))
```

普通函数要求 Content 参数时仍然拒绝 String——"可展示"不会渗透成所有 API 的隐式转换规则；插入转换只存在于 Markup insertion 边界（[[vault::designs::language::evaluation-model]]）。

=== 静态检查

求值之前先执行静态检查，三个职责：

1. 名称解析：名字绑定到 scope 链上的哪一个 binding——解析结果使用稳定的 Symbol identity，不反复按字符串查表；
2. 类型推断与检查：为每个表达式推断类型，检查 `let` 绑定、实参与签名是否一致；形参必须声明类型；嵌套非递归函数的结果可由 body 推断，根级函数必须声明结果类型（递归绑定暂缓，待 `let rec` 引入）；
3. 调用解析（call resolution）：把每个调用点解析为已解析调用——callee 身份、实参到形参的绑定、默认值、coercion、trailing/rest 目标与结果类型；具体规则见 [[vault::designs::language::call-model]]。

错误恢复：语法、未解析名称与类型错误允许产生 partial HIR，使编辑器与预览能继续处理其他源码；恢复节点保留自己的 Error 状态——错误实参不使用默认值，错误 callee 不伪装成返回 `None` 的成功函数。

静态检查的产物——名称解析结果、类型与已解析调用——构成求值器唯一的语义输入：求值器不重新猜测名字指向哪个函数、实参如何绑定，只执行这份输入。求值如何消费这份输入见 [[vault::designs::language::evaluation-model#求值规则]]。
