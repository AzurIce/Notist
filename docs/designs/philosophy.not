@!(ai: true, draft: true, desc: "设计哲学：裁决级设计理念的收录，各层设计与理念冲突时改设计不改理念")

= 设计哲学

收录裁决级设计理念。各层设计（不变量、管线、语法、包）与理念冲突时，改设计不改理念。

== Code 模式为语义核心

Markup 模式只是方便文档编写的额外语法模式，Notist 本身在 Code 模式下完全足够描述：任何 `.not` 模块都存在到 Code 模式语句序列的机械总翻译，Code 模式是模块的规范形态。

心智模型：`.not` 是默认处于内容上下文的 Code 模块，`#` 是从内容上下文逃逸回 Code 的出口；反方向的 `#[...]` 与调用尾随 `[...]` 是 Code 中的内容 quotation。

对 IR 及相关设计的约束：

- 意图层与 Code 模式同形：模块体是语句序列；IR 的词汇与形态由 Code 模式决定，Markup 构造不在意图层留痕。
- Markup 是可选前端：其全部构造经脱糖汇入意图层。
- 糖不创造语义：Markup 糖只是既有 Code 构造的书写形式；新构造先进 Code 词汇，糖按需后配。
- 翻译保语义：脱糖产物的 callee 与用户名字解析隔离，源码级翻译不得因用户 shadow 改变语义。
