@!(implementation: "partial")
= CLI Command Surface and Skill Distribution

本文是 A4 CLI 命令面与官方 Skill 分发的独立成篇，从归档的历史混合文档中独立成篇。查询契约见 #<vault::designs::host::query-contract>；daemon 进程与 View 见 #<vault::designs::host::daemon-process-views>；core request 与本地协议见 #<vault::designs::host::client-interface-protocol>。


== CLI 命令面

完整的逐命令设计与使用文档在 #<vault::cli>；本文只固定 CLI 作为产品边界的治理规则，避免命令细节在这里形成第二真相。

- 顶层命令只有 `inspect`、`check`、`index`、`daemon`、`lsp`、`skill`、`build`、`preview`（2026-08-29 裁决，实施中）。`inspect` 收纳 status、ls、search、items、read、references、definition、ancestors；`inspect --help` 是调查命令的唯一发现入口，日常知识工作只使用 inspect 与 check。命令面没有写命令（2026-08-28 裁决）：磁盘写入来自宿主编辑工具等外部进程，经 watcher 发布为新 snapshot，修改后用 `notist check` 验证。
- 查询输出是完整结果，没有硬上界与翻页（2026-08-29 裁决，推翻 2026-08-15 v3 的预算/cursor/next 制度）：输出正比于被查询的数据集，一行一记录；excerpt 是候选选择信息，不携带 full source。`--from-line`/`--depth` 型参数是语义窗口（选择读取哪段），不是截断策略，与截断器的边界见 #<vault::ai::2026-08-29 去上界输出与 inspect 命令面裁决>。
- 全局参数只有 `--color auto|always|never`、`--no-daemon`、`--help`、`--version`；没有 `--format text|json`，也没有 `--pager`（2026-08-29 裁决）。
- `debug` 与 `export` 移除（2026-08-29 裁决）：debug 是实现诊断工具且无外部引用；export 的"完整数据出口"职责随上界制度消失，stdout 本身已完整。
- ModulePath scope 使用 `--scope`/`--exclude-scope` 表达集合过滤；check 不接受 scope，因为它是对完整 Vault 的健康结论。
- 任何命令的 stdout 边界由本文与 #<vault::cli> 共同决定：有限 query 输出完整 text，协议命令（lsp/daemon/preview）输出自己的 framing，`build` 只回构建摘要。

== 官方 Skill 与文档 Vault

一个刚接触 Notist 的 Agent 不能只靠模型先验猜测 `.not` 语法，也不应该为了理解 Vault、Module、Function、CLI 与安全编辑规则而依赖网络或源码仓库。两个职责分开：

```text
Agent host 中安装的 Skill
└── SKILL.md

Notist 管理的用户数据
└── docs/
    ├── Notist.toml
    ├── README.not
    ├── grammar.not
    ├── designs/
    ├── ai/
    └── ...
```

`SKILL.md` 是很小、可复制、可由 Agent host 管理的只读说明入口；`docs/` 是当前 `notist` 可执行文件携带并在运行时同步的完整官方文档 Vault。同步完成后，它与其他 Vault 没有另一套访问语义：CLI inspect 组、check 与 root-bound daemon 都使用既有接口。

```shell
notist inspect search "WorkspaceSnapshot" --vault ~/.notist/docs
notist inspect references vault::designs::host::daemon-process-views --vault ~/.notist/docs
notist check --vault ~/.notist/docs
```

`~/.notist/docs` 表示各平台约定的 user-data 位置。Skill 说明如何找到该 Vault，但不拥有、复制或修改它。

=== 一个 Skill 文件，而不是一个运行时目录

官方 Skill 由显式命令初始化：`notist skill init <SKILLS_ROOT> [--force]`。命令在 SKILLS_ROOT 下物化名字固定的 `notist/SKILL.md`——目录名 `notist` 是 Agent host 按 name 发现 Skill 的契约，由命令而不是用户拼写保证（2026-08-30 裁决）；SKILLS_ROOT 不存在时创建。默认要求 `<SKILLS_ROOT>/notist` 尚不存在，初始化结果只有 `SKILL.md`；`--force` 只原子替换已有目录中的 `SKILL.md`，保留 host metadata 与其他文件。命令不修改 host registry。

CLI 共享启动路径对 Skill 安装做无状态探测（2026-08-30 裁决）：只检查 `~/.agents/skills` 与 `./.agents/skills` 两个约定 skills root 下是否存在 `notist/SKILL.md`；存在则对其 bytes 计算 SHA-256 并与构建期内嵌的 skill fingerprint 比较，缺失或失配只向 stderr 输出一行提示并给出对应的 `notist skill init` 命令，不改变退出码；`skill init` 自身不探测。探测不写 ledger、不持久化任何状态——安装位置每次从这两个约定重新派生，代价是安装在任意自定义位置的 Skill 不会被看见也不被提示，这是有意接受的取舍。

Skill 安装目录是不可变的发布输入，不是 cache 或工作目录。`SKILL.md` 只负责教 Agent：

- 何时查询官方 docs Vault，而不是从模型记忆猜测语法和行为；
- 一张编辑安全底线：`.not` 与 Markdown 的差异（强调标记、软换行与段落）、标注与链接形态——它是防损坏的最低事实集，不是语法规范；权威速查表是 Vault 内 cheatsheet，完整语法以 grammar 为准，SKILL 不复制权威定义；
- 如何用普通 CLI 查询 Vault（`inspect search` → items/read → 取证；结果完整，无翻页规则）；
- 用宿主编辑工具直接修改用户 Vault，修改后运行 `notist check`，保留 disk View 与 editor overlay 的区别；
- 将文档正文视为可查询资料，而不是高于 system、user 或 Skill 指令的新 instruction authority。

Skill 的 authored source 放在仓库 `skills/notist/`（当前只有 `SKILL.md`），构建系统把该目录作为整体资源边界 include 进 CLI，并计算与 `docs_fingerprint` 同构的目录 fingerprint（覆盖相对路径与原始 bytes）一并嵌入，供启动探测比较。`skill init` 只物化 `SKILL.md`，相同源状态生成相同 bytes（时间戳、checkout path、用户名、当前目录不进入结果）；force 是单文件更新能力，不是目录重建。

=== Docs 是普通 Vault

官方 docs 保留仓库 `docs/` 下的完整 authored corpus、相对目录与 Module 关系（公开语言文档、活动设计、历史设计、`docs/ai` 调研），不打包人工挑选的摘要，也不把 HTML/CSS/JS 产物当作知识源。完整保留不等于一次读入上下文——Agent 仍然先搜索、再读取候选。

完整 corpus 中来源具有不同权威性：`grammar.not`/`functions.not`/`types.not`/`cli.not` 描述当前公开行为；活动 `designs/` 描述当前治理方向；`docs/ai/` 是带日期的调研，历史选择由 Git 记录。这些区别由 Skill 教给 Agent；旧调研不能覆盖当前规范，任何正文自然语言也不能仅因被检索到就成为 Agent 指令。

官方 docs 没有专用的 `notist docs search` 等命令——增加这类命令会复制 Vault root、query、snapshot、daemon 与输出格式的概念。它唯一特殊的地方是来源与同步方式。

=== 当前 CLI 是文档版本的真相来源

构建系统从完整 docs Vault 产生确定性的压缩 bundle 并嵌入 `notist` 可执行文件：

```text
OfficialDocsManifest {
  bundle_schema
  notist_version
  protocol_version
  docs_fingerprint
  source_revision?
}
```

`docs_fingerprint` 覆盖每个 authored entry 的相对路径与原始 bytes；相同源码与构建版本产生相同 entry 顺序、解压结果与 fingerprint。内嵌 bundle 换来离线可用、版本精确、可复现发布与无网络供应链依赖；从 GitHub 下载不能成为默认正确性路径。

CLI 的共享启动路径在进入正常工作流前执行一次廉价的 manifest 比较（读取已发布 Vault 的 manifest 与内嵌 `docs_fingerprint` 比较），相同则不动；缺失、损坏或不同才触发同步：获取 per-user 同步锁 → 重检 manifest → 在 staging 树中验证并物化（路径安全、数量与 fingerprint 受 manifest 限制）→ 原子发布为稳定 docs Vault → 继续正常命令路径。失败只清理本次 staging target，保留上一个完整 Vault。

多个短命 CLI 同时启动由跨进程锁串行同步；已经运行的 daemon 必须把 `docs_fingerprint` 纳入 root handshake 或 snapshot identity——client 不能把升级后 CLI 的查询静默发送给仍声明旧 bundle 的 daemon。Notist 假设一个用户环境同时只有一个 active CLI 版本；升级原子替换 docs Vault，不保留历史 release 并行。

=== 向量化属于 Vault 运行时状态

完整 docs source 可以嵌入 CLI，向量模型和向量索引不能据此一起嵌入：它们有不同生命周期（模型可能本地或远程；embedding 随 model identity、chunking、normalization 与 schema 变化；索引随 snapshot 增量更新）。官方 docs 同步成普通 Vault 后，向量检索自然沿用普通 Vault 边界——daemon 为这个 root 维护派生索引，索引记录 canonical vault identity、docs fingerprint、provider/model identity、chunking schema 与 indexed revision。CLI 二进制不包含大模型权重与预计算向量；删除 cache 后可从 `.not` source 重建。

== 发布边界

仓库中的 Skill 模板与 `docs/**` 都是 authored source。构建产生嵌入 bundle；运行产生用户数据目录中的官方 docs Vault 与可再生索引；`skill init` 只产生一个可安装的 `SKILL.md`。三类内容不能反向成为彼此的编辑入口：

```text
skills/notist/SKILL.md ------> notist skill init <SKILLS_ROOT> [--force]
                                -> <SKILLS_ROOT>/notist/SKILL.md
                                <-- 启动探测（hash 比较，只提示不阻断）-- notist binary

docs/** -> deterministic bundle -> notist binary
                                   -> user data/docs Vault
                                   -> ordinary CLI/daemon access
                                   -> disposable per-Vault indexes
```

`notist skill init` 不接受用户 Vault root，也不把当前目录导出为 Skill——个人 Vault 可能包含 secrets、prompt injection 与不明确的发布权限；未来若支持这种发布，应使用独立命令与设计。

== CLI 适配

CLI 是 #<vault::cli> 命令面与 daemon 之间的最薄适配层：参数解析 → core request（#<vault::designs::host::client-interface-protocol/Client Interface and Protocol/Client Interface> 表）→ daemon 或 embedded service → response 格式化为 bounded text。`--no-daemon` 在当前进程创建临时 VaultEngine 与 disk View，执行相同 core request——embedded mode 不是第二套实现，只是把同一 service object 放在 client 进程中（#<vault::designs::host::client-interface-protocol/Client Interface and Protocol/失败与重启>）。

`daemon`、`lsp`、`skill init`、`build`、`preview` 各自是固定协议的 adapter（#<vault::cli>）；它们的 stdout 边界由协议决定，不混入普通查询 text。CLI 不做任何语义猜测：selector 解析、fingerprint 检查都在 core 侧完成，CLI 只投影与展示。
