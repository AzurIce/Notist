@![implementation = "partial"]

= CLI Agent 评测框架

以量化证据驱动 CLI 只读面迭代的治理规范：语料政策、条件矩阵、任务分类学、记分卡与协议纪律。本文是评测体系的唯一权威参考；搭建过程手册见 #<vault::ai::2026-08-27 cli readonly query eval harness>，五轮迭代的完整过程记录见 #<vault::ai::2026-08-27 cli readonly iteration log>。

== 语料政策

双语料互为对照，全部**显式冻结**——上游文档的任何改动都不会自动进入评测：

| 语料 | 来源 | 指纹（sha256 前 16 位） |
| - | - | - |
| `corpora/notist` | main 仓库 docs+plugins @ **1666adc**（全量含 ai/，插件捆绑）经 `prepare` 增强 | e3177ce57590b04b |
| `corpora/markdown` | 上者经严格转换器 `build_corpus_v2.py convert` 导出 | a77e7cc5623659d7 |

裁剪仅剔除 `.obsidian` 与 `AGENTS.md`；mermaid/shader 插件包随语料捆绑、以仓库相对路径在 Notist.toml 声明，使裸调用可注册。冻结时点上游缺口（未提交的调查条目、slash 引用笔误、断行行内码）已在 main 提交 981f7f4 与 1666adc 先行修复——语料本身零诊断。指纹算法与逐文件清单见 `evals/corpora/README.md`。

=== 同步规则

1. 主仓库后续 docs 更新**不改变**冻结语料；进行中的迭代轮次继续用旧锚。
2. 重同步是一次显式动作：`git archive` 新提交 → 重跑转换器 → 更新本表的 digest 与指针 commit → **必须重跑基线并以新基线开启新一轮记分序列**。
3. 冻结语料的已知缺陷不就地修补（如 README.not 对已剔除 `#<ai>` 目录的悬空引用产生一条 error 级诊断），缺陷与新语料一起在重同步时处理。

=== Markdown 语料 v2 方向

当前转换器透传了 notist 标题糖与单星加粗（产物不是严格 Markdown），v2 改为从 `notist build` 的语义 HTML 投影做结构化转换，已定的映射裁决：

| Notist 构造 | Markdown 目标形态 |
| - | - |
| heading 糖（`= / == / ...`） | ATX `# / ## / ...` |
| `*x*` strong | `**x**` |
| `_x_` emph、`~~x~~` strike | 同形保留 |
| `__x__` underline | 无对应；降级为粗斜体并在注释中保留原义 |
| 有序项 `+ x` | 数字编号列表 |
| callout(kind)[..] | GFM alert（`> [!WARNING]`），kind 未知名回退引用块 |
| details(summary)[..] | `<details><summary>` 原生 HTML |
| 行内标注 `#[..]@id,#tag` | 正文保留 + 元数据写入同位置 HTML 注释 |
| 模块属性 `@![k=v,...]` | YAML frontmatter |
| scope `{..}` / `#[..]` 包裹 | 解包为内容 |

同时执行双向校正：notist 侧语料增强标注密度（模块属性 `@![status/topic=...]`、裁决段 `@ruling,#decision`、术语条目 `@term-entry`），使"检索能吃元数据"成为可测的结构性差异而非摆设。

== 条件矩阵

| 条件 | 语料 | 工具 | 说明 |
| - | - | - | - |
| null-calib | 无 | --no-tools | 参数化知识校准与幻觉率测量，仅建基线时运行 |
| md-bash | markdown | read,bash,find,grep,ls | 生态基线；兼作免费噪声对照组 |
| notist-bash | notist | 同上 | PATH 提供 notist，仅一句存在性提示 |
| notist-skill | notist | 同上 | 叠加官方 SKILL 注入（--skill 显式加载） |
| notist-enforced（v2） | notist | 白名单 bash | KB 内容读取命令强制走 notist，测接口充分性 |
| notist-open（v2） | notist | 全量 | 不限制工具，测竞争优势需用效率口径判读 |

隔离不变量（每次 invocation）：`--offline --no-context-files --no-extensions --no-skills --no-prompt-templates --no-themes`；系统提示整体替换且条件间差异仅为声明增量；thinking 显式钉住；沙箱置于带中性命名的临时目录；二进制以 sha256 入 manifest 且 PATH 断言解析位置；会话全量 JSONL 落至该次输出目录。

=== 审计式执法（v2 生效）

prompt 层禁令不做安全边界，真正的执法是事后审计：runner 逐条扫描会话日志中的 bash 调用，命中禁用命令清单（grep/rg/find/cat/sed/awk/head/tail/less/ls 及对 KB 路径的内容读取变体）即把该格标记为 invalid，不计入任何方向的结论。

== 任务分类学

| 类 | 名称 | 能力靶点 | 判据要点 |
| - | - | - | - |
| T-A | 定位类 | 导航原语的基本可用性 | 单点事实；只作烟雾测试不计主分 |
| T-B | 多跳链 | references 往返经济 | 沿引用边 2–3 跳后的复合答案 |
| T-C | 否定证明 | bounded coverage / 排除 | 要求穷举依据的存在性判断；grep 无法廉价证明不存在 |
| T-D | 结构取证 | span / 锚点经济性 | 答案绑定字节区间或节 id 等接口独有形状 |
| T-E | 预算生存 | 全部能力的效率面 | 所有类共用的预算上限制，超限即负 |

答案格式绑定是让接口改进可见的关键手法：要求引用带 `module#id` 或字节区间时，产出该形状的接口版本才有资格得分。措辞一经锚定即冻结；判分 checks 必须与措辞同步修订（先改措辞再跑锚，禁止沿用旧判分重解释新问句）。

== 记分卡

主指标 **correctness@budget**：机械判分通过 且 tokens/wall 未超该任务预算。次指标：tokens 总量、wall 时长、tool call 数、违规与超时标志。效率以 par 成本比为口径——每任务的确定性最小查询组合由微基准 fixture 测出，实际消耗除以 par 即比值，避免跨轮相对比较的漂移。

统计规则：正式结论 n≥3 取中位；n=1 只作线索并显式标注低置信度。CLI 无关格（null、md 组）永远先看波动再看组间差；同一格连续两轮反向翻转即进入隔离名单，从判定集中移除直至协议变更。锚定重置：任何影响其中一格可比性的事件（措辞修订、判分修订、超时上限调整、语料重同步）之后，受影响格子必须在旧二进制上重锚才能参与下一轮对比。

== 与现有资产的映射

- 框架本体：worktree `feat/cli-readonly-queries` 下 `evals/`（tasks.json 判据、tools/run_eval.py 编排、grader.py 机械判分、convert_notist_to_md.py 转换器、sanity_probes.sh 六探针、compare_runs.py 对位差分）。
- runs 命名：`baseline` / `p2base` 为锚，`itN` / `p2itN` 为尝试；协议第 2 阶段起判定集收敛为工具三条件的 18 格网格。
- 五轮迭代的结果不变量：宏评测 pass 层饱和于 18/18，改进的显影责任落在 T-C/D/E 三类与 efficiency 口径上。

== 已决事项备忘

edit 守卫指纹双实现冲突已于 2026-08-27 裁决修复（commit 2c6ec1a：守卫校验改用 canonical content fingerprint，遗留 fnv 助手更名，回归测试钉死两条路径同源）。修复前针对该缺陷设计的微基准条目随之失效。

== 演进路线

- Phase 3a：转换器按上表重写（build HTML 投影为源）；notist 语料标注增强；双料重新冻结。
- Phase 3b：五类任务集落地 + 记分卡实现 + 审计式执法进 runner；四条件扩为六条件。
- Phase 3c：全条件 n≥3 重锚并出主基线报告；此后恢复"改进→评测"循环，read-only 之外的范围另行立项。
