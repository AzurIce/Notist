@![implementation = "partial"]
= Query Contract

本文是 A2 查询契约的独立成篇，从归档的历史混合文档中独立成篇。CLI 命令面与 Skill 分发见 #<vault::designs::host::cli-command-surface-skill-distribution>；daemon 进程与 View 见 #<vault::designs::host::daemon-process-views>；core request 与本地协议见 #<vault::designs::host::client-interface-protocol>；Analyzer 对查询契约的实现见 #<vault::designs::host::analyzer-snapshot>。


== 查询产品形态

一个 Agent 为了回答"属性表为什么是旁置的"而探索文档时，正常路径应当是：

```shell
notist inspect search "annotation table" docs
notist inspect outline vault::designs::language::type-system docs
notist inspect read vault::designs::language::property-table/属性表 docs
```

第一条命令返回全部有排序的候选和短 excerpt，第二条只展开一个 Module，第三条才读取被选中的原文（按标题默认 id 定位到节）。当前公开投影是同一份完整 text：人、脚本与 Agent 看到相同的 ModulePath、相对路径和 citation。

查询产品分为两种形态：

```text
ordinary query
  complete, line-per-record, composable

text projection
  readable, same logical result, current public stdout
```

完整结果契约（2026-08-29 裁决，推翻 2026-08-15 v3 的预算/cursor/next 制度）：collection 查询返回全部匹配，没有 item/byte 预算，没有翻页。契约护栏是输出形状而非截断器——输出正比于被查询的数据集，一行一记录，excerpt 不携带 full source；`--from-line`/`--depth` 型参数是语义窗口（调用者选择读取哪段），不是截断策略。无 scope 的宽查询灌入大量命中时，对策是收窄 `--scope` 与查询本身，如同 `rg` 的使用者。

== Core Query Contract

CLI 与未来 adapter 都把输入投影到协议无关的 core operation（#<vault::designs::host::client-interface-protocol/Client Interface>）：core operation 接受一个已经捕获的 WorkspaceSnapshot 和结构化 request，返回 typed result；CLI 只负责参数拼写与展示。

颜色、TTY 不能改变匹配集合、排序、source range 或错误分类。当前有限 CLI 命令不提供公开 JSON envelope；`QueryResult` 是 core 内部契约，text renderer 消费它，但它本身不是输出格式。

Collection query 的概念模型：

```text
QueryResult<T> {
  snapshot
  items: [T]
}
```

`snapshot` 是查询开始时捕获的 SnapshotStamp；`items` 是完整匹配集合，没有 page、budget 或 coverage 元数据——一次性完整输出零命中即是 absence 证明，不需要沿翻页走到穷尽。确定性排序（见下）保证同一 snapshot 上同一查询的 items 顺序稳定。

=== ModulePath Scope

集合发现命令可以在 Vault 内部进一步限定逻辑范围。Scope 是可能匹配多个 Module 的集合过滤，不是 selector（selector 只解析一个精确身份）：

```text
ModuleScopeSet {
  include: [ModulePath]  // 空 = 整个 Vault
  exclude: [ModulePath]
}

in_scope(module) =
  (include 为空，或任一 include 按段边界命中)
  && 没有 exclude 按段边界命中
```

- ModulePath 必须是绝对路径（`vault::a::b`）。
- 段边界匹配：`vault::foo` 匹配自身与 `vault::foo::*`，不匹配 `vault::foobar`。
- include 可重复取并集；exclude 可重复，并在 include 之后生效。
- scope 属于查询语义本身：对同一查询改变 scope 即是另一个查询。scope 必须在 candidate generation 前下推，不能先物化全 Vault 结果再过滤。

CLI 投影见 #<vault::cli::inspect/Selector、Scope 与 Citation>。`check` 不接受 scope：它的 exit status 是完整 Vault 的健康结论。

=== Snapshot Stamp

一次 query 在开始时捕获一个 snapshot，所有 item、excerpt、range、source fingerprint 与计数都来自它：

```text
SnapshotStamp {
  vault_id
  view_kind
  revision
  source_set_fingerprint
  daemon_instance
}
```

`daemon_instance` 必填：daemon 模式为实例 id，embedded 模式为本次调用合成的实例 id。Revision 只在所属 View 与 daemon instance 内有意义；`source_set_fingerprint` 覆盖参与该 snapshot 的 source identity、内容 fingerprint 与影响查询的配置 identity，使 `--no-daemon` 的独立进程也能判断当前 source set 是否仍是同一个语义世界。

Source-order query 的稳定 key 是 ModulePath、Vault-relative path、match range 与 record kind；ranked query 的 key 是固定精度 score 的降序值、ModulePath、range 与 search-unit identity——浮点 backend score 必须先按 ranking version 量化再参与排序，使 tie 不依赖 hash 顺序或线程调度。完整结果契约下这份确定性直接对调用者可见：同一 snapshot 上重复同一查询得到逐字节相同的输出。

== Selector 与 Citation

Scope 与 selector 是两种不同的 ModulePath 用法：scope 表达「可能命中哪些 Module」的集合过滤；selector 表达「就是这一个 Module/Path/节」的精确身份。两者不互相替代——模糊发现属于 search 的 scope+query，不属于 selector resolution。

selector 是 ModulePath 或 ItemPath 的规范输入（词汇见 #<vault::designs::world::reference-ref-target>），三类精确身份：

```text
ModuleSelector  { module: "vault::designs::language::type-system" }
PathSelector    { path: "designs/language/type-system.not" }
ItemSelector    { module: "vault::designs::language::property-table", name: "属性表" }
```

规范输入是等价字符串：`vault::designs::language::type-system`、`designs/language/type-system.not` 或 `vault::designs::language::property-table/属性表`（Module 风格用 `/` 携带 ItemName，Path 风格因 `/` 是文件系统分隔符而用 `#`）。ItemName 的匹配顺序与引用寻址一致（#<vault::designs::world::reference-ref-target/ItemName>）：先按显式 `@id`，再按标题默认名（标题文本）精确匹配。短 basename、模糊路径与"最接近的 Module"不能静默解析；零匹配返回 `not_found`，多匹配返回 `ambiguous_selector` 和有界候选。模糊发现属于 search，不属于 selector resolution。

所有可作为证据或编辑位置的 record 使用统一 location：

```text
Location {
  module
  relative_path
  byte_range
  line_range?
  id?
  source_fingerprint
}
```

snapshot 由所在 QueryResult 统一携带，Location 不再重复。普通 text 输出默认不显示 canonical absolute path，也不为每个 location 重复整篇 source。excerpt 是候选选择信息，不是独立事实来源；search 与 references 的结果必须引导调用者在形成事实性回答前使用 `read`。

由于普通 CLI 不输出 JSON envelope，`read` 的 text 投影必须把 citation 所需的机器句柄显式打印出来：ModulePath、relative path、实际 UTF-8 byte range、line range 与 source fingerprint。读取结果保留这些精确值，使引用、后续 query 与外部写入方的前置校验能指向同一 authored source。
