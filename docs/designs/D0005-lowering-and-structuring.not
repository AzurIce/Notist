= D0005: Lowering and Structuring

Notist 的 source syntax、程序语义和最终文档结构不是同一棵树。一个标题既有 `= Title` 的 source form，也有指向 `core::heading` 的 semantic operation，求值后又成为参与段落与大纲组织的 Heading Element。把三者塞进同一个 parser/evaluator pass，会让 source recovery、lexical scope 和 document realization 相互猜测。

Notist 因此使用明确的语义管线：

```text
source
  -> lossless syntax
  -> surface lowering / desugaring
  -> resolution and type checking
  -> typed HIR
  -> evaluation
  -> flat Content
  -> realization and structuring
  -> StructuredDocument
```

Lowering 回答“这个 source form 表达哪一种语言操作”；evaluation 回答“程序在环境中产生什么 Value”；structuring 回答“已经产生的 Element 如何组成可消费的文档”。

== Lossless Syntax

Syntax tree 保存 source 中真实存在的内容：

- Markup、Code 与 Raw mode boundary；
- Text、Wiki Reference、heading/list/table/inline sugar；
- declaration、expression、Content literal 与 call；
- name、argument、Attributes、delimiter 与 trivia；
- recoverable Error node 和精确 source range。

Syntax 不执行 Function，不解析 Module import，也不依据 registry 判断 delimiter。未知 Function、未知 Type 或未解析 name 与已知 name 具有相同的 syntax boundary。

Host Markup sugar 必须由 syntax frontend 建立 node。Evaluator 不重新扫描 Text 查找 heading、list、table、emphasis 或 URL，也不切出 source fragment 再调用 parser。否则同一字符会在不同阶段得到不同语法身份，formatter、LSP 和 source map 无法共享一种事实。

Lossless tree 可以拥有面向编译器的 typed AST facade。CST 保留 token/trivia/recovery，AST 提供 `CallExpression`、`LetDeclaration`、`HeadingSyntax` 等结构化访问；两者不要求 renderer 直接消费 source tree。

== Surface Lowering

Surface lowering 把专用写法归一到 [[vault::designs::D0015-programmable-documents-and-language-core]] 定义的语言核心，同时保留 source provenance：

```text
= Title
  ~= core::heading(level=1, body=[Title])

*important*
  ~= core::strong(body=[important])

#let add(a: Int, b: Int) -> Int = a + b
  ~= let add = fn(a: Int, b: Int) -> Int => a + b
```

`~=` 是 semantic equivalence。HIR 可以用 intrinsic identity 表示 core heading，而不必伪造 source 中不存在的 `core::heading` tokens；但 explicit call 和 sugar 必须解析到同一种 constructor contract，最后产生同一种 Element。

Structural sugar 可以先降低为局部 item：

```text
- A
- B

-> core::list::item([A])
-> core::list::item([B])
```

相邻 item 在 structuring 阶段组合为 List。Lowering 不需要提前复制动态 Function output 也必须遵守的组合算法。

== Resolved and Typed HIR

HIR 不只是一份去掉标点的 AST。它保存后续执行需要的 semantic decision：

```text
HIR
├─ lexical ScopeId and SymbolId
├─ resolved Module/import/core identities
├─ desugared expression and declaration kinds
├─ expression Type
├─ call binding and coercion plans
├─ intrinsic/core constructor identity
└─ source provenance
```

Name resolution 先把 source name 绑定到 lexical、imported、core 或 plugin symbol；type checker 再生成 call plan。Evaluator 不按字符串重新查找“当前大概是哪一个 Function”，也不重新决定 positional/named/trailing argument mapping。

Error HIR 显式保留无法完成的 decision。Unknown call 可以携带 partial argument/body information 供 preview 展示，但不能被当作成功返回 `None` 的普通 call。

== Evaluation

Evaluation 在 runtime environment 中执行 typed HIR：

```text
environment + typed HIR
  -> Value / ModuleResult / evaluation diagnostics
```

Document root 和 Content literal 都按 source order 求值。Declaration 扩展后续 item 的 lexical environment；可展示 Markup 与 embedded expression 通过 Content insertion 追加 flat Content。

Function call 的路径是：

```text
evaluate callee expression
-> evaluate explicit arguments
-> establish parameters in order: coerce provided values or evaluate selected defaults
-> call Function Value
-> validate host/plugin result
-> return Value
```

User Function body、native/core callable 与 plugin callable 共享这条 Value-level path。只有 backend 与 capability 不同。Content literal 已经是 Value；Function 不通过读取其 source String 再次执行 Notist parser。

== Flat Content

Evaluation 产生有序的 flat Content：

```text
Content
└─ ElementNode*
   ├─ Text
   ├─ Reference
   ├─ Heading
   ├─ ListItem
   ├─ Raw
   ├─ Custom
   └─ Parbreak
```

“flat”只描述当前 composition level。Heading、Strong、Quote、Function-produced Custom Element 仍可拥有嵌套 Content。Content concatenation 保留求值顺序；structuring 可以组合相邻 Element，但不能回到 source 猜测它们原先由 sugar 还是 explicit call 产生。

== Annotation Projection

Attributes 从 syntax/HIR 投影 source Annotation，不进入 Value evaluation：

```not
Before #[annotated [[self::target]]]@concept,#important after.
```

同一个 syntax scope 同时拥有：

```text
semantic projection -> typed HIR -> Value / Content
analysis projection -> Annotation(scope, metadata)
```

Lowering 可以为 Annotation 保存与 expression、call output 或 source range 的关系，但不创建伪装成 Content 的 metadata wrapper。Structuring 改变 paragraph/list grouping 后，Annotation identity 仍由 source projection 保持。

== Why Structuring Is Separate

程序执行可以动态产生与静态 sugar 相同的 Element：

```not
#let choose(flag: Bool, trailing body: Content) -> Content =
  if flag { body } else { [] }

#choose(true)[#core::list::item[Generated item]]
```

如果 parser 直接建立最终 Paragraph/List tree，动态 Function output 还需要第二套 grouping。Flat Content 让所有来源汇合后只进行一次 realization：

```text
Text("Intro")
Parbreak
ListItem("A")
ListItem("B")
Heading(level=2, ...)
Text("After")

->

Paragraph(Text("Intro"))
List(ListItem("A"), ListItem("B"))
Heading(level=2, ...)
Paragraph(Text("After"))
```

== Structuring Rules

Structuring 只读取 Element semantics 与顺序：

- 连续 inline Element 组成 Paragraph；
- Parbreak 结束当前 Paragraph，不进入最终 document；
- 相邻兼容 item 组成 List、Enum、Task 或 Terms；
- Heading、block Raw、Table、Quote 等 block Element 独立存在；
- block 到来前 flush 当前 inline/container state；
- container constructor 的 child invariants 在 realization/validation 中明确检查；
- diagnostics 与 source provenance 随 node 保留。

Structuring 不进行 name resolution、type checking、Function execution 或 source scanning。它应能对任意合法 Content Value 独立运行，所以 build、preview、tests 和 plugin output 不会获得不同的 document grammar。

== Ownership Boundaries

这些阶段构成单向依赖：

```text
syntax facts
  -> semantic HIR
  -> runtime Value / Content
  -> document structure
  -> rendering and queries
```

Analyzer 可以缓存、增量计算并把结果组织进 WorkspaceSnapshot，但不新增语义阶段。Renderer 只消费 StructuredDocument；LSP 主要消费 syntax/HIR/source map；CLI/MCP query 消费 snapshot indexes。任何 adapter 若需要重新 parse source 才能回答一个语言问题，说明相应事实尚未归入正确阶段。
