@!(ai: true, draft: true, desc: "list-item 糖的产生式、缩进归属与空行上下文")

= list-item 糖

```text
ListItem[M] → Marker Inline* LINEBREAK Body
Marker      → ("-" | "+") " "
```

侧注与条件：

- ListItem：col >= M；Body : Markup[col+1]——marker 同行内容的余下部分，与所有列深于 col 的续行
- 列不深于 col 的行出本项：同列 marker 行是同级项，其余行属于外层片段
- Marker 后的第一个空格是定界符，其余空格是内容
- 相邻项之间的空行为 gap 元素（L2）
- 有序性由 `+` 置位，是节点字段；无显式起始编号

构造物为 list-item 元素（body 与 ordered 字段）；列表的归组与编号见 #<vault::designs::plugins::core::list>
