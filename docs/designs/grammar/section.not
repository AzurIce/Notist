@!(ai: true, draft: true, desc: "section 糖：行首等号定界的身份容器产生式")

= section 糖

```text
Section[M] → ("=")+ " " Inline* LINEBREAK Body
```

侧注与条件：

- Section：col >= M；等号后恰好一个 U+0020，缺空格时该行按文本处理
- Body : Markup[M]，至下一个等号数不多于本节的 Section 行为止，或至所在容器末尾
- 嵌套：解析期一遍栈式构建，栈顶等号数大于等于当前等号数时弹出
- 树深即层级，节点不携带 level 字段；等号数与树深不一致时以树深为准并报 warn
- 标题行内容即 label，空标题成立

构造物为 section 元素（label 与 body 字段）；元素语义见 #<vault::designs::plugins::core::section>
