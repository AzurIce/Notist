= D0008: CLI and Skill

本文承接 [[vault::designs::D0005-daemon-architecture]]（daemon 进程形态与 core request 表）与 [[vault::designs::D0004-vault-module-and-scope]]（Module 与引用寻址）。Agent 接口不引入独立协议（无 MCP）：Agent 通过 CLI 命令与官方 Skill 使用 Notist，与人类共用同一套查询语义。本文定义查询产品形态、Core Query Contract（预算、cursor、stamp）、Selector 与 Citation、Search 四种模式、CLI 命令面，以及官方 Skill 与内置文档 Vault 的分发。

动线：先定查询产品形态（普通查询 / 人类渲染 / 显式导出），再定义协议无关的查询契约（预算、cursor、stamp），然后定义 selector 与引用、search 模式、CLI 命令面，最后定义 Agent 的入口——官方 Skill 与文档 Vault 的发布方式。

== 查询产品形态

一个 Agent 为了回答"属性表为什么是旁置的"而探索文档时，正常路径应当是：

```shell
notist search "annotation table" docs
notist outline vault::designs::D0002 docs
notist read vault::designs::D0002#属性表 docs
```

第一条命令返回少量有排序的候选和短 excerpt，第二条只展开一个 Module，第三条才读取被选中的原文（按标题默认 id 定位到节）。人在终端中看到的是带颜色、相对路径和续读提示的紧凑页面；脚本与 Agent 看到的是稳定 JSON。两种调用者不使用两套查询语义。

查询产品分为三种形态：

```text
ordinary query
  bounded, composable, resumable

human rendering
  readable projection of the same result

full export
  explicit file or artifact, never an accidental tool result
```

普通查询始终有默认预算与服务端硬上界。调用者可以用参数在硬上界内收紧或扩大页面，但不能关闭上界；需要完整数据时必须显式进入 export。这个规则同时避免终端被大结果淹没、脚本意外缓存整库正文，以及 Agent 把大段正文反复回放进对话上下文。

== Core Query Contract

CLI 与未来 adapter 都把输入投影到协议无关的 core operation（D0005）：core operation 接受一个已经捕获的 WorkspaceSnapshot 和结构化 request，返回 typed result；CLI 只负责参数拼写与展示。

`--format`、颜色与 pager 不能改变匹配集合、排序、source range 或错误分类。

Collection query 的概念模型：

```text
PageRequest {
  limit
  max_bytes
  cursor?
}

QueryPage<T> {
  snapshot
  items: [T]
  page { requested_limit, applied_limit, returned, has_more, next_cursor? }
  budget { requested_bytes, applied_bytes, logical_bytes, exhausted }
  coverage { complete, stop_reason }
  hints? [String]
}
```

`limit` 限制 item 数，`max_bytes` 限制规范化 logical result 的 UTF-8 JSON bytes。两者同时生效；服务端在加入 item 前计算最终序列化大小，绝不截断已经序列化的 JSON 或 UTF-8 scalar。

`coverage.complete=false` 只表示当前页面没有覆盖完整匹配集合，不表示"未找到"。一旦 `read` 已经给出足够 authored evidence，调用者可以停止；只有当前页不足、任务要求穷举，或者结论依赖证明"不存在"时，才需要沿 cursor 读到 complete。`stop_reason` 区分 `item_limit`、`byte_budget` 与 query-specific 上限。

普通 collection 的默认与硬上限（可版本化的产品 policy，不是语言常量）：

| Operation | Default | Maximum |
| --- | ---: | ---: |
| modules | 20 items / 16 KiB | 100 / 64 KiB |
| search | 8 items / 16 KiB | 100 / 64 KiB |
| outline | 100 items / 16 KiB | 100 / 64 KiB |
| references | 20 items / 16 KiB | 100 / 64 KiB |
| diagnostics detail | 20 items / 16 KiB | 100 / 64 KiB |
| read | 120 lines / 16 KiB | 1,000 lines / 64 KiB |

=== Snapshot Stamp 与可恢复 Cursor

一次 query 在开始时捕获一个 snapshot，所有 item、excerpt、range、source fingerprint 与计数都来自它：

```text
SnapshotStamp {
  vault_id
  view_kind
  revision
  source_set_fingerprint
  daemon_instance
}
```

`daemon_instance` 必填：daemon 模式为实例 id，embedded 模式为本次调用合成的实例 id。Revision 只在所属 View 与 daemon instance 内有意义；`source_set_fingerprint` 覆盖参与该 snapshot 的 source identity、内容 fingerprint 与影响查询的配置 identity，使 `--no-daemon` 的独立进程也能判断当前 source set 是否仍是同一个语义世界。

Cursor 是 opaque、self-contained、带 schema version 和完整性校验的 token（紧凑 binary + base64url）。它绑定 Vault/View identity（含 daemon instance 与 View identity）、source-set fingerprint、operation/selector/query/filter/mode 与排序 fingerprint、tokenizer/ranking/index schema version，以及上一 item 的稳定排序位置——token 内可自由选择表示（如预排序结果中的恢复偏移），跨页稳定性契约不变。`limit`、`max_bytes` 与 excerpt 大小可以在翻页时改变；任何改变匹配集合或排序的参数必须与 cursor 一致。source set 变化返回 `cursor_stale`，schema 或 query fingerprint 不匹配返回 `invalid_cursor`。服务不能在新 snapshot 中猜测相近 byte offset 继续，也不能把 cursor 当成权限凭证。

Source-order query 的稳定 key 是 ModulePath、Vault-relative path、match range 与 record kind；ranked query 的 key 是固定精度 score 的降序值、ModulePath、range 与 search-unit identity——浮点 backend score 必须先按 ranking version 量化再参与排序，使 tie 不依赖 hash 顺序或线程调度。

== Selector 与 Citation

CLI selector 只接受三类精确身份：

```text
ModuleSelector  { module: "vault::designs::D0002" }
PathSelector    { path: "designs/D0002-evaluation-model-and-type-system.not" }
SectionSelector { module: "vault::designs::D0002", id: "属性表" }
```

规范输入是等价字符串：`vault::designs::D0002`、`designs/D0002-....not` 或 `vault::designs::D0002#属性表`。`#id` 的匹配顺序与引用寻址一致（D0004）：先按显式 scope id，再按标题默认 id（标题文本）精确匹配。短 basename、模糊路径与"最接近的 Module"不能静默解析；零匹配返回 `not_found`，多匹配返回 `ambiguous_selector` 和有界候选。模糊发现属于 search，不属于 selector resolution。

所有可作为证据或编辑位置的 record 使用统一 location：

```text
Location {
  module
  relative_path
  byte_range
  line_range?
  id?
  source_fingerprint
}
```

snapshot 由所在 QueryPage 统一携带，Location 不再重复。Machine result 默认不返回 canonical absolute path，也不为每个 location 重复整篇 source。excerpt 是候选选择信息，不是独立事实来源；search 与 references 的结果必须引导调用者在形成事实性回答前使用 `read`。读取结果保留精确 byte range 与 fingerprint，使引用、后续 query 与 edit precondition 能指向同一 authored source。

== Search 不是一种模式

公开 search 有四种显式模式：

| Mode | 语义 | 排序 | 典型用途 |
| --- | --- | --- | --- |
| `lexical` | fielded token match + BM25 | relevance | 默认自然语言与主题检索 |
| `exact` | literal UTF-8 substring | source order | Module 名、错误消息、源码片段 |
| `fuzzy` | lexical BM25 + bounded term expansion | relevance | 拼写错误（`langauge`、`WorkspaceSnaphot`） |
| `regex` | Rust-compatible regular expression | source order | CLI 专家级源码调查 |

规范参数是 `--mode lexical|exact|fuzzy|regex`；`--exact`、`--fuzzy`、`--regex` 是互斥的可读 alias。默认 `lexical`。Lexical 不提供隐藏 query mini-language（引号、冒号、减号默认是待分词文本），默认 `--operator all`、可 `any`；不做 stop-word 删除与 stemming——否则 `not`、类型名、数字和缩写会在知识库中丢失真实含义。

可检索字段（权重属于 ranking version）：

| Field | 内容 | 默认权重 |
| --- | --- | ---: |
| `title` | Module 的语义标题 | 5.0 |
| `heading` | 当前 Heading 与祖先 Heading | 4.0 |
| `id` | 显式 scope id 与标题默认 id | 6.0 |
| `tag` | 属性 tag | 4.0 |
| `module` | ModulePath segment 与完整路径 | 6.0 |
| `path` | Vault-relative path 与文件 stem | 3.0 |
| `body` | 普通 authored Content | 1.0 |
| `raw` | Raw 内容 | 0.7 |
| `comment` | source comment | 不默认检索 |

`--fields` 控制参与匹配的字段，不控制 response projection；未知字段立即报 usage error。属性检索字段（class、key=value）随属性索引扩展。

Tokenizer 对 index 与 query 使用同一套 versioned pipeline：Unicode NFKC 与 case folding 生成匹配 term 但保留原 byte offsets；Latin/数字按 punctuation、snake/kebab/camelCase 边界拆分并保留完整 identifier 与组成部分为同一 term group 的 variant；连续 Han 字符用 overlapping bigram（混合中英文在 script boundary 分开）；路径与 ModulePath 额外保留 path/segment/stem 分段。不做语言猜测、远程分词或词干还原；替换 tokenizer 必须提升 version 并使旧 cursor 失效。

Exact 默认大小写敏感，`--ignore-case` 显式启用；不依赖全文索引（index 重建时仍可用）。Fuzzy 先执行与 lexical 相同的分词和 boolean 语义，再对适合纠错的 term 查询词典：默认 Damerau-Levenshtein distance 1（`--fuzzy-distance 2` 是上限），长度 <4 的 term、纯数字、单个 Han 字符与结构 delimiter 不扩展，每 term 至多 32 候选、每 query 至多 128，达到上限返回 `expansion_limited=true`。Regex 使用 Rust regex 语义（无 backreference/look-around），pattern 长度、编译大小、扫描时间与响应 bytes 都有上限——它是 CLI 专家能力，Skill 引导 Agent 优先使用 exact/lexical/fuzzy。

=== Scope、Excerpt 与分组

`--scope` 是精确 ModulePath prefix，可重复取并集；`--fields` 与访问控制在 candidate generation 前生效（Vault 外路径永远不进入 index 或 result）。

```text
SearchHit {
  location
  matched_field
  match_range?
  unit_range
  excerpt
  excerpt_range
  excerpt_truncated
  score?
}
```

默认 excerpt 上限 256 bytes（`--snippet-bytes` 64–2048），截取优先以实际 query term 的 source range 为中心；单行超限在 UTF-8 scalar boundary 截断。`group_by` 控制首页多样性：`source` 每 Module 保留最佳 hit（lexical/fuzzy 默认）、`section` 以最近 Heading 为边界、`match` 返回每个独立 region（exact/regex 默认）。分组属于 query identity，翻页不能改变。零结果仍是成功，hint 只建议（如 operator=any、--exact），不自动重试、不把零结果改成 error。

== 检索单元与索引

Notist 使用独立的 `SearchIndex` 边界，不把 Tantivy type 暴露进 CoreRequest。默认实现选择 Tantivy：Rust-native、fielded BM25、term dictionary、fuzzy term query、增量 segment 与 immutable reader，与 VaultEngine 的 publication 模型契合。

```text
WorkspaceSnapshot
  -> SearchUnitBuilder
  -> SearchIndexWriter (Tantivy)
  -> immutable SearchIndexReader + IndexStamp
  -> SearchService
```

`SearchUnitBuilder` 从 snapshot 已捕获的 source、parse、Module/Heading/属性 facts 构造检索单元，不执行有外部副作用的 plugin。普通 paragraph、list item、Heading section 与 Raw block 优先成为独立 unit；超过 4 KiB 的单元按 source-safe boundary 切分（至多 256 bytes overlap）。每个 unit 存储 FileId、ModuleId、ModulePath、relative path、source fingerprint、source range、Heading ancestry 与 typed field text；完整 source 不复制进 index，excerpt 始终从捕获的 snapshot 读取。

Disk View 的 index 用 WorkspaceDelta 增量更新，写入新 generation 后原子发布 reader；删除 source 产生 tombstone。持久化 index 位于 Notist user cache（按 Vault identity 分区），manifest 记录 index schema、tokenizer、ranking version、Vault identity、source-set fingerprint 与每个 source fingerprint——任何一项在加载时校验不匹配都触发丢弃或重建。Session View 在 disk generation 上叠加小型 in-memory overlay segment，用 FileId tombstone 屏蔽被 overlay 替换或删除的 unit；overlay 释放后整个派生 segment 可丢弃。

Lexical/fuzzy query 只在 `IndexStamp.source_set_fingerprint` 与捕获 snapshot 相同时执行；index lag 时按 `--wait-index` 在短 deadline（默认 2s、最大 10s）内等待，超时返回 typed `index_not_ready` 与 exact fallback hint，绝不把 stale result 当成当前 snapshot。Exact/regex 直接扫描 snapshot，不受 index health 影响。

向量、semantic 与 hybrid mode 不属于默认契约：未来只有在版本化 benchmark 上相对 lexical 明确提升 Recall、nDCG 与 Agent task success，并保留 citation、ACL、snapshot 与 output budget 后，才能增加显式 mode。

== CLI 命令面

全局参数：`--format text|json`（JSON 用 versioned envelope）、`--color auto|always|never`、`--pager auto|always|never`、`--no-daemon`。需要 Vault 的命令把 `[ROOT]` 放在最后一个 positional（默认 `.`，按 D0004 解析最近的 Vault root）；`lsp` 按 editor workspace 动态发现多个 root。Protocol stdio 命令拒绝 text/json/color/pager 组合。

Collection 命令共享 `-n, --limit`、`--max-bytes` 与 `--cursor`（help 展示实际 default 与 maximum）；普通命令不提供 `--all`，完整输出只能使用 export。JSON envelope：

```json
{"schemaVersion":2,"ok":true,"command":"search","result":{"items":[],"page":{},"budget":{},"coverage":{},"snapshot":{}}}
```

`max_bytes` 约束 `result` 的 compact JSON；CLI 完整 stdout 的最坏上界是 `applied max_bytes + 4 KiB`，human renderer 是 `2 × applied max_bytes + 8 KiB`（gutter、颜色、continuation）。

=== 探索与读取命令

- `notist status [ROOT]`：Vault identity、snapshot stamp、source/module/diagnostic counts、daemon mode、index health；固定 8 KiB 上限。
- `notist modules [ROOT] [--prefix MODULE] [--kind any|source|virtual]`：`--prefix` 按段边界匹配（与 search 的 `--scope` 一致）。Module record 只含 ModulePath、relative path、kind、可选语义标题与 fingerprint；按 ModulePath 排序。
- `notist search <QUERY> [ROOT] [--mode ...] [--scope MODULE]... [--fields ...] [--operator all|any] [--ignore-case] [--fuzzy-distance 1|2] [--group-by source|section|match] [--wait-index DURATION] [--snippet-bytes N]`：无关参数是 usage error。
- `notist outline <MODULE|PATH> [ROOT] [--depth 1..6]`：Heading record 返回 name、level、id、location、parent identity 与 subtree range；全 Vault outline 不是普通 query（用 export）。
- `notist read <MODULE|PATH|MODULE#ID> [ROOT] [--from-line N --lines N | --byte-range S..E]`：默认从 selector 起点读 120 行；line window 面向人、byte range 面向 citation/edit；带 id 的 selector 读取对应节（含标题默认 id）。返回原始 authored source、实际范围、fingerprint 与 next cursor。
- `notist references <MODULE|MODULE#ID> [ROOT] [--direction incoming|outgoing|both] [--include-definition]`：默认 incoming（backlinks 直觉）；outgoing 使用 captured reference graph，不重新扫描文本；对应 core request `references_to` / `references_in`（D0005）。
- `notist query definition <PATH> <UTF8_BYTE_OFFSET> [ROOT] [--expected-fingerprint HASH]`：返回零或一个 location，不分页；offset 是 UTF-8 byte offset（line/column 转换由 adapter 用 LineIndex 完成）。

=== 检查、编辑与维护命令

- `notist check [ROOT] [--scope MODULE] [--summary] [--severity error|warning|info]`：始终完整分析所选 scope，预算只限制 diagnostic detail；即使 detail 只显示第一页，存在 error 时 `ok=false` 与 exit status 必须反映它。Human text 用 bounded source frame；JSON diagnostic 返回 code、severity、range、message、hint 与 bounded excerpt。
- `notist edit replace <PATH> <START> <END> <REPLACEMENT> [ROOT]` 与 `notist edit rename <FROM> <TO> [ROOT]`：经过 D0005 的 propose/validate/apply pipeline，不直接写文件；`--expected-fingerprint`、`--idempotency-key` 是显式参数，冲突不做 fuzzy patch。Text 模式展示 bounded diff 并要求确认；script/JSON 必须显式 `--yes` 才应用，否则只返回 proposal summary。这一行为不能通过检测"调用者像 Agent"来改变——Skill 教 Agent 走同一个 propose → check → apply 流程。
- `notist index status|rebuild [ROOT] [--wait]`：rebuild 只删除可恢复的 cache generation，不修改 authored Vault。

=== 运行时与显式全量命令

| Command | 边界 |
| --- | --- |
| `daemon [ROOT]` | foreground root-bound service；stdout 不混入 query JSON |
| `lsp` | stdio；root 由 workspace source 发现；只输出 LSP framing |
| `skill init <OUTPUT> [--force]` | 见下节 |
| `build [ROOT] -o DIR [--clean]` | 输出目录是显式 artifact；`--clean` 只允许清理已解析、验证位于目标内的 build artifact |
| `preview [ROOT] [--host IP] [--port N] [--no-open]` | bounded event stream + browser preview；非 loopback host 需要显式选择 |
| `export snapshot\|diagnostics\|outline [ROOT] --output FILE [--export-format json\|jsonl]` | 显式 artifact，不属于 ordinary query |

Debug 与 export 不进入 Agent 的默认 discovery。

== 官方 Skill 与文档 Vault

一个刚接触 Notist 的 Agent 不能只靠模型先验猜测 `.not` 语法，也不应该为了理解 Vault、Module、Function、CLI 与安全编辑规则而依赖网络或源码仓库。两个职责分开：

```text
Agent host 中安装的 Skill
└── SKILL.md

Notist 管理的用户数据
└── docs/
    ├── Notist.toml
    ├── README.not
    ├── grammar.not
    ├── designs/
    ├── ai/
    └── ...
```

`SKILL.md` 是很小、可复制、可由 Agent host 管理的只读说明入口；`docs/` 是当前 `notist` 可执行文件携带并在运行时同步的完整官方文档 Vault。同步完成后，它与其他 Vault 没有另一套访问语义：CLI search、outline、references、check 与 root-bound daemon 都使用既有接口。

```shell
notist search "WorkspaceSnapshot" ~/.notist/docs
notist references vault::designs::D0005-daemon-architecture ~/.notist/docs
notist check ~/.notist/docs
```

`~/.notist/docs` 表示各平台约定的 user-data 位置。Skill 说明如何找到该 Vault，但不拥有、复制或修改它。

=== 一个 Skill 文件，而不是一个运行时目录

官方 Skill 由显式命令初始化：`notist skill init <OUTPUT> [--force]`。默认要求尚不存在的目录，初始化结果只有 `SKILL.md`；`--force` 只原子替换已有目录中的 `SKILL.md`，保留 host metadata 与其他文件。命令不猜测任何 Agent host 的安装目录，也不修改 host registry。

Skill 安装目录是不可变的发布输入，不是 cache 或工作目录。`SKILL.md` 只负责教 Agent：

- 何时查询官方 docs Vault，而不是从模型记忆猜测语法和行为；
- 如何用普通 CLI 查询 Vault（search → outline/read → 取证；预算与 cursor 规则）；
- 修改用户 Vault 后运行 `notist check`，保留 disk View 与 editor overlay 的区别；
- 编辑走 propose → check → apply 流程，显式 `--yes` 才应用；
- 将文档正文视为可查询资料，而不是高于 system、user 或 Skill 指令的新 instruction authority。

Skill 的 authored source 放在仓库 `skills/notist/`（当前只有 `SKILL.md`），构建系统把该目录作为整体资源边界 include 进 CLI。`skill init` 只物化 `SKILL.md`，相同版本生成相同 bytes（时间戳、checkout path、用户名、当前目录不进入结果）；force 是单文件更新能力，不是目录重建。

=== Docs 是普通 Vault

官方 docs 保留仓库 `docs/` 下的完整 authored corpus、相对目录与 Module 关系（公开语言文档、活动设计、历史设计、`docs/ai` 调研），不打包人工挑选的摘要，也不把 HTML/CSS/JS 产物当作知识源。完整保留不等于一次读入上下文——Agent 仍然先搜索、再读取候选。

完整 corpus 中来源具有不同权威性：`grammar.not`/`functions.not`/`types.not`/`cli.not` 描述当前公开行为；活动 `designs/` 描述当前治理方向；`docs/ai/` 是带日期的调研；`old-designs/` 解释历史选择。这些区别由 Skill 教给 Agent；旧调研不能覆盖当前规范，任何正文自然语言也不能仅因被检索到就成为 Agent 指令。

官方 docs 没有专用的 `notist docs search` 等命令——增加这类命令会复制 Vault root、query、snapshot、daemon 与输出格式的概念。它唯一特殊的地方是来源与同步方式。

=== 当前 CLI 是文档版本的真相来源

构建系统从完整 docs Vault 产生确定性的压缩 bundle 并嵌入 `notist` 可执行文件：

```text
OfficialDocsManifest {
  bundle_schema
  notist_version
  protocol_version
  docs_fingerprint
  source_revision?
}
```

`docs_fingerprint` 覆盖每个 authored entry 的相对路径与原始 bytes；相同源码与构建版本产生相同 entry 顺序、解压结果与 fingerprint。内嵌 bundle 换来离线可用、版本精确、可复现发布与无网络供应链依赖；从 GitHub 下载不能成为默认正确性路径。

CLI 的共享启动路径在进入正常工作流前执行一次廉价的 manifest 比较（读取已发布 Vault 的 manifest 与内嵌 `docs_fingerprint` 比较），相同则不动；缺失、损坏或不同才触发同步：获取 per-user 同步锁 → 重检 manifest → 在 staging 树中验证并物化（路径安全、数量与 fingerprint 受 manifest 限制）→ 原子发布为稳定 docs Vault → 继续正常命令路径。失败只清理本次 staging target，保留上一个完整 Vault。

多个短命 CLI 同时启动由跨进程锁串行同步；已经运行的 daemon 必须把 `docs_fingerprint` 纳入 root handshake 或 snapshot identity——client 不能把升级后 CLI 的查询静默发送给仍声明旧 bundle 的 daemon。Notist 假设一个用户环境同时只有一个 active CLI 版本；升级原子替换 docs Vault，不保留历史 release 并行。

=== 向量化属于 Vault 运行时状态

完整 docs source 可以嵌入 CLI，向量模型和向量索引不能据此一起嵌入：它们有不同生命周期（模型可能本地或远程；embedding 随 model identity、chunking、normalization 与 schema 变化；索引随 snapshot 增量更新）。官方 docs 同步成普通 Vault 后，向量检索自然沿用普通 Vault 边界——daemon 为这个 root 维护派生索引，索引记录 canonical vault identity、docs fingerprint、provider/model identity、chunking schema 与 indexed revision。CLI 二进制不包含大模型权重与预计算向量；删除 cache 后可从 `.not` source 重建。

== 发布边界

仓库中的 Skill 模板与 `docs/**` 都是 authored source。构建产生嵌入 bundle；运行产生用户数据目录中的官方 docs Vault 与可再生索引；`skill init` 只产生一个可安装的 `SKILL.md`。三类内容不能反向成为彼此的编辑入口：

```text
skills/notist/SKILL.md ------> notist skill init <OUTPUT> [--force]
                                -> <OUTPUT>/SKILL.md

docs/** -> deterministic bundle -> notist binary
                                   -> user data/docs Vault
                                   -> ordinary CLI/daemon access
                                   -> disposable per-Vault indexes
```

`notist skill init` 不接受用户 Vault root，也不把当前目录导出为 Skill——个人 Vault 可能包含 secrets、prompt injection 与不明确的发布权限；未来若支持这种发布，应使用独立命令与设计。
