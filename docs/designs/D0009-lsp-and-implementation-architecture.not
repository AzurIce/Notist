= D0009: LSP and Implementation Architecture

本文承接 [[self::D0005-daemon-architecture]]（daemon 进程形态与 core request 表）与 [[self::D0008-cli-and-skill]]（CLI 命令面与查询契约）。本文定义实现层：先定义分析层——Analyzer 与 WorkspaceSnapshot，它是 LSP 与 CLI 的共同基础，并满足 D0008 的查询契约（stamp、cursor 稳定性、预算）；再定义 LSP 适配与 CLI 适配——它们只是协议层，不成为新的语义层。

动线：分析层（输入 → revision → snapshot → 查询 → delta → 并发）→ LSP 适配（文档同步、position 转换、capabilities、安全）→ CLI 适配（协议投影）。

== 分析层：Analyzer 与 WorkspaceSnapshot

同一份正在编辑的文档，可能同时被 diagnostics、definition、preview、静态构建和 Agent 查询读取。它们不能分别在不同时间重新读盘，再把各自得到的 source、Parse 与 reference graph 拼在一起——否则一个结果中的 byte range 可能已经不再对应另一个结果中的文本。

分析分成三个角色：

```text
filesystem sources + configuration + function schemas
                              |
                              v
                         VaultEngine
                     shared identities/cache
                              |
                 client overlays and View config
                              |
                              v
                        Analyzer View
                    build candidate revision N
                              |
                              v
                    Arc<WorkspaceSnapshot N>
```

- **VaultEngine**：Vault 的共享长期状态（D0005）——磁盘 source catalog、稳定 identity、可复用 cache、派生索引；不假设所有客户端看到相同的未保存文本；
- **Analyzer View**：一个客户端会话的长期分析状态——在磁盘状态上叠加 overlay、配置投影与 revision counter；多个 View 共享 Engine 中以 source content 为 key 的 Parse 与分析缓存，发布彼此独立的 snapshot；
- **WorkspaceSnapshot**：Analyzer 对一组确定输入完成分析后的不可变结果——Vault root 与配置、每个 source 的文本/Parse/LineIndex、Module tree（含 virtual module）、import/binding graph 与 lexical environment identity、属性与引用索引、全部 diagnostics、resolved/typed HIR（或足以确定性派生它的 immutable input）。

消费者捕获 snapshot 后只在其上完成一次查询或渲染；即使 Analyzer 发布了 revision N+1，旧查询的 source、range、Module 与 diagnostics 仍彼此对应。消费者可以选择丢弃旧结果，但不能混合两个 revision 的对象。

=== 输入首先成为 Source Set

Analyzer 不允许每个分析阶段自行读取文件。一次 candidate build 开始时先形成确定的 source set：

```text
SourceInput {
  file_id
  canonical_path
  text
  origin          // disk or overlay
  document_version?
}
```

Overlay 按 canonical path 覆盖磁盘文本；只存在于 overlay 中的新 `.not` 文件也可以进入 source set。Nested Vault 的 source 不进入外层 Analyzer——这一边界在收集输入时完成，而不是在引用解析时补救。

source set 一旦开始分析便保持不变。读取失败、路径逃逸或无法确定 Vault 边界属于 candidate build failure：Analyzer 保留上一个可用 snapshot，而不是发布混合新旧文件的结果。Syntax error、unresolved reference 与 type mismatch 不是 build failure——它们是该 revision 的合法分析结果，进入 snapshot diagnostics，其余内容仍可查询与渲染。

=== 一次 Revision 是整个语义世界

Revision 是 View 内单调递增的不透明编号：表达"这些对象来自同一次已发布分析"，不是 wall-clock time、文件 hash，也不能跨 View、Vault 或 daemon instance 比较。只有完整 candidate 构造成功并成为当前结果时，revision 才对消费者可见；较旧任务即使更晚完成，也不能覆盖较新的已请求状态（publication check 是一致性边界，取消旧任务只是优化）。

Preview generation、LSP document version 与 snapshot revision 是三个不同概念，可以互相记录关联，但不共用一个 counter。

=== 身份分为稳定身份与 Snapshot-local 身份

路径、显示名称与 source range 都不足以独自充当语义对象的身份；另一方面，并非每个语法节点都能在任意编辑后保持永久身份。

- `FileId`：VaultEngine 为 canonical source identity 分配，Engine 生命周期内跨 View 与 revision 稳定；文件删除后不立即复用，普通路径重命名产生新 FileId（除非上层以明确 rename event 保留身份）；
- `ModuleId`：Vault 内的逻辑 Module identity，对应一个 ModulePath；virtual 与 source-backed Module 都有；给 virtual Module 增加 `README.not` 不改变 identity，source move 改变 ModulePath 就是语言层面的 Module rename（D0004）；
- 显式 scope id：可跨 revision、可引用（D0004：显式 `@id` 与标题默认 id）；没有显式 id 的节点只获得 snapshot-local identity：

```text
SnapshotNodeId {
  revision
  file_id
  local_id
}
```

不能通过"相同 byte range"假装无 ID 元素在编辑前后是同一对象。未来若需要稳定的自动 ElementId，必须单独设计匹配与冲突语义——snapshot 不把启发式匹配伪装成语言保证。

=== 分析管线

Analyzer 从 source facts 逐步建立更高层语义，每个阶段只引用同一 candidate 中的对象：

```text
captured sources
  -> Parse + LineIndex per FileId
  -> Module tree and source-to-module mapping
  -> imports, module root bindings, lexical definitions and Function schema environment
  -> Reference graph and Code name resolution
  -> static type checking and resolved calls
  -> resolved/typed HIR
  -> immutable indexes and diagnostics
  -> publish WorkspaceSnapshot
```

Parse 保留源码事实与 recoverable syntax errors；静态分析验证调用与类型，但不为了回答编辑器查询而执行具有外部副作用的插件。求值与成型（D0002）可以按 Module 惰性计算，但必须消费 snapshot 捕获或派生的 HIR，缓存 key 至少包含 snapshot revision、ModuleId、imported module-binding identities 与 Function environment identity。

Analyzer 负责 orchestration，不成为新的语言语义层：Parser、checker、evaluator 的规则由各自组件（语言设计 D0001–D0007）定义；Analyzer 只保证它们看到同一组输入，并把结果组织成可查询的世界。

=== 查询属于 Snapshot

definition、references、completion、hover、outline、diagnostics、渲染与 search 都表现为 snapshot query：

```text
snapshot.module_at(file_id)
snapshot.source(file_id)
snapshot.definition_at(file_id, byte_offset)
snapshot.references_to(module_id)
snapshot.hover_at(file_id, byte_offset)
snapshot.document_symbols(file_id)
snapshot.structured_document(module_id)
snapshot.resolve_reference(url)      // RefTarget，D0004
snapshot.references_in(module_id)    // 正向引用，D0005
```

查询结果携带 snapshot revision，优先返回 FileId、ModuleId、显式 scope id 与 TextRange；Path、ModulePath、标题与 snippet 是展示与序列化信息，不取代核心 identity。查询不修改 Analyzer 输入、不推进 revision；completion 或 search 的 memoization cache 可丢弃，只影响性能。

**满足 D0008 的契约**：SnapshotStamp 的 `source_set_fingerprint` 由 source set 的 identity 与内容确定性派生；source-order 查询的稳定 key（ModulePath、relative path、range、record kind）与 ranked 查询的量化 score key 由 snapshot 内的 identity 保证——Analyzer 是这些稳定性的实现者，cursor 的 `cursor_stale`/`invalid_cursor` 判定建立在它的 fingerprint 上。

=== Delta 从两个 Snapshot 推导

Watcher event 只说明"某个路径可能变化"，不能直接充当语义增量。WorkspaceDelta 由两个已完成 snapshot 比较得出：

```text
WorkspaceDelta {
  from_revision
  to_revision
  added_files / changed_files / removed_files
  added_modules / changed_modules / removed_modules
  changed_references
  changed_diagnostics
}
```

LSP 据此只重新发布受影响文件的 diagnostics，preview 只重建受影响页面，索引只更新对应 Module。Delta 是优化与通知接口；任何消费者都必须能丢弃派生状态并从完整 snapshot 重建——增量日志不是真相来源。

=== 并发与发布

发布后的 snapshot 由 `Arc` 共享，消费者不需要持有 Analyzer 的可变锁：

```text
input change
  -> update Analyzer inputs
  -> reserve revision N
  -> build candidate N outside publication lock
  -> if N is still current, atomically publish Arc<Snapshot N>
  -> notify consumers with Delta(N-1, N)
```

请求开始时读取当前 `Arc<WorkspaceSnapshot>`，随后不追随 current pointer——长查询不阻塞编辑器产生新 snapshot，也不在执行中途改变语义世界。每个 Vault 一个 VaultEngine（D0005），每个需要不同 overlay 或配置投影的客户端一个 Analyzer View；跨 Vault 查询位于更高层（分别捕获 snapshot 再合并，不制造伪全局 revision）。Vault 隔离意味着引用解析、capability 与权限检查默认不跨 Engine 边界。

== LSP 适配

`notist lsp` 是编辑器与 `notist-analysis` 之间的协议适配层：负责文档同步、position 转换、请求路由与结果发布，不重新实现 parser、模块解析、类型检查或求值。它与 CLI 共享同一 daemon 与 snapshot（D0005），不维护第二套 module graph。

=== 进程与传输

stdio transport；stdout 专用于 LSP JSON-RPC（不输出日志、进度或诊断，日志只进 stderr），普通 CLI 的彩色输出完全禁用。adapter 生命周期跟随编辑器进程；按 canonical Vault root 连接该 Vault 的共享 daemon，或在 embedded mode 中承载同一 core service。编辑器集成从 PATH 查找 `notist` 并以 `lsp` 启动；自动下载与版本管理属于扩展分发问题。

=== Vault Discovery 与未保存文档

编辑器提供的 workspace root 只是 worktree，不一定是 vault。初始化时在 worktree 内发现所有 Notist.toml marker（D0004），为每个 root 建立 `VaultState { root, daemon connection, ViewHandle }`；source 归属最近祖先 marker，嵌套 marker 建立新 vault 且外层停止递归；没有 marker 时 worktree root 作为单个 implicit vault。

每个 vault 的 Analyzer View 接收自己的 overlays：

- 已打开文档的 overlay 优先于磁盘内容；
- `didChange` 更新 overlay，不写入用户文件；
- `didSave` 可以保留 overlay，直到收到保存后的最终文本或磁盘确认；
- `didClose` 移除 overlay，重新采用磁盘内容；
- overlay 只进入 source 所属的最近 vault；
- snapshot 内的 source、Parse、TextRange、引用与 diagnostics 必须属于同一 revision；
- definition、references、completion、hover 不跨 vault 查询。

=== 文本同步与 Position

声明 `TextDocumentSyncKind::FULL`：每次 `didChange` 接收完整文本，document store 始终先形成完整 source 再交给 analysis——parser 的增量能力不必与 LSP edit protocol 同步演进。文件系统 watcher 只负责未打开的文件（客户端能力不足时的补充）。

内部 `TextRange` 用 UTF-8 byte offset，LSP `Position` 通常用 UTF-16 code unit。analysis source 附带统一 `LineIndex`：byte offset ↔ line + UTF-8 column ↔ LSP line + UTF-16 column。所有 diagnostics、definition、references、hover 与 symbol range 经过同一个转换实现，覆盖 ASCII、中文、emoji、组合字符、CRLF 与文件末尾位置测试；协商到 UTF-8 encoding 时可用，但 UTF-16 必须始终正确支持。

=== Capabilities

**Diagnostics**：syntax errors、duplicate module、unresolved module reference、引用目标不存在/歧义（Missing，D0004）、import cycle 与 unresolved module binding、名称解析与类型诊断、只依赖 snapshot input/capability 的确定性求值诊断。`textDocument/publishDiagnostics` 在每次 snapshot 更新后为受影响文件发布完整集合，错误消失必须发布空数组清除。Diagnostic 带稳定的 `source = "notist"`、可机器识别的 code、准确 range 与简洁 message；跨文件冲突用 related information 指向另一处定义。

**Go to Definition**：引用路径跳到目标 Module 文件开头（virtual Module 无 `README.not` 时不伪造源码位置）；scope id 与标题默认 id 跳到对应节；函数、变量等符号统一走 `definition_at(file, offset)`，handler 不针对每种符号复制逻辑。

**Find References**：基于 workspace 引用图——指向当前 Module 的引用、同一目标的其他引用、可选包含定义位置；符号统一用 SymbolId，不按显示文本搜索。

**Completion**：由 source context 与语义环境共同决定。`[[...]]` 内补全可达 ModulePath 与 `#id`（显式 id + 标题默认 id）；`vault::`/`self::`/`super::` 后补全合法 segment；`#` 后按 context 补全声明关键字、lexical/imported binding 与内置构造器；call arguments 中补全参数名；属性列表中补全已知属性 key。Module 补全插入相对当前 Module 的自然路径；query 返回协议无关数据（label、insert text、kind、documentation、replace range），LSP 层再生成 CompletionItem。

**Hover**：引用目标的逻辑路径与 source path、Module 是否 virtual、函数签名（参数、类型、default、trailing、capability）、符号的定义位置。Content preview 复用渲染器产物，不在 LSP 层另写渲染语义。

**Document Symbols**：`= Heading` 与显式 `#heading[...]` 都进入 DocumentSymbol（名称来自标题文本，range 是整个 heading，按 level 组成父子层级）；带 id 的节点、函数与变量定义也进入符号树；Fenced Raw 中的示例标题不进入。Workspace Symbol 索引 Module 与带稳定 id（显式 id + 标题默认 id）的文档元素。

=== Syntax 真相与求值安全

编辑器中的 Tree-sitter grammar 只负责即时高亮、折叠与注入；LSP 的语义必须调用主仓库的 parser（CLI、build、preview、diagnostics 与编辑器共享同一 AST 与错误恢复行为，不因 grammar 更新节奏不同产生两套语义）。Tree-sitter 与语义 server 是互补关系。

Hover、completion 与 diagnostics 需要 lexical/import environment、函数签名与 typed HIR，但语言服务器不应为普通编辑操作执行有外部副作用的插件代码：core 与纯 User Function 允许分析并在确有消费者时受控常量求值；plugin schema 只读取声明的 signature/capability；plugin runtime 默认不在 LSP 请求路径执行。未知 Function 仍可依据 schema 提供 completion 与类型诊断，动态结果降级为 unknown，不阻塞整个 workspace analysis。

=== 并发

每个 vault 维护 ViewHandle 并观察发布的 revision；`didChange` → 提交 overlay → 调度 revision N → 发布 snapshot N（仅在 N 仍是最新时）。旧 revision 的慢任务不能覆盖更新结果；读取请求捕获 snapshot 后在其上完成，客户端取消时尽快停止昂贵查询。

== CLI 适配

CLI 是 D0008 命令面与 daemon 之间的最薄适配层：参数解析 → core request（D0005 表）→ daemon 或 embedded service → response 格式化为 text 或 versioned JSON envelope。`--no-daemon` 在当前进程创建临时 VaultEngine 与 disk View，执行相同 core request——embedded mode 不是第二套实现，只是把同一 service object 放在 client 进程中（D0005）。

`daemon`、`lsp`、`skill init`、`build`、`preview`、`export` 各自是固定协议的 adapter（D0008 命令表）；它们的 stdout 边界由协议决定，不混入 ordinary query JSON。CLI 不做任何语义猜测：selector 解析、cursor 验证、fingerprint 检查都在 core 侧完成，CLI 只投影与展示。
