= D0015: Programmable Documents and the Language Core

Notist 文档不是带有若干动态占位符的静态文本，而是一个程序。Module 求值产生可导出的 Value 环境和一个 `Content` 值；renderer、索引器与 Agent 消费的是这个结果，而不是绕过语言再次猜测源码含义。

```not
#let project: String = "Notist"

#let warning(kind: String = "warning", trailing body: Content) -> Content =
  core::callout(kind=kind, body=body)

= #project

#warning[
  A document is a program whose result is Content.
]
```

这里的 `project` 是普通 String binding，`warning` 是普通 Function binding，`[...]` 是普通 Content value。标题、callout 和文字最后都进入同一种 Content 组合关系。语言不能只让这些写法看起来像变量、函数与值；它们必须遵守与其名称相称的作用域、类型和求值规则。

== 语义诚实

Notist 用“语义诚实”约束表面语言与内部模型：一个语法形式如果被称为变量、Function、Type 或 Module，它就必须拥有这个概念通常承诺的操作和定律，而不是只在某一条实现路径上模仿其外观。

这条原则带来几项直接后果：

- `let` 可以绑定任意 Value，而不是一种只能登记 Function 的特殊标记。
- Function 是可传递、可返回、可存入容器并通过表达式调用的 Value，而不是 registry 中的字符串名字。
- lexical scope 由语法嵌套和声明顺序决定，不能通过扫描整篇 source 收集定义来模拟。
- Type 只有在语言中存在相应 Value、构造方式和操作时才进入公开模型。
- Function 的结果只能取决于参数 Value、捕获环境与显式 capability；不能暗中读取 argument 原来使用哪种 literal 写法。
- 语法糖具有可说明的核心语义，但 parser 不依赖运行时 registry 才能识别语法边界。
- Reference、import 与 Content inclusion 是三种不同操作，不能因为都提到另一个 Module 就共享含糊语义。

语义诚实不要求所有方便语法都被机械改写成 Function call AST。它要求无论实现采用 intrinsic、专用 syntax node 还是优化路径，用户都能从稳定的语言模型推导其结果。

== 文档程序的结果

一个 source-backed Module 的求值结果是：

```text
ModuleResult {
  bindings: Environment,
  content: Content,
}
```

`bindings` 供其他 Module 的 Code import；`content` 是该 Module 自己的文档结果。两者来自同一次 lexical evaluation，但不互相冒充。

Module 的普通文字、Content syntax 和 embedded Code 按源码顺序求值。声明可以扩展后续源码看到的环境，可展示项则向 Content accumulator 追加内容：

```text
(environment, content) -- next source item --> (environment', content')
```

因此 Module root 和每个 Content literal 都是一个顺序 lexical block。Binding 在声明之后可见；离开 Content literal 或 Code block 后，其中的局部 binding 不再可见。没有显式递归标记的声明不会因为 evaluator 预先收集名字而获得递归能力。

== Markup 是 Content quotation

Markup 与 Code 不是两个彼此无关的小语言。Markup 是构造 `Content` 的 quotation form，Code 是产生一般 Value 的 expression language：

```text
Markup quotation  [ ... ]  -> Content
Code escape        #code   -> declaration or value insertion
```

Document root 是一个隐式 Content quotation。Code 中的 `[...]` 创建显式 Content literal；其中重新进入 Markup，并在当前 lexical environment 下求值：

```not
#let name = "Alice"

#let greeting: Content = [Hello, #name.]

#greeting
```

Content literal 不是 source String，也不是延迟等待 Function 再次解析的模板。它在表达式求值时读取 lexical environment 并产生结构化 Content。需要延迟求值时，作者显式构造 `fn() -> Content` Function；惰性不由 body delimiter 暗中引入。

`#` 表示从 Markup 进入一个 Code item。Code item 可以是 expression，也可以是 `let`、`import` 等声明。Expression 的结果经过 Content insertion 进入外层 quotation；声明扩展当前 lexical environment 并产生 empty Content。声明是独立的语言构造，不伪装成一个求值结果为 `None` 的普通 expression。

== Content 的组合定律

`Content` 是不可变的有序语义内容。它至少具有 empty value `ε` 和 concatenation `⊕`：

```text
ε ⊕ c = c
c ⊕ ε = c
(a ⊕ b) ⊕ c = a ⊕ (b ⊕ c)
```

普通 Text、Reference、Raw 和 Function 产生的 Element 都通过同一 concatenation 进入 Content。Paragraph、List 和 Table 等后续结构可以由 realization/structuring 建立，但不能改变源码求值得到的 Element 顺序。

Content 的语义值是这条有序 Element sequence，而不是某一种 rope/tree 存储形状。Source range、literal form 和 lowering origin 可以作为 node provenance 或 side table 保留给 diagnostics 与 source map，但语言中的 Function 不能观察它们，concatenation 也不能暴露内部 grouping。这样带有不同 source provenance 的等价 Content 仍遵守同一组 monoid 定律。

Code Value 插入 Markup 使用一条独立且封闭的 conversion relation：

```text
Content -> 原样 splice
String  -> Text Content
None    -> empty Content
```

这不是一般 subtyping，也不是任意 stringify。Int、Bool、Array 等 Value 必须通过显式转换得到 String 或 Content，因而添加新的 Value type 不会偷偷改变文档展示。

== Binding 是 lexical binding

`let` 把 expression 求值得到的 Value 绑定到当前 lexical scope：

```not
#let depth: Int = 2
#let title = "Language Core"

#heading(level=depth)[#title]
```

Binding 默认不可变。名称解析先考虑最内层 lexical binding，再考虑显式 import 和 prelude；同一 scope 的重复 binding 产生 diagnostic。Shadowing 是否发生由源码嵌套决定，不由 native/plugin registry 的插入顺序决定。

Named Function declaration 是 Function literal binding 的语法糖：

```not
#let add(a: Int, b: Int) -> Int = a + b
```

具有与下式相同的 binding 语义：

```not
#let add = fn(a: Int, b: Int) -> Int => a + b
```

普通 `let` 的右侧不能看到正在声明的 binding。递归 Function 使用显式 `let rec`，使递归是否成立能够从 source 和 symbol graph 中直接读出。

== Function 是 Value

Function Value 包含：

```text
FunctionValue {
  signature,
  implementation,       // user body, native callable, or plugin callable
  closure_environment,
  required_capabilities,
}
```

User、core/native 与 plugin Function 共享名称解析、argument binding、调用、返回类型和 first-class Value 语义。实现来源只决定 body 如何执行以及需要哪些 capability，不产生三套相似但不相同的调用语言。

Call 的 callee 是 expression，不限于 qualified name：

```not
#let selected = if compact { short } else { detailed }
#selected(data)

#factory()(data)
```

Function type 记录 callable signature，而不是一个无参数信息的 `Function` marker。Function 可以作为参数或返回值；任何不能按同一规则调用、传递与返回的 callable form，都不公开成 Function Value。

== Argument 与 trailing Content

Positional、named、default 和 trailing Content 都属于同一个 callable signature。Trailing Content parameter 必须在定义中显式声明，不能通过“最后一个 Content parameter”之类的实现猜测获得：

```not
#let warning(kind: String = "warning", trailing body: Content) -> Content =
  core::callout(kind=kind, body=body)
```

因此：

```not
#warning(kind="tip")[Remember this.]
```

与显式提供 `body` 具有相同的 binding 结果。`[...]` 仍然先作为普通 Content expression 求值；Function 若需要延迟 body，应声明 `fn() -> Content` 并接收显式 Function value。

Default expression 在每次调用时求值。它看到 Function definition 的 closure environment 和此前已经绑定的 parameter，不能引用后面的 parameter。这样 default 仍是普通 expression，而不是只能存放少数 literal 的另一种 Value 系统。

一次 Call 先求值 callee，然后按 source order 求值显式 argument 与 trailing Content，再按 parameter order 建立 binding：provided argument 应用计划好的 coercion，缺省 parameter 在 closure 与此前 binding 中求值 default。最后执行 Function body。每个 expression 只求值一次；typed CallPlan 固定 mapping 与 coercion，但不改变这条动态顺序。

== Core Function 与语法糖

Notist 的稳定内容构造器位于不可被 shadow 的 `core` namespace：

```text
core::text
core::heading
core::raw
core::strong
core::list
core::list::item
```

常用 Markup syntax 具有相对于这些构造器的稳定语义：

```text
plain text       ~= core::text(...)
= Title          ~= core::heading(level=1, body=[Title])
*strong*         ~= core::strong(body=[strong])
- item           ~= core::list::item(body=[item])
`raw`            ~= core::raw(text="raw", block=false)
fenced raw       ~= core::raw(text=..., block=true, lang=...)
```

`~=` 表示语义等价，不要求 parser 先创建普通 Call AST。专用 syntax node 可以保留更好的 source range、错误恢复和 editor information，但 evaluation 必须与显式 core constructor 汇合到同一种 Element。

显式 `#heading[...]` 可以通过 prelude 解析到 `core::heading`，也可以被 lexical binding shadow；宿主 heading syntax 始终指向 `core::heading` 的稳定语义。这个区别由 namespace 和 desugaring 明示，而不是由 registry lookup 偶然决定。

`core::...` 在 ordinary lexical lookup 之前由 language version 直接解析；`core` 不能成为 `let` binding、import alias 或 plugin namespace。Bare/prelude name 则只走普通 sequential environment，因此 local binding 或 explicit import 可以 shadow `heading`，却永远不能替换 `core::heading`。

Function 只能观察实际传入的 Value。Inline/multiline、escaped/raw 等 String literal provenance 属于 syntax/source map；若 Raw Element 的 `block`、language 或 layout 不同，这些差异必须成为显式参数或不同 Value，不能让同一个 String value 因书写方式不同而改变 Function 结果。

== Module、import 与 Reference

Vault 中的每个 source-backed Module 都拥有 lexical environment、module binding environment 和最终 Content。文件树决定 Module identity；Code import 从目标 Module 选择 root binding：

```not
#import self::theme::{warning, accent}

#warning[Imported Function values participate in ordinary calls.]
```

Module root 自己声明的 binding 进入 ModuleResult.bindings：

```not
#let warning(kind: String = "warning", trailing body: Content) -> Content =
  core::callout(kind=kind, body=body)
```

`bindings` 只包含由当前 Module root 声明并拥有的 binding。Imported、core/prelude 与 nested binding 不自动成为当前 Module 的 binding；有意转发 imported Value 时，作者创建一个由当前 Module root 拥有的新 binding。

Wiki Reference 只构造指向 Module 或 Annotation identity 的 Content：

```not
See [[self::theme]].
```

它不执行目标 Module、不导入目标 binding，也不把目标 Content 内联到当前结果。Value import cycle 与 Reference cycle 因而属于不同图：前者影响求值，必须有明确的 cycle rule；后者可以合法表达互相链接的知识。

所有 import edge 在 Module evaluation 前静态收集。请求一个 ModuleResult 时，先完成它的 transitive dependency DAG，再求值 importer；同一 evaluation snapshot 中相同 Module/environment key 至多求值一次。互不依赖的 Module 没有可由语言观察的 initialization order，host 可以串行或并行计算；若 capability backend 能观察调度或共享外部可变状态，它必须把这种 effect 明示并退出 pure deterministic cache contract，不能让 import lookup 顺序暗中成为语言状态。

Virtual Module 只提供 namespace tree，不凭空产生 source environment 或 Content。当前语言没有跨 Module Content insertion；import 与 Reference 都不承担这项语义。

== Annotation 是另一种投影

Attributes 描述 source expression，不参与 Value 求值：

```not
#[A programmable document.]@principle,#language
```

同一 source 同时产生两种投影：

```text
evaluation projection -> Content / Value
analysis projection   -> Annotation(scope, id, tags, classes, properties)
```

删除 Attributes 不改变名称解析后的 expression、Type 或 Value。Annotation property 只能使用可静态读取的 metadata literal，不能执行任意 Code，从而保持 indexing deterministic。

Annotation 可以覆盖 Function call 产生的 Content，但它不是 runtime container。需要把 metadata 作为真正 Value 传给 Function 时，作者使用普通 argument；需要标记源码身份时，作者使用 Attributes。这条边界避免“看似参数的 annotation”和“看似 scope 的 Element wrapper”。

== Static 与 dynamic semantics

语言核心使用三组彼此对应的判断：

```text
Γ ⊢ expression : Type
ρ ⊢ expression ⇓ Value
Value ↪ Content
```

`Γ` 是静态 lexical environment，`ρ` 是运行时 environment。Markup sequence 还按顺序传递扩展后的环境和累积 Content：

```text
Γ ⊢ markup ⇒ Content ▷ Γ'
ρ ⊢ markup ⇓ content ▷ ρ'
```

Name resolution、argument-to-parameter mapping、default selection 与 numeric coercion 在 typed representation 中形成一次明确的 plan。Evaluator 消费这个 plan；它不重新根据源码字符串、registry 顺序或 Value variant 猜测静态 binding。Native/plugin boundary 仍验证实际返回 Value，作为不信任外部实现时的防御，不形成另一套语言规则。

Syntax、resolution 或 type error 产生显式 Error node/Type。Error 不是 empty union、None、empty Content 或缺省参数；这些都是合法语言值，不能同时承担恢复标记。

== Capability 与确定性边界

Core Function 和普通 User Function 默认纯且确定。User Function 调用 effectful callee 时，required capability/effect 必须沿 call graph 进入它自己的 callable metadata；它不再伪装成 pure Function。相同 Module source、imported module environments、language version 与显式 input 应产生相同 ModuleResult。

Native/plugin Function 若读取 filesystem、clock、network 或进程环境，必须在 Function metadata 中声明 capability，并由 evaluation host 提供。Static analysis 只读取 signature 与 effect/capability schema，不通过执行插件回答 completion、definition 或 type diagnostics。

这使 vibe 产生的实现可以被限制在清楚的边界内：黑箱可以实现一个经过验证的 Function backend、renderer 或 query adapter，但不能凭局部便利改变 grammar、lexical scope、Type relation、Content insertion 或 Module identity。

== 从 source 到文档结果

Notist 的语言管线是：

```text
source
  -> lossless syntax tree
  -> surface AST
  -> desugared and resolved HIR
  -> typed HIR with call binding plans
  -> evaluation to Value / flat Content
  -> realization and structuring
  -> StructuredDocument
```

Syntax tree 保存所有 source fact、trivia 与 recoverable error；HIR 表达 lexical identity 和语义等价物；typed HIR 是 evaluator 的输入；Content 和 StructuredDocument 不再反向读取 source 来补做 parsing。

WorkspaceSnapshot 可以保存或惰性派生这些阶段，但 snapshot identity 不创造新的语言语义。CLI、LSP、MCP、preview 和 renderer 都是同一 ModuleResult 的消费者，不能各自实现 Function binding、Reference resolution 或 document structuring。

== 这套模型拒绝什么

Notist 不把一组看起来相似的专用机制称作语言统一性。下面这些方案与本设计不兼容：

- 只有 Function declaration、没有一般 Value binding 的 `let`。
- 只保存名字、不能作为普通 callee expression 使用的 Function Value。
- 扫描任意嵌套 source 并把所有 Function 自动提升到 Module scope。
- 由 Function name、plugin registry 或 parameter type 决定 parser 如何寻找 delimiter。
- 暴露 Array、Dict、Function 等 Type，却没有对应 Value construction 与 operations。
- 让 String literal provenance 暗中改变普通 Function 的结果。
- 在 evaluator 中重新扫描或 parse source 来识别 heading、list、table 或 inline syntax。
- 让 import 或 Reference 同时暗中承担 Content insertion。

可编程文档语言并不要求取消领域语法。它要求领域语法建立在可解释的 Value、binding、quotation、Function、Module 与 Content 定律上；方便形式可以很多，核心语义只能有一套。
