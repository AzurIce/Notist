= Design Records

本目录是当前世代的设计文档。目录结构直接表达设计分层，README 负责阅读顺序；文档没有编号 ID，历史追溯交给 Git。

```text
language/  语言层：与实现宿主无关的语义规范
world/    Vault 层：模块、引用与运行边界
host/      宿主层：CLI、LSP、daemon、分析与渲染架构
archive/   已退休的旧设计；当前为空
```

== 阅读顺序

=== 入口

1. [[vault::designs::overview]]：语言概览与最小示例。

=== 语言层

按概念依赖顺序阅读：

1. [[vault::designs::language::markup-surface]]：Markup 表面语法与 scope 书写形态。
2. [[vault::designs::language::code-grammar]]：Code 核心语法。
3. [[vault::designs::language::type-system]]：核心类型系统。
4. [[vault::designs::language::collection-types]]：集合类型草案，尚未进入 current surface。
5. [[vault::designs::language::scope-environment]]：词法作用域与值层 scope。
6. [[vault::designs::language::call-model]]：函数签名与调用模型；rest 部分待实现。
7. [[vault::designs::language::evaluation-model]]：求值模型与元素序列。
8. [[vault::designs::language::shaping-structure-tree]]：成型与结构树。
9. [[vault::designs::language::property-table]]：旁置属性表。
10. [[vault::designs::language::annotation-syntax]]：标注语法。
11. [[vault::designs::language::builtin-constructors]]：内置构造器签名与校验。
12. [[vault::designs::language::syntax-sugar]]：Markup 语法糖规格。

=== Vault 层

1. [[vault::designs::world::boundary-discovery]]：Vault 边界、marker 与发现规则。
2. [[vault::designs::world::module-result]]：Module 与 ModuleResult。
3. [[vault::designs::world::import]]：显式 import 与依赖图。
4. [[vault::designs::world::reference-ref-target]]：引用寻址与 RefTarget。
5. [[vault::designs::world::core-namespace-plugin-boundary]]：core namespace、prelude 与插件边界。

=== 插件层

1. [[vault::designs::plugin-call-reduction]]：插件求值模型：统一 Call + Reduction。

=== 宿主层

1. [[vault::designs::host::analyzer-snapshot]]：分析层与 WorkspaceSnapshot。
2. [[vault::designs::host::query-contract]]：查询契约与 Selector/Citation。
3. [[vault::designs::host::search-retrieval-index]]：Search 模式与检索索引。
4. [[vault::designs::host::daemon-process-views]]：daemon 进程与 Analyzer View。
5. [[vault::designs::host::client-interface-protocol]]：client interface、本地协议与生命周期。
6. [[vault::designs::host::cli-command-surface-skill-distribution]]：CLI 产品边界与官方 Skill 分发；完整逐命令规范和使用文档在 [[vault::cli]]。
7. [[vault::designs::host::lsp-adapter]]：LSP 适配。
8. [[vault::designs::host::html-renderer]]：HTML 渲染。
9. [[vault::designs::host::build-preview]]：静态构建与本地预览。

== 状态属性

每篇 active design 在文件开头用模块属性声明实现对齐状态：

```notist
@![implementation = "aligned"]
```

取值：

| 值 | 含义 |
| --- | --- |
| `aligned` | 设计与当前实现一致 |
| `partial` | 设计是现行方向，但实现尚未完全对齐 |
| `missing` | 设计尚未进入实现 |

当前已知的非 `aligned` 文档：

- [[vault::designs::language::collection-types]]：`missing`
- [[vault::designs::language::call-model]]：`partial`（rest 调用模型未实现）
- [[vault::designs::world::core-namespace-plugin-boundary]]：`partial`（`core::` qualified call 未启用）
- [[vault::designs::host::query-contract]]：`partial`（text-only 投影、`read` citation footer 与 `--exclude-scope` 尚未完整实现）
- [[vault::designs::host::search-retrieval-index]]：`partial`（`--exclude-scope` 未实现）
- [[vault::designs::host::cli-command-surface-skill-distribution]]：`partial`（v3 命令面未落地：`next`/`locate`/`refs`/`definition` 与 text-only 投影待实现）
- [[vault::cli]]：`partial`（完整 CLI 命令设计+使用规范；目标 v3，当前 binary 仍为过渡 surface）
- [[vault::designs::host::lsp-adapter]]：`partial`（Document Symbol 尚未覆盖函数与变量）

位置本身表达治理状态：active 目录中的文档是现行设计；移入 [[vault::designs::archive]] 的文档即退休。Git 是完整历史，不维护编号 ID 或 `Design:` trailer。

== 写作规范

- 一篇文档回答一个概念或一组强耦合概念；目录层级只到 language/world/host 为止，避免深层嵌套。
- 模块路径一旦发布，不因标题或阅读顺序调整而重命名；拆分或合并时才允许移动，并同步更新全部引用。
- 使用语义文件名；需要区分同名退休设计时，archive 中可加日期前缀，如 `2026-08-15-type-system.not`。
- 设计正文解释模型、边界与后果；实现状态只在文件头属性中声明，不写进行文。
- 保持正文干练；判定规则用表，状态迁移用伪代码；每篇可独立 `notist check` 验证。

== 历史编号

本次重构前，设计文档使用 `D0001`–`D0036` 扁平编号。旧编号与语义路径的映射见 [[vault::designs::archive]]。旧提交仍可通过 Git 查看，不需要在当前树中保留编号壳。
