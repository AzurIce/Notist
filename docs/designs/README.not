= Designs

@(type: "user", class: "user")
#[
  Notist 的设计文档。

  目前 Notist 的 crate 架构：

  #mermaid(
    source: r#"""
    graph TD
        cli["notist-cli<br>binary：命令面 / LSP / preview / skill"]
        service["notist-service<br>daemon / transport / watcher / query"]
        analysis["notist-analysis<br>分析层：check / 快照 / Analyzer View<br>语义侧组合根"]
        htm["notist-html<br>HTML target package<br>投影 registry + serializer<br>（内置 native 实现）"]
        phost["notist-plugin-host<br>Wasm 组件宿主<br>运行时加载 vault 内第三方插件"]
        pcore["notist-plugin-core<br>core 标准语义 package<br>（内置 native 实现）"]
        psdk["notist-plugin-sdk<br>插件作者 SDK"]
        evl["notist-eval<br>求值引擎与规约"]
        syn["notist-syntax<br>mode-aware 解析与 AST"]
        model["notist-model<br>共享数据模型"]

        cli --> service
        service --> analysis
        analysis --> phost
        htm --> evl
        phost --> evl
        pcore --> evl
        evl --> syn
        syn --> model
        psdk --> model

        analysis -.->|"预装 core package"| pcore
        service -.->|"HTML target"| htm
    """#,
    )[]
  ]

```text
pipeline/      编译管线：parse → check → evaluate → structure → project
language/      语言层：与实现宿主无关的语义规范
world/         Vault 层：模块、引用与运行边界
host/          宿主层：CLI、daemon、分析与渲染架构
plugin-system/ 插件系统：package、ABI、贡献、信任模型与生命周期
```

== 阅读顺序

=== 入口

1. #<vault::designs::pipeline>：编译管线总览：parse → check → evaluate → structure → project。
2. #<vault::designs::overview>：语言概览与最小示例。

=== 管线阶段

按数据流阅读：

1. #<vault::designs::pipeline::parse>：解析阶段与 mode-aware 语法树。
2. #<vault::designs::pipeline::evaluate>：求值 + Call Reduction 与 call 森林。
3. #<vault::designs::pipeline::structure>：递归成型与结构化重写。
4. #<vault::designs::pipeline::project>：按 name 分发到 target 投影。
5. #<vault::designs::pipeline::plugin-call-reduction>：插件调用在 reduce 阶段如何规约。

=== 插件系统

按模块依赖阅读：

1. #<vault::designs::plugin-system>：PluginSystem 模块入口与 pipeline / world / host 边界。
2. #<vault::designs::plugin-system::package>：插件 package、manifest、core package 与生命周期。
3. #<vault::designs::plugin-system::core>：core package 的内容词表——内置构造器签名与校验。
4. #<vault::designs::plugin-system::abi>：共享类型、WIT 边界与语义 ABI。
5. #<vault::designs::plugin-system::eval-contribution>：语义函数贡献与 FunctionRegistry。
6. #<vault::designs::plugin-system::capability>：插件信任模型与能力系统删除裁决。
8. #<vault::designs::plugin-system::shaping>：成型 schema 贡献。
9. #<vault::designs::plugin-system::projection>：target 投影贡献与 fallback。

=== 语言层

按概念依赖顺序阅读：

1. #<vault::grammar::markup>：Markup 表面语法与 scope 书写形态。
2. #<vault::grammar::code>：Code 核心语法。
3. #<vault::types>：类型系统、调用模型与集合类型的权威规范（逐节标注实现状态）。
4. #<vault::designs::language::property-table>：旁置属性表。
5. #<vault::grammar::annotation>：标注语法。
6. #<vault::designs::plugin-system::core>：内置构造器与 Markup 语法糖的目标语义。

=== Vault 层

1. #<vault::designs::world::boundary-discovery>：Vault 边界、marker 与发现规则。
2. #<vault::designs::world::module-result>：Module 与 ModuleResult。
3. #<vault::designs::world::import>：显式 import 与依赖图。
4. #<vault::designs::world::reference-ref-target>：引用寻址与 RefTarget。
5. #<vault::designs::world::core-namespace-plugin-boundary>：core namespace、prelude 与插件边界。

=== 宿主层

1. #<vault::designs::host::analyzer-snapshot>：分析层与 WorkspaceSnapshot。
2. #<vault::designs::host::search-retrieval-index>：Search 模式与检索索引。
3. #<vault::designs::host::daemon-process-views>：daemon 进程与 Analyzer View。
4. #<vault::designs::host::client-interface-protocol>：client interface、本地协议与生命周期。
5. #<vault::designs::host::cli-command-surface-skill-distribution>：CLI 产品边界与官方 Skill 分发；完整逐命令规范和使用文档在 #<vault::cli>。
6. #<vault::lsp>：LSP 设计（vault 根下的独立目录）。
7. #<vault::lsp::test>：LSP 无头契约测试：黑箱层判据、harness 确定性规则与场景目录。
8. #<vault::designs::host::html-renderer>：HTML 渲染。
9. #<vault::designs::host::build-preview>：静态构建与本地预览。

== 状态属性

每篇 active design 在文件开头用模块属性声明实现对齐状态：

```notist
@!(implementation: "aligned")
```

取值：

| 值 | 含义 |
| --- | --- |
| `aligned` | 设计与当前实现一致 |
| `partial` | 设计是现行方向，但实现尚未完全对齐 |
| `missing` | 设计尚未进入实现 |

当前已知的非 `aligned` 文档：


- #<vault::types>：`partial`（`T?` 归一为 union、期望类型下字面量逐成员 coercion、集合观察 builtin、rest 与实参交错未实现）
- #<vault::designs::pipeline::evaluate>：`partial`（Node 值域已对齐；递归绑定与部分语言细节仍待实现）
- #<vault::designs::plugin-system>：`partial`（完整 native/Wasm App API 与独立 runtime crate 尚未抽出）
- #<vault::designs::plugin-system::package>：`partial`（package 模型与 core/native contribution 已实现；完整 package lifecycle/API 尚未收口）
- #<vault::designs::plugin-system::runtime-composition>：`partial`（WorkspaceSnapshot 与统一 `PluginContribution` 已实现；独立 App/runtime crate 尚未抽出）
- #<vault::designs::plugin-system::eval-contribution>：`partial`（native/Wasm 已走统一 contribution；高层 native Plugin trait 尚未提供）
- #<vault::designs::world::core-namespace-plugin-boundary>：`partial`（namespace policy 已明确；完整 package policy 校验仍需收口）
- #<vault::designs::plugin-system::capability>：`partial`（backend 信任边界已裁定；统一 App composition 尚未收口）
- #<vault::designs::plugin-system::projection>：`partial`（Node 投影规约与 fallback 已实现；trusted/词表收口仍待定）
- #<vault::designs::host::search-retrieval-index>：`partial`（`--exclude-scope` 未实现）
- #<vault::designs::host::cli-command-surface-skill-distribution>：`partial`（命令面已落地为现行 inspect 面；`--scope`/`--exclude-scope` 集合过滤与 text-only 投影仍待实现）
- #<vault::cli>：`partial`（完整 CLI 命令设计+使用规范；命令参考与 binary 已对齐现行 inspect 面，attrs 注解面目标形态仍在推进）
- #<vault::lsp::test>：`partial`（错误规范接线、exit 非零码、cancel、跨文件联动、didSave 待补）

位置本身表达治理状态：active 目录中的文档是现行设计；退休的文档直接移除。Git 是完整历史，不维护编号 ID 或 `Design:` trailer。

== 写作规范

- 一篇文档回答一个概念或一组强耦合概念；目录层级只到 pipeline/language/world/host/lsp/plugin-system 为止，避免更深嵌套。
- 模块路径一旦发布，不因标题或阅读顺序调整而重命名；拆分或合并时才允许移动，并同步更新全部引用。
- 使用语义文件名；同名退休设计的区分交给 Git 历史。
- 设计正文解释模型、边界与后果；实现状态只在文件头属性中声明，不写进行文。
- 保持正文干练；判定规则用表，状态迁移用伪代码；每篇可独立 `notist check` 验证。
