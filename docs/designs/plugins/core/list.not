@!(ai: true, desc: "列表 Item 的当前原型边界")

= list

Markup 的 `-`、`+` 和 `1.` 行首标记生成 list 与 list-item；缩进表示嵌套，数字标记保留起始编号。`/ 术语: 解释` 生成 terms 与 term-item。

HTML consumer 根据 `ordered` 字段选择 `ul` 或 `ol`，根据 `body` 字段渲染列表项。
