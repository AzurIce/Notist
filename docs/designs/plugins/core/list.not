@!(ai: true, desc: "列表 Item 的当前原型边界")

= list

当前原型没有列表投影或自动分组阶段。列表使用普通 List 值表示；需要渲染列表时，由 `.notc` 显式构造 `item("list", ...)` 和 `item("list-item", ...)`。

HTML consumer 根据 `ordered` 字段选择 `ul` 或 `ol`，根据 `body` 字段渲染列表项。
