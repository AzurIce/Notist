@!(ai: true, desc: "属性标注 Item、成型期附着及目标边界")

= 属性标注

Markup 的 `@expr` 在求值时计算 expr，要求结果为 Dict，并产生携带该 Dict 的 annotation Item。表达式使用出现位置的词法环境；属性的值与属性的目标在两个阶段确定。

标准文档成型按照当前内容位置寻找目标，将属性写入目标的语义结果，并消费本次标注。属性表达式无需在成型时再次执行。`@!expr` 的目标由语法确定为当前 Module，可在求值时完成附着。

== 完整段落作为目标

```not
@(label: "说明")
asdjahdjka dhajsd hajk dsha kd
ahdjsdh
```

这里标注的对象是由这些文字形成的完整 paragraph。求值时可以得到 annotation、text、space 等节点；成型识别下一文档单元后，再完成附着。无需先标记首个 text 再将属性搬到 paragraph。

“应用给下一个 Item”中的目标必须由文档语义解释。annotation 本身也是 Item，但连续 annotation 不互相成为属性目标；它们可累积到同一个目标上。

== 显式分组

```not
@(a: 1) @(b: 2) xxx

@(a: 1) #[@(b: 2) xxx]
```

第一例的两个标注应用于同一个后续文档单元。第二例中，外层标注以显式 seq 为目标，内层标注在 seq 内解析；两个目标保持不同层级。具体内部单元取决于 seq 的成型上下文。

seq 的空内容、单元素和多元素都遵守同一分组边界。内部尚未匹配的标注不能无声越过边界，附着到外部节点。

== 合并与观察

连续标注按源码顺序合并，同名键后者覆盖。属性与节点字段独立，不沿树继承。

求值过程中读取 `content.attributes`，只能观察该值已经携带的属性。依赖文档位置的标注尚未附着；完成成型后的查询才能观察其最终结果。`with_attributes(content, dict)` 返回附加了属性的独立值，同名键由 dict 覆盖。成型时附着的标注再次覆盖目标原有的同名属性。

成功附着的 annotation 无须继续占据语义树。其来源位置仍可保留供诊断和调试使用。标注失败时应给出可定位的结果，避免静默丢失元数据。

== 目标与边界

- Flow 中的标注结束此前正在聚合的段落。后续为普通行内内容时，标注以新形成的完整 paragraph 为目标；strong 和自定义行内节点遵守同一规则。
- 下一内容为显式 seq 或独立块时，标注附着到该节点本身；函数名称不参与判断。
- Inline 内容槽内，标注附着到下一个非 space 节点；连续标注仍合并，显式 seq 仍保留边界。
- 普通空白、Unit 和 parbreak 不成为延迟标注的目标；Flow 标注可以跨空行寻找后续内容。空 seq 是有效目标。
- 到 seq 末尾仍未找到目标时产生 `content_constraint` 错误，附加 error Item，标注不越出所属 seq。
- 显式携带属性的 space 保留。被消费的 parbreak 或 annotation 自身若携带属性，产生错误，避免无声丢失元数据。annotation 要应用的 Dict 存在 args.attributes 中，与其自身属性分开。

因此，同一个字面量在 Flow 与 Inline 内容槽中的内部标注可指向不同种类的语义单元，但显式分组内外始终保持独立目标。
