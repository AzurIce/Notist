@!(ai: true, desc: "section Item 的当前原型边界")

= section

`section` 是 HTML consumer 识别的 Item 名称，不是特殊语言构造。`.notc` 可以显式构造 `item("section", (body: ..., ...))`；当前 HTML renderer 直接把它映射为 `<section>`，不计算 label 链、outline 或树深。
