@!(ai: true, desc: "当前原型的内置函数和 HTML 内置 Item")

= Core

Core 是运行时直接提供的一组普通函数。它没有独立 registry，也不拥有特殊的 rewrite 或 handler 阶段。扩展 package 可以用 `.notc` 定义同样的函数形态。

== 函数

| 名称 | 形式 | 结果 |
| - | - | - |
| `item` | `item(name: String, args: Dict)` | Item |
| `text` | `text(value: String)` | 文本 Content |
| `error` | `error(message: String)` | Error Content |
| `is_error` | `is_error(value: Any)` | Bool |
| `recover` | `recover(value: Any, fallback: Any)` | Value |
| `str` | `str(value: String\|Int\|Bool)` | String |
| `len` | `len(List\|Dict)` | Int |
| `get` | `get(Dict\|List, key: String\|Int)` | Value 或 Error Content |
| `concat` | `concat(List)` | Content |
| `map` | `map(Function, List)` | List |

== Item 与 HTML

`item` 只负责构造数据，不验证是否存在对应的组件。HTML consumer 对少数名称使用原生 HTML：`paragraph`、`section`、`strong`、`em`、`list`、`list-item` 和 `heading`。其他合法名称映射为 `notist-<name>`。

Item 字段序列化为 `data-<field>` 属性；Content 字段由组件通过 `renderContent` 显式渲染。字段不能序列化或名称不能成为 HTML 属性时，由渲染器产生 `notist-error`。

== 当前边界

段落分组、列表投影、section label 身份、annotation、Target、outline 和引用索引不属于当前 core 运行时。`components/` 中的 Web Component 是 package 资源，不是 core 函数注册项。
