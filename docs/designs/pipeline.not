@!(ai: true, desc: "语言构建、文档成型与消费者投影的阶段及责任边界")

= 构建管线

管线由语法解析、求值、标准文档成型和消费者投影组成，查询与展示共享成型结果。

== 三种处理

*语言求值*执行表达式、调用函数，构造值。*文档成型*解释内容的组合规则，形成段落、确定属性目标并检查内容约束。*消费者投影*将文档映射为 HTML、排版结果、纯文本或其他形式。

这些处理都可以在一次文档构建中完成。“整个语言在构建时执行”描述执行时机；“静态类型检查”描述检查是否先于被检查代码的执行。构建时求值仍有运行时状态、参数绑定和实际值检查。

```text
Markup / Code 源码
    ↓ 解析：保留表达式、词法边界和源码位置
语法树
    ↓ 求值：名称解析、参数绑定、实际值检查、普通函数与内容表达式执行
原始 Content：全部由 Item 组成，含组合及控制节点
    ↓ 标准文档成型
语义 Content：全部由 Item 组成，属性归属与文档单元确定
    ↓ Item 索引、label、引用解析、结构查询
可查询文档
    ↓ 消费者展开、渲染或布局
HTML / 几何布局 / 其他输出
```

类型系统的保证范围定义于 [[vault::designs::value_and_types]]。模块加载、导入、引用查询可以按需触发其他模块的构建；图中顺序描述一个输出的依赖关系。

== 同一种节点表示，不同阶段的不变量

原始内容与语义内容使用 [[vault::designs::item]] 定义的同一种 Item 表示。Text、Space、Seq、Paragraph、Link、Error 和属性标注均为 Item。

求值输出允许组合标记、尚待附着的属性和未分段的文字。标准文档成型消费这些信息，产出满足所选内容模型的文档。宿主 `Evaluation.raw_content` 保存原始值，`Evaluation.content` 保存语义结果；模块导出绑定保存求值产生的原始值。

成型是有上下文的树变换，可以增加 paragraph、处理边界标记、改变组合关系、附着属性或产生诊断。文字通常保留在 paragraph 内。输入输出同型不保证变换终止，也不意味着所有中间节点都应消失。

模块语义输出必须具有可索引的确定结构。内部可惰性计算，但查询与消费者需要观察同一份语义结果。布局结果可以使用另一种表示，例如带尺寸和坐标的框。

== 抽象树与文档模型

通用 Item 树可以仅保存 text、space、seq 和自定义数据。语言构造这些值时，无须为所有节点规定 inline 或 block。

标准文档模型进一步解释哪些内容能参与同一段落、哪些位置允许什么内容，以及列表项的主内容范围。段落也服务于引用、检索、摘要和编辑操作，因此成型具有共享的文档语义。

例如 `@A a X b` 中，X 的参与方式可能使目标成为一个完整段落，也可能使文字分成两个段落。如果各 renderer 独立决定该边界，属性 A 的目标范围就可能随输出方式变化。标准文档管线在消费者投影前确定这些边界，查询与展示共同使用结果。

将属性绑定到独立逻辑范围，再由各消费者自行分段，是另一种可行模型，但需要另外定义范围身份、查询和引用规则，未纳入本文目标。

== 成型的输入与上下文

`DocumentRules::form` 接收 Content，返回内容及诊断。成型上下文使用 Flow 和 Inline：Flow 聚合段落，Inline 校验并保留行内序列。paragraph 的 body 使用 Inline；未声明的字段作为数据保留。公开模型接口定义于 [[vault::designs::plugins::core]]。

对同一 Content 的不同使用可以给出不同上下文。成型应产生新值，或使用不可变共享及写时复制，避免改变原始值以及其他出现位置。缓存不能只以内容地址为键，还需区分上下文和规则快照。

相同规则下，成功成型的结果具有幂等性：再次成型不增加 paragraph，不重复附着属性。根 seq 和显式 seq 保留；自动 paragraph 的 body 也用 seq 保存。

== 规约与普通函数

普通函数在求值阶段执行，返回自己的结果。只有显式构造的 Item 才成为节点。函数名和参数承担计算含义；节点名和字段承担结果结构含义。

成型执行确定的段落聚合、属性附着和内容约束，不回调用户函数。规则依据本次求值加载的元素声明；遍历上限为 128 层、100000 个节点，超过预算产生内容约束错误。

任意用户重写规则是独立扩展，需要规定调用协议、顺序、终止预算、诊断来源和属性保留规则。同型的输入输出不能保证重写收敛。当前管线不含布局反馈、计数器迭代或多轮重排。

== 源码与节点身份

文档结构无须保存每一次函数调用的外壳，也无须依赖完整的调用来源树。错误定位、跳转及调试仍需要源码范围和必要的调用位置。合成 paragraph 等节点应能说明对应的内容范围。

一个值被重复嵌入后形成多个出现位置。索引身份属于本次文档中的出现位置，不能由共享指针、创建位置或函数名代替。原始树和语义树的索引属于不同阶段，跨阶段对应关系如有需要应单独表达。

== 共享构建入口

查询、package check、预览与调试器均调用同一求值及成型入口。原始树供调试和值传递使用；label 匹配、输出链接和文档查询使用语义树。成型约束错误的诊断代码为 `content_constraint`，调试协议标记为 form 阶段。
