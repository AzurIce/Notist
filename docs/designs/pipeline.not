@!(ai: true, draft: true, desc: "管线设计：三真相、指纹派生 DAG、八个派生、查询分层与增量")

= 变换路径

本文从理念出发设计管线：一份源文本，三层真相，被两组读者按需取用。三条公理：文档即程序，一次求值产出权威产物，产物有三张面孔（结构、绑定、内容与属性）；身份、引用、旁置属性是 Vault 的本体，不是渲染副产品；人与 Agent 两个读者，消费语法与延迟要求不同。

== 三真相

| 真相 | 载体 | 内容 | 铸造者 |
| - | - | - | - |
| 源真相 | CST | 写了什么：无损、带 range、warn-only 恢复 | parse，前端可换 |
| 意图真相 | PlannedForest | 想表达什么：调用森林、绑定结论、身份声明、引用点、属性声明 | plan |
| 物质真相 | ModuleResult | 成为了什么：求值后内容、成型树、绑定值、生效属性表 | reduce、shape、assemble |

三条裁决：

- 身份在意图层铸造：标题链、显式 id、scope 声明的 Item 在 plan 期获得身份；插件的成型变换不能铸造或销毁身份。结构查询（outline、refs、graph）从此零求值。
- 诊断分层：源层语法诊断、意图层绑定与引用诊断、物质层求值诊断，逐层发布。
- 产物完备性：任何消费者的任何问题必须能从某层真相回答；查询需要重扫源文本即为产物缺陷。

== 派生 DAG

每个派生是纯函数，以输入指纹为键，至多计算一次、按需发生。恰一次是纪律，至多一次是定理：打开千模块 Vault 请求 outline，求值次数为零；只有 read、render、深查询沿依赖边 force 具体模块。

```text
text → parse → CST → plan → PlannedForest → link → 引用图、导入种子
                                        │
      registry 加导入绑定值加预算 → reduce → ContentForest
                                        │
                          shape → 成型树 → assemble → ModuleResult
                                                              │
                                  derive → 身份表、引用图、符号、属性索引
                                                              │
                                          project → HTML 与未来投影
```

- parse：文本到 CST，前端可换（not、未来 md），全函数
- plan：糖溶解、名字解析、单一绑定（CallPlan）、身份铸造、引用点与属性声明提取、类型诊断
- link：workspace 级引用解析与 import 组织。两级依赖：A 的 plan 只需被引模块的导出签名指纹，A 的 reduce 才需绑定值；环产生诊断与降级产物
- reduce：严格调用、confluent 不动点、统一分发（core 非特权）、预算进指纹——同输入同预算同产物，部分产物可复现
- shape：同型重写（成型），不变换身份
- assemble：ModuleResult 装配——bindings、content、生效属性表（三层继承在此物化）、分层诊断、指纹
- derive：workspace 索引（身份表、引用图、符号、属性索引），全部为记忆化查询而非 pass
- project：面向人的投影

== 查询面

查询与管线同刻设计，每条查询声明消费的真相层：结构类查询（outline、refs、absence 证明）永不触发求值；内容类查询（read、render）才 force 具体模块。这是 Agent 读者的延迟保证，也是 bench 教训（发现层、寻址工效）的管线侧回应。

== 增量

粗粒度是指纹 DAG 的结构赠品：改一个字符，该文件重 parse 与 plan；导出签名指纹未变则 importer 零重跑，变才级联。自建指纹基底（模块粒度、内容寻址、依赖边记最小指纹），同时是浏览器同步协议的地基——端侧拿指纹加 delta 即可按需派生。

细粒度在 derive 与 project 层引入 comemo 式约束记忆化（函数只复查它看过的一部分输入），采纳前以 spike 验证冷启动、编辑后延迟、长会话内存。salsa 排除：其子模块粒度增量现阶段不要。

五条裁决：

- 值域规范哈希：f64 负零与 NaN、Dict 迭代序必须规范化，指纹确定性是确定性的延伸
- 依赖边最小指纹：importer 依赖导出签名指纹而非整模块指纹
- registry 指纹：插件身份、wasm 哈希、贡献集合进 plan 与 reduce 的键
- 预算档进键：同输入同预算同部分产物
- 长会话逐出与隔离：daemon 记忆增长策略、多客户端隔离、import 环重入守卫

增量正确性可测：派生执行计数器——改一个字符，断言哪些派生跑了哪些没跑。

== 理念与管线

| 理念 | 管线性质 |
| - | - |
| 文档即程序 | 单一调用森林、统一分发 |
| 三张面孔 | 产物分层，各层独立派生 |
| 引用是身份 | plan 期铸身份，引用图一等，absence 是证明 |
| 标注旁置细到区间 | 声明层在 plan，生效层在 assemble |
| 两个读者 | 三真相加查询分层延迟保证 |
| warn-only | 每个派生全函数，诊断分层 |
| IR 中心多前端 | parse 可换，三真相不依赖具体语法 |
| 浏览器可运行 | 纯派生加指纹缓存，天然增量与端侧计算 |
| 确定性与预算 | 预算进指纹，部分产物可复现 |

== 与既有裁决的关系

存活：值域无 pending、严格调用、单一绑定、confluent 规约、身份词汇在 model、lossless CST。值域与类型细节见 #<vault::designs::value-and-types>。

升级：不变量由每模块每快照恰一次求值改为每个派生按指纹至多计算一次（更强，覆盖所有层）；身份推导从成型树改为 plan 期铸造。
