= D0007: Surface Language and Core Grammar

本文定义语言的表面语法。承接：[[self::D0001-language-overview]]（双模式与表达式边界）、[[self::D0002-evaluation-model-and-type-system]]（求值模型、类型与调用）、[[self::D0003-builtin-functions-and-syntax-sugar]]（语法糖）、[[self::D0004-vault-module-and-scope]]（Module 与引用寻址）、[[self::D0006-scope-and-annotation-syntax]]（scope 形态与标注）。

Markup 模式只定义最小骨架（文本流、段落分隔、`#` 嵌入、转义）——其余一切 Markup 构造都是语法糖或标注，本文给出指向 D0003 / D0006 / D0004 的引用索引；Code 模式是本文的主体，定义完整的字面量、运算符、表达式、声明与类型语法。

动线：lexical context 总览 → Markup 最小骨架 → Code 核心语法 → 字面量载荷 → 引用索引。

== 两种 lexical context

语义模式只有两个，递归 quotation：

```text
Markup —— 产生 Content 片段（文档根；Code 中的 [...] 回到这里）
Code   —— 产生 Value（Markup 中的 # 进入这里；{...} Code block）
```

不存在第三种语义 context。围栏代码块与 String 字面量的内部是词法层的字面量载荷（见"字面量载荷"一节），不参与 quotation，也不产生语义对象。

== Markup 最小骨架

Markup 由文本流、段落分隔、块构造与转义组成：

- **文本流**：连续文本按粒度规则成为 `Text` 元素（D0002 属性表一节）——表达式嵌入、标注、元素构造、段间分隔处切开；
- **段落分隔**：空行产生 `Parbreak`；
- **`#` 嵌入式表达式**：边界规则见 D0001（Unicode 标识符字符集、`;` 与括号强制终止、句号规则、空格是文本）；
- **块构造**：`[...]` Content literal、`{...}` Code block、`#[...]` 手动 scope——见 D0006 scope 语法形态；
- **转义**：`\` 转义 `#`、`[`、`]`、`@`、`\` 本身；转义不产生额外输出（`\#` 输出一个 `#`）。

其余一切 Markup 构造（标题、围栏、分割线、列表、引用、强调等）都是语法糖或标注，见"引用索引"。

== Code 核心语法

=== 标识符

标识符由 Unicode 字母（含中文）、数字、`-`、`_` 组成，首字符不能是数字或 `-`。`-` 属于标识符字符：`my-var` 是一个名字，不是减法——想写减法要加空格（`my - var`）。标识符边界与 `#` 表达式规则见 D0001。

=== 字面量

```text
Int       十进制数字序列
Float     数字 + `.` + 数字（`0.5`；`.5`、`1.` 不合法，写 `0.5`、`1.0`）
Bool      true | false
None      none
String    见下（四种形态）
Content   [...]（回到 Markup）
```

负数是一元 `-` 运算符（字面量本身无符号）。科学计数法暂缓。

String 字面量四种形态（继承旧设计 D0010 的决定）：

```text
"..."          转义 inline
"""..."""      转义多行（opening 三引号后立即换行才进入多行形态）
r#"..."#       raw inline（hash 级别可升级：r##"..."##）
r#"""..."""#   raw 多行（同样要求 opening 后立即换行）
```

- 转义集：`\"` `\\` `\n` `\r` `\t`；raw 形态不做任何转义处理；
- Multiline 形态的 opening framing newline 不属于值；closing 前紧邻的一个 framing newline 被裁掉；除此之外不裁空白、不 dedent；
- 四种形态产生同一个 `String` 类型——form/style 是字面量 provenance，不影响值语义（D0002）。

=== primary 与运算符

primary：字面量、名字、括号表达式、调用、Content literal、Code block、lambda。

运算符（有限集合）：

```text
一元：-    not
乘除：*    /
加减：+    -
比较：<   <=   >   >=
相等：==   !=
逻辑：and  or（短路）
```

优先级从高到低：一元（`-`、`not`）→ `* /` → `+ -` → 比较与相等 → `and` → `or`。

- `-a * b` 是 `(-a) * b`；
- `not a == b` 是 `not (a == b)`；
- `a and not b` 是 `a and (not b)`；
- 相等定义在 None/Bool/Int/Float/String 上（同族值比较，跨族不等）；Content 与 Function 没有自动相等（D0002）。

=== 表达式

**调用**：`callee(实参)` 加可选 trailing block `[...]`——绑定、默认值、coercion 与调用点检查见 D0002。

**if**：表达式，typst 风格：

```not
#if 条件 { ... } else { ... }
#if 条件 [内容] else [内容]
```

- 分支是 Code block（求值为值）或 Content block（求值为内容），复用既有块语法；
- `else` 可省略：两个分支都存在时，类型为两分支类型的 join；**无 `else` 时**，条件为假产生 `None`——Code block 分支的类型 `T` 提升为 `T?`（Content 分支无 else 时类型仍是 `Content`，空内容合法）；
- 条件必须是 `Bool`。

**lambda**：`(形参表) => 表达式`：

```not
#let double = (x: Int) => x * 2
#let add = (a: Int, b: Int) => a + b
```

- 形参必须声明类型（与命名函数一致，D0002）；返回类型由 body 推断；
- 参数列表括号必写（单参数省略括号暂不支持）；
- `let f(形参表) -> R = body` 定义糖与 `let f = (形参表) => body` 等价（定义糖的返回类型声明与推断结果一致）；
- lambda 是普通 Function 值：可以绑定、传递、调用（D0002）。

=== 声明

**let**：`let name = 表达式` 或 `let name: T = 表达式`——右侧先在旧环境中求值，绑定对后续可见；类型标注可选（未标注时由右侧推断）。

**函数定义糖**：`let name(形参表) -> R = body`（≡ lambda 绑定，见上）。形参语法与 D0002 一致：带 `=` 的形参是 named 可省略，不带的是 positional 必填。

**import**：见 [[self::D0004-vault-module-and-scope]]。

**模块属性**：`@![...]` 见 [[self::D0006-scope-and-annotation-syntax]]。

（递归绑定暂缓：`let` 的右侧看不到正在声明的名字，递归函数待引入 `let rec` 时再设计——见 D0002 静态检查。）

=== 类型语法

```text
Type
├─ None / Bool / Int / Float / String / Content
├─ T?                    # 可空（D0002）
└─ fn(形参表) -> R       # 形参：名字: 类型，带 = 表示可省略（D0002）
```

标注位置：`let name: T = ...`、函数形参、函数返回类型、lambda 形参。

（Content 形状 refinement（Inline/Block）是类型系统的未来扩展，暂不进入本语法。）

== 字面量载荷

围栏代码块（```` ``` ````）内部与 String 字面量内部是词法层的字面量载荷：lexer 以"扫到闭合围栏/闭合引号"为唯一状态，不识别任何 Markup/Code 构造。它们不是语义 context，不参与 quotation。围栏的完整语法（opener 长度、闭合规则、lang 标注、error recovery）见 [[self::D0003-builtin-functions-and-syntax-sugar]] 的 raw 节；编辑器按载荷 range 做语言注入。

== 引用索引

Markup 中除最小骨架外的一切构造，定义处如下：

```text
= Title / == Title        → D0003 heading 节（含默认 id）
```lang ... ```            → D0003 raw 节
---                       → D0003 rule 节
- A / + A                 → D0003 item 节
[[target]]                → D0003 ref 节
*text* / _text_ / __text__ / ~~text~~  → D0003 strong/emph/underline/strike 节
@id,#tag,.class,key=value → D0006 标注
@[...] / @![...]          → D0006 标注
[...] / {...} / #[...]    → D0006 scope 语法形态
#scope-id 引用寻址        → D0004
# 表达式边界              → D0001
```
