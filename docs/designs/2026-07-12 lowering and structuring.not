#heading(level=1)[Lowering and Structuring]

Notist 使用两步语义管线：Lowering 负责把 syntax 转换为线性的语义 Element sequence，Structuring 再把这条 sequence 组合成 Paragraph、List 和独立 Block。

#raw(lang="text")![
source
-> syntax Parse
-> lowering and function evaluation
-> flat Content / Element sequence
-> structuring
-> StructuredDocument
]!

这两步都属于 `notist-eval`。它们共享 Content 和 Element model，但职责不同：Lowering 理解语法与函数；Structuring 只理解已经产生的 Element 排列。

#heading(level=2)[Syntax Parse]

`notist-syntax` 保留源码事实和精确 range，包括：

- WikiLink。
- TransparentScope。
- Content Call 与 Raw Call。
- 函数名、原始参数 range、raw delimiter level、body range、BodyForm 和 Attribute。
- recoverable SyntaxError。

Syntax AST 不尝试成为最终文档结构，也不执行函数。它必须适合 diagnostics、formatter、Tree-sitter/LSP 对齐和增量解析。

#heading(level=2)[Lowering]

Lowering 将 syntax 解释为语义模型：

- 普通文本变为 Text Element。
- WikiLink 变为 Reference Element。
- 空行变为 Parbreak。
- 语法糖变为对应的内置 Element。
- Transparent Scope 产生 Annotation，并继续 lowering 内部内容。
- Call 查找 Function，准备 Content 或 RawSource body，并执行函数。

结果是 flat Content：

#raw(lang="text")![
Content
└─ Vec<ElementNode>
   ├─ Text
   ├─ Reference
   ├─ Heading
   ├─ ListItem
   ├─ Raw
   ├─ Custom
   └─ Parbreak
]!

“flat”只表示顶层按执行顺序存放 Element，不表示 Element 内部不能有树。Heading、Strong、Quote 或 Custom Element 仍然可以拥有嵌套 Content。

#heading(level=2)[Content Call]

#raw(lang="not")![
#quote[Inside [[self::target]] and #heading(level=2)[Title].]
]!

Content Call 的 body 是普通 Notist。Lowering 先递归得到 Content，再调用函数：

#raw(lang="text")![
Call(mode = Content)
-> lower body
-> CallBody::Content(Content)
-> Function::call
-> FunctionOutput
]!

函数不需要自行重新调用 parser。内部产生的 Annotation 和 diagnostics 在同一 lowering 过程中保留。

#heading(level=2)[Raw Call]

#raw(lang="not")!![
#raw(lang="rust")![fn main() {}]!
]!!

Raw Call 的 body 对宿主 parser 不透明：

#raw(lang="text")![
Call(mode = Raw)
-> RawSource { text, range }
-> Function::call
-> FunctionOutput
]!

Function 可以保留源码、交给其他语言 parser，或显式使用 FunctionContext 将它重新解析为 Notist。重新解析是函数主动选择的运行时行为，不是普通 Content Call 的默认路径。

如果函数不存在，lowering 产生 unknown function diagnostic，并创建可恢复的 UnresolvedCall：Content mode 保存已经 lowering 的 Content，Raw mode 保存原文。

#heading(level=2)[Transparent Scope]

#raw(lang="not")![
Before #[annotated [[self::target]]]@concept,#important after.
]!

Lowering 移除 `#[`、`]@...` 语法边界，继续输出内部 Text 和 Reference，同时记录：

#raw(lang="text")![
Annotation {
    range: body_range,
    metadata: { id, tags, classes, properties },
}
]!

Annotation 不必成为 Element 父节点，因此可以跨越 Element 或 block 边界。Structuring 改变 Content 的组合方式时，源码 range annotation 仍保持稳定。

#heading(level=2)[Why Structuring Is Separate]

Lowering 后的 Element 只表达执行顺序：

#raw(lang="text")![
Text("Intro")
Parbreak
ListItem("A")
ListItem("B")
Heading(level=2, ...)
Text("After")
]!

Structuring 根据 Element 的 inline/block 性质和相邻关系建立文档结构：

#raw(lang="text")![
StructuredDocument
├─ Paragraph(Text("Intro"))
├─ List
│  ├─ ListItem("A")
│  └─ ListItem("B")
├─ Heading(level=2, ...)
└─ Paragraph(Text("After"))
]!

如果 parser 直接建立最终 Paragraph/List 树，那么函数、用户代码和未来控制流动态产生 Element 后，还需要复制一套组合规则。两步设计让所有静态语法和动态函数输出都走同一个 structuring pass。

#heading(level=2)[Structuring Rules]

第一阶段规则可以保持简单：

- 连续 inline Element 组成 Paragraph。
- Parbreak 结束当前 Paragraph，但不进入最终 document。
- 相邻 ListItem 组成 List。
- Heading 和 block Raw/Custom 作为独立 Block。
- block Element 到来前先 flush 当前 Paragraph 或 List。
- diagnostics 与 Annotation 原样保留。

Structuring 不重新读取源码、不再次执行 Function，也不负责名称解析或类型检查。它只处理已经 lowering 完成的 Element sequence。

#heading(level=2)[Ownership]

#raw(lang="text")![
notist-syntax
  source ranges, Parse, calls, scopes, syntax errors

notist-model
  Content, Element, Annotation, StructuredDocument

notist-eval
  Function runtime, lowering, structuring, eval diagnostics

notist-analysis
  modules, names, types, cross-document semantics

notist-lsp / CLI
  expose diagnostics and navigation to tools
]!

Lowering 与 Structuring 暂时放在同一个 `notist-eval` crate 足够。只有当编译时间、依赖边界或复用场景真正要求时，再考虑拆分 crate。

#heading(level=2)[Summary]

Lowering 回答“语法和函数按顺序产生了哪些 Element”，Structuring 回答“这些 Element 如何组成可消费的文档块”。Content Call 在 lowering 前递归处理 body，Raw Call 保留 RawSource，Transparent Scope 独立产生 Annotation。这个分层让解析、类型系统、插件执行和最终文档结构各自拥有清晰边界。
