= D0004: Core Functions and Syntax Sugar

Notist 的内容构造能力不是一组只有 evaluator 知道的 Processor。Heading、Raw、Quote 等 constructor 作为真正的 Function Value 暴露在稳定 `core` namespace 中；User Function、native Function 与 plugin Function 共享 [[vault::designs::D0006-type-system]] 定义的 call model。

```not
#core::heading(level=2)[Language model]

#let make_title = core::heading
#make_title(level=3)[First-class Function]
```

常用 Markup syntax 可以更简洁，但其语义必须与这些 core constructor 汇合到相同 Element。方便写法不创造只有 parser 才能产生的假 Element，Function API 也不靠重新解释 source 模仿宿主语法。

== Core Namespace

`core` 是语言提供且不可被 lexical shadow 的 namespace。它保存稳定的基础 Value：

```text
core::text
core::heading
core::raw
core::quote
core::strong
core::list
core::list::item
...
```

普通 prelude 可以把其中常用 Function 以 `heading`、`raw`、`quote` 等短名带入 Module scope。短名遵守普通 lexical shadowing；qualified `core::heading` 始终指向语言定义的 constructor。

Resolver 在 ordinary lexical lookup 之前按 language version 解析 `core::...`。`core` 不能成为 local binding、import alias 或 plugin namespace；extension 只能通过自己的显式 namespace 和普通 import/prelude 进入环境。

这个区别让两种需求同时成立：

- `= Title` 等宿主语法具有稳定含义，不因 local/plugin binding 改变；
- 显式 `#heading[...]` 是普通 name resolution 的结果，而不是 parser 写死的动态特例。

== Element Constructor

Core content Function 接收已经求值、完成 binding 的 Value，并返回 Content：

```text
heading(level: Int = 1, trailing body: Content) -> Content
raw(text: String, lang: String? = none, block: Bool = false) -> Content
quote(attribution: Content? = none, trailing body: Content) -> Content
```

Trailing parameter 在 signature 中显式声明。User Function 使用相同的 declaration position：

```not
#let note(kind: String = "note", trailing body: Content) -> Content =
  core::callout(kind=kind, body=body)
```

Function 只观察 Value。它不读取 argument source、不判断 String 原先是 raw/escaped 或 inline/multiline literal，也不按 call site 的 delimiter 选择另一套 body grammar。

== heading

显式 constructor：

```not
#core::heading(level=1)[Introduction]
#core::heading(level=2)[Design with *emphasis*]
```

宿主语法：

```not
= Introduction
== Design
```

二者产生相同 Heading Element。Syntax frontend 可以保留专用 Heading node 与精确 source range，surface lowering 则赋予它 `core::heading` 的 stable intrinsic identity。实现不必伪造 source 中不存在的 Call tokens，也不能让一个被 shadow 的 `heading` binding 改变 `=`。

Heading constructor 校验 level 和 body/document constraint。若 heading 只允许 inline body，这个限制由 constructor validation 或未来的 refined Content Type 明确表达，不通过 renderer 默默丢弃 block child。

== raw

显式 constructor：

```not
#core::raw(text="cargo test")
#core::raw(text=r#"fn main() {}"#, lang="rust")

#core::raw(
  text=r#"""
fn main() {}
"""#,
  lang="rust",
  block=true,
)
```

`block` 是 Raw Element 的普通 semantic argument。Multiline String 仍然只是 String；computed String 与 literal String 传入相同参数时必须产生相同结果。宿主 backtick/fence 将 layout information 显式带入 core constructor 语义：

```text
`inline`      ~= core::raw(text="inline", block=false)
fenced source ~= core::raw(text=source, lang=tag, block=true)
```

这样 String type 不携带隐藏 layout，Function 也不需要 literal provenance 才能执行。Provenance 仍保留在 syntax/source map 中供 diagnostics、formatter 和 language injection 使用。

== quote

```not
#core::quote[
First paragraph with [[self::source]].

Second paragraph with #core::heading(level=3)[Nested content].
]
```

Quote 接收已经求值的 Content Value，并包装为 Quote Element。它不接收 RawSource 再执行一轮 parser。Attribution 是真正改变 Quote Element 的 argument：

```not
#core::quote(attribution=[RFC 123])[content]@citation,#external
```

这里 `attribution` 参与 Function semantics，`@citation,#external` 建立 source Annotation。二者不能因为都像“metadata”就共用一种不透明 property bag。

若未来采用 `>` quote sugar，它必须拥有完整的 block/continuation/nesting syntax 规则，并降低到同一 Quote constructor。未选择该 sugar 时，`>` 保持 Text；相似的 Markdown 外观不能代替语言决定。

== Typed Call Path

显式 Function call 经历：

```text
callee expression
-> resolved Function Value and signature
-> typed argument expressions
-> CallPlan for positional/named/default/trailing binding
-> argument evaluation and coercion
-> Function invocation
-> result validation
```

Core Function 与 User/plugin Function 都消费已经绑定的 Value。Native implementation 可以使用强类型 host struct/accessor，但该 struct 是 Function signature 的后端表示，不是第二套参数语言。

Call 的 callee 是 expression，因此 core Function 也可以像其他 Function 一样传递：

```not
#let constructor = core::quote
#constructor[Ordinary Function call.]
```

语法糖则在 HIR 中携带稳定 core identity，不通过 lexical callee expression lookup；这正是语言级 sugar 与普通 call 的明示区别。

== Structural Sugar

List、Enum、Task 等宿主语法先产生与 core item constructor 相同的局部 Element：

```text
- Apple  ~= core::list::item([Apple])
- Pear   ~= core::list::item([Pear])
```

相邻 item 在 realization/structuring 阶段组成 List。显式 Function、User Function 或控制流动态产生的 item 进入同一 grouping algorithm，因此 parser 不需要预测执行结果。

Table、heading、inline delimiter 和 URL 等 sugar 都由 syntax frontend 建 node，再在 lowering 中取得 core semantic identity。Evaluator 不能扫描普通 Text 补做 sugar recognition。

== Extension Boundary

Plugin Function 是带 signature、implementation 与 capability schema 的普通 Function Value：

```not
#diagram::mermaid(
  source=r#"graph TD; A --> B"#,
  theme="neutral",
)
```

注册 plugin 可以增加 Value、Function 和 Custom Element schema，但不修改宿主 grammar，也不让 parser 在加载 plugin 后重新解释已有 token。需要外部 source 的 plugin 接收 String；需要 Notist Content 的 plugin 接收 Content；需要 lazy computation 的 plugin 接收 Function。

Plugin 执行 backend 可以是 vibe 产生的黑箱，但边界不是黑箱：signature、result Type、capability、determinism 与 Custom Element contract 都是 host 可验证的。Static analysis 读取 schema 而不执行 plugin；runtime 验证其 result 与 declared contract。

用户可扩展语法若未来存在，应由独立的 macro/grammar-extension design 定义 hygiene、expansion、tooling 和 version boundary。普通 Function registration 本身永远不授予修改 parser 的能力。
