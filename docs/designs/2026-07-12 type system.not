#heading(level=1)[Type System]

Notist 最终需要完整的静态类型系统，同时保留运行时 Value。静态类型负责在执行前验证表达式和函数调用；Value 负责承载已经求值的数据，并交给 native、user 或 plugin function 执行。

实现可以分阶段推进，但 Function API 应从一开始朝结构化参数设计，避免每个函数长期自行解释字符串。

#heading(level=2)[Functions Produce Content]

heading、raw、quote 等“processor”本质上都是 callable function：

```not
#heading(level=1)[Introduction]
#raw(lang="rust")![fn main() {}]!
#quote[quoted *content*]
```

它们可以具有普通函数签名：

```text
heading(level: Int = 1, body: Content) -> Content
raw(lang: String? = none, body: RawSource) -> Content
quote(body: Content) -> Content
```

因此不需要长期维护独立的 Processor type 和 ProcessorRegistry。统一系统包含 Function、FunctionRegistry、FunctionInput、FunctionOutput 和 FunctionContext。

#heading(level=2)[Body Mode Is Syntax]

函数签名声明 body 需要 `Content` 还是 `RawSource`，调用处用 `!` 明确选择解析方式：

```not
#heading[Title with *emphasis*]  // body: Content
#raw![not *parsed*]!             // body: RawSource
```

Parser 不需要先解析名称或读取函数签名即可建立语法树和高亮边界。Type checker 在名称解析后验证模式是否匹配签名：

```not
#raw[parsed content]     // type error: expected RawSource, found Content
#quote![raw source]!      // type error: expected Content, found RawSource
```

这种分工保证 syntax tooling 是确定的，同时保留强类型检查。

#heading(level=2)[Core Types]

第一阶段建议支持：

- `None`
- `Bool`
- `Int`
- `Float`
- `String`
- `Content`
- `RawSource`
- `Array<T>`
- `Dict<K, V>` 或结构化 record
- `Function`

`Content` 是可组合的语义 Element sequence，不是普通字符串。`RawSource` 保存原始文本、Source ID 和 TextRange，用于诊断、外部 parser 与源码映射。两者不能隐式互换。

类型还需要可选类型、联合类型或等价能力，以表达 `String?`、`none` 和未来更精确的参数约束。

#heading(level=2)[Runtime Value]

即使拥有静态类型，运行时仍需要 Value：

```text
Value
├─ None
├─ Bool(bool)
├─ Int(i64)
├─ Float(f64)
├─ String(String)
├─ Content(Content)
├─ RawSource(...)
├─ Array(Vec<Value>)
├─ Dict(...)
└─ Function(...)
```

Type 描述表达式“可以产生什么”，Value 表示本次执行“实际产生了什么”。常量求值、默认参数、用户函数和控制流都需要 Value。

#heading(level=2)[Expressions and Binding]

函数参数的最终流程：

```text
source arguments
-> Expression AST
-> name resolution
-> type inference/checking
-> evaluation to Value
-> signature binding
-> Function call
```

例如：

```not
#heading(level=1 + depth)[Title]
```

parser 产生 Expression AST；analysis 解析 `depth`；type checker 确认表达式为 Int；eval 得到具体 Value；binder 将它绑定到 `level`。

Function Signature 至少描述：

- 参数名称与顺序。
- positional/named 调用规则。
- 参数类型。
- required/optional/default。
- trailing body 类型。
- 返回类型。

完成 binding 后，native function 不再读取参数源码字符串。

#heading(level=2)[User-Defined Functions]

统一 Function 模型后，可以自然支持 Typst 风格的用户函数。建议先采用显式类型：

```not
#let add(a: Int, b: Int) -> Int = a + b

#let warning(
    title: String = "Warning",
    body: Content,
) -> Content = #quote[
    #heading(level=3)[#title]
    #body
]
```

Function 可以有三种实现来源：

- Native Function：由 Notist 核心实现。
- User Function：由 Notist 源码定义。
- Plugin Function：由扩展提供。

三者共享名称解析、signature、参数 binding、Value 和返回类型检查。差异只在执行后端与权限边界。

RawSource body 对用户函数是否开放需要谨慎设计，因为它携带源码位置和语言边界；第一阶段可以只允许 native/plugin function 声明 RawSource body。

#heading(level=2)[Syntax Sugar]

语法糖由 parser 直接识别，但其语义应归一到内置 Element 构造器：

```text
= Title
-> builtin heading constructor

`raw`
-> builtin raw constructor
```

语法糖不能通过动态名称查找改变含义。即使用户定义同名 `heading`，`= Title` 仍表示核心 Heading。显式 `#heading[...]` 是否允许局部遮蔽，则由名称解析规则决定。

插件可以注册函数并使用通用 Call 语法，但不能直接修改核心 grammar。未来的用户宏或声明式 sugar 应作为独立系统设计。

#heading(level=2)[Analysis and Evaluation]

静态 analysis 负责：

- 建立 module 和 lexical scope。
- 名称解析与 symbol identity。
- 推断或检查 Expression 类型。
- 解析 Function Signature。
- 校验参数、body mode 和返回类型。
- 产生不执行用户代码的 diagnostics。

Evaluation 负责：

- 求值 Expression AST 为 Value。
- 应用默认参数并完成运行时 binding。
- 执行 Native/User/Plugin Function。
- 产生 Content 和运行时 diagnostics。

Type checking 不执行 Function。这样 LSP 可以快速、确定地分析未保存或不完整的文档。

#heading(level=2)[Staged Implementation]

第一阶段已经建立 Content/RawSource CallBody、FunctionRegistry、基础 Expression literal、Type、Value、Function Signature 和参数 binding。heading、raw、quote 已使用这套 typed interface 执行。

后续顺序建议为：

1. 扩展 Expression AST，支持名称、运算、数组与字典。
2. 实现 lexical scope 和 name resolution。
3. 将当前 literal checking 扩展为一般 Expression type checking。
4. 实现变量、`#let` 和 User Function。
5. 扩展 Function、Array、Dict 与用户定义类型对应的 Value。

当前 native Function 已经只接收完成 binding 的 `BoundArguments`；后续扩展可以保持这一运行时边界不变，只增强 binding 之前的 analysis 与 evaluation。
