@![implementation = "partial"]
= Plugin System

本文是 Notist 插件系统的完整设计。它回答：一个插件 package 如何声明自己、插件能导入哪些宿主类型、插件如何实现语义与投影、宿主如何保证 capability / 终止性 / 确定性，以及 core 为什么只是第一个特权 package。

Reduction 的求值语义见 [[vault::designs::pipeline::plugin-call-reduction]]；core namespace 与插件边界见 [[vault::designs::world::core-namespace-plugin-boundary]]；成型规则见 [[vault::designs::pipeline::structure]]。

== 目标

```text
插件 = 声明式 package
       ├── 函数 schema + 语义实现
       ├── 成型 schema（block / body-mode / role）
       └── 投影贡献（renderer + assets）
```

三条原则：

1. **统一 ABI**：core 与第三方插件共享 `Call` / `Leaf` / `Stream` 与同一 registry。
2. **封闭 core**：core 是宿主拥有的特权 package；插件不能覆盖 `core::*`。
3. **宿主执规约与成型**：插件贡献规则与实现，宿主执行 dispatch、shaping、capability 与终止检查。

== Package 模型

```text
PluginPackage {
  id: PackageId              // shader
  namespace: String          // shader::
  api-version: String
  trust: Core | Wasm
  functions: Vec<FunctionDecl>
  elements: Vec<ElementDecl>
  render: Vec<RenderContribution>
  capabilities: CapabilitySet
  wasm: Option<WasmModule>
}
```

- `Core` package 是语言版本的一部分，由宿主 native 实现；它使用同一 schema，但不受 capability 限制。
- `Wasm` package 是第三方组件，只通过受限 host import 与宿主交互。
- `PackageId` 成为 `ElementName` 的 namespace：插件 Leaf 写作 `shader::canvas`。

== 共享类型与 WIT 边界

`Stream<Call | Leaf>` 是宿主与 guest 共享的语义 schema，但 WIT 不允许直接表达该递归类型（wasmtime/wit-parser 会拒绝递归 type alias / record / list 环）。因此 component 边界保持 opaque bytes，语义 schema 由宿主发布为版本化的 serde payload：

```wit
package notist:plugin;

interface host {
    // 按当前插件 principal 检查 capability，消耗同一 depth/fuel 预算，
    // 并返回规约后的 Leaf 流字节或错误。
    call: func(request: list<u8>) -> result<list<u8>, string>;
}

world semantic-host {
    import host;
    export evaluate: func(request: list<u8>) -> result<list<u8>, string>;
}
```

字节内是 `Call` 请求 / `Stream` 响应的规范编码；字段语义仍由本文和
[[vault::designs::pipeline::plugin-call-reduction]] 定义：

```text
Call { name, arguments, body }
Stream<Call | Leaf>
```

`range` 只用于诊断；宿主为插件返回的节点继承最近调用点 range 或标记 synthetic。

== 语义 ABI

一个 Wasm package 导出一个 dispatcher；manifest 中的每个 `FunctionDecl` 都对应 dispatcher 的一个可调用名字。

插件因此有两种语义实现模式：

1. **宏展开式**：`evaluate` 返回 `Stream<Call | Leaf>`，把继续规约交给宿主；
2. **主动规约式**：调用 `host.call(core::details, ...)` 等，组合结果后返回。

两种模式都经过宿主，插件无法绕过 capability 或无限递归。

=== 插件不能做什么

- 不能直接返回 `core::*` Leaf；`core::*` Leaf 只能由宿主调用对应函数后生成；
- 不能 import 文件系统、网络、随机数或进程；
- 不能修改 parser、不能重新解释已有 token；
- 不能覆盖其他 package 的 namespace。

== 函数与成型 schema

=== FunctionDecl

```json
{
  "name": "canvas",
  "parameters": [
    { "name": "source", "ty": "String" },
    { "name": "width", "ty": "Int", "default": 800 }
  ],
  "trailing-content": "body",
  "result": "Content"
}
```

宿主用它在静态检查、completion、诊断和 runtime 校验中提供签名。

=== ElementDecl 与 shaping registry

```json
{
  "name": "canvas",
  "block": true,
  "body-mode": "flow",
  "role": "none"
}
```

`body-mode` 与 `role` 是成型元数据：

| body-mode | 成型行为 |
|---|---|
| `inline` | body 是 inline 流，不产生 paragraph |
| `flow` | body 执行完整 paragraph / list / section 折叠 |
| `cells` | body 保持 cell 序列，不做段落组合 |
| `none` | body 不参与成型 |

| role | 成型行为 |
|---|---|
| `none` | 普通 Leaf |
| `heading` | 开启 `core::section` 归组 |
| `item` | 相邻同类合并为 `core::list` |

宿主成型引擎固定，规则全部来自 schema：

```text
shape_flat(stream, shaping_registry)
```

core package 的 `core::parbreak` / `core::heading` / `core::item` 只是该 registry 中的特权条目。

== 投影贡献

插件不输出 HTML 字符串。它声明后端无关的投影贡献，由宿主 target renderer 消费：

```json
{
  "render": {
    "html": {
      "element": "canvas",
      "trusted": false,
      "web-component": {
        "tag": "shader-canvas",
        "module": "assets/canvas.js",
        "style": "assets/canvas.css"
      }
    }
  }
}
```

- 宿主 renderer registry 按 `ElementName` 分发；
- 未注册 renderer 的插件 Leaf 走安全 fallback；
- 富渲染必须经过 trusted renderer 边界；
- core Leaf 的 HTML 投影由宿主 native renderer 提供（`core::heading` → `<h1>` 等）。

== Capability

```json
{
  "capabilities": {
    "request": [
      "core::details",
      "core::raw",
      "core::text",
      "core::parbreak",
      "shader::canvas"
    ]
  }
}
```

归属规则：

- 插件 A 返回对 B 的调用，检查 `A → B`；
- B 返回的 Calls 归属于 B；
- 嵌套在 arguments / body 里的 Calls 归属于生成该子树的插件；
- 内置函数自身展开出的 Calls 使用 host 权限。

`filesystem` / `network` / `process` / `randomness` 属于独立 capability 面，v1 默认全部关闭。

== 终止性与确定性

- `MAX_REDUCTION_DEPTH` 与总调用 fuel；
- 总输出节点数 / 字节数限制；
- 相同 `(function, 规范化参数)` 重入可选精确循环检测；
- Wasm 插件是纯函数：相同输入必须相同输出；
- host import 与 export 之间不持有 Wasm store 锁，避免重入死锁；
- 满足以上条件后，语义函数可安全 memoize。

== core package

core 不写成一个 Wasm 组件，而是宿主内的特权 native package：

```text
CorePackage {
  functions: 所有 core::* 构造器实现
  elements:  core 成型 schema
  render:    core 投影（native HTML / future native render node）
  authority: host
}
```

它和插件 package 共享：

- `FunctionRegistry`；
- `ElementSchema`；
- `RendererRegistry`。

但不共享信任等级：core 的实现与 schema 随语言版本发布，宿主静态链接。

== 加载与生命周期

```text
Notist.toml
  → package manifest
  → compile Wasm component
  → validate schema / capability / namespace
  → register functions + elements + renderers
  → WorkspaceSnapshot identity
```

- 插件失败是 host boundary diagnostic，不伪装成 `None` 成功；
- 静态分析与 completion 只读 schema，不执行 Wasm；
- 插件 namespace 冲突在加载时拒绝；
- 插件更新进入 source-set fingerprint。

== 当前实现映射

| 组件 | 状态 |
|---|---|
| `ElementInstance` / `FieldValue` / `ElementName` | 已实现 |
| `StreamNode` / `FlatContent` / `ElementTree` | 已实现 |
| Stream lowering | 全部 Markup item 已直接产出 `Stream<Call | Leaf>`；text 的 inline sugar 扫描仍复用 legacy leaf factory |
| reduction engine（校验 / depth / fuel） | 已实现 |
| `FunctionRegistry` 与 `core::*` 别名 | 已实现 |
| shaping registry / `body-mode` / `role` | 已实现 `ShapingRegistry` / `ElementSchema`；core schema 内置，插件 schema 可注册 |
| WIT 类型包 + host `call` import | bytes-in/bytes-out WIT 已由 wasmtime bindgen 编译校验；`evaluate` 与 `host.call` 均已接入 loader（`plugins/component-echo` / `plugins/component-host-call` 端到端可用），host.call 走共享 registry + capability policy |
| 插件 manifest 统一 package 描述 | 部分实现；已解析 elements / body-mode / role / kind / capabilities，支持无 Wasm 的声明式 package |
| `ElementFunction` 声明式 eval contribution | 已实现；无 Wasm package 由 host 投影为 `Custom` |
| capability checker | 已实现 `Principal` / `CapabilityPolicy`，legacy 与 Stream reduction 每次 dispatch 均检查；Wasm host import 路径待接入 |
| core native package 迁移 | `plugins/core/lib.rs` 承载全部内置内容函数，`plugins/core/plugin.json` 为其声明式镜像，并由 parity test 校验 |
| PluginSet 指纹 | `FunctionEnvironmentId` 已结合 view salt、插件 surface、配置与签名集合；插件 package 变更会分配新语义世界 |
| 插件文件 watcher | daemon 会递归 watch `Notist.toml` 声明的外部 package 目录；plugin.json / wasm / assets 变化触发 disk view reload |
| Wasm 终止/资源预算 | 已实现 fuel-per-call、16 MiB memory cap、table cap，以及 component response 1 MiB / 10k 节点上限 |

== 实施顺序

1. 把 `shape_flow` 的名字硬编码抽成 shaping schema registry；
2. 发布共享 WIT 类型包；
3. Wasm ABI 改为 `export evaluate` + `import host.call`；已完成
4. manifest 升级为统一 package 描述并注册 functions/elements/renderers；
5. capability checker 接入 reduction frame；
6. ~~把 legacy List/Table lowering 迁移到直接产出 Stream。~~（已完成）

== 已裁定 / 待定

- 已裁定：插件 = manifest + Wasm component + assets；core 是特权 native package。
- 已裁定：插件通过 schema 参与成型，成型引擎由宿主执行。
- 已裁定：插件语义 ABI 为 `export evaluate` + `import host.call`；插件不能直接产生 `core::*` Leaf。
- 待定：host import 是否包含 `inspect` / `typecheck` 等更多接口；v1 只提供 `call`。
- 待定：native target 的投影贡献 schema；HTML target 先行。
