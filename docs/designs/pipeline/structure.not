@![implementation = "aligned"]
= Structure Stage

本文承接 [[vault::designs::pipeline::evaluate#求值结果]]（求值结果 `content` 与 call 森林）、[[vault::designs::language::property-table]]（`annotations` 属性表）和 [[vault::designs::language::annotation-syntax#属性]]（属性区间与挂载语义）。[[vault::designs::pipeline::evaluate#Content 值]] 定义值层：求值结束时的 `content` 是 call 森林；[[vault::designs::language::property-table]] 定义属性不挂在树上。本文定义 [[vault::designs::pipeline]] 的成型阶段（shaping）：把 call 森林折叠成成型后的森林，并规定投影必须保持的区间稳定不变量。

```text
call 森林 ── shape ──> 成型后的森林
（属性表不在树上；消费者联合消费结构树与属性表）
```

Call Reduction 模型把求值结果定义为 call 森林：成型前的 `content` 是未归组的 call 森林，其中 `core::parbreak` 是合法 transient 节点；成型后得到不含 `core::parbreak` 的成型森林。完整模型见 [[vault::designs::pipeline::plugin-call-reduction]]。

成型是规范纯函数：输入相同则输出相同；不读源码、名称环境、配置或插件状态。它作用于任何 call 森林，不只整份文档——scope 内部、单个元素等局部视野使用同一套规则，输入随视野选择。

== 输入与输出

- 输入：一个已求值的 call 森林。透明 scope（Content literal 与手动 scope）在进入成型视野前溶解为其 call 森林（[[vault::designs::language::scope-environment#与成型、属性表的关系]]），属性与身份转为区间条目（[[vault::designs::language::property-table]] 属性表、[[vault::designs::language::property-table]]）。
- 输出：成型后的森林。成型产物也是 call 节点：
  - 连续 inline 节点合并为 `core::paragraph`；
  - 相邻同类 `core::item` 合并为 `core::list`；
  - Heading 与其后内容归组为 `core::section`。
- `core::parbreak` 是输入流中的 transient 节点，成型时被消费，不进入输出树；渲染上也不产生节点（[[vault::designs::host::html-renderer#notist-html]]）。

成型是递归的：每个 body 按其容器模式（flow / inline / table-cells 等）执行同一套流折叠，最终整棵树都不含 `core::parbreak`。

== 元素分类

分类由节点名字的 schema 决定：

```text
inline    core::text / core::reference / core::strong / core::emph /
          core::underline / core::strike /
          core::raw(block: false) / 插件节点(block: false) /
          未注册名字的 call(block: false)

block     core::heading / core::rule / core::item / core::table-cell /
          core::table / core::figure / core::callout / core::details /
          core::raw(block: true) / 插件节点(block: true) /
          未注册名字的 call(block: true)

separator core::parbreak
```

分类是封闭的：进入语言的新节点必须同时取得 inline/block 分类，否则不能通过成型。行内节点参与段落组合；块级节点独立成块，并在到来前收束当前段落与列表组合状态。

== 段落与列表组合

对 call 森林做一次左折叠：

```text
S1 inline    → 追加到当前 paragraph buffer。
S2 parbreak  → flush paragraph；flush list（列表不允许跨段）。
S3 item      → flush paragraph；与相邻同 ordered 条目合并为 list；
               条目之间出现其他块级节点或 parbreak 时，当前列表收束。
S4 block     → flush paragraph；flush list；输出独立块。
结束          → flush paragraph；flush list。
```

- 相邻同类条目（`ordered` 相同、之间无块级间隔）合并；`-`/`+` 糖与显式 `item(ordered: ...)` 产生同一输入，走同一算法（[[vault::designs::language::syntax-sugar#item]]）。
- 段落只包含行内节点；`core::paragraph` 的 source range 从首节点起点到末节点终点。
- `core::list` 是成型产生的容器节点，其 source range 从首条目起点到末条目终点；`core::parbreak` 的区间由段落断开这一事实覆盖，不产生独立输出节点。

== Section 归组

对任意块序列递归应用：

```text
G1 Heading(level: h) 开启 Section(h)，并先收束所有 level >= h 的 open section。
G2 非 heading 块进入最深的 open section；没有 open section 时输出为顶层块。
G3 序列结束收束全部 open section。
```

- `=`/`==` 糖与显式 `#heading(level: n)` 产生同一个 Heading 节点，归组不区分来源（[[vault::designs::language::syntax-sugar#heading]]）。
- `core::section` 的 source range 从 heading 起点到 body 最后一个块的终点（空节到 heading 终点）。
- 归组递归作用于任何 call 森林；属性表的节条目边界由同一规则确定（[[vault::designs::language::property-table]]）。

== 不变量

- **I1 投影**：成型只重组元素，不合并、不删除个体；结构树是值层的投影，可丢弃、可重建，任何消费者都不能把投影当作新的真相。
- **I2 区间稳定**：属性表以节点序列区间为键（[[vault::designs::language::property-table]]）；成型重组元素但保持个体区间在求值、成型与投影之间稳定。文本拼接是渲染层优化，不是成型语义。
- **I3 确定性**：shape 是纯函数，因此不需要 reconcile——没有内容变换规则、没有迭代收敛、没有增量 diff。每次变更都对整份代码重新求值；增量更新只属于分析层的优化，不进入求值语义。
- **I4 节层级**：Section 嵌套只由 heading level 比较产生；同一序列中不出现跨越层级的平铺归组。

== 诊断

成型消费已经过静态检查与求值的结果。当前 surface 中 shape 是总函数，不产生新诊断；求值诊断原样透传（`structuring_preserves_evaluation_diagnostics`）。元素值的约束在构造器校验（[[vault::designs::plugin-system::core#构造器校验]]）完成；成型只组合节点序列中的个体。Table 的填行/span 不变量随 Table 构造器校验（[[vault::designs::plugin-system::core#table]]），不由成型检查；当前尚未命名其他成型期容器不变量，因此成型检查表为空。

== Conformance

| 输入 | 预期结构 | 测试 |
|---|---|---|
| `plain *content*` | 单个 Paragraph | `structuring_groups_plain_paragraphs` |
| 文本 + 空行 + 两个 item + heading + 文本 | Paragraph、List(2)、Section(body=Paragraph) | `structuring_groups_paragraphs_and_adjacent_list_items` |
| `- One\n- Two` 与 `#item[One]#item[Two]` | 同一个 List(2) | `structuring_unifies_list_sugar_and_item_calls` |
| pipe table 三行（header/separator/body） | 单个 `core::table` 节点 | `lowers_pipe_table_sugar_to_table_element` |
| `#figure[...]` | 单个 `core::figure` 节点 | `figure_wraps_captioned_block_content_with_typst_style_kind` |
| ordered=true 两条目 + ordered=false 一条目 | 先 List(ordered, 2)，后 List(unordered, 1) | `structuring_groups_ordered_items_separately` |
| `= 一级` / `== 二级` / `= 一级二` | 两个顶层 Section，二级 Section 嵌套于第一个 | `sections_nest_and_receive_section_level_projection` |
| 带求值诊断的输入 | 结构树保留全部诊断 | `structuring_preserves_evaluation_diagnostics` |

== 已裁定 / 待定

- 已裁定：`core::parbreak` 是 transient 节点，不进入结构树也不渲染；Section 递归归组；item 组合只看 ordered 与块级间隔；shape 是规范纯函数。
- 已裁定：成型是递归流折叠，输入为 call 森林，输出为成型后的森林；完整模型见 [[vault::designs::pipeline::plugin-call-reduction]]。
- 待定：段落起点的纯空白 `core::text` 段是否属于可丢弃的段间副产物。当前实现跳过这类段，与 I1 的字面"不删除个体"存在张力；需要裁决空白段是"分段副产物"还是"个体"，并补 conformance。
- 待定：容器子元素不变量的第一条命名、诊断与恢复形状待定；[[vault::designs::plugin-system::core#构造器校验]] 引入第一条此类约束时，本文同步补充成型期检查规则。
- 待定：Section 在成型后的森林中是否保留独立 `Block::Section` 包装（`Block::Section` 是 legacy 投影术语），还是完全使用 `core::section` 节点；插件设计当前记录选择后者。
