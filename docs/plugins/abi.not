@!(implementation: "aligned")
= Plugin ABI

本文定义宿主与 Wasm guest 之间的共享类型、载荷帧与模块调用约定。package 如何声明 Wasm module 见 #<vault::plugins::package>；call 森林在 reduce 阶段的求值语义见 #<vault::language::evaluation>。

== 共享类型与 WIT 边界

`Node` 是唯一的语义表示；`PluginElementDecl` / `PluginParamDecl` / `DefaultValue` 是唯一的插件声明表示。它们都定义在 `notist-model` 并由宿主与 guest 直接共享，WIT 不引入声明或节点镜像。

- WIT 的单一 `plugin` world 只暴露 `init` 与 `evaluate` 两个入口，参数与返回都是 opaque bytes；WIT 已由 wasmtime bindgen 编译校验，两个导出均接入 loader。
- 每个 payload 的第一个字节是 wire version byte，其后是 postcard：`init` 承载 `Vec<PluginElementDecl>`，`evaluate` 的请求与响应承载 `Vec<Node>`。
- 宿主与 guest 共享 `notist_model::wire` 的 declarations / forest encode / decode API；解码严格检查 wire version byte，不提供 JSON 或其他编码 fallback。
- 参数默认值直接使用共享 `DefaultValue`，不经过 JSON 字符串。
- `range` 随共享 `Node` 传输，使插件透传的子树保留诊断位置；它只是可被 guest 伪造的定位元数据，不参与 call identity、规约或权限判定。
- *响应森林继续参与 fixpoint*：handler 返回 call 森林后，宿主统一把它重新送入不动点，其中指向已注册名字的 call 会再次派发。handler 返回与输入完全相同的 call 表示 identity；持续产生新的自返 call 是作者错误，由 depth / fuel 兜底并给出诊断；纯数据元素不注册运行时 handler。

== 零导入 core module

Wasm 插件统一为零导入 core module：同一份 `semantic.wasm` 能被 wasmtime `Module` 与浏览器 `WebAssembly.Instance` 直接实例化。

边界两侧是两套隔离的线性内存，指针互不可见。跨界传数据的唯一方式是把字节拷进对方内存，而目标内存归对方分配器管理——宿主不能在 guest 的堆上分配，guest 也不能在宿主的堆上分配，所以*每一方都必须向对方暴露 alloc / free*。这是跨 wasm 内存传字节的最小必要集。

只有宿主调插件（导出），插件零导入、从不调宿主。规约不动点、终止性与资源预算都在宿主手里。

两条约束：

- *零导入不得破坏*：任何 import 都把可实例化面缩小到实现了它的 embedder；宿主能力（时钟、随机、IO）需求走独立 capability 设计，不进本 ABI。
- *两层版本各自演进*：`notist_abi` 门调用约定（当前版本 1），wire version byte 门载荷编码；解码严格，无 fallback。

== 裸内存调用约定

| 导出 | 职责 |
|---|---|
| `notist_abi()` | 调用约定版本门（当前 1） |
| `notist_init()` | 返回元素声明 |
| `notist_alloc(len)` / `notist_free(ptr, len)` | 为载荷借出 / 回收缓冲 |
| `notist_evaluate(ptr, len)` | 请求缓冲进，返回响应句柄 |

每次派发的时序：`alloc` → 宿主写入请求 → `evaluate` → 宿主读出结果 → `free`。所有权两条：请求缓冲随 `evaluate` 移交 guest，无论成败由 guest 释放；结果缓冲由 guest `shrink_to_fit` 后移交（不变量 `len == capacity`），宿主拷贝后 `free`。

句柄是一个 i64 位模式：高 32 位 `ptr`，低 31 位 `len`，最高位为错误标志——置位时缓冲是 UTF-8 错误消息而非载荷。单次载荷上限 2 GiB − 1，实际先被宿主的 16 MiB 内存帽约束。

== postcard 帧契约

host 与 plugin 之间只传两种字节，均为 wire version byte + postcard：

- 装载时一次：元素声明（`Vec<PluginElementDecl>`）；
- 每次派发：请求（call 节点森林 `Vec<Node>`）进，响应（`Node` 森林）出。

JSON 只用于读取人工编辑的 `plugin.json` 信封；semantic declarations、defaults、`Node` / `Value`、request / response 全部使用共享类型的 wire version byte + postcard frame。

== init 注册语义

`init` 在实例化后、任何 `evaluate` 之前被调用恰好一次，消耗与 `evaluate` 相同的 fuel 预算：

1. 宿主按共享 wire codec 解码 `Vec<PluginElementDecl>`；
2. 整体校验：每个元素名必须落在 package 自己的 namespace 内，参数类型必须是可解析的 Notist 类型，签名合法；
3. 校验通过后按声明生成 `FunctionSignature` 与 `ElementSchema` 并注册；任一失败则整个 package 装载失败，产生 host boundary diagnostic。

注册即自描述：schema 的唯一真相来源是插件代码里的 `init` 实现，manifest 不携带 semantic 接口。静态分析、completion 与 runtime 读到的是同一份注册结果，不存在漂移面。

== evaluate dispatcher

`evaluate` 是单个 dispatcher；`init` 声明的每个元素都对应它的一个可调用名字。

插件只有一种语义实现模式：`evaluate` 返回 call 森林，宿主把整个返回森林统一送回规约不动点。插件通过在森林中写 `core::details` 或其它 package 的名字表达组合，不在模块调用期间嵌套执行宿主规约。无限递归由统一 depth / call budget 与模块 fuel 预算兜底，持续自返属于作者 bug。

插件不能做什么：

- 不能 import 文件系统、网络、随机数或进程；Rust std 所需的 WASI 0.2 imports 由宿主以空 `WasiCtx` 满足；
- 不能修改 parser、不能重新解释已有 token；
- 不能覆盖其他 package 的 namespace。

== 运行时与装载

Wasm 插件统一以 wasmi 执行。插件是零导入纯计算模块，解释器的翻译成本比 JIT 编译低一个量级以上，且同一次实现直接覆盖 native 与 web（引擎编译为 wasm 后经两个无逻辑 syscall 原语实例化插件；加载策略全部在引擎 Rust 侧，字节获取由无策略的异步泵交给引擎公开 API）。

装载按二进制版本字节校验：第 5 字节必须是 `0x01`（core module），否则拒绝。fuel 每次派发重置，口径锚定输入 wasm 字节码；线性内存以调用后检查封顶 16 MiB，另有 table cap 与响应 1 MiB / 10k 节点上限。

实测（docs 全库，shader + mermaid 两个插件）：`check` 0.14 CPU s，`build` 0.24 CPU s，峰值 RSS 46/65 MB。

== SDK 与 export_plugin!

插件以 Rust crate 编写，经 `notist-plugin-sdk` 编译为零导入 core module：`Plugin::init(reg)` 注册元素，`ElementFn::reduce` 实现规约，`export_plugin!` 生成模块导出，挡住全部 ABI 细节。

== 为什么不是 canonical ABI

canonical ABI 提供跨语言的类型化调用，而 `plugin` world 已经窄到字节进字节出，类型层是零收益的仪式；代价却是每次派发的 trampoline、component-model 依赖与浏览器侧转译加 shim。约束没有消失，只是换位：Rust 作者由 `export_plugin!` 挡住全部 ABI 细节；手写非 Rust guest 的义务是本篇的调用约定 + postcard codec。
