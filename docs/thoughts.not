@!(desc: "想法积累，逐条记录，讨论后裁决去留")

= 想法

- 引用扁平化：像 Obsidian wikilink 一样用短名直接引用（如 IR），不要求拼完整标题链；
  渲染时仍展示标题链（面包屑形态）。待讨论：短名的唯一性与歧义裁决、与显式 id 的关系
- 执行预算（现实现为 fuel）：从未认真讨论敲定，不是既定设计决策。待讨论：是否需要预算
  与可终止性保证（表达式层递归是否可能、要不要步数或内存或时间上限）；
  若保留，严格求值下预算语义更简单
- 管线级按需求值：与表达式级 lazy 是两个层面。整库大而访问局部时（agentic CLI 按查询范围、
  浏览器宿主冷启动），只算查询的依赖闭包。开放问题：哪些层必须 eager（诊断与引用图所需的
  身份表），哪些可按需（内容求值、成型、渲染）；身份表能否从静态面（CST 与标注）导出而不等
  全量求值（标题文本含嵌入表达式时怎么办）；daemon 作为缓存层与按需计算的组合方式
