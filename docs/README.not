@!(ai: true, desc: "Notist 项目文档入口")

= Notist

Notist 使用 .not Markup 与 .notc Code 编写模块，共享 Content、Item 和求值器。仓库根的 Notist.toml 声明本 package，本文档目录是它的源码目录。

== 文档

- [[vault::designs]]：语言、值、Item 与基础内容结构。
- [[vault::draft]]：调试快照协议。
- [[vault::thoughts]]：尚待讨论的想法。
- [[vault::_logs]]：按日期保存的讨论和研究记录。

== 验证与预览

在仓库根运行 `cargo run -p notist-cli -- check .` 检查所有文档模块；运行 `cargo run -p notist-cli -- preview .` 启动浏览器预览。

== 文档标记

文档用 `@!(ai: true, desc: "摘要")` 标注 Module，用 `@(label: "intro")` 标注后续 Item。标注表达式须求值为 Dict，连续标注后者覆盖同名键，属性不继承。

反引号代码区原样保留内容，不参与求值。数学使用 `$x$` 或 `$ x $`，生成携带源码 String 的 math Item；预览暂显示源码。
