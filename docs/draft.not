@!(ai: true, desc: "当前 .notc 调试快照协议")

= 调试快照

当前调试器使用 `crates/notist-next` 生成 JSON 快照。native CLI 和浏览器 WASM evaluator 接收同一类虚拟文件请求；HTML 渲染不属于语言快照。

== 请求

请求包含 `request_id`、入口文件 `entry`、虚拟源码表 `files`、WASM 字节表 `binaries` 和 package 依赖映射 `dependencies`。组件资源由 package bundle 和 HTML consumer 使用，不进入 evaluator 请求。

== 响应

顶层字段为 `protocol`、`request_id`、`platform`、`source`、`syntax`、`evaluation`、`result` 和 `truncated`。

- `source.files` 保存源文件内容，`entry_source_id` 指向入口。
- `syntax.tokens`、`syntax.statements` 和 `syntax.errors` 保存 lexer/parser 结果。语句类型是 `let`、`use`、`wasm`、`expression` 和 `error`。
- `evaluation.events` 记录普通函数调用事件。当前事件的 `node` 和 `name` 没有稳定绑定身份；`out` 是 JSON 字符串摘要。
- `result.content` 是 Content JSON；`result.bindings` 是入口模块导出绑定；`result.diagnostics` 保存 setup failure 和 evaluation warning；`result.stats` 目前只有事件数和 token 数。
- `platform` 保存 target 和耗时。native 与 WASM 比较时忽略该字段。
- `truncated` 当前固定为空数组，表示尚未实现预算截断。

Content JSON 使用以下编码：`{"text": ...}`、`{"sequence": [...]}`、`{"item": name, "args": {...}, "source": ..., "offset": ...}` 和 `{"error": ..., "source": ..., "offset": ...}`。Dict 使用 `{"dict": {...}}` 包装；`none` 使用 JSON null。

== 阶段边界

当前快照只有 syntax、evaluation 和 result 三个可观察处理阶段。没有 normalization 或 rule trace，也没有 `origin_id`；语句和表达式仍带 snapshot-local id，但 evaluation 事件当前不引用这些 id。诊断位置在部分运行时错误中只能定位到模块入口 `[0, 0]`。

== 浏览器比较

`just web-compare` 运行 native 和 browser evaluator，比较 `result.content` 与 `result.diagnostics`。source id、耗时、platform 和其他调试字段不属于当前跨平台比较契约。

== 范围

快照按源文件扩展名解析 `.not` Markup 或 `.notc` Code，共享表达式表示与求值器。当前快照不承诺增量分析、事件预算、取消请求、完整源范围映射、静态 resolve 阶段或规约步进。新增字段可以扩展实现，但消费者只能依赖这里列出的字段和语义。
