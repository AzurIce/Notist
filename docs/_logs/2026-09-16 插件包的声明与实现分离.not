@!(ai: true, type: "log", desc: "插件包即模块的可行性：声明与实现分离在 FFI、wasm 与宿主插件三类系统中的做法对比")

= 插件包的声明与实现分离

设想：每个插件包本身是一个 .not 模块，函数签名与元素声明写在 .not 里，实现可以是 notist 自己的代码，也可以是 wasm 产物，因此需要一个 extern 之类的声明形态。本文对照三类既有系统，只收事实与判据，不含裁决。

三条判据贯穿全文：

- 声明能否在加载实现之前读到——决定 check、补全、outline 要不要把 wasm 拉起来；
- 只有声明没有实现时是否合法——决定"包没装好"这一档有多大的降级空间；
- 声明与实现不匹配时失败成什么形态——编译失败、链接失败、加载失败、首次调用异常，还是未定义行为。

== 语言 FFI：声明在语言里，实现在别处

=== C 与 C++：头文件加动态库

- 声明在头文件、实现在共享库；符号在链接期解析，动态库在加载期由动态链接器接管。
- C++ 符号经名字修饰，跨编译器不稳定，`extern "C"` 是关掉它的开关；参数类型进符号名而返回类型不进，只改返回类型的漂移连符号名都不变，链接干净、结果是未定义行为。
- 只有声明没有实现：不合法，链接期未定义符号；弱符号是唯一例外，未解析时取空值、可判空。
- 教训：绑定放到链接期，等于放弃"只有声明"这一档，也放弃类型契约的全部校验——名字之外的一切都不设防。

=== Python：存根加原生扩展

- 声明写在 `.pyi` 存根里（PEP 484 与 PEP 561 的 `py.typed`，存根可独立成包），实现是原生扩展或 `ctypes` 绑定；运行时不读存根。
- 校验只有类型检查器一侧，漂移靠外部工具比对运行时对象与存根来兜；该工具自己也承认盲区——它判断不了存根里的返回类型写得准不准。
- 只有声明没有实现：合法，调用时才失败。
- 教训：声明可以完全脱离实现存在，代价是漂移；漂移只能靠"比对制品"的工具补，靠约定补不住。

=== SQL：CREATE FUNCTION 与 LANGUAGE c

- 声明是目录里的一行，实现是共享库里的符号，且写在同一句声明里（`AS` 后跟库文件与符号名）。
- 绑定发生在会话内首次调用；建函数时不检查库与符号是否存在。
- SQL 层强校验：同名加同输入参数类型即冲突。SQL 签名到 C 符号之间零校验。
- 只有声明没有实现：合法，失败推迟到首次调用；替换共享库后旧实现常驻内存，必须开新会话。
- 教训：声明即数据、实现按名引用最省事；把实现写进声明，就失去"同一批声明换实现"的能力。

=== Java 与 .NET：native 与 DllImport

- Java 的声明是只有分号的方法，实现在原生库的修饰名符号或显式注册表里；JVM 规范特意用 binding 而不是 linking，说明绑定独立于类链接。
- 只有声明没有实现：合法，类照常加载与实例化，调用时抛链接类错误。
- .NET 的特性声明是纯元数据，定位库、加载库、找函数地址都推迟到首次调用，失败拆成"库缺失"与"入口点缺失"两个可捕获异常。
- 教训：把失败拆成具名的可捕获类别，比一个笼统错误可用得多。

=== OCaml 与 Haskell：external 与 foreign import

- 声明在源文件里，实现是 C 符号，绑定在链接期；`external` 的文法里没有函数体这一项。
- 只有声明没有实现：不合法，链接期报"外部函数不可用"；OCaml 有 `-no-check-prims` 一类的旁路，绕过检查后失败推迟到启动。
- Haskell 2010 报告明确写不要求检查外部类型一致性；GHC 不再把外部头文件包含进生成代码，C 编译器那层检查也取消了。
- 教训：把边界交给链接器，等于放弃语法之外的全部检查。

== wasm 一侧

=== core module 与组件模型

- core wasm 只有导出名与数字类型，没有结构类型；组件模型用 WIT 把接口变成独立制品，由 canonical ABI 从声明推导出调用约定。
- 接口可以先于实现存在，也可以从实现里提取后比对；类型不符在校验期就是硬错误，报出期望与实际。
- 代价是跨组件调用必须经适配层，组件不能导出内存。
- 教训：可校验的前提是接口本身是一个可读制品，而不是只能从实现里推断。

=== Typst 的 wasm 插件

- `plugin` 读入 wasm 后返回一个模块，为其中每个函数导出一个 Typst 函数：签名由实现推断，不在语言侧声明。
- 协议纯结构化、无版本字段：导出函数收 n 个 32 位整数表示各参数长度，返回一个状态码，错误消息走 UTF-8 缓冲；必须导出内存；WASI ABI 不能直接用。
- 校验在调用期惰性发生，且只查三件事：参数是否为 32 位整数、返回值是否恰为一个、个数是否相符；实参整体按字节交给实现，类型语义完全由包装函数承担。
- 插件函数必须纯，要状态得走单独的 transition 接口。
- 官方说明"包等于 wasm 加一层惯用包装函数"，这层包装是惯例而不是机制。
- 教训：签名由实现推断，宿主就必须先加载才知道接口；包装层是协议太底层长出来的补偿。

== 宿主清单与能力协商一侧

=== Typst 包与 VS Code 扩展

- Typst 的 `typst.toml` 只有元数据（名字、版本、入口文件、编译器版本下界），接口靠导入时的实际导出，无签名声明；编译器版本不满足即编译失败，不降级。
- VS Code 的扩展清单把能力面写成纯声明（贡献点、激活事件、引擎版本），实现在激活函数里才出现；校验在扫描与安装期，不兼容宁可装不上或改选兼容版本。
- 教训：声明面越完整，宿主越能在加载实现之前决策。

=== mdBook 与 Sphinx

- mdBook 的表存在即启用，`optional` 把"未安装"从报错降为警告，支持哪些渲染器靠一次退出码握手协商。
- Sphinx 扩展没有清单，靠 `setup` 函数命令式注册，宿主静态不可知；元数据从注册函数的返回值读，其中并行安全默认关闭、要显式声明才开。
- 教训：可选性与能力协商是两个独立开关；注册式接口对静态分析不友好。

== 横向对比

| 系统 | 声明载体 | 实现载体 | 绑定时机 | 只有声明 | 不匹配的失败形态 |
| - | - | - | - | - | - |
| C 与 C++ | 头文件 | 共享库符号 | 链接期与加载期 | 弱符号可空，否则不合法 | 链接错误或静默错调 |
| Python | 存根文件 | 原生扩展 | 运行时导入 | 合法 | 调用时异常 |
| SQL | 目录行 | 共享库符号 | 首次调用 | 合法 | 首次调用失败 |
| Java | 方法声明 | 原生库 | 首次调用 | 合法 | 链接类错误 |
| .NET | 方法元数据 | 非托管库 | 首次调用 | 合法 | 两个具名可捕获异常 |
| OCaml | 源文件声明 | C 符号 | 链接期 | 不合法 | 链接期报错 |
| Haskell | 源文件声明 | C 符号 | 链接期 | 不合法 | 链接错误 |
| wasm 组件 | WIT 制品 | 组件导出 | 加载期比对 | 合法 | 加载期校验失败 |
| Typst 插件 | 无，由实现推断 | wasm 导出 | 加载后 | 不适用 | 调用期只查个数与整数类型 |
| Typst 包 | 清单元数据 | 该语言源码 | 导入时 | 不适用 | 编译失败 |
| VS Code | 扩展清单 | 脚本模块 | 扫描与安装期 | 不合法 | 拒绝安装 |
| mdBook | 配置表 | 可执行程序 | 每次构建 | 合法 | 报错或降为警告 |
| Sphinx | 无，代码注册 | 该语言模块 | 运行时 | 不适用 | 中止构建 |

== 共性事实

- 签名校验一律止步于语言自己的类型系统：没有一个系统校验语言声明与外部实现的类型一致性，越过边界的错误是异常或未定义行为。
- 数据语义层的漂移谁都不查：结构布局、返回类型、编码约定对不上时，四类工具链都发现不了，分别表现为未定义行为、检查器假绿后崩溃、拿到乱码字节。
- "只有声明没有实现"是否合法由绑定时机决定：运行时绑定支持，链接期绑定不支持。
- 失败被建模成什么决定它有多可用：具名可捕获异常优于链接错误，链接错误优于未定义行为。
- 别名机制普遍存在，版本管理几乎没有语言级支持，一律靠路径、库名与加载器。
- 声明的位置决定宿主能多早决策：清单与独立接口制品可以在加载实现前校验，代码内注册与实现推断都不能。

== 对 notist 的含义

- 声明必须静态：plan 只需要声明，check、补全、outline 都不应拉起 wasm。声明写成值构造器（求值才知道签名）会让整个静态面依赖求值。
- 声明与实现分居两处：声明在 .not 模块里，实现产物在包清单里（包内清单已经在声明 wasm 模块与投影贡献）。声明只说"没有体，实现由外部提供"，不说实现是谁，同一批声明才能被宿主内建、wasm 包、测试桩分别满足。
- 缺失与不匹配要有各自的诊断码与降级：加载期比对声明与实现，不匹配就不注册该实现；调用一个有声明无实现的名字，与调用一个拼错的名字，诊断必须不同，且都走可见兜底而不是丢内容。当前插件边界是零导入 core 模块加不透明字节，产物里没有类型信息，因此"比对"只到名字与存在性；要校验到类型，得让实现产物携带接口摘要。
- 元素声明比函数签名宽：要带流角色、尾随参数名、分组角色，还要能标"只有声明没有实现"；现有插件元素声明里的 computed 字段就是这一档的先例。
- 命名空间不需要新机制：包既然是模块，导入即命名空间；糖钉 core intrinsic 的规则不变。
- core 是同一机制的实例：声明与别人一样，只是实现由宿主内建，"core 非特权"因此从承诺变成结构事实。

== 待裁决点

- extern 的形态：独立的声明形式（关键字加签名）、无体的函数定义，还是绑定到外部符号的值构造器。
- 实现清单的位置：包内清单、vault 清单，还是声明所在模块的模块属性。
- 声明版本与实现版本如何表达：现有元素声明带一个整数版本，是否需要以及如何进指纹。
- 包级依赖：对照里除 VS Code 外普遍没有清单级依赖，notist 是保持"代码里导入"，还是引入清单级依赖。

== 来源

- `https://www.postgresql.org/docs/current/sql-createfunction.html`
- `https://docs.oracle.com/javase/specs/jls/se21/html/jls-8.html`
- `https://docs.oracle.com/javase/specs/jvms/se21/html/jvms-5.html`
- `https://learn.microsoft.com/en-us/dotnet/standard/native-interop/pinvoke`
- `https://ocaml.org/manual/5.3/intfc.html`
- `https://www.haskell.org/onlinereport/haskell2010/haskellch8.html`
- `https://downloads.haskell.org/ghc/latest/docs/users_guide/exts/ffi.html`
- `https://typst.app/docs/reference/foundations/plugin/`
- `https://github.com/typst/packages/blob/main/docs/manifest.md`
- `https://rust-lang.github.io/mdBook/format/configuration/preprocessors.html`
- `https://www.sphinx-doc.org/en/master/extdev/index.html`
- `https://code.visualstudio.com/api/references/extension-manifest`
