@!(ai: true, draft: true, desc: "编辑器内核讨论：通用文本内核、Notist 语言栈作为消费者、部署拓扑与未裁决项")

= 编辑器内核讨论

本文记录 2026-09-20 关于编辑器内核的架构讨论。分层形状与拒绝路线是讨论收敛结果，尚未整体裁决；未裁决项单列待拍板，落为正式设计时移入 designs 并删除本文的讨论语气。

== 分层

```text
消费者面：一方 Web 编辑器 / LSP 适配器 / HTML 渲染 / Agent 与 CLI
  |
  v
Notist 语言栈（消费者）：.not 与 .notc 文法 · 包/模块 · 求值 · Item IR · 分析
  |
  v
通用语言机制（语言无关）：增量重析机制 · 语法树容器 · 诊断/补全订阅 · provider 注册
  |
  v
编辑器内核（对任意文本生效）：rope 文本 · 编辑事务 · undo · 选区 · 锚点
  · CRDT 副本与物化 · 快照 · 文档会话管理
```

依赖箭头全部向下：编辑器内核不知道 Notist 的存在。边界判据：换一门语言也不必重写的属于内核；只对某个宿主有意义的属于内核之外。

== 编辑器内核

内核包含：

- rope 文本与编辑事务。rope 采用现成实现（ropey 或同构库），不自研。编辑事务是所有编辑来源的单一汇合点：本地按键、远端 CRDT 操作、文件系统变化、Agent 补丁走同一入口，增量管线不区分来源
- undo 采用协作语义（每人撤销自己的操作），不用全局栈
- 选区与锚点词汇。锚点为 CRDT 插入 id 加偏移，对任意文本有效；协作光标与评论锚定使用锚点
- CRDT 副本管理与物化。算法采用 Loro 或 yrs（依赖，不自研）：副本与物化文本互转、操作导入导出、快照与日志的持久化钩子；存储本体归宿主
- 文档会话管理：打开/关闭、脏状态、文件监听、provider 挂载

内核不包含，各归其位：

| 能力 | 归属 |
| - | - |
| 文本输入 / IME / 渲染 | 各端输入层（CM6 等）；GUI 端输入层持有文本真源，内核做镜像 |
| CRDT 算法 | 依赖（Loro / yrs） |
| LSP 协议 | 薄适配器（notist-cli lsp），箭头指向内核与语言栈 |
| tree-sitter / TextMate 投影 | 姊妹仓库，供外部宿主高亮 |
| 网络中继 / presence 扇出 / 房间 | 部署服务器 |
| 快照与文件持久化 | 宿主（服务器存储 / 文件系统 / IndexedDB），内核只给钩子 |
| HTML / 预览渲染 | 消费者面 |

== Notist 语言栈

语言栈以 provider 身份注册进会话（声明处理 `.not` / `.notc`），不再长在内核上。语法、包/模块、求值、Item IR、分析整体属于本层；无损 CST 单树路线（2026-09-15 裁决）不变，是本层内部决策。Item 稳定身份是本层概念，映射到内核锚点；引用存活依赖它，协作锚定不依赖。

package 的 `components/*.js` 等 Vault 内非 `.not` 文本是内核通用性的现实消费者；一方编辑器为其他文件类型挂语言 provider 不动内核。

== 部署拓扑

- 协议对等，产品星型：所有副本遵守同一 CRDT 协议，经由常驻部署服务器会合。服务器是普通参与者而非权威：无头、可产生编辑（如 Agent 会话），另担三个宿主职责——中继操作流与 presence、持久快照与操作日志、运行无头分析
- 无权威、最终一致：冲突消解预编在数据结构内，无需仲裁。同步语义是最终同步而非断网前同步；他人看到你的编辑，不早于你与服务器相遇的时刻
- Web 端：CM6 持文本真源（tinymist 模式），内核以 Wasm 镜像，进程内直调语言栈，零协议；UTF-16 换算隔离在 LSP 适配器
- 插件端（obsidian / vscode / zed）：只消费语言栈（经 LSP），不涉内核

== 拒绝路线

- 进程级前后端与 headless 权威编辑器：按键过网络、离线残废、放弃 CRDT 的意义；xi-editor（Google，已终止）复盘的正面死因
- 自研文本 CRDT 与自研 rope：算法买（Loro / yrs 已到 Fugue / Eg-walker 一代）；Zed 的实现不发布，无法借用
- 自研文本输入栈：IME、bidi、a11y 全行业只有浏览器 DOM 成熟；egui / iced / Tauri 的开放 IME 议题是路标
- 二进制主文件格式冒进：git、插件、语料生态压在纯文本上；Logseq 2.0 的数年成本为先例
- 为未裁决的产品形态预建协作服务器
- 内核独立产品化：对内保持通用边界（独立 crate、不 import 语言栈类型），不提前发布、不为想象的使用者打磨 API

== 与已有决策的衔接

- 2026-09-15 的单树 CST 裁决落在语言栈内部，不受影响
- 2026-09-19 建议的 `[service]` 对应会话层方向；LSP 内脏（workspace 发现、overlay、增量同步、轮询）下沉进会话层，协议面保持不动，姊妹项目契约无感
- native 与 WASM 双端比较契约（`just web-compare`）延伸覆盖内核
- 通用内核天然配文本级 CRDT；结构级 CRDT 要求复制层理解 Item，与内核通用性冲突，此项随落盘裁决一并定

== 未裁决项

- 协作产品形态：多设备同步、多人共库还是同文档实时共写；决定 CRDT 何时启用、服务器是否开建
- 落盘红线：`.not` 保持纯文本加旁挂账本，还是 CRDT 为真源、文本做投影；文本级与结构级 CRDT 的选择与此绑定
- Item 稳定身份规则：协作方向把它的优先级从装饰升为承重墙
- 客户端副本持久化（IndexedDB）：网页版还是本地优先网页版
- E2EE 与服务器侧分析的按文档取舍
- Web Editor 里程碑与三个插件面的优先级
