= 语料库

`corpus/` 只放输入：`.notc` 源文件覆盖语法构造，期望输出按相对路径镜像到 `crates/notist-ir/tests/golden/`。

覆盖：文本与行内元素、绑定与遮蔽、函数与实参绑定、内容块与闭包、运算符、代码块与条件、
高阶与递归、core 函数、声明与缺实现、降级路径。

- 文件按意图命名，编号只用于控制顺序；
- 降级路径是正面语料：未知名字、类型不匹配、缺实参与缺实现都只产生 warn，内容不丢；
- golden 随源码一起提交：改设计就是改 golden，diff 本身就是裁决证据。

重写 golden：

```text
UPDATE_GOLDEN=1 cargo test -p notist-ir
```
