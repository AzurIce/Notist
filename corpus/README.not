= 语料库

三层，每层都是一份可执行真值：`syntax` 覆盖语法构造，`packages` 覆盖包与实现，根下的 `vault` 与 `broken` 覆盖依赖树。

本目录只放输入。期望输出在各消费方的测试目录里按相对路径镜像（当前是 `crates/notist-ir/tests/golden/`），语料因此保持纯净，可以被直接当 vault 打开，不会混进生成物、也不会被包扫描收成资源文件。

== 纪律

- 文件按意图命名，编号只用于控制顺序；
- 每个文件 5 到 30 行，宁可多文件不要大文件；
- golden 随源码一起提交：改设计就是改 golden，diff 本身就是裁决证据；
- 文档里写的每条规则都应在某一层有对应文件，找不到对应文件说明规则没落地。

== 约定

- 声明模块约定名为 `api.not`：它只有 `#extern` 声明，内容测试跳过它；
- `artifact.toml` 冒充实现产物携带的接口描述（接口文本加导出符号表），用来验证「声明与实现在加载期比对」这条链路；真接 wasm 之后它由构建产出，不再是手写文件；
- 根下带 `Notist.toml` 的目录是一个依赖树的根，其余包放在 `packages` 下；
- `vault` 是正常形状的库，`broken` 专门喂坏东西，覆盖依赖图的诊断。

== 重写 golden

```text
UPDATE_GOLDEN=1 cargo test -p notist-ir
```
