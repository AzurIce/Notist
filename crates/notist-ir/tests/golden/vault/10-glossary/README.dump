module
  section(label=[text("术语表")])
    section(label=[text("包")])
      text("一个包是分发与实现的单位：模块树、清单、可选实现、可选依赖。")
    section(label=[text("模块")])
      text("一个模块是寻址与派生的单位，身份是 ModulePath。")
    section(label=[text("声明模块")])
      text("只有 extern 声明的模块，约定名为 api.not。")
bindings
diagnostics
