package missing-symbol 0.1.0
entry api.not
exports
  fn up(x: Int) -> Int
  fn down(x: Int) -> Int
implementation external artifact.toml
  interface match
  symbols
    up -> up
    down -> missing
diagnostics
  warn implementation-missing [29..59] artifact `artifact.toml` does not export `down` for declared `down`
