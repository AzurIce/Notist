= Reference 与 RefTarget

Reference 连接知识：`#<target>` 产生 Reference 元素（糖的文法见 #<vault::03-language::grammar::markup/Markup/语法糖/link>，构造器签名见 #<vault::03-language::functions/Built-in Functions/link>）。解析针对 ModulePath 与可选的 ItemId，不针对磁盘路径；引用不执行目标 Module、不读取其 bindings、不内联其 content。词汇——Module/ModulePath、Item/ItemId/ItemPath、匿名与具名——统一定义于 #<vault::04-world::model/数据模型/Item 树>；本文描述引用解析机制。

== 目标书写形式

```not
#<setup>                        # 相对 ModulePath（单段）：从当前模块解析
#<self::setup>                  # 当前模块路径前缀
#<super::intro>                 # 父级
#<vault::guide/install>         # 绝对 ModulePath + ItemId（ItemPath）
#<vault::docs::images/logo.png> # 资源文件（完整文件名）
```

- 相对 ModulePath（裸单段与 `self::`/`super::` 前缀）在补全时插入相对路径，保持源码可读与可移动；
- 路径必须自我锚定：不存在「裸 ItemId」形式——单段字符串已被相对 ModulePath 占用，也不做「先试模块、再试 ItemId」的回退解析；
- `/` 后缀进入模块内的扁平 ItemId 空间：第一个 `/` 是结构符，其后字符皆为字面（ItemId 可再含 `/` 与 `#`）。

== ItemId 匹配与消歧

每个 Module 拥有一个扁平的 ItemId 空间——「名字 → 模块内实体」的映射，正是 POSIX directory entry 的结构。Item/ItemId 的定义、具名与匿名之别、唯一性见 #<vault::04-world::model/数据模型/Item 树>。

引用 `/` 后缀的匹配顺序：先按显式 `id:` 标注精确匹配，再按标题默认名精确匹配；ItemId 是字面字符串，不需要符合标识符语法。重复标题产生歧义诊断，作者用显式 `id:` 标注消歧；重复 ItemId 是 `duplicate-item-id` 诊断。

匿名 Item 不可寻址：想被引用或查询，就创建带 `id:` 标注的节点（这正是手动 scope 的意义之一）——可引用性由作者显式授予。

== 资源文件解析

`vault::docs::images/logo.png` 的解析：

- `vault::docs::images` 是资源目录 virtual module；
- `/logo.png` 是完整文件名，在目标目录中精确匹配；
- 目标不存在 → 存在性诊断（编辑器可提示）。完整文件名消除扩展名歧义——文件名即身份，不依赖目录扫描猜测。

资源文件是模块命名空间中的一等 Item（kind: Resource）；ResourceKind（Image | File）由解析给出，不属于值层。

== 解析产物：RefTarget

Reference 元素的值层携带 `target: Target | String`：`Target` 是 ModulePath 加可选 ItemId 的结构化拼写（ModulePath 或 ItemPath），`String` 只表示外部 url。目标指向什么由分析层解析，解析产物：

```text
RefTarget
├─ Module(path)                 # 文档模块
├─ Item(id, kind)               # 模块内 Item；kind: Scope | Resource
│                               #   Resource 再携带 ResourceKind: Image | File
│                               # 解析到节头部时，查询返回整棵节子树与节属性
├─ External(url)                # 外部目标
└─ Missing(...)                 # 不存在/歧义/不支持——不吞掉，进入诊断
```

两条关键原则：

- 目标类型是解析产物，不是元素字段：ItemPath 后缀命名的是 scope 还是资源文件，只有解析才知道；值层的 Reference 元素不携带目标类型；
- 「是什么目标」（分析层）与「以什么关系引用」（元素层）正交：relation 是引用元素自己的属性，当前只有 Reference（导航）。

== 两个寻址世界：结构地址与词法绑定

语言里存在两个世界，切分线是*唯一性*：

```text
结构世界（Markup 寻址）   Item / ItemId / ModulePath —— 模块内唯一，可被 #<> 引用
词法世界（Code 寻址）     Binding（let/fn/参数）—— 允许遮蔽，只按位置导航
```

ItemId 要求模块内唯一；绑定名允许遮蔽（最近绑定者胜出），没有稳定的模块内身份，因此不能成为地址。引用解析不读取绑定环境，与 Rust 路径不解析局部变量同构。LSP 是两个世界之间唯一的桥：`definition/hover/references/completion` 按位置分派——在 `#<...>` 上走结构解析，在代码标识符上走词法解析（见 #<vault::06-lsp>）。文档引用 code 实体（如 API 文档链接函数）的唯一入口是把绑定升格进名空间（挂 `id:` 标注并接受唯一性约束），而不是让引用读取绑定环境。

== 外部 URL

带 scheme（`https://`、`mailto:` 等）的外部目标不在引用目标空间内：引用目标只包含 vault 内部目标。`#link("https://...")` 语法合法（target 是字面 url 字符串）：解析产物为 `External(url)`，渲染为未解析的可见文本并产生 info 级 `external-reference-unsupported` 诊断——不是语法错误。

== 引用图

引用环合法：两篇文档可以互相链接。renderer 与 indexer 从同一 module/reference graph 派生 URL、backlink 与定义，不能各自扫描目录猜测身份。import graph 与引用图共用同一 Module identity 而环语义不同（import 边的求值前解析与环诊断）见 #<vault::04-world::import/Import/依赖图与环语义>。
